# Visual audit — `iclr-2026-86ded246ec76`

## 范围与事实源

- 论文标题：*Addressing divergent representations from causal interventions on neural networks*。
- 唯一事实源是 `corpus/pdfs/iclr-2026-86ded246ec76.pdf`；`pdfinfo` 报告 27 个物理页、Letter 页面（612 × 792 pt），Creator 为 LaTeX with hyperref，Producer 为 pdfTeX-1.40.26，无加密。
- 已完整阅读 PDF 正文、参考文献、附录和表述为图/表的全部对象。所有 27 页均以 200 dpi 渲染（1700 × 2200 px，超过 180 dpi 要求），逐页检查；含视觉对象的物理页 2、5、10、21、22、23、24、25、26、27 又逐一检查原始 200 dpi 页面，而不是只看 reading inventory。
- PDF 实际对象清单得到 **9 幅 Figure、1 个未编号的 OLS table-like object**。Algorithm 1 位于物理页 19，是排版算法而非 Figure/Table，未计入视觉对象。正文 Figure 1–3；附录 Figure 4–9 与 OLS summary。

### PDF 对象清单核对

| 物理页 | PDF 对象与版面事实 | 审计结论 |
|---|---|---|
| 2 | Figure 1；一个 6000 × 2579 RGB 栅格 XObject，包含左侧 pathway schematic 和右侧两幅 scatter/functional-landscape 图 | 计为 Figure 1，正文、page-width |
| 5 | Figure 2；多个 RGB/透明 mask 栅格 XObject，正文三 panel（坐标 patch、三种方法 PCA、EMD bars） | 计为 Figure 2，正文、page-width |
| 10 | Figure 3；一个 6000 × 1566 RGB 栅格 XObject，六 panel（PCA、曲线、CL diagram、两组 scatter、bar） | 计为 Figure 3，正文、page-width |
| 19 | Algorithm 1；文字/规则组成的算法环境，无 Figure/Table caption | 从 Figure/Table 清单排除 |
| 21 | 未编号的 `OLS Regression Results`；Nimbus Roman 文本和横线组成的 statsmodels 摘要，含 `No. Observations: 30` 等 | 计为 1 个附录 table-like object |
| 22 | Figure 4；8 个带透明 mask 的 RGB 栅格 panel，2 × 4 divergence-measure bar grid | 计为 Figure 4，附录、page-width |
| 23 | Figure 5；8 个 RGB/透明图像片段叠加在 vector text 上，最终为 Default/OOD 两个 scatter panel | 计为 Figure 5，附录、single-column |
| 24 | Figure 6；无 `pdfimages` 栅格对象，DejaVu Sans 文本和矢量 bars/lines，3 × 3 grid | 计为 Figure 6，附录、page-width |
| 25 | Figure 7；无栅格对象，DejaVu Sans 文本和矢量 bars/lines，3 × 3 grid | 计为 Figure 7，附录、page-width |
| 26 | Figure 8；无栅格对象，DejaVu Sans 文本和矢量 bars/lines，3 × 3 grid | 计为 Figure 8，附录、page-width |
| 27 | Figure 9；无栅格对象，DejaVu Sans 文本和矢量 bars/lines，3 × 3 grid | 计为 Figure 9，附录、page-width |

`reports/tables/visual_source_inventory.csv` 将该 paper 的自动状态列为 `no_public_source_found`，候选为 `grantsrb/rep_divergence`；`corpus/visual_sources/iclr-2026-86ded246ec76/` 没有本地文件。该自动结果不是对象清单事实源：PDF 的页脚明确给出公开仓库链接，下面的 `gh` 只读核验后将源状态修正为 `partial_visual_source`。

## 公开视觉源检索

PDF 明确链接 [github.com/grantsrb/rep_divergence](https://github.com/grantsrb/rep_divergence)。使用 `gh repo view`、`gh api repos/grantsrb/rep_divergence/git/trees/main?recursive=1` 和只读 raw/blob 内容核验；默认分支 `main` 的 HEAD 为 `f2548d2ea9b4f4b87a87ba5d53db43838d15c521`（2026-04-24，`add slides`）。README 的项目标题、引用标题和作者与论文严格匹配，故该候选不是仅凭文件名命中。仓库包含：

- `divergence/emd_vis.ipynb`：用 Seaborn/Matplotlib 绘制额外 divergence barplots、`divergence_fig.png` 风格的 bars，以及 CL epsilon 的 IIA/EMD twin-axis 图；cell 8/13/23 可解释 Figure 2(c) 与 Figure 4 的视觉语法，但没有公开最终 Figure 2/4 合成图。
- `divergence/clloss_synthetic_task.ipynb`：cell 9 的 natural/intervened scatter、cell 47 的 synthetic CL/DAS bars 和 baseline band、cell 78 的 3 × 3 hyperparameter grids；它与 Figure 3、Figure 6–9 的方法/布局直接匹配。
- `divergence/CL_Boundless_DAS.ipynb`、`divergence/Boundless_DAS.ipynb`：Boundless DAS/CL 实验上下文；`divergence/plotting_utils.py` 与 `divergence/mean_diff_section_4.2_sanity_check.py`：scatter/mean-difference 辅助绘图。
- `divergence/csvs/das_emd_cleps0.0.csv`、`mean_diff_emd.csv`、`sae_emd.csv`、`cl_synthetic_ood.csv`：对应 divergence/CL 结果的数据文件。

仓库没有论文 `.tex`、TikZ/PGF、完整 Figure 1 schematic 源，也没有逐幅公开的 Figure 2–5/最终复合资产；公开 notebook 的图像输出不能冒充 PDF 的 exact composite。因此源状态是 `partial_visual_source`：Figure 3、Figure 6–9 有高可信绘图逻辑/数据线索，Figure 2/4 有部分绘图逻辑，Figure 1/5 与最终合成布局主要依赖 PDF 对象和渲染观察。

## 全文视觉风格

- **版式与叙事**：正文以 Figure 1 的概念机制起笔，Figure 2 把 divergence 做成 coordinate/PCA/EMD 对照，Figure 3 以 CL loss 连接 divergence 与 OOD generalization；附录 Figure 4–5 扩展 metric 与 synthetic task，Figure 6–9 展开超参数敏感性。主图为 page-width 或内部双栏宽度的大型 raster composite；附录 grid 图信息密度高。
- **字体**：`pdffonts` 显示正文/caption 的 Nimbus Roman No9 L（Regular/Medium/Italic）与数学字体，Figure 5 的 vector text 使用 `SFNS`，Figure 6–9 使用 `DejaVuSans` Type3。主 Figure 1–4 为栅格，图中文字只能作 rendered estimate：约 6–14 pt，panel letter/标题较大；Figure 5 的 SFNS 约 8–10 pt；Figure 6–9 的 PDF XML 对象约 11.3–16 pt（对应 XML 15/17/21/24 的缩放）。附录图的相对字体比主图统一，但 Figure 4–9 的轴/legend 仍偏小。
- **颜色**：主图用橙/蓝表达 natural/intervened，用紫、青、薄荷色表达 baseline/EMD 或 CL/DAS；十类 synthetic state 用多色 hue。Figure 6–9 用 pastel 五级 CL epsilon，并用粉色虚线/带表达 behavior-only DAS baseline。语义通常有 legend/明暗/线型辅助，但多系列和十类 scatter 对灰度不稳健。
- **矢量/栅格**：Figure 1、2、3、4 是 RGB 栅格 XObject；Figure 5 为图像片段加 vector SFNS 标签；Figure 6–9 为 vector paths/text；OLS summary 为 vector text/rules。栅格对象在论文尺寸可读，但放大时 Figure 1/3 与 Figure 4 的边缘先像素化。
- **不确定性**：Figure 2(c)、Figure 3(f)、Figure 4、Figure 6–9 可见 error bars；Figure 3(b) 还有 translucent band，Figure 6–9 还有 baseline band。caption 没有给出这些 bars/bands 的统计定义或重复单位，构成全篇最明显的证据语义缺口。

## Figure 1 — causal intervention mechanism

- **位置与职责**：物理页 2，正文 introduction；`page_width`；类型 `architecture`、`conceptual_diagram`、`scatter`；purpose 为 `headline`、`theory_mechanism`、`method_interface`、`qualitative_evidence`；复杂度 4/5（2 panel，约 4 条 pathway series，约 8 个 legend/encoding items，约 15 个注释，点数为 schematic estimate）。
- **图形语法**：PDF 中是 raster composite；右侧 scatter 的 x/y 均为 linear 的 `Causal Dim 1/2`，左侧 schematic 没有数值轴；无 grid；legend 在底部，主要服务左侧 natural/intervened path，不与右 panel 共享；panel 内有 direct labels。marker 为圆点 1 类；line styles 为 dashed natural 与 solid intervened 两类；无 hatching；约 4 条虚/实 decision-boundary/reference lines；无 uncertainty；估计线宽约 1.5 pt，provenance `rendered_estimate`。
- **布局与编码**：左 panel 从 Input `x` 经 Hidden `h¹/h²` 到 Output `y`，虚线 natural path A/B 和实线 intervened path 1/2 落到 affirming/refuting/untested outputs；右 panel 上下为 Harmless/Pernicious，两组十类别 hue cluster，深色点为 natural、浅色点为 intervened，虚线为 decision boundary，底部另以 orange hidden-circuit region 与 red dormant-behavior region 标出失败机制。
- **颜色**：渲染估计主色 `#004AAD`、`#CB6CE6`（natural paths A/B）、`#FF3131`、`#FF914D`（intervened paths/regions），另有十类 hue、`#CED4D5`、`#A8E7C6`、`#F3E5E9` 等浅色。模式 `mixed`，约 8 个语义色；线型、深浅和区域标签提供冗余，但浅色区灰度不安全。
- **字体**：sans-serif raster estimate，约 8–20 pt（panel heading/annotation 到 axis text），regular/bold，roman/italic；无可追溯图内字体对象，provenance `rendered_estimate`、confidence `medium`。
- **数据与统计**：这是机制示意而非样本统计。右 panel 以 10 类、自然/干预点、决策边界和潜在功能区域说明 divergence 可能 harmless 或 pernicious；没有 n、seed、误差或置信区间。
- **Caption（PDF 逐字提取；空格规范化；187 个空白分词）**：
  > Figure 1: Causal interventions can recruit hidden circuits that produce misleadingly confirmatory or dormant behavior. (a) Consider natural pathways (dashed arrows) for two classes A and B that carry activity to different behavioral outputs y. In a hypothetical intervention meant to find path A, patching h¹ with a divergent representation can activate distinct, hidden pathways (solid arrows) that result in misleadingly confirmatory behavior (orange) and/or undetected behavior (red). (b) Consider 2D projections of the neural activity of h¹ for a different network that classifies states into one of 10 classes (denoted by hue). Suppose that natural representations (dark points) lie within well-defined decision boundaries (dashed lines) and covary along causal axes, and that intervened representations (light points) are constructed by patching the first axis from a sampled natural representation. Although these representations diverge from the natural distribution, this can be harmless (top) or pernicious (bottom) depending on the network’s functional landscape. In particular, it can be pernicious if the network has a functional landscape where intervened activity unknowingly recruits hidden circuits (visualized as an orange region) or crosses dormant behavioral boundaries (red regions), depending on the claims.

  Caption 具备 title、setup、encoding key、comparison、main finding；Figure 1 的标题句直接给出 hidden-circuit/dormant-behavior 机制，粗体 headline 为 `true`，`self_contained=true`，`main_finding_stated=true`。
- **证据关系**：引言的 causal intervention 问题 → 方法对 hidden representation 的 patching → Figure 1 作为 harmless/pernicious divergence 的总览 → Figure 2 的实证 divergence 与 Figure 3 的 CL mitigation。它是机制地图，不是删除组件消融。
- **优点**：用 pathway schematic 与 functional-landscape scatter 把抽象的“分布偏离”连接到行为后果；深浅、虚实线、区域填充和 direct labels 为主要语义提供多重锚点。
- **缺陷**：右侧类别 hue 和浅色区域依赖颜色，灰度下弱；左侧路径和右侧散点没有统一的数值比例；示意性 decision boundary 可能被误读为估计结果，caption 也没有量化数据定义。
- **可复用模式**：先用结构图呈现 intervention 如何改变路径，再用同页的二维分布反例展示 harmless/pernicious 分叉；复用时保留线型/明暗冗余，并把 schematic 与 empirical panel 明确分隔。

## Figure 2 — divergence across interventions

- **位置与职责**：物理页 5，正文 results；`page_width`；类型 `scatter`、`bar`；purpose 为 `headline`、`main_comparison`、`method_interface`、`mechanism`；复杂度 4/5（3 panel，约 3 个 intervention method series，4 个 legend entries，约 12 个注释，点/柱总量约 320 的 rendered estimate）。
- **图形语法**：raster composite；panel (a)/(b) 的 scatter x/y 为 linear，panel (c) 的 x 为 categorical、y 为 linear，整体以 linear 为主；无 grid；底部有 Natural/Intervened 与 EMD/Baseline EMD 的 legend，属于 shared legend；panel title/axis 直接标注；marker 1 类（点）；无 line style、hatching 或 reference line；panel (c) 有 error bars；估计线宽约 1 pt，provenance `rendered_estimate`。
- **布局与编码**： (a) 用 orange natural/source 与 blue patched/intervened，展示替换一个 coordinate 后沿 `Dim1/Dim2` 的几何分离；(b) 三个小 PCA scatter 分别是 Mean Difference Patching、Sparse Autoencoder、Boundless DAS，orange/blue 对应 natural/intervened；(c) 三组方法的 bars 比较 EMD 与 natural-to-natural Baseline EMD，黑色小 error bars 位于柱顶。panel labels 和方法标题承担 facet/text encoding。
- **颜色**：`#FF7B02`（natural/source）、`#3565FF`（intervened）、`#4B9E99`（EMD）、`#A4D7BE`（baseline），模式 `mixed`，4 色；legend、panel title、scatter-vs-bar 形状有冗余，灰度安全性仍低。
- **字体**：raster 中为 sans-serif/DejaVu-like estimate，约 6–12 pt，regular/bold，roman；provenance `rendered_estimate`、confidence `medium`。
- **数据与统计**： (a) 是 coordinate patch 几何；(b) 将三种 intervention 的 residual-stream natural/intervened vectors 合并做 PCA 后画前两主成分；(c) 同时给 paired vector 的 L2 distance 与分布 EMD，并用 natural-vs-natural baseline 校准 EMD。error bar 的聚合单位/定义没有在 caption 中说明。
- **Caption（PDF 逐字提取；空格规范化；137 个空白分词）**：
  > Figure 2: Representational divergence is a common occurrence across various interventions. (a) Directly replacing a coordinate value in one natural representation (orange) with the value from another will eventually create divergent representations (blue). (b) Top two principal components of natural and corresponding intervened representations, taken from the residual stream at the intervention position and with PCA is performed over the combined set of natural and intervened vectors, for three popular causal intervention techniques: a replication of Feng & Steinhardt (2024) for mean difference patching, reconstructed vectors for a single transformer layer using SAELens (Bloom et al., 2024) for sparse autoencoder, and interchange interventions for Boundless DAS (Wu et al., 2023). (c) L2 distance between natural and corresponding intervened representations, and Earth Mover’s Distance (EMD) between natural and intervened distributions (with baseline comparing the natural distribution to itself).

  Caption 具备 title、setup、encoding key、comparison、main finding、abbreviation definition；标题句为 bold headline，`self_contained=true`，`main_finding_stated=true`，但 PCA 语法的语法错误和 error bar 定义缺失降低复现性。
- **证据关系**：Figure 1 的潜在 divergence 机制 → 三类实际 causal intervention → Figure 2 的 coordinate/PCA/EMD 对照 → Figure 3 的 CL loss 作为降低 divergence 的方法。它是主比较和机制桥，不是组件消融。
- **优点**：从局部 coordinate patch 到跨方法 PCA 再到分布指标，三 panel 形成由几何到统计的证据链；baseline EMD 给出可解释的零偏离参照。
- **缺陷**：三种方法/两种状态在小型 raster panel 中主要靠颜色与 legend；柱顶 error bars 没有定义，panel (c) 轴和分布/paired distance 的统计单位需回看正文；合成图放大后像素化。
- **可复用模式**：将 intervention geometry、低维投影和 summary metric 纵向串联；同时提供 paired distance 与 distribution baseline，并在图注显式定义重复单位、误差和 PCA 样本集合。

## Figure 3 — CL loss and OOD generalization

- **位置与职责**：物理页 10，正文 results；`page_width`；类型 `scatter`、`line`、`bar`、`conceptual_diagram`；purpose 为 `headline`、`method_interface`、`main_comparison`、`mechanism`、`robustness`、`ablation`；复杂度 5/5（6 panel，10 类 hue 与 IIA/EMD/CL/DAS 多系列，约 12 legend items，约 25 注释，数据 marks rendered estimate 约 500）。
- **图形语法**：raster composite；(a)/(d)/(e) 的 scatter x/y 为 linear，(b) 的 x 是 CL weight ε（log-like numeric progression）/y 为 IIA/EMD 双 linear axes，(f) categorical bars/y linear，整体 `x_scale=linear`、`y_scale=linear` 只是主导估计；无 grid；底部/面板内有 shared legends，direct labels 为 panel letters、标题和轴；marker 1 类；line styles 1 类（solid curves/bands）；无 hatching/reference line；(b) 有 band，(f) 有 error bars，故 uncertainty 为 `multiple`；估计线宽 3–4 pt，provenance `rendered_estimate`。
- **布局与编码**： (a) 两个 CL 权重下的 natural/intervened PCA；(b) IIA orange 与 divergence purple 随 ε 变化；(c) source/target representation、DAS patch 与 CL average 的 construction diagram；(d)/(e) 十类 synthetic causal dimensions，分别是 behavior-only DAS 与 CL-only DAS，深/浅点表达 natural/intervened；(f) Cross Task IIA 的 DAS/CL 对齐矩阵比较，黑色 error bars。
- **颜色**：`#FF9633`（IIA/natural）、`#3666FF`（intervened）、`#C7D1F8`/`#FDD9C4`（bands/light states）、`#98DAA7`（CL/DAS bars）、`#C7EBED`、`#FFF9CA`、`#DDA5BC` 等 synthetic hue，约 8 个代表色，模式 `mixed`；dark/light、point-vs-line、panel labels 提供部分冗余，灰度安全性低。
- **字体**：sans-serif raster estimate，约 7–14 pt，panel letters/title 为 bold，普通 axis text regular，math/ε 为 italic；provenance `rendered_estimate`、confidence `medium`。
- **数据与统计**： (a) 比较相同最终 IIA 的两种 CL 权重；(b) 从 Section 5.1 的 intervention 得 IIA 与 divergence；(d)/(e) 是十类 synthetic task 的 natural/intervened causal-feature projections；(f) 是 one-task trained、other-task evaluated 的 Cross Task IIA。bars 的 error bars 与 (b) band 没有在 caption 中定义其 SE/重复单位。
- **Caption（PDF 逐字提取；空格规范化；198 个空白分词）**：
  > Figure 3: The CL loss reduces representational divergence and can improve out-of-distribution generalization. (a) PCA of natural (orange) and intervened (blue) representations in the Boundless DAS setting presented in Wu et al. (2023) for two CL loss weightings with the same final IIA. (b) IIA (orange) and divergence (purple) of intervened representations from Section 5.1 as a function of CL loss weight (ε). (c) Diagram of CL loss; rectangles are model representations and x₁ and x₂ are deterministic values of the representations along the two synthetic causal dimensions shown in panels (d) and (e). We patch the x₂ value from source to target using DAS and define the CL representation as the average of all natural representations that possess the same variable values as the post-intervention representation. (d) and (e) two causal feature dimensions of representations from a synthetic dataset consisting of ten classes (colors), with both natural (dark) and intervened (light) representations shown. (d) shows results from DAS trained using behavior only, (e) shows DAS trained using only the CL loss. (f) performance of alignment matrices trained on one task and evaluated on another that uses the same causal dimensions. CL loss leads to higher OOD performance.

  Caption 具备 title、setup、encoding key、comparison、main finding、abbreviation definition；标题句为 bold headline，`self_contained=false`（IIA/OOD/DAS 未全部展开且依赖 Section 5.1/Wu et al.），`main_finding_stated=true`。
- **证据关系**：Figure 2 的 divergence observation → CL construction 与 Boundless DAS interface → Figure 3 的 weight sweep、synthetic causal geometry 和 cross-task IIA → 附录 Figure 5–9 的 task/超参数与 regression 支撑。它把方法、主比较和 CL ablation 放在一个对象中。
- **优点**：同一 figure 同时给出方法 construction、representation geometry、metric trade-off 和 transfer outcome；natural/intervened 与 DAS/CL 的颜色和明暗在主结构上连贯。
- **缺陷**：六 panel 信息密度过高且小 raster 字体难读；(b)/(f) 的不确定性没有定义；十类别 hue 与多条曲线对灰度不稳健；caption 对 Section 5.1 的依赖降低独立可读性。
- **可复用模式**：把“方法如何生成表示—表示偏离如何变化—任务外结果是否改善”按阅读顺序排成 composite，并让一个独立的 ablation panel 给出最终效用；复用时应把误差定义和缩写放入图注。

## Figure 4 — additional divergence measures

- **位置与职责**：物理页 22，附录 A.1；`page_width`；类型 `bar`；purpose 为 `mechanism`、`robustness`、`qualitative_evidence`、`reproduction`；复杂度 4/5（8 panel，natural/intervened 2 series，每 panel 2 legend entries，约 8 panel labels，约 48 bars）。
- **图形语法**：raster 2 × 4 grouped bars；x categorical（DAS、Mean Diff、SAE），y linear，八个 panel 的 y metric 由 panel title/axis 给出；无 grid；Natural/Intervened legend 在 panel 内重复而非 shared；direct labels 为八个 metric 标题；无 marker/line style/hatching/reference line；可见小 error bars，`uncertainty_display=error_bar`；估计线宽约 1 pt，provenance `rendered_estimate`。
- **布局与编码**：上排为 Nearest Cos Distance、Nearest L2 Distance、Min Cos Pairing、EMD；下排为 Min L2 Pairing、Local Reconstruction、Local PCA Distance、KDE。每个 intervention 方法有 Natural 深紫柱和 Intervened 蓝柱，柱顶黑色误差线。
- **颜色**：深紫 `#474473`、蓝 `#446791`，模式 `categorical`，2 色；没有 shape/line 的额外状态编码，legend 只命名颜色，`redundant_encoding=false`、`grayscale_safe=false`。
- **字体**：默认 Matplotlib/DejaVu-like sans-serif rendered estimate，约 6–10 pt，regular，roman；provenance `rendered_estimate`、confidence `medium`。
- **数据与统计**：每个 metric 在 random natural vectors 上估计 natural manifold，并对 ground-truth paired intervened/natural vectors 比较；包含 nearest cosine、nearest L2、exclusive min-cost pairing、local PCA、EMD、KDE、local linear reconstruction 等。图注描述平均/归一化方式，但未定义 error bars 的统计量、样本数或 seed。
- **Caption（PDF 逐字提取；空格规范化；276 个空白分词）**：
  > Figure 4: A number of additional divergence measures to demonstrate the difference between the natural and intervened distributions. Each is labeled by its y-axis. Each metric is computed over a random sample of natural vectors to simulate the natural manifold, and a sampled set of intervened or natural vectors for which to measure the distance from the natural distribution. We refer to this distribution as the "compared" distribution. The sampled intervened and natural vectors are always the "ground-truth pair" described at the beginning of Appendix A.1. Nearest Cosine Distance: refers to the cosine distance to the nearest sample in the natural manifold. Multiple sampes in the compared distribution can share the same natural sample. This value is averaged over all compared samples. Nearest L2 Distance refers to the cosine distance to the nearest sample in the natural manifold. Multiple sampes in the compared distribution can share the same natural sample. This value is averaged over all compared samples. Min Cos Pairing refers to the lowest cost pairing where cost is the cosine distance between two samples. Vector pairs are exclusive. This value is normalized by the number of samples. Min L2 Pairing refers to the lowest cost pairing where cost is the L2 distance between two samples. Vector pairs are exclusive. This value is normalized by the number of samples. Local PCA Distance refers to the distance to the manifold created using a local PCA of the nearest neighbors. See Appendix A.1.2. EMD refers to the Earth Mover’s Distance. See Appendix A.1.2. KDE refers to the Kernel Density Estimation score. See Appendix A.1.2. Local Linear Reconstruction refers to the local linear reconstruction error. See Appendix A.1.2.

  Caption 有 title、setup、encoding key、abbreviation/appendix pointers，但没有 bold headline、明确 main finding 或 error-bar definition；`headline_bold=false`、`self_contained=false`、`main_finding_stated=false`。PDF 中的拼写 `sampes` 和把 Nearest L2 描述为 cosine distance 原样保留。
- **证据关系**：Figure 2(c) 的 L2/EMD 主比较 → Appendix A.1.2 的 manifold/divergence 定义 → Figure 4 的八种 metric 交叉检查。它是 robustness/reproduction evidence，不是新方法消融。
- **优点**：统一 2 × 4 small multiples 让 natural/intervened 差异在多个 metric 上横向比较；metric 名直接置于 y-axis，避免额外 legend。
- **缺陷**：标题/刻度小且颜色是唯一的状态编码；error bars 没有统计定义；caption 很长但没有在图内给出 sample size/重复单位，且存在原文 typo 和 metric 描述冲突。
- **可复用模式**：用多指标 small multiples 检查一个 divergence claim 是否依赖单一度量；复用时统一 y-axis 语义、给出误差聚合规则，并以纹理/边框补足颜色编码。

## Figure 5 — synthetic task partitions

- **位置与职责**：物理页 23，附录 A.5.4；`single_column`；类型 `scatter`、`qualitative_grid`；purpose 为 `dataset`、`experimental_design`、`method_interface`、`qualitative_evidence`；复杂度 3/5（2 panel，2 partition series，2 legend items，约 10 个 cluster/annotation，约 500 点的 rendered estimate）。
- **图形语法**：图像片段与 vector SFNS 标签混合；x/y 为 linear `Causal Dim 1/2`，无 grid；左/右各有 partition legend，非 shared；cluster/标题为 direct labels；marker 为点 1 类，无 line style、hatching、reference line、uncertainty；估计线宽约 0.8 pt，provenance `mixed`（图形栅格 + PDF text object）。
- **布局与编码**：左 panel `Default Task`，右 panel `OOD Task`；Default 的 Partition One/Two 各 withholding 两个对方包含的 classes；OOD 的 Sparse/Dense 各含 4 classes，Dense 是更紧 cluster，Sparse 更分散。十类状态以 cluster hue 显示，partition 本身用粉/蓝色 legend。
- **颜色**：PDF 对象可见 `#DDA5BC`（粉）与 `#A1C0D8`（蓝），模式 `categorical`，2 色；partition label/legend 提供语义但没有 shape 冗余，`redundant_encoding=false`、`grayscale_safe=false`。
- **字体**：PDF vector text 的 `SFNS`，约 8–10 pt，regular、roman；provenance `pdf_object`、confidence `high`。
- **数据与统计**：Default task 为 10 类 synthetic grid 的两个互补训练 partitions，OOD task 为两个不重叠的 4-class partitions，另有 2 类完全排除；图只呈现几何和类别分布，不报告点数、噪声参数或误差。
- **Caption（PDF 逐字提取；空格规范化；56 个空白分词）**：
  > Figure 5: Visualization of the different synthetic tasks used for Figure 3. The Default Task is split into two partitions, both withholding two classes that are contained in the other partition. The OOD task is also split into two partitions, both consisting of 4 classes. The Dense partition consists of a tighter cluster than the Sparse.

  Caption 有 title、setup、encoding key、comparison、main finding，`headline_bold=false`、`self_contained=true`、`main_finding_stated=true`；“Dense tighter than Sparse”是明确可读结论。
- **证据关系**：Figure 3 的 synthetic OOD claim → Appendix A.5.4 的 partition construction → Figure 5 提供两种任务几何的直接视觉检查 → Figure 6–9 的 hyperparameter/Row EMD 结果。它是实验设计证据，不是消融。
- **优点**：以极少文字显示训练/held-out partition 的几何差异，为 Figure 3 的 CL transfer setting 提供必要上下文；Dense/Sparse 结论直接可见。
- **缺陷**：自然类别 hue 与 partition 色语义叠加，灰度和小尺寸阅读成本高；图像片段的清晰度低于相邻矢量图；caption 未重述两组具体 class ID。
- **可复用模式**：在主结果前提供 synthetic split 的几何“data card”，让 OOD 结果不必由读者从实验文字反推；保持 partition legend 独立于 class hue。

## Figure 6 — OOD IIA hyperparameter grid

- **位置与职责**：物理页 24，附录 A.5.5；`page_width`；类型 `bar`；purpose 为 `robustness`、`efficiency_cost`、`ablation`、`main_comparison`；复杂度 5/5（9 panel，5 个 CL epsilon bar series 加 behavior-only baseline，5 legend items，约 9 个 row/column annotations，约 90 bars）。
- **图形语法**：vector 3 × 3 grouped bar grid；x categorical（CL 与 DAS+CL），y linear Cross Task IIA（约 0.6–1.0）；无 grid；一个底部 shared legend；row/column title 与 y/x labels 为 direct labels；无 marker，1 种 baseline line style（pink dashed），无 hatching，1 条 reference line/带；bars 有 error bars，baseline 有 translucent band，故 `uncertainty_display=multiple`；source cell 78/渲染估计线宽约 1 pt，provenance `mixed`（PDF vector + source plotting logic）。
- **布局与编码**：行是 extra_dims=0/16/64，列是 learning rate=0.005/0.01/0.05；每 panel 比较 CL 与 DAS+CL，五种 ε level 用 pastel hue；粉色 dashed horizontal line/band 是 behavior-only DAS。y 轴为 OOD validation trained-task partition 的 Cross Task IIA。
- **颜色**：约 6 色：`#98DAA7`、`#C0EBE9`、`#D3C3F6`、`#F2AAA7`、`#FAC8EC`（五种 CL epsilon）和 `#F14CC1`（baseline）；模式 `mixed`，颜色数量 6；baseline 的虚线/带提供冗余，但 epsilon 级别仍主要依赖 hue，`grayscale_safe=false`。
- **字体**：PDF XML/对象为 `DejaVu Sans`，约 11.3 pt（刻度）至 16 pt（row/column title），regular、roman；provenance `pdf_object`、confidence `high`。
- **数据与统计**：Cross Task IIA 随 DAS learning rate、extra noisy input dimensions、CL epsilon 的九格敏感性；DAS+CL bars 包含 behavioral loss，CL bars 不含 behavioral loss；误差线和 baseline band 的 SE/重复单位未在 caption 定义。
- **Caption（PDF 逐字提取；空格规范化；69 个空白分词）**：
  > Figure 6: Out of distribution hyperparameter search showing the DAS IIA on OOD validation data for the trained task partition. We see the DAS loss learning rate (lr) and extra concatenated noisy input dimensions (extra_dim) across the panel columns and rows. The DAS+CL reported values include the behavioral loss whereas the CL label excludes the behavioral loss. The pink dashed lines represent DAS trained with the behavioral loss only.

  Caption 有 title、setup、encoding key、comparison、main finding、abbreviation definition；无粗体 headline，`self_contained=false`（IIA/OOD/DAS/lr 未完整展开且 error bars 未定义），`main_finding_stated=true`（超参数搜索与 baseline 语义）。
- **证据关系**：Figure 3 的 CL-vs-DAS OOD outcome → Appendix A.5.5 的 learning-rate/noisy-dimension search → Figure 6 检查 Cross Task IIA robustness。它是实用超参数 ablation，不是新的 causal mechanism。
- **优点**：把两个训练超参数与 CL epsilon 正交放进统一 3 × 3 grid；behavior-only baseline line 让“加入 CL 是否超过行为训练”有固定参照。
- **缺陷**：五个 pastel series 对色觉/灰度不友好；error bars/band 未定义；标题和刻度虽为矢量仍偏小，caption 的 `extra_dim` 与 row label `extra_dims` 命名不完全一致。
- **可复用模式**：用 row/column hyperparameter grid 取代多张独立图，并用一条 baseline line 跨 panel 建立一致参照；复用时给 series 加 marker/纹理，直接写出误差统计和实验重复数。

## Figure 7 — in-distribution IIA hyperparameter grid

- **位置与职责**：物理页 25，附录 A.5.5；`page_width`；类型 `bar`；purpose 为 `robustness`、`efficiency_cost`、`ablation`、`main_comparison`；复杂度 5/5（9 panel，5 epsilon series，5 legend items，约 9 annotations，约 90 bars）。
- **图形语法**：与 Figure 6 相同的 vector 3 × 3 grouped-bar grammar：x categorical CL/DAS+CL，y linear Trained Task IIA；无 grid；底部 shared legend；direct labels；无 marker/hatching，1 条 pink dashed baseline/reference；bar error bars + baseline band，uncertainty `multiple`；线宽约 1 pt，provenance `mixed`。
- **布局与编码**：行 extra_dims=0/16/64，列 lr=0.005/0.01/0.05；五种 CL epsilon 用 pastel bars，pink dashed/filled band 为 behavior-only DAS；y 轴换成 trained-task IIA。
- **颜色**：复用 Figure 6 的 `#98DAA7`、`#C0EBE9`、`#D3C3F6`、`#F2AAA7`、`#FAC8EC` 与 baseline `#F14CC1`；`mixed`、6 色、baseline line 提供有限冗余、灰度不安全。
- **字体**：`DejaVu Sans`，约 11.3–16 pt，regular/roman，PDF object provenance、confidence high。
- **数据与统计**：同一超参数九格下的 trained-task IIA，DAS+CL 含 behavioral loss、CL label 排除 behavioral loss；图中 error bars 的统计来源/样本单位仍未定义。
- **Caption（PDF 逐字提取；空格规范化；68 个空白分词）**：
  > Figure 7: In distribution hyperparameter search showing the DAS IIA on in-distribution validation data for the trained task partition. We see the DAS loss learning rate (lr) and extra concatenated noisy input dimensions (extra_dim) across the panel columns and rows. The DAS+CL reported values include the behavioral loss whereas the CL label excludes the behavioral loss. The pink dashed lines represent DAS trained with the behavioral loss only.

  Caption 有 title、setup、encoding key、comparison、main finding、abbreviation definition；`headline_bold=false`、`self_contained=false`、`main_finding_stated=true`。它与 Figure 6 的结构几乎完全同构，主要改变 validation regime。
- **证据关系**：Figure 6 的 OOD robustness 对照 → Figure 7 的 in-distribution control → Figure 3 的 OOD generalization 解释。它是 ablation/robustness control。
- **优点**：保持 Figure 6 的 row/column/series contract，使 OOD 与 in-distribution 差异可归因于 validation regime；共享 baseline 语法降低比较成本。
- **缺陷**：同样的色彩依赖和未定义误差；三行 panel 的底部 x labels 与小字体增加扫描成本；caption 只说 “in distribution” 而未在图内直接标出 train/test partition 细节。
- **可复用模式**：将关键 OOD 结果配一个完全同构的 in-distribution control grid，不改变视觉编码，只改变数据切片并在标题明确 regime。

## Figure 8 — OOD row-space EMD grid

- **位置与职责**：物理页 26，附录 A.5.5；`page_width`；类型 `bar`；purpose 为 `robustness`、`efficiency_cost`、`ablation`、`mechanism`；复杂度 5/5（9 panel，5 epsilon series，5 legend items，约 9 annotations，约 90 bars）。
- **图形语法**：vector 3 × 3 grouped bars；x categorical CL/DAS+CL，y linear Cross Row EMD（约 0–0.15）；无 grid；底部 shared legend；direct labels；无 marker/hatching，1 条 pink dashed reference/baseline band；bars error bars + band，uncertainty `multiple`；线宽约 1 pt，provenance `mixed`。
- **布局与编码**：行 extra_dims=0/16/64，列 lr=0.005/0.01/0.05；五个 CL epsilon hue，DAS behavior-only baseline 用 dashed pink line/band；指标是 OOD validation 的 causal row-space EMD。
- **颜色**：复用 Figure 6/7 六色 pastel/pink palette；颜色模式 `mixed`，6 色；虚线/带与柱形是有限冗余，灰度不安全。
- **字体**：`DejaVu Sans`，约 11.3–16 pt，regular/roman，PDF object provenance、confidence high。
- **数据与统计**：Row EMD 是附录定义的 causal dimensions-only EMD，值按 extra noisy dimensions 缩放；DAS+CL 与 CL 的 behavioral-loss inclusion 同 Figure 6；误差线/带仍未定义。
- **Caption（PDF 逐字提取；空格规范化；70 个空白分词）**：
  > Figure 8: Out of distribution hyperparameter search showing the DAS row-space EMD on OOD validation data for the trained task partition. We see the DAS loss learning rate (lr) and extra concatenated noisy input dimensions (extra_dim) across the panel columns and rows. The DAS+CL reported values include the behavioral loss whereas the CL label excludes the behavioral loss. The pink dashed lines represent DAS trained with the behavioral loss only.

  Caption 有 title、setup、encoding key、comparison、main finding、abbreviation definition；`headline_bold=false`、`self_contained=false`、`main_finding_stated=true`。
- **证据关系**：Figure 6 的 OOD IIA robustness → Figure 8 的同一 grid 上 Row EMD mechanism readout → Figure 2/3 关于 divergence 与 OOD utility 的桥接。它是 mechanism/ablation support。
- **优点**：把性能 grid 与 representation-level Row EMD 对齐，读者可在相同 lr/extra-dimension 条件下比较行为与表示指标；baseline line 跨 panel 可追踪。
- **缺陷**：EMD 的量纲/缩放只在附录文字解释；误差定义缺失；pastel epsilon levels 和小字号阻碍精细比较；bar height 接近零时误差线重叠。
- **可复用模式**：复制性能超参数 grid 的布局，仅替换 y estimand，以形成 outcome-to-mechanism paired analysis；必须在图注写出缩放、单位和误差聚合。

## Figure 9 — in-distribution row-space EMD grid

- **位置与职责**：物理页 27，附录 A.5.5；`page_width`；类型 `bar`；purpose 为 `robustness`、`efficiency_cost`、`ablation`、`mechanism`；复杂度 5/5（9 panel，5 epsilon series，5 legend items，约 9 annotations，约 90 bars）。
- **图形语法**：与 Figure 8 同构的 vector grouped-bar grid：x categorical CL/DAS+CL，y linear Trained Row EMD（约 0–0.15）；无 grid；底部 shared legend；direct labels；无 marker/hatching，pink dashed reference/band 1 条；error bars 与 band 为 `multiple` uncertainty；约 1 pt，provenance `mixed`。
- **布局与编码**：row extra_dims、column lr、bar hue CL epsilon、pink behavior-only DAS baseline 均保持 Figure 8；y 轴换成 validation trained partition 的 Row EMD。
- **颜色**：复用 Figures 6–8 的五级 pastel 与 `#F14CC1` baseline；6 色、`mixed`，baseline 线提供有限冗余，灰度不安全。
- **字体**：`DejaVu Sans`，约 11.3–16 pt，regular/roman，PDF object provenance、confidence high。
- **数据与统计**：在 in-distribution validation 上报告 trained Row EMD；同一 CL/DAS+CL、lr、extra_dims、epsilon 组合和 behavior-only baseline；error bars/band 的统计定义未给出。
- **Caption（PDF 逐字提取；空格规范化；68 个空白分词）**：
  > Figure 9: In distribution hyperparameter search showing the DAS row-space EMD on validation data for the trained task partition. We see the DAS loss learning rate (lr) and extra concatenated noisy input dimensions (extra_dim) across the panel columns and rows. The DAS+CL reported values include the behavioral loss whereas the CL label excludes the behavioral loss. The pink dashed lines represent DAS trained with the behavioral loss only.

  Caption 有 title、setup、encoding key、comparison、main finding、abbreviation definition；`headline_bold=false`、`self_contained=false`、`main_finding_stated=true`。
- **证据关系**：Figure 8 的 OOD Row EMD grid → Figure 9 的 in-distribution control → Figure 3 的 CL/OOD mechanism claim。它是 paired robustness/ablation evidence。
- **优点**：与 Figure 8 完全同构，隔离 validation regime 对 representation metric 的影响；保持 baseline line 与 epsilon palette，使 cross-page comparison 直接。
- **缺陷**：同 Figure 8 的误差定义、颜色依赖和小字体问题；caption 的 “on validation data” 没有明确写 in-distribution，但标题和上下文可恢复；无法仅凭图区分每个柱的 seed 数。
- **可复用模式**：将 metric 的 OOD/in-distribution control 做成同构 grid，避免每张图引入新的颜色或布局；在图中直接标明数据 split。

## Table 清单

### Unnumbered OLS summary

- **位置与职责**：物理页 21，附录 A.6；`single_column`；这是 PDF 中唯一 table-like object，label 为 `Unnumbered OLS summary`，purpose 为 `mechanism`、`robustness`、`reproduction`。它没有 `Table N` caption，只有 statsmodels 的 `OLS Regression Results` header，因此 JSON 的 caption 文本明确标记“no explicit caption”。
- **表头/布局**：Nimbus Roman 的单层 coefficient header：`coef`、`std err`、`t`、`P>|t|`、`[0.025`、`0.975]`；左侧 row label；三组内容为 fit summary、coefficient rows、residual diagnostics，约 15 data rows、最多 7 columns。横向只有 partial horizontal rules，属于 `minimal`，无 cell color；bold 用于模型 header、row labels 和 section-like labels。
- **Typography**：PDF object 为 `Nimbus Roman No9 L`，body/header 约 10 pt，header weight bold，regular/italic math 混用；provenance `pdf_object`、confidence high。
- **数据与统计**：因变量 IIA、自变量 Training EMD；30 trainings（two tasks × two partitions × five seeds × three alignment trainings），OLS fit：R-squared 0.729、Adj. R-squared 0.719、F-statistic 75.28；Training EMD coefficient −0.3424、std err 0.039、t −8.677、P=0.000、区间 [−0.423, −0.262]；同时显示 Omnibus/Jarque–Bera/Durbin–Watson/condition number 等 diagnostics。常规 nonrobust standard errors 与 coefficient intervals 被完整印出，但没有图形化 residual/分组信息。
- **Caption/自包含性**：`Unnumbered OLS summary (no explicit caption in PDF).`（8 个空白分词；无 caption headline、无直接 main finding），`headline_bold=false`、`self_contained=false`、`main_finding_stated=false`；解释 EMD→IIA 的意义依赖 A.6 前文和 Figure 3。
- **证据关系**：Figure 3 的 divergence/OOD IIA 主张 → Appendix A.6 的 linear regression → OLS summary 提供跨 30 个 training observations 的定量关系。它是 mechanism/robustness support，而非独立 ablation。
- **优点**：把 sample size、coefficient、standard error、fit 和 residual diagnostics 放在一个紧凑标准化输出中；Training EMD 行和负系数直接承载方向性证据。
- **缺陷**：无编号、无显式 caption、无单位和无分组列； conventional intervals/p-values 的解释依赖正文；多位小数和诊断行挤在窄表内，读者无法从表中判断 Default/OOD 或 alignment type 的异质性。
- **可复用模式**：当需要把一个机制回归作为附录审计证据时，保留系数/不确定性/样本数/诊断的完整 block，但增加明确 Table 标题、变量单位、分组或 paired plot，并说明标准误定义。

## 跨对象系统判断

- **视觉叙事**：Figure 1 的 harmless/pernicious mechanism map → Figure 2 的 intervention geometry/PCA/EMD → Figure 3 的 CL loss mechanism 与 OOD IIA → Figure 4 的多 metric robustness → Figure 5 的 synthetic partition design → Figures 6–7 的 IIA hyperparameter controls → Figures 8–9 的 Row EMD mechanism controls → A.6 OLS summary。主线是“干预表示偏离—潜在行为后果—CL mitigation—任务外效用—超参数和回归验证”。
- **Caption 系统**：Figure 1–3 使用 bold conclusion-style opening，直接说 hidden circuits/divergence/CL generalization；Figure 4–9 多为 regular descriptive opening，F6–9 用固定句式解释 row/column/baseline。Figure 2/3/4/6–9 交代了颜色或 baseline，但 error bars/bands 没有统计定义。Figure 4 caption 有 `sampes` 与 Nearest L2/cosine 描述冲突；F6–9 的缩写、split 和 extra_dim 依赖附录上下文；OLS 没有正式 caption。
- **表头系统**：只有一个 statsmodels OLS 输出，使用一层 coefficient header、fit/coefficient/diagnostic blocks 和 partial rules；没有与 Figure 2/3 的 EMD/IIA 对应的显式表格列，也没有统一的 `n`/uncertainty footer。
- **方法—结果—消融链接**：Figure 1/2 建立 intervention 与 divergence 的机制/现象；Figure 3 把 CL representation construction、DAS comparison、OOD IIA 汇总；F6/7 是 trained/OOD IIA 的超参数与 behavior-only baseline ablation，F8/9 是同 grid 的 Row EMD mechanism readout；OLS summary 将 Training EMD 与 IIA 做跨训练关联。没有传统的组件删除实验，CL-vs-behavior-only 更接近 loss-term ablation。
- **正文—附录链接**：正文 Figure 1–3 承担 headline；附录 A.1 Figure 4 定义 divergence alternatives，A.5.4 Figure 5 定义 synthetic split，A.5.5 Figures 6–9 扩展超参数/Row EMD，A.6 OLS 解释 EMD→IIA。Figure 4–9 的 caption 多次把定义推回 Appendix，正文阅读需要来回跳转。
- **Typography consistency**：主图 1–4 的 raster sans-serif estimate、Figure 5 的 SFNS vector labels、F6–9 的 DejaVu Sans vector typography 与 Nimbus Roman captions/OLS 并存。图内 sans-serif 视觉相近，但字号范围从主图约 6–14 pt 到 F6–9 约 11–16 pt；图形来源不统一造成 Figure 5 与邻近对象的渲染质感差异。
- **Color consistency**：natural/intervened 的 orange/blue 语义在 Figures 1–3、Figure 5 重复；purple/teal/mint 承担 EMD/baseline；F6–9 用 pastel epsilon + pink behavior-only baseline。语义有跨图延续，但十类 hue 和五级 pastel series 仍以颜色为主，灰度/色觉不稳健。

## 最终判断

### 最可复用模式

1. **机制示意 + 几何反例**：Figure 1 将 pathway intervention 与 harmless/pernicious functional landscape 并置，适合把抽象 failure mode 变成可见机制。
2. **几何—分布指标—效用三段链**：Figure 2/3 依次把 paired geometry、PCA/EMD、IIA/OOD outcome 连起来，避免单一 summary metric 独自承担机制结论。
3. **同构 paired grids**：Figures 6–9 固定 row/column/series/baseline contract，仅交换 OOD/in-distribution 与 IIA/Row EMD，适合做可归因的 robustness 与 mechanism control。

### 最高价值对象

- **Figure 1**：全篇最强的 headline mechanism map，把 divergence 的可能后果变成可读的路径与功能景观。
- **Figure 2**：最清楚的现象/方法桥，覆盖 coordinate patch、三种 intervention PCA 与 EMD baseline。
- **Figure 3**：核心方法—效用 composite，直接把 CL loss、divergence、OOD IIA 和 synthetic task 放入一个证据闭环。
- **Figures 6/8**：在附录中分别展示 IIA 与 Row EMD 的 OOD hyperparameter/control 关系，是评估主张稳健性的最高信息密度对象。

### 失败模式

- Figure 1–4 的 raster composite 与 Figure 5 的图像片段在放大时像素化；F6–9 虽为 vector，panel/legend 仍偏小。
- 多类 scatter、五级 pastel bars 和多系列主图主要依赖颜色；灰度、色觉异常和打印条件下缺乏足够 shape/line/texture 冗余。
- Figure 2(c)、Figure 3(b/f)、Figure 4、Figure 6–9 的 error bars/bands 没有明确定义统计量、重复单位或 seed，OLS 也只有 nonrobust interval block 而无分组可视化。
- Caption/标记存在局部会改变解释的质量问题：Figure 4 的 `sampes` 与 Nearest L2/cosine 文字，F6–9 的 split/缩写依赖和 `extra_dim` 命名漂移，以及未编号、无正式 caption 的 OLS summary。
- 自动 visual inventory 把公开源误判为不存在；必须以 PDF 明确链接和仓库元数据验证候选，不能把相似文件名或公开 notebook 输出当作 exact final source。

### 一句话视觉策略

用 pathway 机制图、natural/intervened 几何与 EMD/IIA paired grids 把表示偏离推进到 CL mitigation 和 OOD 效用，但需要减少颜色依赖、补齐误差定义、修正文案并给唯一回归表增加正式标题与分组语义。
