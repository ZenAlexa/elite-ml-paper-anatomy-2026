# icml-2026-a6577855483d 视觉审计

## 审计边界与事实源

- **论文**：*Loss-aware distributionally robust optimization via trainable optimal transport ambiguity sets*，Jonas Ohnemus、Marta Fochesato、Riccardo Zuliani、John Lygeros。
- **PDF 事实源**：`/Users/zimingwang/Developer/GitHub/elite-ml-paper-anatomy-2026/corpus/preprints/icml-2026-a6577855483d.pdf`。PDF 共 36 个物理页：正文 p.1–10，参考文献 p.10–13，附录 p.14–36。`readings/icml-2026-a6577855483d.json` 与对应 Markdown 只作为起始清单和语义交叉核对；Figure/Table 数量、标签和页码以 PDF 为准。
- **完整性核对**：对 36 页执行 `pdftotext -layout`，并以 200 dpi（每页 1700×2200 px）渲染全部页面。逐页核对后，PDF 含 **15 幅 Figure、3 张 Table**；正文为 Figure 1–5，附录为 Table 1–3 和 Figure 6–15。p.7 的 Algorithm 1、p.29 的 Algorithm 2 是算法块而不是 schema 中的 Figure/Table，因此不进入 `figures` 或 `tables` 数组，但在对象清单和证据链中保留。
- **含对象页面**：Figure 1 在 p.4；Figures 2、3、4、5 分别在 p.8、p.9、p.9、p.10；Table 1 在 p.29；Table 2 在 p.30；Figure 6、Table 3、Figure 7 均在 p.31；Figures 8、9、10、11、12、13、14、15 分别在 p.32、p.33、p.33、p.34、p.34、p.35、p.36、p.36。其余页面没有漏登记的 Figure/Table。
- **渲染与字体**：`pdfimages -list` 未发现全尺寸嵌入图像，仅 p.36 有 8×162 与 9×162 的微小索引图像对象；图和表主体均为 PDF 矢量对象。`pdffonts` 显示 Nimbus Roman No9 L、Computer Modern（CMR/CMMI/CMSY/CMEX）、Nimbus Roman No9 L italic/bold 和少量 Nimbus Mono；图内数学标记沿用 Computer Modern。颜色和字号的视觉数值均标为 `rendered_estimate`，字体对象证据标为 `pdf_object`。
- **caption 异常保留**：Figure 8 的 caption 写作 “Gaussian reference distribution” 但又写 “underlying true discrete distributions”；Figure 12 相邻正文把 squared error 称为 “absolute error”。审计记录 PDF 的实际 caption 和版面，不替作者改写。

## 公共视觉源核查

- PDF p.2 的 Software 段明确链接 `https://github.com/JonasOhn/trainable-ot-dro`。`gh repo view` 的仓库描述为 trainable optimal-transport ambiguity sets 的端到端 bilevel pipeline，README 标题、作者和 ICML 2026 论文标题均与本论文直接对应，默认分支为 `main`。
- 递归 GitHub tree 含 `examples/linreg_dro.py`、`examples/portfolioopt_discrete.py`、`examples/portfolioopt_gaussian.py`、`examples/portfolioopt_gaussianmixture.py`，以及 `src/trainable_ot_dro/utils/ellipses.py` 等实现文件。示例脚本只生成各自的单个双 y 轴折线 demo，`results/` 输出被 gitignore；没有与 PDF Figure 1–15 一一对应的多面板 figure/plot 脚本、表格生成器、`.ipynb`、`.tex`/TikZ/PGF、最终图像或实验数据文件。
- 因此仓库是可信的论文代码仓库，但没有可核对的 **PDF 视觉源**。`source_acquisition.status` 记为 `repository_without_visual_source`，`visual_source_files` 留空；上述 examples 作为相关实现路径写入搜索记录而不冒充本文 Figure 的精确生成源。没有另一个公开仓库能补足图表视觉源，`corpus/visual_sources/icml-2026-a6577855483d/` 也不存在已取得的紧凑源文件。

## PDF 对象清单

| 对象 | 物理页 | 模块 | 放置 | 宽度/结构 |
|---|---:|---|---|---|
| Figure 1 | 4 | method | main | inset；几何概念图 |
| Figure 2 | 8 | results | main | page-width；3 个横向面板 |
| Figure 3 | 9 | ablation | main | page-width；1 个三条件箱线图面板 |
| Figure 4 | 9 | results | main | page-width；3 个横向面板 |
| Figure 5 | 10 | results | main | page-width；2 个横向折线面板 |
| Table 1 | 29 | appendix | appendix | single-column；参数/数值两列 |
| Table 2 | 30 | appendix | appendix | page-width；左右两个 25 行数据块 |
| Figure 6 | 31 | appendix | appendix | page-width；3 个横向面板 |
| Table 3 | 31 | appendix | appendix | single-column；10 个模型三列 |
| Figure 7 | 31 | appendix | appendix | page-width；前后两个折线面板 |
| Figure 8 | 32 | appendix | appendix | page-width；前后两个折线面板 |
| Figure 9 | 33 | appendix | appendix | page-width；前后两个折线面板 |
| Figure 10 | 33 | appendix | appendix | page-width；前后两个折线面板 |
| Figure 11 | 34 | appendix | appendix | page-width；3 个横向面板 |
| Figure 12 | 34 | appendix | appendix | page-width；2 个横向折线面板 |
| Figure 13 | 35 | appendix | appendix | page-width；前后两个折线面板 |
| Figure 14 | 36 | appendix | appendix | page-width；上下两个箱线图面板 |
| Figure 15 | 36 | appendix | appendix | page-width；前后两个 3×3 热图 |

## 全文视觉系统

- **版式与几何**：论文为 letter 单栏。正文 Figure 1 是右侧小型浮动 inset；其余图大多横跨正文可用宽度。附录延续单栏，Table 2 以两块重复表头并排压缩 50 个 Gaussian 分布；Figure 14/15 用上下或左右配对面板表达同一参数网格。
- **字体**：caption 的 PDF 字体中位字号约 9.963 pt；正文和表格由 Nimbus Roman No9 L 承担，公式/坐标变量使用 Computer Modern 数学字形。图中坐标、图例和刻度约 7–10 pt，Figure 1 的概念标记约 7–12 pt，均为渲染估计。表格主体约 8–9.5 pt，表头约 9.5–10 pt。
- **颜色**：初始/最终 transport cost 反复使用紫色约 `#9104AC` 与橙色约 `#FF8F00`；worst-case/objective 使用蓝色约 `#3665BF`，out-of-sample/true 使用红色约 `#CE2D2C`；Figure 3 的 no-penalty 条件为灰色约 `#7F7F7F`。Figure 4/6/11 的 true density 是红色透明渐变，Figure 15 是深海军蓝→紫红→橙白的顺序色阶。颜色与文字/线型有一定冗余，但灰度下紫/橙和蓝/红的区分仍不完全安全。
- **统计表达**：Figure 2、5、7–10、12、13 含 error bar；附录正文将部分 error bar 解释为 bootstrap confidence intervals，但主 Figure 2/5/7/12/13 caption 没有统一定义区间或误差条。Figure 3/14 使用箱线图，Figure 15 把 90th-percentile violation 直接标在热图单元格；没有表格中的 uncertainty 数值列。
- **统一性**：黑色轴框、浅灰网格、蓝/红结果曲线、紫/橙 cost geometry 在各任务间复用，形成稳定的“before/after + worst-case/true”语法；不足是双 y 轴和局部图例较多，caption 对统计语义的完整性不一致。

## 逐对象审计

### Figure 1（p.4，main）

- **设计/类型**：`conceptual_diagram`；右侧 inset，单面板。两个重叠的椭圆 ambiguity sets（紫色 `Aθ1`、橙色 `Aθ2`）围绕黑色 `P` 与 `P̂`，红色和蓝色叉号分别表示两种保守解；右上/左下虚线箭头标记 loss 增长方向。复杂度 2（一个几何面板、两套形状和少量直接标注）。
- **plot grammar**：矢量；x/y 均为 `none`；无网格、统计 legend 或不确定性；`direct_labels=true`，`marker_types=2`（点与叉），`line_styles=1`（虚线 loss 方向），无 hatching/reference line，线宽约 1.1 pt；图内标签直接承担 legend 功能。
- **编码与数据**：椭圆方向/大小编码 transport geometry，紫/橙编码 `Aθ1/Aθ2`，黑点编码真实/经验分布，叉号编码对应决策，虚线箭头编码 loss 方向。纯解释性构造，没有样本聚合、分母或误差。
- **Caption**：`Figure 1: Simplified problem.`（2 词）；动作 `title`；无粗体结论标题，`self_contained=false`（caption 本身没有解释椭圆、点和叉号），`main_finding_stated=false`。
- **证据关系**：连接 §4.1 的 Eq. (5)–(8) 与正文关于“同一 coverage 下选择更低 worst-case loss 几何”的解释；在方法接口之后由 Figure 2/4 的实验几何和 Figure 3 的 coverage 检查回收。〔p.4，Figure 1；`rendered_observation`〕
- **优点**：用同一坐标空间并置两种 geometry，红/蓝叉号直接把抽象 conservatism 连接到决策结果。
- **弱点**：坐标无数值单位，`Aθ1/Aθ2` 与 P/P̂ 的语义依赖正文；颜色在灰度下区分较弱，caption 过短。
- **可复用模式**：先用两套重叠可行集合和不同决策点给出“同一约束、不同保守性”的最小几何反例。

### Figure 2（p.8，main）

- **设计/类型**：`conceptual_diagram`、`line`；page-width，三个横向面板。左：`L0/L⋆` transport-cost 椭圆；中：迭代轨迹的蓝/红双 y 轴曲线；右：按样本数的蓝/红平均相对改善与误差条。复杂度 4（多面板、双轴、两类 metric 和 10 个样本点）。
- **plot grammar**：矢量；三面板 x/y 均 `linear`，灰色网格为 `both`；左图例在上部，中心/右没有额外图例，`shared_legend=false`；`direct_labels=false`，`marker_types=1`（右侧圆点），`line_styles=1`，无 hatching/reference line；`uncertainty_display=error_bar`；线宽约 1.4 pt，`rendered_estimate`。
- **编码与数据**：左面板以紫/橙椭圆表达初始与学习后 geometry；中面板蓝色 `CVaR^Q_γ` 和红色 `CVaR^P_γ` 随 iteration；右面板蓝/红分别为 `(f0−f⋆)/|f0|` 与 `(ℓ0−ℓ⋆)/|ℓ0|`，x 为 `J=10…100`。正文给出单实例 `k=2,J=30,nb=20,γ=.05,β=.1` 和 50 个真 Gaussian×10 datasets；误差条含义没有在 caption 中定义。
- **Caption**：`Figure 2: Results of the bilevel optimization. Change in transportation cost parametrization (left), convergence plot over iterations (center), average improvement over multiple experiments w.r.t. number of samples (right)`（28 词）；动作 `title`、`setup`、`comparison`；无粗体标题，`self_contained=false`（未说明颜色、误差条和 metric 定义），`main_finding_stated=false`。
- **证据关系**：把 §4 的 geometry-learning 机制、§5 Algorithm 1/定理后的迭代行为和 §6.1 的 portfolio 主比较放在一行；右面板由单实例过渡到多实例，Figure 3 再检查 coverage。〔p.8，Figure 2；`rendered_observation`〕
- **优点**：左→中→右形成 geometry→optimization→aggregate outcome 的阅读顺序，蓝/红双 metric 贯穿全文。
- **弱点**：中心和右图双 y 轴提高了读取成本；右图 error bar 缺少统计定义，caption 不能脱离正文复现图意。
- **可复用模式**：将机制快照、优化轨迹和按样本规模的聚合结果以同一横向 panel sequence 连接。

### Figure 3（p.9，main ablation）

- **设计/类型**：`box`；page-width，单面板。x 为 10 个样本量（10–100），每个样本量并列 Before/After/no-penalty 三组箱线图；灰色组高于紫/橙，并有黑色水平 reference at 1。复杂度 3（30 个 box、离群点密集）。
- **plot grammar**：矢量；x `linear`（数值样本量），y 为对数/近似 symlog 样式刻度（渲染中同时可见 0 与 `10^0/10^1/10^2`，具体 transform 未由 caption 说明）；网格 `both`；图例在上方居中，`legend_items=3`，非 shared；无 direct labels，离群圆点 1 种 marker，实线箱须+虚线 reference，`reference_lines=1`，无 hatching；`uncertainty_display=box`，线宽约 1.1 pt。
- **编码与数据**：紫色为 `L0`、橙色为 `L⋆`、灰色为 `λp=0` 的 no-penalty；纵轴是 normalized distance `d(Q,P̂;θ)`，按 `J` 分组。图形表达分布的中位数、四分位范围、须和离群点，但 caption 未写具体 boxplot summary convention。
- **Caption**：`Figure 3: Normalized distance d(Q, P̂; θ) between reference and true distribution before (violet) and after (orange) the bilevel procedure is applied (i.e., for L0 and L⋆, respectively). The grey boxplot corresponds to running our scheme without enforcing (13), that is, by setting λp = 0.`（45 词）；动作 `title`、`encoding_key`、`comparison`、`setup`；无粗体标题，`self_contained=true`（颜色和 no-penalty 有定义，但统计摘要未定义），`main_finding_stated=false`。
- **证据关系**：是 Eq. (13) coverage penalty 的直接消融，验证 Figure 2 所示 geometry 学习没有在经验层面排除真实分布；正文 §7 明确 coverage guarantee 仍无理论 lower bound。〔p.9，Figure 3；`rendered_observation`〕
- **优点**：把 penalty 开启/关闭放在同一 x 分组中，reference=1 和灰色对照同时可见。
- **弱点**：对数样式 y 轴含 0 基线但未说明变换；boxplot convention、实验重复和 coverage 阈值未在 caption 完整定义。
- **可复用模式**：在每个数据规模上固定 before/after/removed-component 三组，并保留 reference threshold。

### Figure 4（p.9，main）

- **设计/类型**：`scatter`、`line`、`conceptual_diagram`；page-width，三面板。左为真分布红色等密度/阴影与 20 个黑色 samples；中为 `L0/L⋆` 椭圆；右为 `wtrue` 红虚线、`w⋆(L0)` 紫线、`w⋆(L⋆)` 橙线。复杂度 3。
- **plot grammar**：矢量；三面板 x/y `linear`、网格 `both`；每个面板有局部 legend（左 samples、中 cost、右 decisions），不共享；无 direct label，marker 为黑色样本点/无 marker 曲线，line styles 为 solid/dashed 两类，无 hatching/reference line；无 uncertainty，线宽约 1.2 pt。
- **编码与数据**：左面板用红色 density contour 表示生成分布、黑点表示 `J=20` samples；中面板以椭圆方向/大小表达 unit transportation cost；右面板的线斜率表达回归决策。单实例 synthetic linear model，`x∼U(−10,10)`、`w=1`、`σ=10`。
- **Caption**：`Figure 4: Underlying true distribution P (red contour) and samples (black), unit-cost ellipses defined by the matrices Li in the upper-level optimization, and optimal decisions before (w⋆ (L0)) and after (w⋆ (L⋆)) optimization. We set nb = 20.`（35 词）；动作 `title`、`setup`、`encoding_key`；无粗体标题，`self_contained=true`，`main_finding_stated=false`。
- **证据关系**：将 §6.2 absolute-loss regression 的 data geometry、learned cost 和 decision line 接到 Figure 5 的 worst-case/true-process error；Appendix Figure 11 复用完全相同布局并改为 squared loss。〔p.9，Figure 4；`rendered_observation`〕
- **优点**：样本、分布、cost 和 decision line 的空间对应关系清晰，三面板共享阅读方向。
- **弱点**：红色 density 的数值尺度未给出，右图多个线条需要图例辨认，caption 未说明坐标单位或如何生成 contour。
- **可复用模式**：用 data geometry→ambiguity geometry→decision function 的三联图解释优化对象如何改变预测/决策。

### Figure 5（p.10，main）

- **设计/类型**：`line`；page-width，两个横向面板。左为 worst-case `e_wc`（蓝）与 true-process `e_oos`（红）随 iteration 的双 y 轴曲线；右为按样本数的两种相对改善和 error bars。复杂度 3。
- **plot grammar**：矢量；x/y `linear`，网格 `both`；双轴颜色对应轴标签，图例不显式出现，`shared_legend=null`；direct labels=false，右侧圆点 marker 1 种，实线 1 种，无 hatching/reference line；`uncertainty_display=error_bar`，线宽约 1.4 pt，`rendered_estimate`。
- **编码与数据**：左蓝/红分别表达 worst-case 与 data-generating process expected absolute error；右蓝/红表达 `(f0−f⋆)/|f0|` 与 `(ℓ0−ℓ⋆)/|ℓ0|`，x 为 sample count 10–50。`e_oos` 用 10 million samples 近似；多实例为 10 个真分布×10 datasets，误差条未在 caption 定义。
- **Caption**：`Figure 5: Expected absolute error on the worst-case distribution ewc and expected absolute error on the true data generating process eoos approximated with 10 million samples (left). Average improvement over multiple experiments w.r.t. the number of samples (right).`（38 词）；动作 `title`、`setup`、`comparison`；无粗体标题，`self_contained=false`（未解释误差条和相对改善式），`main_finding_stated=false`。
- **证据关系**：是 Figure 4 单实例几何后的任务级效果，连接 §6.2 的 objective、out-of-sample performance 与 Appendix Figure 7 的多实验 absolute-loss 汇总。〔p.10，Figure 5；`rendered_observation`〕
- **优点**：把迭代下降和跨样本量的改善同时呈现，轴标签颜色有一定冗余编码。
- **弱点**：双 y 轴及无 legend 增加阅读成本，error-bar 语义和重复层级不明确。
- **可复用模式**：将单实例优化轨迹和多实例样本规模结果配对，但应在 caption 明确定义误差统计。

### Table 1（p.29，appendix）

- **设计/表头**：`single_column`，9 个数据行×2 列，1 层表头 `Parameter / Value`，无 row group。参数包括 `αi≡ᾱ`、`tol`、`λp`、`ηp`、`maxiter`、梯度上下界和 `λM` 特征值上下界；数值混合科学记数法、整数和一位小数。规则为 `booktabs`（顶线、表头线、底线），无竖线、无高亮、无不确定性列。
- **Caption**：`Table 1: List of Hyperparameters used for all numerical examples.`（8 词）；动作 `title`、`setup`；无粗体标题，`self_contained=true`，`main_finding_stated=false`。
- **数据与证据关系**：为 Algorithm 2 的实验复现接口，补充正文 Algorithm 1 未展开的 clipping、step size、penalty 和 stopping 参数；与 Figures 2–15 的所有数值实验相连。〔p.29，Table 1；`pdf_object`〕
- **优点**：最小两列结构集中呈现可执行参数，科学记数法保持实现值。
- **弱点**：没有单位、参数作用、任务覆盖差异或随机种子；上/下界符号在窄表中需要回算法正文解释。
- **可复用模式**：用参数/值两列承载跨实验默认配置，并在相邻正文解释每个剪裁边界。

### Table 2（p.30，appendix）

- **设计/表头**：page-width，物理上 25 行×6 列，由两个重复的 `Dist. / Mean μi / Covariance Matrix Σi` 三列表块并排组成；语义上覆盖 50 个 Gaussian distributions。1 层表头、无 row group；均值保留两位、协方差三位。外框、列分隔和表头线构成 `partial_grid`，无 bold/underline/color/highlight，无 uncertainty。
- **Caption**：`Table 2: List of Gaussian distributions (means and covariance matrices) used in the experiments.`（12 词）；动作 `title`、`setup`；无粗体标题，`self_contained=true`，`main_finding_stated=false`。
- **数据与证据关系**：列出 Appendix E.1 生成的 50 个 `(μi,Σi)`，为正文 Figure 2 portfolio 多实例和 Appendix Figures 6/8 的 synthetic distribution identity 提供可复现输入；左右块重复表头降低长表高度。〔p.30，Table 2；`pdf_object`〕
- **优点**：并排块在有限页面中容纳完整 distribution registry，均值与 covariance 的矩阵结构可直接读出。
- **弱点**：物理行数与语义 50 条记录不直观；没有生成 seed、dataset 重采样信息或单位，列内小字号较密。
- **可复用模式**：当记录数固定且字段结构短时，可用重复表头的并排 block 压缩附录，但应在 caption 明确物理/语义行数。

### Figure 6（p.31，appendix）

- **设计/类型**：`scatter`、`line`、`conceptual_diagram`；page-width，三个横向面板。左是 `k=3` 真分布红 contour 与黑 samples；中是 `L0/L⋆` 椭圆；右是 allocation plane 中紫/橙两个 decision points 及黑色虚线预算/可行边界。复杂度 3。
- **plot grammar**：矢量；x/y `linear`、网格 `both`；左/中/右各有局部 legend，非 shared；direct_labels=false，黑色 sample dots 与彩色 allocation circles 两类 marker，solid/dashed 两类 line styles，右面板 `reference_lines=1`；无 uncertainty/hatching，线宽约 1.2 pt。
- **编码与数据**：左/中与 Figure 4 同语法但维度为三资产的二维投影；右 x=`w1`、y=`w2`，点颜色代表 `w⋆(L0)` 与 `w⋆(L⋆)`，虚线为 allocation constraint。正文描述 10 个资产/portfolio allocation 变化，caption 未说明右图边界式。
- **Caption**：`Figure 6: Underlying true distribution P (red contour) and samples (black), unit-cost ellipses defined by the matrices Li in the upper-level optimization, and optimal decisions before (w⋆ (L0)) and after (w⋆ (L⋆)) optimization.`（31 词）；动作 `title`、`setup`、`encoding_key`、`comparison`；无粗体标题，`self_contained=false`，`main_finding_stated=false`。
- **证据关系**：Appendix E.1 将 Figure 2 的单实例 portfolio 展开为完整 distribution table 后，用 Figure 6 回收 cost geometry→allocation；与 Table 2、Figure 8 的高维结果组成 appendix extension。〔p.31，Figure 6；`rendered_observation`〕
- **优点**：复用 Figure 4 的三联结构，右图把 abstract decision vector 变成可见 allocation movement。
- **弱点**：三维分布被投影到二维，投影规则未说明；右图虚线约束和点的数值没有表格支持。
- **可复用模式**：对于 portfolio，将分布、transport geometry 和 allocation plane 置于同一三联图。

### Table 3（p.31，appendix）

- **设计/表头**：`single_column`，10 个数据行×3 列，1 层表头 `Model / Weight (w) / Variance (σ²)`，无 row group。weight 四位小数、variance 三位小数；顶线、表头线、底线构成 `booktabs`，无竖线、无高亮、无 uncertainty。
- **Caption**：`Table 3: Summary of the 10 models used for linear regression. For each model, x ∼ U(−10,10) and y = wx + e with e ∼ N(0,σ²).`（25 词）；动作 `title`、`setup`、`abbreviation_definition`；无粗体标题，`self_contained=true`，`main_finding_stated=false`。
- **数据与证据关系**：给出 Appendix E.2 的 10 个 `(w,σ²)` 真模型，作为 Figure 7 多实验 absolute-loss regression 的数据生成 registry；与正文 Figure 5 的 `e_oos`/worst-case 比较相连。〔p.31，Table 3；`pdf_object`〕
- **优点**：列方向明确、精度与参数量级匹配，caption 给出生成分布。
- **弱点**：没有 seed、每模型数据集数、样本数或误差定义；表头不写 units。
- **可复用模式**：把 synthetic model identity 单独列成窄表，再让结果图引用 model registry。

### Figure 7（p.31，appendix）

- **设计/类型**：`line`；page-width，前/后两个折线面板。每面板 x 为 samples 10–50，y 为 Mean Error；黑色 Optimal、蓝色 `f0/f⋆`、红色 `ℓ0/ℓ⋆`，每点带 error bar。复杂度 3。
- **plot grammar**：矢量；x/y `linear`、网格 `both`；每个面板上方有三项局部 legend，不 shared；圆点 marker 1 种、实线 1 种、无 hatching/reference line；`uncertainty_display=error_bar`，线宽约 1.3 pt。
- **编码与数据**：左为 initial objectives，右为 final objectives；黑线是 all experiments 的 average best possible expected error，蓝/红分别是 worst-case/out-of-sample。表格 Table 3 支撑 10 个 underlying models；正文/附录文字说 error bars 为 bootstrap CIs，但 caption 未定义。
- **Caption**：`Figure 7: Average initial (left) and final (right) objectives (worst-case objective in blue and out-of-sample performance in red) of the distributionally robust linear regression model with absolute error over multiple experiments. The black line shows the average best possible expected error over all experiments.`（42 词）；动作 `title`、`setup`、`encoding_key`、`comparison`；无粗体标题，`self_contained=false`，`main_finding_stated=false`。
- **证据关系**：补足 Figure 5 右侧 aggregate result 的 objective-level 对照，把 `L0→L⋆` 的变化对齐到 Table 3 的模型集合；为 Figure 13 squared-loss extension 提供模板。〔p.31，Figure 7；`rendered_observation`〕
- **优点**：before/after 镜像 panel 与黑色 optimum baseline 使保守性 gap 可见。
- **弱点**：error bar 统计含义只在相邻正文出现，模型/数据集分母未在 caption 自足说明。
- **可复用模式**：固定三系列颜色和同 y 轴结构，用左右 panel 比较初始化与学习后 objective。

### Figure 8（p.32，appendix）

- **设计/类型**：`line`；page-width，左右两个 panel，x 为 samples 10–100，y 为 Mean CVaR。每面板 black Optimal、blue `f0/f⋆`、red `ℓ0/ℓ⋆`，带 bootstrap error bars；复杂度 3。
- **plot grammar**：矢量；x/y `linear`、网格 `both`；panel-local legend，非 shared；圆点 marker、实线、无 hatching/reference line；`uncertainty_display=error_bar`，线宽约 1.3 pt。
- **编码与数据**：左右表示 before/after bilevel optimization；蓝/红分别 worst-case objective/out-of-sample performance，x 为 higher-dimensional `k=10` portfolio sample size。相邻正文说明 10 true Gaussian distributions×10 datasets×每个 J，error bars 为 bootstrap confidence intervals，但 nominal level 未写出。
- **Caption**：`Figure 8: Average results for the higher-dimensional portfolio optimization example using a Gaussian reference distribution (worst-case objective in blue and out-of-sample performance in red) when considering multiple experiments with underlying true discrete distributions, before (left) and after (right) the bilevel optimization.`（39 词）；动作 `title`、`setup`、`encoding_key`、`comparison`；无粗体标题，`self_contained=false`，`main_finding_stated=false`。
- **证据关系**：Appendix F.1 扩展 Figure 2 的 portfolio 到 `k=10`，连接 Table 2 的 Gaussian parameter generation 与高维可扩展性叙述；caption 的 Gaussian-reference/true-discrete 表述冲突需保留。〔p.32，Figure 8；`rendered_observation`〕
- **优点**：保持 Figure 7 的 before/after + black optimum 视觉模板，跨样本趋势直观。
- **弱点**：caption 同时写 Gaussian reference 和 discrete true distributions，error bars nominal level 不明，跨图数值精度无法复核。
- **可复用模式**：用同一两面板 objective grammar 做维度扩展，并在 caption 明确 reference/true distribution 的角色。

### Figure 9（p.33，appendix）

- **设计/类型**：`line`；page-width，左右两个 panel，x 渲染为 10–50 的 samples，y 为 Mean Expected Loss；black Optimal、blue worst-case、red out-of-sample，圆点和 error bars。复杂度 3。
- **plot grammar**：矢量；x/y `linear`、网格 `both`；左右各有 panel-local legend，非 shared；圆点 marker、实线、无 hatching/reference line；`uncertainty_display=error_bar`，线宽约 1.3 pt。
- **编码与数据**：比较 empirical-reference portfolio with discrete true distributions 的 before/after；蓝/红为 worst-case/out-of-sample，误差条由相邻正文说明为 bootstrap CIs。正文实验设计写 `J∈{10,…,100}`，但 PDF Figure 9 x 轴实际显示到 50，审计按渲染轴记录。
- **Caption**：`Figure 9: Average results (worst-case objective in blue and out-of-sample performance in red) when considering multiple experiments with underlying true discrete distributions, before (left) and after (right) the bilevel optimization.`（28 词）；动作 `title`、`setup`、`encoding_key`、`comparison`；无粗体标题，`self_contained=false`，`main_finding_stated=false`。
- **证据关系**：Appendix F.2.1 将正文 Gaussian-reference 结果换为 empirical reference/discrete true distribution，作为 Figure 10 GMM 变体前的分布鲁棒性扩展。〔p.33，Figure 9；`rendered_observation`〕
- **优点**：与 Figure 8/10 保持完全相同的 panel grammar，便于横向看分布变体。
- **弱点**：caption 未给 sample-size range、重复分母或 bootstrap 定义；正文 range 与渲染 x 轴存在可见边界差异。
- **可复用模式**：固定 before/after、black optimum 和 blue/red metric token，替换 true-distribution family。

### Figure 10（p.33，appendix）

- **设计/类型**：`line`；page-width，左右两个 panel，x 为 10–50 samples，y 为 Mean Expected Loss（负值）；black Optimal、blue `f0/f⋆`、red `ℓ0/ℓ⋆`，圆点+error bars。复杂度 3。
- **plot grammar**：矢量；x/y `linear`、网格 `both`；panel-local legends，非 shared；圆点 marker、实线、无 hatching/reference line；`uncertainty_display=error_bar`，线宽约 1.3 pt。
- **编码与数据**：Appendix F.2.2 的 3-component Gaussian mixture true distributions；左右为 before/after，蓝/红为 worst-case/out-of-sample。均值跨 10 underlying distributions×10 resampled datasets，error bars 由相邻文字说为 bootstrap CIs，level 未指定。
- **Caption**：`Figure 10: Average results (worst-case objective in blue and out-of-sample performance in red) when considering multiple experiments with underlying true GMM distributions, before (left) and after (right) the bilevel optimization.`（28 词）；动作 `title`、`setup`、`encoding_key`、`comparison`；无粗体标题，`self_contained=false`，`main_finding_stated=false`。
- **证据关系**：与 Figure 9 配对，把 empirical-reference portfolio 的 discrete→GMM true-distribution 变化接到同一结果 grammar；为附录“分布族变化仍改善”的范围主张提供描述性证据。〔p.33，Figure 10；`rendered_observation`〕
- **优点**：颜色/面板结构稳定，负值 y 轴仍保留黑色 optimum baseline。
- **弱点**：caption 没有解释 GMM 组件数、误差条或 sample range；仅有曲线没有数值表。
- **可复用模式**：对不同 nominal/true distribution family 复用相同的 paired objective panel，以避免比较时引入新视觉变量。

### Figure 11（p.34，appendix）

- **设计/类型**：`scatter`、`line`、`conceptual_diagram`；page-width，三面板，结构复制 Figure 4。左为 squared-loss regression 的红色 true-density contour+black samples；中为 `L0/L⋆` 椭圆；右为 true and two fitted decision lines。复杂度 3。
- **plot grammar**：矢量；x/y `linear`、网格 `both`；三 panel-local legends，非 shared；黑点/无 marker 曲线，solid/dashed line styles 两类，无 hatching/reference line；无 uncertainty，线宽约 1.2 pt。
- **编码与数据**：右图红虚线为 `wtrue`、紫/橙为 `w⋆(L0)/w⋆(L⋆)`；left/center geometry 与 Figure 4 相同但 loss 换为 `ℓ2`。caption 写 `nb=10`，附录正文给 `J=20`、`w=1`、`σ=10`。
- **Caption**：`Figure 11: Underlying true distribution P (red contour) and samples (black), unit-cost ellipses defined by the matrices Li in the upper-level optimization, and optimal decisions before (w⋆ (L0)) and after (w⋆ (L⋆)) optimization when using the squared error ℓ2. We set nb = 10.`（41 词）；动作 `title`、`setup`、`encoding_key`、`comparison`、`abbreviation_definition`；无粗体标题，`self_contained=true`，`main_finding_stated=false`。
- **证据关系**：是 Figure 4 的 loss-function extension，连接 Appendix F.3 的 squared-error setup 与 Figure 12/13 的 task-level curves；通过同一 geometry 骨架支持跨 loss 对照。〔p.34，Figure 11；`rendered_observation`〕
- **优点**：复用三联图降低比较成本，decision-line 偏移对 loss 变化敏感。
- **弱点**：caption 内 `nb` 与正文 `J` 的角色需要回实验段，红色 contour 仍无数值尺度。
- **可复用模式**：对 loss variant 保留同一分布/geometry/decision 三联 layout，仅改变 loss 标识。

### Figure 12（p.34，appendix）

- **设计/类型**：`line`；page-width，两个横向面板。左为 squared-error `e_wc`/`e_oos` 双 y 轴 iteration curves；右为按 samples 的 relative improvements 与误差 bars。复杂度 3。
- **plot grammar**：矢量；x/y `linear`、网格 `both`；左用彩色轴标签，右无显式 legend，`shared_legend=null`；右有圆点 marker、实线，左无 marker；无 hatching/reference line；`uncertainty_display=error_bar`，线宽约 1.3 pt。
- **编码与数据**：左蓝/红对应 worst-case/true-process squared error，右蓝/红为 `(f0−f⋆)/|f0|` 与 `(ℓ0−ℓ⋆)/|ℓ0|`；`n_oos=10^6`，多实验沿用 Table 3 models。caption 正确写 squared error，但相邻正文出现 “absolute error” 用词。
- **Caption**：`Figure 12: Expected squared error on the worst-case distribution ewc and expected squared error on the true data generating process eoos (left). Average improvement over multiple experiments w.r.t. the number of samples (right).`（33 词）；动作 `title`、`setup`、`comparison`；无粗体标题，`self_contained=false`，`main_finding_stated=false`。
- **证据关系**：连接 Figure 11 单实例 geometry 与 Figure 13 多实验 initial/final objectives，闭合 squared-loss appendix 的“机制→效果”链。〔p.34，Figure 12；`rendered_observation`〕
- **优点**：保留 Figure 5 的两阶段组织，并明确 squared-error 量纲。
- **弱点**：双轴和缺少 legend 让颜色语义依赖正文；error-bar 统计定义和实验分母缺失。
- **可复用模式**：loss variant 的曲线图应镜像主实验 panel，并在 caption 明确 out-of-sample estimator。

### Figure 13（p.35，appendix）

- **设计/类型**：`line`；page-width，左右 initial/final 两个 panel。x 为 samples 10–50，y 为 Mean Error；黑 Optimal、蓝 worst-case、红 out-of-sample，圆点和 error bars。复杂度 3。
- **plot grammar**：矢量；x/y `linear`、网格 `both`；每 panel 局部 legend，非 shared；圆点 marker、实线、无 hatching/reference line；`uncertainty_display=error_bar`，线宽约 1.3 pt。
- **编码与数据**：左 `f0/ℓ0`、右 `f⋆/ℓ⋆`，黑线为 average best possible expected error；对应 squared-loss regression 多实验，误差条由前文说明为 bootstrap CIs，nominal level 未提供。
- **Caption**：`Figure 13: Average initial (left) and final (right) objectives (worst-case objective in blue and out-of-sample performance in red) of the distributionally robust linear regression model with squared error over multiple experiments. The black line shows the average best possible expected error over all experiments.`（42 词）；动作 `title`、`setup`、`encoding_key`、`comparison`；无粗体标题，`self_contained=false`，`main_finding_stated=false`。
- **证据关系**：与 Figure 7 组成 absolute/squared loss 的平行结果对象，回收 Figure 12 的相对改善并把 black optimum baseline 放回多实验决策面。〔p.35，Figure 13；`rendered_observation`〕
- **优点**：平行 panel 和一致颜色允许跨 loss 直接比较趋势。
- **弱点**：caption 没有给 error-bar summary、sample range 或 model registry 引用；没有附带可读数值表。
- **可复用模式**：用固定的 objective/optimal 三系列和 initial/final 镜像 panel 承载多实验 loss comparison。

### Figure 14（p.36，appendix）

- **设计/类型**：`box`；page-width，上下两个 panel，共 9 个 `(λp,ηp)` categorical combinations。上排蓝色 worst-case relative improvement，y≈0–2；下排红色 out-of-sample relative improvement，y≈0–0.75；均有 box/whisker/outlier。复杂度 3。
- **plot grammar**：矢量；x `categorical`、y `linear`；无 legend（y 轴直接标量），`shared_legend=null`；下排离群点为圆形 marker，box/whisker line styles 1，无 hatching/reference line；`uncertainty_display=box`，线宽约 1.1 pt。
- **编码与数据**：每一列是一个 `(λp,ηp)` 配置，蓝/红分上下两种相对改善；每个配置聚合 10 个 underlying distributions×10 repeats。箱体表达分布位置和离散程度，caption 未定义 quartile/whisker/outlier convention。
- **Caption**：`Figure 14: Mean relative improvement for different combinations of (λp, ηp).`（9 词）；动作 `title`、`setup`；无粗体标题，`self_contained=false`（没有说明上下排指标或实验分母），`main_finding_stated=false`。
- **证据关系**：Appendix G.2 用于检验 penalty hyperparameters 的 robustness，承接 Figure 3 的 λp ablation，并为 Figure 15 coverage-violation heatmap 提供同一网格索引。〔p.36，Figure 14；`rendered_observation`〕
- **优点**：上下两排共享 categorical grid，能同时看性能收益与跨配置分布。
- **弱点**：caption 没有定义上下排指标和箱线图统计，长的 tuple 标签拥挤，不能直接知道每格的实验数量。
- **可复用模式**：对同一超参数网格上下排列 performance metrics，但应共享明确的 grid key 和 summary definition。

### Figure 15（p.36，appendix）

- **设计/类型**：`heatmap`、`matrix`；page-width，左右两个 3×3 热图（before/after），每格有精确数值标注，右侧各有 0–0.010 colorbar。复杂度 3。
- **plot grammar**：矢量；x/y `categorical`（ηp=50/100/500，λp=1/5/10），无网格但单元边界可见；两 panel 各自 colorbar，`shared_legend=false`；`direct_labels=true`，无 marker/line/hatching/reference line；`uncertainty_display=none`，线宽不适用/约 0.8 pt。
- **编码与数据**：颜色与 cell text 表达 Eq. (11b) coverage-constraint violation 的 90th percentile；左/右分别 before/after，色阶深色为接近 0、亮色为较大 violation。100 experiments 汇总，λp 和 ηp 的行列与 Figure 14 对齐。
- **Caption**：`Figure 15: Coverage constraint (11b) violations (90th percentile) for different combinations of (λp, ηp) over the 100 experiments.`（16 词）；动作 `title`、`setup`、`uncertainty_definition`、`comparison`；无粗体标题，`self_contained=true`（定义了 percentile、网格对象和实验数，但未定义颜色范围），`main_finding_stated=false`。
- **证据关系**：与 Figure 14 共同构成 Appendix G 的 performance/coverage sensitivity pair；回收 Figure 3 的 no-penalty/coverage reliability 问题，并与 §7 “coverage 尚无理论保证”的限制相连。〔p.36，Figure 15；`rendered_observation`〕
- **优点**：数值直接写入 3×3 cells，before/after 和相同 colorbar 范围便于检查配置敏感性。
- **弱点**：两个 colorbar 仍是局部重复，caption 未解释 acceptable bounds；单元格数值精度与颜色的跨 panel 比较规则不明。
- **可复用模式**：对双阶段约束诊断使用同一超参数网格和 cell labels，同时发布原始长表以避免只读色阶。

## 跨对象证据系统

- **视觉叙事**：Figure 1 先把 Eq. (5)–(8) 的 ambiguity-set geometry 变成可见的保守性反例；Figure 2 依次展示 cost geometry、迭代轨迹和 portfolio aggregate；Figure 3 删除 coverage penalty 做直接 ablation；Figure 4–5 把同一逻辑换到 absolute-loss regression。Appendix Table 1–3 固定实验参数和 synthetic registries，Figures 6–10 扩展 portfolio 的维度/reference/true-distribution family，Figures 11–13 换 squared loss，Figures 14–15 检验 penalty sensitivity 与 coverage violation。
- **Caption 系统**：统一使用 `Figure n:`/`Table n:` 的普通 caption，几乎没有粗体 headline；主图 caption 多描述对象、颜色、左右面板和 setup，但 error bar/boxplot 统计语义常留在邻近正文。Figure 8 的 distribution wording 冲突、Figure 12 正文的 absolute/squared wording 不一致，均保留为 PDF 证据。
- **表头系统**：Table 1 用 `Parameter/Value`，Table 2 重复 `Dist./Mean/Covariance Matrix` 三列表头，Table 3 用 `Model/Weight/Variance`；都是单层表头、无方向箭头和无 uncertainty 列。精度通过小数位或科学记数法表达，未用颜色/粗体承载结果结论。
- **方法—结果—消融链**：Figure 1 → Figure 2/4（geometry 到任务结果）→ Figure 3（coverage penalty）→ Figure 5/7/13（多实例 objective）→ Figures 14/15（超参数 robustness/constraint）；Algorithm 1/2 和 Tables 1–3 是执行接口与复现注册表。理论 Theorem 5.1 解释收敛条件，但没有专门的 criticality plot。
- **正文—附录链**：正文显式指向 Appendix A.2.2、A.3.1、E.1/E.2、F.2/F.3；附录补全 conic reformulation、stabilized Algorithm 2、分布/模型清单、额外分布和 loss 变体。附录承担复现和范围扩展，正文承担唯一几何入口、主任务结果和限制声明。
- **字体/颜色一致性**：Nimbus Roman/Computer Modern 和矢量绘图使所有对象风格相对统一；紫/橙 cost、蓝/红 worst-case/true 的 token 在正文和附录重复。局部双 y 轴、独立 legend/colorbar 和缺少统计定义，是跨对象一致性的主要断点。

## 最终判断

- **最可复用模式**：以 Figure 1 的共同 geometry 反例定义方法接口；用 Figure 2/5 的“单实例轨迹→多实例样本规模”配对；用 Figure 7/8/9/10/13 的镜像 before/after + black optimum 模板；用 Figure 14/15 的同一超参数网格对照性能与 coverage。
- **最高价值对象**：Figure 1 是 loss-aware ambiguity geometry 的最短解释；Figure 2 是 portfolio 主结果的完整视觉链；Figure 3 是 coverage penalty 的关键消融；Figure 4–5 是 regression 的机制—效果闭环；Figure 15 是附录中最可复核的逐 cell coverage 数值对象。
- **主要失败模式**：caption 常未定义 error bar、bootstrap interval、boxplot summary 和分母；Figure 8 的 reference/true distribution wording 冲突；Figure 3 的对数样式刻度与 0 baseline 未说明；双 y 轴、局部 legend/colorbar 提高比较成本；公开仓库有通用 demo plot 但没有 PDF Figure/Table 的精确视觉生成源。
- **一句话视觉策略**：论文用紫/橙 geometry 和蓝/红 worst-case/true token，把“学习 transport cost 以减少保守性并保留 coverage”的机制从概念图推进到 portfolio、regression、分布/loss 扩展和 penalty 网格诊断，但统计编码说明和精确视觉源仍不完整。
