# iclr-2026-24987e898413 视觉审计

## 1. 审计边界与事实源

- 论文：*mCLM: A Modular Chemical Language Model that Generates Functional and Makeable Molecules*；`paper_id`：`iclr-2026-24987e898413`。
- PDF 事实源：`corpus/pdfs/iclr-2026-24987e898413.pdf`，共 33 个物理页；`pdfinfo` 确认未加密、letter 页面。正文/摘要为 p.1–11，参考文献为 p.12–19，Appendix A–I 为 p.20–33。
- 渲染检查：使用 `pdftoppm -png -r 180` 生成 `/tmp/iclr_24987_visual_audit_20260828/render180/page-*.png`，逐页检查 p.1–33，并对含对象页面（p.2、4、5、7–9、20–24、26、28–30、32–33）进行原尺寸视觉复核。
- PDF 对象清单与 reading 中的 `visual_inventory` 对齐：13 个 Figure、11 个 Table。p.21 还出现 Algorithm 1；本协议只审计 Figure/Table，因此在此记录但不计入 JSON 对象。
- 未发现跨页 Figure/Table；正文和附录的对象均已纳入，参考文献页和无对象页也完成整页检查。

## 2. 公开视觉源检索

- `reports/tables/visual_source_inventory.csv` 将作者仓库候选锁定为 `blender-nlp/mCLM`，状态为 `repository_without_visual_source`；同一行的候选包括 `maplightrx/MapLight-TDC`、`hhw11/AI-for-science-paper-oral-and-spotlight-`、`inoue0426/Paper-reading`。
- 本地 `corpus/visual_sources/iclr-2026-24987e898413/` 为空；GitHub `https://github.com/blender-nlp/mCLM` 的 `main` 树已核查，包含模型/tokenizer/训练/聊天代码与 `resources/synth_blocks_all.txt`，没有 Figure/Table 的直接绘图脚本、notebook、LaTeX/TikZ/PGF、SVG 或图表数据导出文件。
- README 明确链接 website、Hugging Face data/model、demo 与 paper；MapLight-TDC 仅作为 ADMET benchmark 参考。由于没有可逐图对应的公开视觉文件，本审计不把模型/训练代码标为 `source_exact`。

## 3. PDF 对象清单

| 类型 | 标签 | 页 | 模块 | 宽度 | 角色/要点 |
|---|---|---:|---|---|---|
| Figure | `Figure 1` | 2 | `introduction` | `page_width` | headline；method_interface |
| Figure | `Figure 2` | 4 | `introduction` | `page_width` | method_interface；theory_mechanism |
| Figure | `Figure 3` | 5 | `method` | `page_width` | method_interface；theory_mechanism |
| Figure | `Figure 4` | 9 | `results` | `page_width` | mechanism；qualitative_evidence |
| Figure | `Figure 5` | 9 | `results` | `single_column` | mechanism；qualitative_evidence |
| Figure | `Figure 6` | 21 | `appendix A.3` | `page_width` | failure；qualitative_evidence |
| Figure | `Figure 7` | 22 | `appendix C` | `page_width` | experimental_design；method_interface；reproduction |
| Figure | `Figure 8` | 28 | `appendix F.2` | `page_width` | qualitative_evidence；reproduction |
| Figure | `Figure 9` | 28 | `appendix F.2` | `page_width` | qualitative_evidence；reproduction |
| Figure | `Figure 10` | 29 | `appendix F.2` | `page_width` | qualitative_evidence；reproduction |
| Figure | `Figure 11` | 29 | `appendix F.2` | `page_width` | qualitative_evidence；reproduction |
| Figure | `Figure 12` | 30 | `appendix F.2` | `page_width` | qualitative_evidence；reproduction |
| Figure | `Figure 13` | 30 | `appendix F.2` | `page_width` | qualitative_evidence；reproduction |
| Table | `Table 1` | 7 | `results` | `page_width` | headline；main_comparison |
| Table | `Table 2` | 8 | `results` | `page_width` | main_comparison；efficiency_cost；ablation |
| Table | `Table 3` | 20 | `appendix A.1` | `page_width` | efficiency_cost；robustness；main_comparison |
| Table | `Table 4` | 21 | `appendix A.2` | `page_width` | robustness；main_comparison |
| Table | `Table 5` | 23 | `appendix C.2` | `page_width` | dataset；reproduction |
| Table | `Table 6` | 24 | `appendix C.2` | `page_width` | dataset；reproduction |
| Table | `Table 7` | 26 | `appendix D.2.3` | `page_width` | experimental_design；reproduction |
| Table | `Table 8` | 32 | `appendix H` | `page_width` | robustness；main_comparison |
| Table | `Table 9` | 33 | `appendix H` | `page_width` | efficiency_cost；main_comparison |
| Table | `Table 10` | 33 | `appendix H` | `page_width` | ablation；efficiency_cost |
| Table | `Table 11` | 33 | `ablation` | `page_width` | ablation；main_comparison |

## 4. 全文版式与视觉系统

- 主文对象：5 个 Figure、2 个 Table；附录对象：8 个 Figure、9 个 Table。正文/附录整体以 Nimbus Roman No9 L 为主体，图内架构标签多为 sans-serif，表格以黑白规则和粗体最佳值为主。
- 配色：主文 Figure 1–7 使用深蓝边框/标题、橙粉或浅色模块、化学元素色与直接标签；Figure 4–6 和 Figure 8–13 沿用分子图的元素颜色。表格以黑白 booktabs/partial-grid 为主，Table 1 额外用绿色/红色相对 FDA 和浅绿色粗体标记。
- 矢量/栅格：正文文字、表格规则、框线和箭头多为 PDF 矢量；分子结构、案例卡片和 Figure 8–13 的邻居图以嵌入栅格对象呈现，整体为 mixed。Figure 8–13 的分子 montage 主要是 raster。
- 一致性：caption 和正文保持 Nimbus Roman No9 L；架构/流程图内部使用 Myriad Pro/Nunito/Arial-like sans-serif。Figure 1–7 的 rounded boxes、深蓝标题和箭头相对一致；Figure 8–13 以大留白和 2×3 chemical montage 为另一组一致模板。表格共享方向箭头、粗体最佳值和少量竖分隔线，但 Table 5 的完整网格明显更密。

## 5. Figure 逐对象审计

### Figure 1（p.2，introduction）

- **版面与类型**：`page_width`；类型 `conceptual_diagram, image_montage`；用途 `headline, method_interface`。复杂度 3/5，3 个 panel，约 6 个 annotation。
- **Plot grammar**：rendering=`mixed`；x/y scale=`none`/`none`；grid=`none`；legend=`False`；direct labels=`True`；line styles=0；reference lines=0；uncertainty=`none`；line width≈0.8 pt；provenance=`rendered_estimate`。
- **Encodings**：x=三种表示列：natural-language names、peptide blocks、small-molecule blocks；y=natural language、peptides、small molecules 三行；color=building blocks、功能 token 与元素颜色；shape=文本段、完整分子结构、模块化 token/结构；line=None；facet=按表示列和示例行分面；text=行标签、功能描述、tokenization 结果与底部结论标签。
- **Typography/color**：字体 `Arial, Myriad Pro, Nunito`，字号约 5.5–10.0 pt（中位 8.0），字重 `regular, bold, medium`，来源=`rendered_estimate`；颜色模式 `categorical`、约 8 色、来源=`rendered_estimate`，语义：三列表示比较与三行分子/语言层级；深蓝框、彩色 building-block/元素编码和功能文字共同指示模块角色。 灰度安全=False。
- **Caption**（83 词；moves=`title, setup, comparison, main_finding`；headline bold=False；self-contained=True；main finding=True）：Figure 1: mCLM adopts a modular chemical vocabulary, which uses synthesis robot-friendly molecular building blocks as tokens together with natural language tokens. Compared with using natural language names, SMILES strings, or holistic embeddings for whole molecules, this building block level of tokenization better enables the prediction of compounds with improved properties, and guarantees automated synthesizability a priori, thus building a direct link between the digital and physical worlds. This approach stands to substantially enable AI-guided discovery of new small molecules with targeted functions.
- **数据与证据关系**：概念性对照图，不报告样本、重复或误差；通过 imatinib 的功能描述、完整分子和模块化 token 说明 representation choice，结论性文字声称 property prediction 与 a-priori synthesizability 的优势。 引言的 atom/SMILES 表示缺口 → Figure 1 的 block-level vocabulary 主张 → Figure 2/3 的模型与 tokenizer 实现；图内的 imatinib 示例把数字表示与物理合成约束连接起来。
- **设计优点**：三列对照和三行层级让 vocabulary claim 可快速扫读。；文字、分子结构和 token 图形形成互补证据。；底部标签把 synthesis-friendly、function-infused 与 LLM-enabled 设计意图显式化。
- **设计弱点**：彩色 token 语义依赖图例外的阅读，图内没有单独 legend。；小字号化学结构和密集自然语言在缩放后不易读取。；“better enables”与“guarantees”是设计主张，图本身没有量化验证。
- **可复用模式**：用同一分子在完整结构、功能描述和模块化 token 三层并列展示表示变化，并在底部给出物理约束与模型接口的短结论。
- **物理证据**：p.2 渲染对象 `Figure 1 on p.2; 180 dpi rendered page inspection`，basis=`rendered_observation`。

### Figure 2（p.4，introduction）

- **版面与类型**：`page_width`；类型 `architecture, pipeline, conceptual_diagram`；用途 `method_interface, theory_mechanism`。复杂度 5/5，2 个 panel，约 15 个 annotation。
- **Plot grammar**：rendering=`mixed`；x/y scale=`none`/`none`；grid=`none`；legend=`False`；direct labels=`True`；line styles=2；reference lines=0；uncertainty=`none`；line width≈0.9 pt；provenance=`rendered_estimate`。
- **Encodings**：x=左侧由 Molecule Module Library 经 GNN/feature space/decoder 到 mCLM output 的数据流；y=None；color=模块类别、功能/化学元素与 decoder 层；shape=rounded boxes、decoder stack、arrows、molecule structures；line=实线数据流和虚线 similarity search；facet=左 Text-Molecule Bilingual Language，右 Training mCLM with Modular Chemical Reasoning Knowledge；text=GNN、Chemical feature space、function labels、reaction example 与 module library 标签。
- **Typography/color**：字体 `Arial, Myriad Pro, Nunito`，字号约 5.5–10.0 pt（中位 8.0），字重 `regular, bold, medium`，来源=`rendered_estimate`；颜色模式 `categorical`、约 8 色、来源=`rendered_estimate`，语义：深蓝标题和边框区分模块，橙粉 decoder、黄色背景、青紫/绿色分子元素表达语言—分子通路与知识类别。 灰度安全=False。
- **Caption**（56 词；moves=`title, setup, main_finding`；headline bold=False；self-contained=True；main finding=False）：Figure 2: An overview of the mCLM. It is a multimodal chemical-language model that tokenizes molecules into synthesis robot-friendly building blocks, thus creating a direct link between the digital and physical worlds. After being trained on datasets consisting of properties, functions and synthesis data, the mCLM can conduct critical chemical reasoning through an iterative refinement process.
- **数据与证据关系**：架构/流程示意，无坐标轴与样本统计；左 panel 表示 GNN 编码、相似度检索、decoder 和 in-context molecule sequence，右 panel 表示功能文字、related chemical knowledge 与 learned module library 的训练接口。 Figure 1 的模块化 vocabulary → Figure 2 的 bilingual encoder/decoder 与 knowledge training interface → Figure 3 tokenizer 保证；后续 Table 1/2 和 Figure 4 使用该接口产生 property editing 与 makeability 结果。
- **设计优点**：把模型组件、数据流和一个具体 imatinib 例子放在同一张图中。；左右 panel 的标题提供清晰的 architecture/training 分工。；箭头方向和局部文字可解释 GNN、decoder、similarity search 的接口。
- **设计弱点**：信息密度高，多个小分子示例和标签在单页缩放时接近可读性下限。；没有明确区分训练时检索与推理时生成的所有路径。；图示的“direct link”仍是机制叙事，未给独立对照指标。
- **可复用模式**：用左右双 panel 分开“模型接口”和“知识训练”，再以箭头把 graph encoder、语言 decoder 和具体化学例子闭合。
- **物理证据**：p.4 渲染对象 `Figure 2 on p.4; 180 dpi rendered page inspection`，basis=`rendered_observation`。

### Figure 3（p.5，method）

- **版面与类型**：`page_width`；类型 `pipeline, conceptual_diagram`；用途 `method_interface, theory_mechanism`。复杂度 4/5，5 个 panel，约 14 个 annotation。
- **Plot grammar**：rendering=`mixed`；x/y scale=`none`/`none`；grid=`none`；legend=`False`；direct labels=`True`；line styles=2；reference lines=0；uncertainty=`none`；line width≈0.9 pt；provenance=`rendered_estimate`。
- **Encodings**：x=从 tokenization rules 到 synthesis-guaranteed tokenizer、coverage check、rule-based tokenizer 的左到右流程；y=上方 coupling-rule 栈与下方 pretraining 输出；color=三类 coupling、原子/片段类别及 Pass/Fail 路径；shape=过程框、分子结构、片段连接箭头；line=实线流程箭头与虚线规则分隔线；facet=规则列表、保证 tokenizer、coverage check、fallback tokenizer；text=Coupling type、Tokenizer、Pass/Fail、Used for pretraining 和结构覆盖提示。
- **Typography/color**：字体 `Arial, Myriad Pro, Nunito`，字号约 5.5–10.0 pt（中位 8.0），字重 `regular, bold, medium`，来源=`rendered_estimate`；颜色模式 `categorical`、约 8 色、来源=`rendered_estimate`，语义：深蓝过程标题、蓝/绿/紫/橙分子元素、橙色 Fail 箭头与蓝色 Pass 箭头编码 tokenizer 路径和分子片段。 灰度安全=False。
- **Caption**（76 词；moves=`title, setup, comparison, main_finding`；headline bold=False；self-contained=True；main finding=True）：Figure 3: An overview of the tokenization process. A functional molecule is first processed by the synthesis-guaranteed tokenizer to produce a set of building blocks compatible with automated modular synthesis. These blocks are then evaluated via a structure coverage check to determine whether they fully reconstruct the original molecule. If coverage is complete, the blocks are used directly for pretraining. Otherwise, the molecule is reprocessed using a rule-based tokenizer to ensure full representation for training purposes.
- **数据与证据关系**：流程图不含样本或不确定性；展示 synthesis-guaranteed tokenizer 生成 blocks 后进行 structure coverage check，Pass 进入预训练，Fail 回退 rule-based tokenizer。 §2.1 的三类化学 coupling 与 coverage/fallback 机制 → Figure 3 的显式流程 → Figure 7 数据创建与 Table 2/3 的合成/validity 结果；它是“合成保证优先、覆盖率补足”的方法接口证据。
- **设计优点**：Pass/Fail 分支将合成保证与覆盖率权衡画成可执行路径。；左侧规则栈、中央示例分子和右侧 fallback 形成因果顺序。；底部 Used for pretraining 标明输出边界。
- **设计弱点**：流程框内的分子片段和说明文字较小。；没有给出 coverage failure 比例或 fallback 规模。；箭头语义依赖颜色与英文标签，灰度下分支区分减弱。
- **可复用模式**：把“约束生成 → 结构覆盖检查 → 规则 fallback”画成单向决策链，保留一个具体分子作为输入/输出锚点。
- **物理证据**：p.5 渲染对象 `Figure 3 on p.5; 180 dpi rendered page inspection`，basis=`rendered_observation`。

### Figure 4（p.9，results）

- **版面与类型**：`page_width`；类型 `qualitative_grid, image_montage, pipeline`；用途 `mechanism, qualitative_evidence`。复杂度 4/5，8 个 panel，约 12 个 annotation。
- **Plot grammar**：rendering=`mixed`；x/y scale=`categorical`/`categorical`；grid=`none`；legend=`False`；direct labels=`True`；line styles=2；reference lines=0；uncertainty=`none`；line width≈1.0 pt；provenance=`rendered_estimate`。
- **Encodings**：x=四个迭代状态（初始到第三步）；y=Evobrutinib 与 TNG348 两个案例；color=元素颜色与深蓝迭代箭头；shape=八个圆角 molecule cards；line=卡片间实线箭头与 property 方向标注；facet=药物案例行；每行四个时间/迭代状态；text=药名、靶点、失败原因、DILI/PGP/AMES/BBBP/CYP3A4 数值和 Increase/Decrease labels。
- **Typography/color**：字体 `Arial, Myriad Pro, Nunito`，字号约 6.0–10.0 pt（中位 8.0），字重 `regular, bold`，来源=`rendered_estimate`；颜色模式 `categorical`、约 7 色、来源=`rendered_estimate`，语义：化学元素颜色显示分子结构，深蓝卡片边框/箭头和顶部或底部 property 文本编码迭代方向。 灰度安全=False。
- **Caption**（8 词；moves=`title`；headline bold=False；self-contained=False；main finding=False）：Figure 4: Examples of fallen angel property modification.
- **数据与证据关系**：两个案例各显示初始结构和三次修改；每张卡给出 3 个属性 point values，Evobrutinib 的 DILI/PGP/AMES 与 TNG348 的 DILI/BBBP/CYP3A4 沿步骤变化。没有重复、误差条、置信区间或样本汇总。 §3.3 的 fallen-angels 多步 reasoning → Figure 4 的逐步 property editing → Figure 6 的 MoleculeSTM invalid 对照；结果支持案例级 rescue 叙事，但不构成跨药物的总体效应估计。
- **设计优点**：两行同构卡片使两个案例可直接对照。；每步同时给结构、方向和 property values，路径可被脱离正文复读。；箭头标签突出 DILI 起点及后续 trade-off 修复。
- **设计弱点**：只有两个案例，且未报告生成重复或选择规则。；化学结构、数值和箭头在单页占据较多视觉层级。；caption 只有标题，未定义数值是 oracle 预测还是其它评估。
- **可复用模式**：用“案例行 × 迭代列”的卡片网格同时编码 molecule edit、目标属性方向和每步数值，显式展示 trade-off。
- **物理证据**：p.9 渲染对象 `Figure 4 on p.9; 180 dpi rendered page inspection`，basis=`rendered_observation`。

### Figure 5（p.9，results）

- **版面与类型**：`single_column`；类型 `qualitative_grid, image_montage`；用途 `mechanism, qualitative_evidence`。复杂度 3/5，4 个 panel，约 4 个 annotation。
- **Plot grammar**：rendering=`mixed`；x/y scale=`none`/`none`；grid=`none`；legend=`False`；direct labels=`True`；line styles=1；reference lines=0；uncertainty=`none`；line width≈0.8 pt；provenance=`rendered_estimate`。
- **Encodings**：x=每行左侧原始 fragment 到右侧 mCLM fragment；y=四类 frequent DILI modifications；color=化学元素颜色；shape=前后分子结构 pair；line=每行中间的弧形 mCLM arrow 与横向分隔线；facet=四行 modification examples；text=mCLM 标签与连接位点标记。
- **Typography/color**：字体 `Arial, Myriad Pro, Nunito`，字号约 6.0–10.0 pt（中位 8.0），字重 `regular, bold`，来源=`rendered_estimate`；颜色模式 `categorical`、约 7 色、来源=`rendered_estimate`，语义：元素颜色区分分子原子类别；蓝色圆角外框、灰色分隔线和 mCLM 曲线箭头区分四条 modification pair。 灰度安全=False。
- **Caption**（15 词；moves=`title`；headline bold=False；self-contained=False；main finding=False）：Figure 5: Four of the most frequent mCLM-proposed modifications to improve DILI in FDA-approved drugs.
- **数据与证据关系**：四个 DILI modification 的结构性例子；caption 称它们是最频繁类别，但图中没有 frequency count、分母、重复数或不确定性。 §3.4 的 functionally-important blocks/virtual screening 主张 → Figure 5 的四个结构 transformation → 与 Figure 4 的案例编辑、Table 1 的 DILI property 改善相互补。
- **设计优点**：单栏 inset 适合快速比较四个局部结构变化。；行分隔和中心箭头强化 before/after 语法。；与 Figure 4 使用相同元素颜色和边框语言。
- **设计弱点**：没有给出“most frequent”的频数或筛选基数。；caption 未说明连接位点或方向含义。；结构图较小，非化学读者难以识别变化。
- **可复用模式**：用固定的 before/after 分子 pair 与中心模型箭头展示可复用的 modification grammar；若用于主文，应附频数或代表性规则。
- **物理证据**：p.9 渲染对象 `Figure 5 on p.9; 180 dpi rendered page inspection`，basis=`rendered_observation`。

### Figure 6（p.21，appendix A.3）

- **版面与类型**：`page_width`；类型 `qualitative_grid, image_montage, pipeline`；用途 `failure, qualitative_evidence`。复杂度 4/5，8 个 panel，约 12 个 annotation。
- **Plot grammar**：rendering=`mixed`；x/y scale=`categorical`/`categorical`；grid=`none`；legend=`False`；direct labels=`True`；line styles=1；reference lines=0；uncertainty=`none`；line width≈1.0 pt；provenance=`rendered_estimate`。
- **Encodings**：x=四个 property-editing steps；y=Evobrutinib 与 TNG348 两个 MoleculeSTM 案例；color=元素颜色与红色 INVALID 状态；shape=八个圆角卡片；line=步骤间实线箭头及 Decrease/Increase labels；facet=两个药物案例行；text=drug/target/failure labels、property 数值与 INVALID。
- **Typography/color**：字体 `Arial, Myriad Pro, Nunito`，字号约 6.0–10.0 pt（中位 8.0），字重 `regular, bold`，来源=`rendered_estimate`；颜色模式 `categorical`、约 7 色、来源=`rendered_estimate`，语义：元素颜色显示结构，深蓝框/箭头显示迭代，红色 INVALID 文本编码失败状态。 灰度安全=False。
- **Caption**（10 词；moves=`title`；headline bold=False；self-contained=False；main finding=False）：Figure 6: Examples of fallen angel property modification using MoleculeSTM.
- **数据与证据关系**：MoleculeSTM baseline 的两行四步案例；Evobrutinib 在第一步后、TNG348 在第三步后出现 syntactically incorrect molecule，INVALID 卡片没有属性值或误差。 Figure 4 的 mCLM fallen-angels 路径 → Appendix A.3 的 MoleculeSTM 复现 → 失败证据补充 Table 2 validity 与 Figure 4 的成功案例，界定案例比较的边界。
- **设计优点**：与 Figure 4 保持相同网格和箭头语法，便于 baseline 对照。；INVALID 直接写入对应步骤，失败位置清晰。；左侧 drug/target/failure context 保留案例解释。
- **设计弱点**：caption 未说明 INVALID 的判定器或重试策略。；失败卡片留白较大，数值与结构信息不对称。；仍只有两个案例，无法估计 baseline failure rate。
- **可复用模式**：复制主结果的案例网格，只替换生成器并显式标记失效步骤，形成可审计的失败对照。
- **物理证据**：p.21 渲染对象 `Figure 6 on p.21; 180 dpi rendered page inspection`，basis=`rendered_observation`。

### Figure 7（p.22，appendix C）

- **版面与类型**：`page_width`；类型 `pipeline, architecture, conceptual_diagram`；用途 `experimental_design, method_interface, reproduction`。复杂度 5/5，7 个 panel，约 18 个 annotation。
- **Plot grammar**：rendering=`mixed`；x/y scale=`none`/`none`；grid=`none`；legend=`False`；direct labels=`True`；line styles=3；reference lines=0；uncertainty=`none`；line width≈0.9 pt；provenance=`rendered_estimate`。
- **Encodings**：x=Training Corpus → tokenizer/selection → generation/inference outcomes；y=corpus、tokenizer、vocabulary selection、retrieval/generation stage；color=四类 corpus data、tokenizer type、function-aware selection 与 outcomes；shape=rounded boxes、molecule diagrams、colored arrows；line=实线、黑色双向箭头、黄色长回路与虚线/分隔线；facet=上方 corpus；中部三 tokenizer；下方三 outcome blocks；text=任务模板、tokenization、Fully Synthesizable Vocab、Large-scale Pretraining Vocab 等阶段标签。
- **Typography/color**：字体 `Arial, Myriad Pro, Nunito`，字号约 5.5–10.0 pt（中位 8.0），字重 `regular, bold, medium`，来源=`rendered_estimate`；颜色模式 `categorical`、约 8 色、来源=`rendered_estimate`，语义：青/橙/蓝/黄数据源、浅黄 synthesis outcome、浅绿 vocabulary selection、深蓝 tokenizer 标题共同编码数据类型与流程阶段。 灰度安全=False。
- **Caption**（6 词；moves=`title`；headline bold=False；self-contained=False；main finding=False）：Figure 7: Overview of Data Creation.
- **数据与证据关系**：数据流概念图，没有数量轴或不确定性；展示 training corpus 的四类任务、synthesis-aware/flexible/text tokenizer、function-aware vocabulary selection 和 inference-time retrieval 的关系。Table 5/6 提供图中数据类别的样本/分子数量。 §2.1–§2.2 的数据/词汇构造 → Figure 7 的完整 pipeline → Tables 5–7 的数据计数、oracle task 与 AUC；它把实现细节接到复现材料，但不替代代码和原始数据。
- **设计优点**：从 corpus 到 inference 的全链路在一张图内闭合。；三种 tokenizer 与三种输出块的并列结构清晰。；用箭头回路表达词汇选择和 inference retrieval 的反馈关系。
- **设计弱点**：面板、箭头和小分子图形很多，阅读顺序需要依赖颜色/箭头。；没有把样本计数直接放在图中，需跳转 Table 5/6。；图内颜色兼具数据类型和流程角色，语义层级较多。
- **可复用模式**：以“数据源—表示转换—词汇选择—生成路径”四级流程图统一呈现训练和推理接口，并将量化规模下沉到配套表格。
- **物理证据**：p.22 渲染对象 `Figure 7 on p.22; 180 dpi rendered page inspection`，basis=`rendered_observation`。

### Figure 8（p.28，appendix F.2）

- **版面与类型**：`page_width`；类型 `image_montage, qualitative_grid`；用途 `qualitative_evidence, reproduction`。复杂度 3/5，6 个 panel，约 6 个 annotation。
- **Plot grammar**：rendering=`raster`；x/y scale=`none`/`none`；grid=`none`；legend=`False`；direct labels=`True`；line styles=0；reference lines=0；uncertainty=`none`；line width≈None pt；provenance=`rendered_estimate`。
- **Encodings**：x=molecule position: Ground Truth plus five neighbors；y=两行分子 montage；color=元素类别；shape=二维化学结构绘图；line=None；facet=ground-truth token 与 nearest-neighbor examples；text=Ground Truth、Score 数值和连接位点标记。
- **Typography/color**：字体 `Arial`，字号约 5.0–8.0 pt（中位 6.5），字重 `regular`，来源=`rendered_estimate`；颜色模式 `categorical`、约 6 色、来源=`rendered_estimate`，语义：RDKit 风格的元素颜色编码 O/N/卤素等原子类别，黑色键线保留结构轮廓。 灰度安全=False。
- **Caption**（21 词；moves=`title, encoding_key`；headline bold=False；self-contained=True；main finding=False）：Figure 8: Example token from pretraining dataset. Similarity above each molecule is ECFP4 Fingerprint Tanimoto similarity from the ground truth molecule.
- **数据与证据关系**：六个分子图（一个 Ground Truth、五个邻居）按 ECFP4 Fingerprint Tanimoto similarity 排序/标注；ground truth 加 5 个 nearest neighbors，分数 0.736、0.730、0.716、0.700、0.699。 没有误差、重复抽样说明或邻居检索阈值。 Appendix F.2 的 token embedding/nearest-neighbor 例子 → Figures 8–13 的结构相似性证据 → Figure 1/3/7 的模块化 token 解释；这些图只展示定性样例，不能单独支持词汇整体质量。
- **设计优点**：六个例子固定为 2×3，Ground Truth 与分数位置一致。；分子结构和数值分数并置，便于检查相似性是否具有化学直觉。；重复 caption 明确给出 ECFP4/Tanimoto 定义。
- **设计弱点**：六图的重复 caption 占据版面，未提供 token ID、检索库规模或选择规则。；分子图内标签字号很小，颜色对灰度读者不友好。；分数是单次邻居示例，没有分布、基线或系统性检验。
- **可复用模式**：用固定 2×3 montage 展示 token 的 ground truth 与五个结构邻居，并把相似度数值放在每个结构上方。
- **物理证据**：p.28 渲染对象 `Figure 8 on p.28; 180 dpi rendered page inspection`，basis=`rendered_observation`。

### Figure 9（p.28，appendix F.2）

- **版面与类型**：`page_width`；类型 `image_montage, qualitative_grid`；用途 `qualitative_evidence, reproduction`。复杂度 3/5，6 个 panel，约 6 个 annotation。
- **Plot grammar**：rendering=`raster`；x/y scale=`none`/`none`；grid=`none`；legend=`False`；direct labels=`True`；line styles=0；reference lines=0；uncertainty=`none`；line width≈None pt；provenance=`rendered_estimate`。
- **Encodings**：x=molecule position: Ground Truth plus five neighbors；y=两行分子 montage；color=元素类别；shape=二维化学结构绘图；line=None；facet=ground-truth token 与 nearest-neighbor examples；text=Ground Truth、Score 数值和连接位点标记。
- **Typography/color**：字体 `Arial`，字号约 5.0–8.0 pt（中位 6.5），字重 `regular`，来源=`rendered_estimate`；颜色模式 `categorical`、约 6 色、来源=`rendered_estimate`，语义：RDKit 风格的元素颜色编码 O/N/卤素等原子类别，黑色键线保留结构轮廓。 灰度安全=False。
- **Caption**（21 词；moves=`title, encoding_key`；headline bold=False；self-contained=True；main finding=False）：Figure 9: Example token from pretraining dataset. Similarity above each molecule is ECFP4 Fingerprint Tanimoto similarity from the ground truth molecule.
- **数据与证据关系**：六个分子图（一个 Ground Truth、五个邻居）按 ECFP4 Fingerprint Tanimoto similarity 排序/标注；ground truth 加 5 个 nearest neighbors，分数 0.999、0.984、0.981、0.971、0.969。 没有误差、重复抽样说明或邻居检索阈值。 Appendix F.2 的 token embedding/nearest-neighbor 例子 → Figures 8–13 的结构相似性证据 → Figure 1/3/7 的模块化 token 解释；这些图只展示定性样例，不能单独支持词汇整体质量。
- **设计优点**：六个例子固定为 2×3，Ground Truth 与分数位置一致。；分子结构和数值分数并置，便于检查相似性是否具有化学直觉。；重复 caption 明确给出 ECFP4/Tanimoto 定义。
- **设计弱点**：六图的重复 caption 占据版面，未提供 token ID、检索库规模或选择规则。；分子图内标签字号很小，颜色对灰度读者不友好。；分数是单次邻居示例，没有分布、基线或系统性检验。
- **可复用模式**：用固定 2×3 montage 展示 token 的 ground truth 与五个结构邻居，并把相似度数值放在每个结构上方。
- **物理证据**：p.28 渲染对象 `Figure 9 on p.28; 180 dpi rendered page inspection`，basis=`rendered_observation`。

### Figure 10（p.29，appendix F.2）

- **版面与类型**：`page_width`；类型 `image_montage, qualitative_grid`；用途 `qualitative_evidence, reproduction`。复杂度 3/5，6 个 panel，约 6 个 annotation。
- **Plot grammar**：rendering=`raster`；x/y scale=`none`/`none`；grid=`none`；legend=`False`；direct labels=`True`；line styles=0；reference lines=0；uncertainty=`none`；line width≈None pt；provenance=`rendered_estimate`。
- **Encodings**：x=molecule position: Ground Truth plus five neighbors；y=两行分子 montage；color=元素类别；shape=二维化学结构绘图；line=None；facet=ground-truth token 与 nearest-neighbor examples；text=Ground Truth、Score 数值和连接位点标记。
- **Typography/color**：字体 `Arial`，字号约 5.0–8.0 pt（中位 6.5），字重 `regular`，来源=`rendered_estimate`；颜色模式 `categorical`、约 6 色、来源=`rendered_estimate`，语义：RDKit 风格的元素颜色编码 O/N/卤素等原子类别，黑色键线保留结构轮廓。 灰度安全=False。
- **Caption**（21 词；moves=`title, encoding_key`；headline bold=False；self-contained=True；main finding=False）：Figure 10: Example token from pretraining dataset. Similarity above each molecule is ECFP4 Fingerprint Tanimoto similarity from the ground truth molecule.
- **数据与证据关系**：六个分子图（一个 Ground Truth、五个邻居）按 ECFP4 Fingerprint Tanimoto similarity 排序/标注；ground truth 加 5 个 nearest neighbors，分数 0.998、0.995、0.990、0.989、0.982。 没有误差、重复抽样说明或邻居检索阈值。 Appendix F.2 的 token embedding/nearest-neighbor 例子 → Figures 8–13 的结构相似性证据 → Figure 1/3/7 的模块化 token 解释；这些图只展示定性样例，不能单独支持词汇整体质量。
- **设计优点**：六个例子固定为 2×3，Ground Truth 与分数位置一致。；分子结构和数值分数并置，便于检查相似性是否具有化学直觉。；重复 caption 明确给出 ECFP4/Tanimoto 定义。
- **设计弱点**：六图的重复 caption 占据版面，未提供 token ID、检索库规模或选择规则。；分子图内标签字号很小，颜色对灰度读者不友好。；分数是单次邻居示例，没有分布、基线或系统性检验。
- **可复用模式**：用固定 2×3 montage 展示 token 的 ground truth 与五个结构邻居，并把相似度数值放在每个结构上方。
- **物理证据**：p.29 渲染对象 `Figure 10 on p.29; 180 dpi rendered page inspection`，basis=`rendered_observation`。

### Figure 11（p.29，appendix F.2）

- **版面与类型**：`page_width`；类型 `image_montage, qualitative_grid`；用途 `qualitative_evidence, reproduction`。复杂度 3/5，6 个 panel，约 6 个 annotation。
- **Plot grammar**：rendering=`raster`；x/y scale=`none`/`none`；grid=`none`；legend=`False`；direct labels=`True`；line styles=0；reference lines=0；uncertainty=`none`；line width≈None pt；provenance=`rendered_estimate`。
- **Encodings**：x=molecule position: Ground Truth plus five neighbors；y=两行分子 montage；color=元素类别；shape=二维化学结构绘图；line=None；facet=ground-truth token 与 nearest-neighbor examples；text=Ground Truth、Score 数值和连接位点标记。
- **Typography/color**：字体 `Arial`，字号约 5.0–8.0 pt（中位 6.5），字重 `regular`，来源=`rendered_estimate`；颜色模式 `categorical`、约 6 色、来源=`rendered_estimate`，语义：RDKit 风格的元素颜色编码 O/N/卤素等原子类别，黑色键线保留结构轮廓。 灰度安全=False。
- **Caption**（21 词；moves=`title, encoding_key`；headline bold=False；self-contained=True；main finding=False）：Figure 11: Example token from pretraining dataset. Similarity above each molecule is ECFP4 Fingerprint Tanimoto similarity from the ground truth molecule.
- **数据与证据关系**：六个分子图（一个 Ground Truth、五个邻居）按 ECFP4 Fingerprint Tanimoto similarity 排序/标注；ground truth 加 5 个 nearest neighbors，分数 1.000、0.865、0.847、0.818、0.816。 没有误差、重复抽样说明或邻居检索阈值。 Appendix F.2 的 token embedding/nearest-neighbor 例子 → Figures 8–13 的结构相似性证据 → Figure 1/3/7 的模块化 token 解释；这些图只展示定性样例，不能单独支持词汇整体质量。
- **设计优点**：六个例子固定为 2×3，Ground Truth 与分数位置一致。；分子结构和数值分数并置，便于检查相似性是否具有化学直觉。；重复 caption 明确给出 ECFP4/Tanimoto 定义。
- **设计弱点**：六图的重复 caption 占据版面，未提供 token ID、检索库规模或选择规则。；分子图内标签字号很小，颜色对灰度读者不友好。；分数是单次邻居示例，没有分布、基线或系统性检验。
- **可复用模式**：用固定 2×3 montage 展示 token 的 ground truth 与五个结构邻居，并把相似度数值放在每个结构上方。
- **物理证据**：p.29 渲染对象 `Figure 11 on p.29; 180 dpi rendered page inspection`，basis=`rendered_observation`。

### Figure 12（p.30，appendix F.2）

- **版面与类型**：`page_width`；类型 `image_montage, qualitative_grid`；用途 `qualitative_evidence, reproduction`。复杂度 3/5，6 个 panel，约 6 个 annotation。
- **Plot grammar**：rendering=`raster`；x/y scale=`none`/`none`；grid=`none`；legend=`False`；direct labels=`True`；line styles=0；reference lines=0；uncertainty=`none`；line width≈None pt；provenance=`rendered_estimate`。
- **Encodings**：x=molecule position: Ground Truth plus five neighbors；y=两行分子 montage；color=元素类别；shape=二维化学结构绘图；line=None；facet=ground-truth token 与 nearest-neighbor examples；text=Ground Truth、Score 数值和连接位点标记。
- **Typography/color**：字体 `Arial`，字号约 5.0–8.0 pt（中位 6.5），字重 `regular`，来源=`rendered_estimate`；颜色模式 `categorical`、约 6 色、来源=`rendered_estimate`，语义：RDKit 风格的元素颜色编码 O/N/卤素等原子类别，黑色键线保留结构轮廓。 灰度安全=False。
- **Caption**（21 词；moves=`title, encoding_key`；headline bold=False；self-contained=True；main finding=False）：Figure 12: Example token from pretraining dataset. Similarity above each molecule is ECFP4 Fingerprint Tanimoto similarity from the ground truth molecule.
- **数据与证据关系**：六个分子图（一个 Ground Truth、五个邻居）按 ECFP4 Fingerprint Tanimoto similarity 排序/标注；ground truth 加 5 个 nearest neighbors，分数 0.980、0.967、0.941、0.939、0.923。 没有误差、重复抽样说明或邻居检索阈值。 Appendix F.2 的 token embedding/nearest-neighbor 例子 → Figures 8–13 的结构相似性证据 → Figure 1/3/7 的模块化 token 解释；这些图只展示定性样例，不能单独支持词汇整体质量。
- **设计优点**：六个例子固定为 2×3，Ground Truth 与分数位置一致。；分子结构和数值分数并置，便于检查相似性是否具有化学直觉。；重复 caption 明确给出 ECFP4/Tanimoto 定义。
- **设计弱点**：六图的重复 caption 占据版面，未提供 token ID、检索库规模或选择规则。；分子图内标签字号很小，颜色对灰度读者不友好。；分数是单次邻居示例，没有分布、基线或系统性检验。
- **可复用模式**：用固定 2×3 montage 展示 token 的 ground truth 与五个结构邻居，并把相似度数值放在每个结构上方。
- **物理证据**：p.30 渲染对象 `Figure 12 on p.30; 180 dpi rendered page inspection`，basis=`rendered_observation`。

### Figure 13（p.30，appendix F.2）

- **版面与类型**：`page_width`；类型 `image_montage, qualitative_grid`；用途 `qualitative_evidence, reproduction`。复杂度 3/5，6 个 panel，约 6 个 annotation。
- **Plot grammar**：rendering=`raster`；x/y scale=`none`/`none`；grid=`none`；legend=`False`；direct labels=`True`；line styles=0；reference lines=0；uncertainty=`none`；line width≈None pt；provenance=`rendered_estimate`。
- **Encodings**：x=molecule position: Ground Truth plus five neighbors；y=两行分子 montage；color=元素类别；shape=二维化学结构绘图；line=None；facet=ground-truth token 与 nearest-neighbor examples；text=Ground Truth、Score 数值和连接位点标记。
- **Typography/color**：字体 `Arial`，字号约 5.0–8.0 pt（中位 6.5），字重 `regular`，来源=`rendered_estimate`；颜色模式 `categorical`、约 6 色、来源=`rendered_estimate`，语义：RDKit 风格的元素颜色编码 O/N/卤素等原子类别，黑色键线保留结构轮廓。 灰度安全=False。
- **Caption**（21 词；moves=`title, encoding_key`；headline bold=False；self-contained=True；main finding=False）：Figure 13: Example token from pretraining dataset. Similarity above each molecule is ECFP4 Fingerprint Tanimoto similarity from the ground truth molecule.
- **数据与证据关系**：六个分子图（一个 Ground Truth、五个邻居）按 ECFP4 Fingerprint Tanimoto similarity 排序/标注；ground truth 加 5 个 nearest neighbors，分数 0.841、0.841、0.805、0.802、0.799。 没有误差、重复抽样说明或邻居检索阈值。 Appendix F.2 的 token embedding/nearest-neighbor 例子 → Figures 8–13 的结构相似性证据 → Figure 1/3/7 的模块化 token 解释；这些图只展示定性样例，不能单独支持词汇整体质量。
- **设计优点**：六个例子固定为 2×3，Ground Truth 与分数位置一致。；分子结构和数值分数并置，便于检查相似性是否具有化学直觉。；重复 caption 明确给出 ECFP4/Tanimoto 定义。
- **设计弱点**：六图的重复 caption 占据版面，未提供 token ID、检索库规模或选择规则。；分子图内标签字号很小，颜色对灰度读者不友好。；分数是单次邻居示例，没有分布、基线或系统性检验。
- **可复用模式**：用固定 2×3 montage 展示 token 的 ground truth 与五个结构邻居，并把相似度数值放在每个结构上方。
- **物理证据**：p.30 渲染对象 `Figure 13 on p.30; 180 dpi rendered page inspection`，basis=`rendered_observation`。

## 6. Table 逐对象审计

### Table 1（p.7，results）

- **版面与用途**：`page_width`；用途 `headline, main_comparison`；body/header≈7.8/7.8 pt；9 行 × 8 列（不含 caption，行数为 body rows），表头层级 1，row groups 2。
- **表头/规则**：六项 ADMET 属性（AMES、BBBP、CYP3A4、DILI、HIA、PGP）加 Avg. Improv.；FDA Drug 为基准，8 个生成/编辑模型行含 mCLM (Ours)。 rules=`partial_grid`；decimal_precision 主导值=1（若列间混合已在不确定性/数据描述中注明）；highlighting=`bold, cell_color, text_color`。
- **Caption**（39 词；moves=`title, setup, encoding_key`；headline bold=False；self-contained=True；main finding=False）：Table 1: Average pharmacokinetic and toxicity properties of FDA drugs composed of synthesis-guaranteed blocks, as well as their proposed modifications. (↓: lower is better, ↑: higher is better). Green = better than FDA, Red = worse, Light green bold = best overall per column.
- **不确定性与证据关系**：没有统计不确定性；122 个 synthesis-guaranteed FDA drug structures 的各列平均值，绿色/红色相对 FDA，浅绿粗体为列内最佳。 §3.2 的 122-drug/OOD property claim → Table 1 主比较 → Figure 4 的案例与 Table 4 的无 guarantee 扩展。
- **设计优点**：把效果、方向和 baseline 放到同一决策面。；绿色/红色相对 FDA 和底部 mCLM 粗体使 headline row 易定位。；列名箭头明确 lower/higher-is-better。
- **设计弱点**：颜色语义在 caption 中定义但灰度打印难以保留。；不同模型、不同 property 的 trade-off 仍需读者逐列解码。；均值无分母/重复/不确定性，不能从表面判断统计稳健性。
- **可复用模式**：以 FDA reference row、方向箭头和相对颜色把多指标模型比较压缩为一个可扫读的主结果表。
- **物理证据**：p.7 渲染对象 `Table 1 on p.7; caption/header/body inspected at 180 dpi`，basis=`rendered_observation`。

### Table 2（p.8，results）

- **版面与用途**：`page_width`；用途 `main_comparison, efficiency_cost, ablation`；body/header≈8.0/8.0 pt；6 行 × 5 列（不含 caption，行数为 body rows），表头层级 1，row groups 2。
- **表头/规则**：FDA、MoleculeSTM、FineMolTex 与 mCLM 主行比较 SA/Validity/Synthesizability/Makeability；No GNN 和 No Synth. Tokenizer 两行是组件消融。 rules=`partial_grid`；decimal_precision 主导值=2（若列间混合已在不确定性/数据描述中注明）；highlighting=`bold`。
- **Caption**（42 词；moves=`title, setup, encoding_key`；headline bold=False；self-contained=True；main finding=False）：Table 2: Synthetic accessibility (SA) (Ertl et al., 2009), validity, and retrosynthetic results across baselines. Synthesizability is the percent of valid molecules where a retrosynthetic route was found. Makeability is the overall percent of generations which can be synthesized (Makeability = Valid × Synth.).
- **不确定性与证据关系**：没有重复、区间或误差；SA 是启发式点值，Validity 是 syntactically correct 百分比，Synthesizability 是 valid molecules 中找到 retrosynthetic route 的百分比，Makeability = Valid × Synth. §3.2 的 property editing → Table 2 的 synthesis decision surface → Appendix Table 3 的按属性切片、Tables 9/10 的额外 baseline。
- **设计优点**：将 validity、route finding 和总体 makeability 并列，避免只看 SA。；横向指标少，主模型与两条消融容易识别。；caption 给出 Synth./Makeability 定义。
- **设计弱点**：表中百分比精度混杂（一位与两位小数）。；Allchemy 采样和计算预算不在表内。；No GNN/No Synth. Tokenizer 的训练和评估条件需回到 Appendix I。
- **可复用模式**：把“化学合理性—逆合成—总体可制造性”串成连续三层指标，并在同表放组件消融。
- **物理证据**：p.8 渲染对象 `Table 2 on p.8; caption/header/body inspected at 180 dpi`，basis=`rendered_observation`。

### Table 3（p.20，appendix A.1）

- **版面与用途**：`page_width`；用途 `efficiency_cost, robustness, main_comparison`；body/header≈6.2/6.2 pt；7 行 × 19 列（不含 caption，行数为 body rows），表头层级 2，row groups 1。
- **表头/规则**：Dataset 行为 AMES、BBBP、CYP3A4、DILI、HIA、PGP 和 Mean；每个模型分为 SA 与 Valid (%) 两列，覆盖 FDA、mCLM、MoleculeSTM、FineMolTex、GPT-4o、GPT-5、Gemini-2.5-F、LDMol、Claude-3.5-H。 rules=`partial_grid`；decimal_precision 主导值=2（若列间混合已在不确定性/数据描述中注明）；highlighting=`bold`。
- **Caption**（12 词；moves=`title, setup`；headline bold=False；self-contained=True；main finding=False）：Table 3: Synthetic Accessibility (SA) scores with RDKit validity percentages across datasets.
- **不确定性与证据关系**：没有区间、重复或误差；六个 ADMET 数据集行加 Mean 行，SA 保留两位小数，Validity 多为两位但 100.0 以一位显示。 主文 Table 2 的总体 synthesis claim → Table 3 的按 property/模型 SA 与 RDKit validity → Appendix A.1 的 Allchemy protocol caveat。
- **设计优点**：多级模型表头让 9 个模型的 SA/validity 配对可追踪。；按数据集拆分，补充主表无法显示的属性异质性。；Mean 行提供压缩汇总。
- **设计弱点**：19 列导致字号明显小，横向比较成本高。；Mean 行和六个 property 行没有不确定性或样本数。；caption 未说明 validity 的分母和 SA 的方向。
- **可复用模式**：用两级表头把每个 baseline 的 SA 与 RDKit validity 成对展开，再以 Mean 行保留总体摘要。
- **物理证据**：p.20 渲染对象 `Table 3 on p.20; caption/header/body inspected at 180 dpi`，basis=`rendered_observation`。

### Table 4（p.21，appendix A.2）

- **版面与用途**：`page_width`；用途 `robustness, main_comparison`；body/header≈8.0/8.0 pt；2 行 × 8 列（不含 caption，行数为 body rows），表头层级 1，row groups 0。
- **表头/规则**：FDA Drug 与 mCLM 两行；AMES Mut.、BBBP、CYP3A4 Inhib.、DILI、HIA、PGP 六列加 Mean Improv.，方向箭头写在表头。 rules=`partial_grid`；decimal_precision 主导值=1（若列间混合已在不确定性/数据描述中注明）；highlighting=`bold`。
- **Caption**（32 词；moves=`title, setup, encoding_key`；headline bold=False；self-contained=True；main finding=False）：Table 4: Average pharmacokinetic and toxicity properties of FDA drugs with 3 or more blocks and their proposed modifications. Note, these molecules do not have synthesis-guarantees. (↓: lower is better, ↑: higher is better.
- **不确定性与证据关系**：没有统计不确定性；430 个不带 synthesis guarantees 的 FDA drugs 及 mCLM 修改的六属性平均值，property 列多为一位小数，Mean Improv. 为两位。 正文脚注/§3.2 的 expanded distribution → Table 4 的 confirmatory OOD slice → 与主 Table 1 的 122 synthesis-guaranteed distribution 对照。
- **设计优点**：两行 reference/model 结构直接呈现外部分布上的变化。；caption 明确这些 molecules 没有 synthesis guarantees。；方向箭头和 Mean Improv. 使多属性摘要可读。
- **设计弱点**：PDF caption 的末尾方向括号缺少右括号，属于可见排版瑕疵。；只有均值，没有分布、样本级异质性或验证集信息。；与 Table 1 的 property 命名略有差异（AMES Mut./CYP3A4 Inhib.）。
- **可复用模式**：用同一组 property header 对齐 FDA 与修改后均值，并明确把无 synthesis guarantee 的扩展分布标成附加证据。
- **物理证据**：p.21 渲染对象 `Table 4 on p.21; caption/header/body inspected at 180 dpi`，basis=`rendered_observation`。

### Table 5（p.23，appendix C.2）

- **版面与用途**：`page_width`；用途 `dataset, reproduction`；body/header≈7.0/7.0 pt；20 行 × 5 列（不含 caption，行数为 body rows），表头层级 1，row groups 3。
- **表头/规则**：20 个 body rows，5 个 count columns；粗体行标记数据来源/合成来源组，整数使用千位分隔。 rules=`grid`；decimal_precision 主导值=0（若列间混合已在不确定性/数据描述中注明）；highlighting=`bold`。
- **Caption**（9 词；moves=`title, setup`；headline bold=False；self-contained=False；main finding=False）：Table 5: Dataset categories and their respective sample counts.
- **不确定性与证据关系**：全为样本计数，没有抽样误差或不确定性；All、Existing Data Sources、Synthetic Real Data (Ours)、Synthetic Oracle Data (Ours) 及其 instruction 子类按 Full Data/Only Blocks/Top 100k/Top 10k/Top 1000 计数。 Figure 7 的 training corpus → Table 5 的 instruction sample counts → Table 6 的 molecule/token breakdown → Appendix C 的过滤与预训练词汇选择。
- **设计优点**：把五个 vocabulary/data subset 放在同一横向表头。；数据源组与任务子类通过粗体和行顺序区分。；整数计数和总行便于复现数据混合。
- **设计弱点**：表头中的 Full/Only Blocks/Top-k 依赖正文定义。；完整网格和 20 行使层级关系需要逐行追踪。；没有数据版本、去重规则或时间点。
- **可复用模式**：用“来源组—任务子类—词汇子集”三级行结构报告训练样本量，并保留总量行作为 sanity check。
- **物理证据**：p.23 渲染对象 `Table 5 on p.23; caption/header/body inspected at 180 dpi`，basis=`rendered_observation`。

### Table 6（p.24，appendix C.2）

- **版面与用途**：`page_width`；用途 `dataset, reproduction`；body/header≈6.5/6.5 pt；15 行 × 10 列（不含 caption，行数为 body rows），表头层级 2，row groups 5。
- **表头/规则**：两级表头：Molecules 下 Total/Tokenized/Untokenized/Scaffolds，Molecule Tokens 下 Cap/Mid/Total/Synthesis-Aware；Subset 与 Source 为左侧识别列。 rules=`partial_grid`；decimal_precision 主导值=0（若列间混合已在不确定性/数据描述中注明）；highlighting=`none`。
- **Caption**（9 词；moves=`title, setup`；headline bold=False；self-contained=False；main finding=False）：Table 6: Breakdown of molecule data by dataset category.
- **不确定性与证据关系**：全为整数计数，没有抽样误差；每个 subset（Full Data、Only Blocks、Top 100k、Top 10k、Top 1000）含 SMolInstruct、Ours、Total 三行。 Table 5 的 instruction counts → Table 6 的 molecule/token representation counts → Figure 7 tokenizer paths；正文定义 Caps、Mid、Synthesis-aware。
- **设计优点**：二级表头把 molecule 数与 token 数的单位分开。；每个 subset 的 Total 行便于检查分项和。；垂直分隔线帮助在 10 列中保持列边界。
- **设计弱点**：字号小且列数多，Cap/Mid/Synthesis-Aware 需要正文解码。；没有比例、coverage rate 或数据版本。；部分网格线和 Total 行层级在灰度小尺寸下不够突出。
- **可复用模式**：用分组表头同时报告 molecule coverage 与 token vocabulary composition，按 subset 重复同构三行。
- **物理证据**：p.24 渲染对象 `Table 6 on p.24; caption/header/body inspected at 180 dpi`，basis=`rendered_observation`。

### Table 7（p.26，appendix D.2.3）

- **版面与用途**：`page_width`；用途 `experimental_design, reproduction`；body/header≈8.0/8.0 pt；4 行 × 7 列（不含 caption，行数为 body rows），表头层级 1，row groups 0。
- **表头/规则**：Model 加 AMES、Pgp、DILI、BBBP、CYP3A4、HIA 六列；Ensemble 行与各列最佳值以粗体标出。 rules=`booktabs`；decimal_precision 主导值=2（若列间混合已在不确定性/数据描述中注明）；highlighting=`bold`。
- **Caption**（15 词；moves=`title, setup`；headline bold=False；self-contained=True；main finding=False）：Table 7: Performance (AUC) of individual models and the ensemble across six selected ADMET tasks.
- **不确定性与证据关系**：没有区间、seed 聚合或误差；表格报告 six selected ADMET tasks 上各 oracle model 与 Ensemble 的 AUC point estimates。 Appendix D.2.3 的 oracle construction/threshold selection → Table 7 的 AUC model check → Table 1/4 的 property editing evaluation。
- **设计优点**：紧凑地验证 ensemble 与 base learner 的六任务覆盖。；粗体使每列最佳 AUC 快速可见。；booktabs 风格和单级表头简洁。
- **设计弱点**：caption 没有说明数据 split、样本数或阈值选择细节。；AUC 与后续 property score 的关系需回到正文。；粗体最佳值可能被误读为统计显著优势。
- **可复用模式**：先以逐任务 AUC 表核查 oracle，再把 ensemble 作为后续 property optimization 的评估器。
- **物理证据**：p.26 渲染对象 `Table 7 on p.26; caption/header/body inspected at 180 dpi`，basis=`rendered_observation`。

### Table 8（p.32，appendix H）

- **版面与用途**：`page_width`；用途 `robustness, main_comparison`；body/header≈8.0/8.0 pt；2 行 × 8 列（不含 caption，行数为 body rows），表头层级 1，row groups 0。
- **表头/规则**：Model 加 AMES、BBBP、CYP3A4、DILI、HIA、PGP、Avg. Improv.；箭头给方向，粗体显示各列较优值。 rules=`partial_grid`；decimal_precision 主导值=1（若列间混合已在不确定性/数据描述中注明）；highlighting=`bold`。
- **Caption**（30 词；moves=`title, setup`；headline bold=False；self-contained=True；main finding=False）：Table 8: Comparison of pharmacokinetic and toxicity property scores between mCLM and HierVAE. Note that HierVAE is fine-tuned separately for each property, whereas mCLM is used in a instruction-following setting.
- **不确定性与证据关系**：没有不确定性；HierVAE 与 mCLM 在六项 property 和 Avg. Improv. 上的平均 point scores，HierVAE 每个 property 单独 fine-tune，mCLM 是 instruction-following。 Appendix H 的 additional baseline → Table 8 property comparison → Table 9 synthesis comparison；同时补充主 Table 1 的 baseline coverage。
- **设计优点**：caption 明确 HierVAE 与 mCLM 的训练/推理条件不对称。；六属性与平均改善值共用一行式 header。；粗体突出列内较优但保留全部 raw values。
- **设计弱点**：比较条件不对称，表格本身不能消除 unfair advantage。；单一均值没有 property-level variance 或分布。；Caption 的 “a instruction-following” 语法问题影响专业度。
- **可复用模式**：在同一 property header 下并列 baseline 与 proposed model，同时把训练条件不对称写进 caption。
- **物理证据**：p.32 渲染对象 `Table 8 on p.32; caption/header/body inspected at 180 dpi`，basis=`rendered_observation`。

### Table 9（p.33，appendix H）

- **版面与用途**：`page_width`；用途 `efficiency_cost, main_comparison`；body/header≈7.8/7.8 pt；3 行 × 5 列（不含 caption，行数为 body rows），表头层级 1，row groups 0。
- **表头/规则**：Model 加 SA (↓)、Validity (%)、Synthesizability (%)、Makeability (%)；mCLM validity/synthesis/makeability 粗体，HierVAE SA 较低但未用额外颜色。 rules=`partial_grid`；decimal_precision 主导值=2（若列间混合已在不确定性/数据描述中注明）；highlighting=`bold`。
- **Caption**（42 词；moves=`title, setup, encoding_key`；headline bold=False；self-contained=True；main finding=False）：Table 9: Synthetic accessibility (SA) (Ertl et al., 2009), validity, and retrosynthetic results for HierVAE. Synthesizability is the percent of valid molecules where a retrosynthetic route was found. Makeability is the overall percent of generations which can be synthesized (Makeability = Valid × Synth.).
- **不确定性与证据关系**：没有区间或重复；FDA、mCLM、HierVAE 三行报告 SA、RDKit validity、Allchemy synthesizability 与 Makeability 点值，百分比混有一位/两位小数。 Table 8 的 property baseline → Table 9 的 synthesis baseline → Table 2 的主 synthesis decision surface 与 Appendix A.1 protocol。
- **设计优点**：与 Table 2 相同的四个 synthesis metrics 便于直接对照。；将 HierVAE 的 synthesis trade-off 置于同一表。；caption 复述 Synthesizability/Makeability 定义。
- **设计弱点**：SA 与实际 retrosynthesis 的关系仍是两个不同 metric。；无抽样数、计算预算或重复。；精度不统一，mCLM/HierVAE 条件不完全可比。
- **可复用模式**：沿用主 synthesis 表的四指标列，在附录中加入新 baseline 并保持字段可拼接。
- **物理证据**：p.33 渲染对象 `Table 9 on p.33; caption/header/body inspected at 180 dpi`，basis=`rendered_observation`。

### Table 10（p.33，appendix H）

- **版面与用途**：`page_width`；用途 `ablation, efficiency_cost`；body/header≈7.8/7.8 pt；2 行 × 5 列（不含 caption，行数为 body rows），表头层级 1，row groups 0。
- **表头/规则**：Model 加四个 synthesis columns；mCLM 在 Synthesizability、Makeability 与 SA Score 中以粗体显示优势，Validity 两者均为 100.0。 rules=`partial_grid`；decimal_precision 主导值=1（若列间混合已在不确定性/数据描述中注明）；highlighting=`bold`。
- **Caption**（9 词；moves=`title, setup`；headline bold=False；self-contained=True；main finding=False）：Table 10: Synthesizability comparison using Allchemy on QM9 molecules.
- **不确定性与证据关系**：没有不确定性；QM9 上 DGAE (VQ-based) 与 mCLM 的 Validity、Synthesizability、Makeability 和 SA Score 点值。 Appendix H 的 vector-quantized codebook discussion → Table 10 QM9 controlled comparison → Figure 3 的 chemically grounded tokenizer 与 Table 2 的 No Synth. Tokenizer ablation。
- **设计优点**：数据集和指标少，DGAE-vs-mCLM 机制对照明确。；同时保留 validity 与 route-based makeability。；caption 直接说明 Allchemy 与 QM9。
- **设计弱点**：mCLM 使用 synthesis-guaranteed QM9 blocks，条件边界需读正文。；没有生成数、采样策略或置信度。；“nearly doubles” 的叙述依赖表外解释。
- **可复用模式**：用一个外部 representation baseline 与 proposed chemically grounded blocks 做小规模、同列 synthesis 对照。
- **物理证据**：p.33 渲染对象 `Table 10 on p.33; caption/header/body inspected at 180 dpi`，basis=`rendered_observation`。

### Table 11（p.33，ablation）

- **版面与用途**：`page_width`；用途 `ablation, main_comparison`；body/header≈7.8/7.8 pt；4 行 × 8 列（不含 caption，行数为 body rows），表头层级 1，row groups 0。
- **表头/规则**：Model 加 AMES、BBBP、CYP3A4、DILI、HIA、PGP、Avg. Improv.；粗体在列内突出较优值，但没有 cell color。 rules=`partial_grid`；decimal_precision 主导值=1（若列间混合已在不确定性/数据描述中注明）；highlighting=`bold`。
- **Caption**（18 词；moves=`title, setup`；headline bold=False；self-contained=True；main finding=False）：Table 11: Comparison of pharmacokinetic and toxicity property scores between using Qwen-2.5-3B and Llama-3.2-3B as backbones.
- **不确定性与证据关系**：没有不确定性；四个 mCLM 配置在六项 property 与 Avg. Improv. 上的平均 point scores，配置包括 Llama-3.2-3B、Qwen-2.5-3B、No GNN、No Synth. Tokenizer。 Appendix I 的 backbone/encoding ablations → Table 11 property consequences → Table 2 synthesis ablations 与 Appendix E training procedure。
- **设计优点**：同一表同时承载 backbone 替代与 tokenizer/GNN 删除。；与 Table 1/8 共用 property header，便于横向读取。；粗体让 Qwen 3B headline 与退化行可定位。
- **设计弱点**：不同 ablation 改变的组件不等价，需依赖行名和正文。；平均值没有 seed/重复或训练曲线，overfitting 证据在表外。；精度混杂（98.99、15.0、-7.53、-19.0）。
- **可复用模式**：在固定 property header 下并列 backbone 与结构组件消融，并用同一 Avg. Improv. 汇总列保持决策面一致。
- **物理证据**：p.33 渲染对象 `Table 11 on p.33; caption/header/body inspected at 180 dpi`，basis=`rendered_observation`。

## 7. 跨对象系统判断

- **visual_narrative**：Figure 1 先提出模块化 chemical vocabulary，Figure 2 展开 bilingual architecture，Figure 3 给出 synthesis-guaranteed tokenizer 的 coverage/fallback。Table 1/2 再给 property 与 synthesis 主比较，Figure 4/5 提供案例和 frequent modification；Appendix Figure 6 给 MoleculeSTM failure，Figure 7 给 data pipeline，Tables 3–11 补齐 property slice、数据规模、oracle、额外 baseline 和 ablation，Figures 8–13 用 token-neighbor montage 收束 representation 例子。
- **caption_system**：主文 Figure 1–3 和 Table 1/2 的 caption 同时承担标题、设置和编码定义；Figure 4–7 多为标题式短 caption。Figure 8–13 重复说明 ECFP4 Fingerprint Tanimoto similarity，但未写 token ID、检索库大小或抽样规则。Table 2/9 的 caption 定义 Synthesizability/Makeability，Table 1/4 定义方向与颜色。
- **table_header_system**：多数表使用单级 Model/Property header，并以 (↓)/(↑) 表示优化方向和 (%) 表示比例。Table 3 按模型分组 SA/Valid 两级表头，Table 6 按 Molecules/Molecule Tokens 两级表头；主结果、附录 baseline 和 ablation 共用 AMES–PGP–Avg. Improv. 这组 header。
- **method_result_ablation_link**：Figure 3/7 解释 tokenizer 和数据创建接口；Table 1/2 与 Figure 4 实例化 property/synthesis 结果；Table 3、8、9、10 扩展 synthesis 与 baseline，Table 11 直接隔离 backbone、GNN 和 tokenizer 组件。
- **main_appendix_link**：正文 Table 1/2 指向 Appendix A 的 Table 3/4；正文 Figure 4 对应 Appendix A.3 的 Figure 6；§2 与 reproducibility statement 指向 Figure 7 和 Tables 5–7；Appendix H/I 的 Tables 8–11 提供额外 baseline/ablation。Figures 8–13 展开 Appendix F.2 的 token examples。
- **typography_consistency**：caption/正文和表格的主体字体对象一致（Nimbus Roman No9 L），表头/最佳值用 bold；图内 sans-serif 标签与 chemical drawing 文字的字号跨度大，Figure 8–13 的 score label 特别小。
- **color_consistency**：化学元素颜色在 Figure 1–7 和 Figure 8–13 中重复出现，深蓝框线/箭头构成主图 UI 语言；Table 1 的绿/红相对基准语义只在该表使用，未与图中颜色冲突，但依赖颜色的区分在灰度输出会减弱。

## 8. 最终判断

### most_reusable_patterns
- 以 Figure 1–3 的 vocabulary→architecture→tokenizer 三步链把抽象方法主张落到可视化接口。
- 用 Figure 4/6 同构的案例卡片网格同时编码结构、属性方向、数值和失败位置。
- 用 Table 1/2 的方向箭头、相对颜色、makeability 分解和组件行建立单一决策面。
- 用 Figure 7 与 Tables 5/6 把数据流和可复现规模分离但互相引用。
### highest_value_objects
- Figure 3（synthesis guarantee、coverage check 与 fallback 的方法边界最清楚）。
- Table 2（validity、synthesizability、makeability 与 ablation 的主决策面）。
- Figure 4 + Figure 6（成功案例与 baseline invalid failure 的成对证据）。
- Table 1（六项 property 与 Avg. Improv. 的 headline comparison）。
### failure_patterns
- 多个 caption 只有标题，未定义数值/箭头/结构标记，脱离正文难以复核。
- 图表大量依赖颜色和小字号，Figure 8–13 及 Table 3/5/6 缩放后可读性下降。
- 均值和单次案例缺少重复、样本分母和不确定性，视觉上容易被读成总体保证。
- Table 4 caption 缺右括号、Table 8 有语法瑕疵、数表小数位混杂，削弱编辑完成度。
- **one_sentence_visual_strategy**：论文用深蓝流程框和元素色把“功能模块化 token → 合成约束 → property editing”串成方法叙事，再用方向明确的表格、成功/失败案例卡片和附录数据/邻居图扩展证据，但主要结论仍受 caption 简略、无不确定性和公开视觉源缺失的限制。

> 完成本篇后停止；本次只写入该 paper_id 的两个视觉审计文件。
