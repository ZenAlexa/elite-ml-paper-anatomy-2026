# Visual audit — `icml-2026-61bea4dcf717`

## 审计范围与清单事实

- 论文：**MetaphorVU: Towards Metaphorical Video Understanding**（ICML 2026；arXiv:2605.25461v1，25 May 2026）。
- PDF 事实源：`corpus/preprints/icml-2026-61bea4dcf717.pdf`；物理页数 34（Letter 612×792 pt，PDF 1.7）。正文 p.1–9，参考文献 p.9–13，附录 p.14–34；无独立 supplementary PDF。
- 已将 PDF 全部 34 页按 180 dpi 渲染为 PNG，并逐页阅读正文、参考文献和附录；以 PDF 中出现的对象为清单事实源。可见对象与 reading inventory 对齐：25 个 Figure（Figure 1–25）和 6 个 Table（Table 1–6）；无算法对象计入。
- Figure 1–7、Table 1–4 在正文；Figure 8–25、Table 5–6 在附录。附录 prompt、案例图和数据表均纳入逐对象审计。
- PDF 样式核查：正文/表格为 Nimbus Roman No9 L、Computer Modern 与 Times/Arial 混合；图中既有 PDF text/path，也有视频帧 JPEG 与嵌入卡片资源；按 180 dpi 检查后，未将 PDF 截图之外的推断当作清单对象。

## source inventory、GitHub 与本地视觉源

- `reports/tables/visual_source_inventory.csv` 的目标行：`no_public_source_found`；候选为 `icip-cas/MetaphorVU|YaoFANGUK/video-subtitle-remover`，来源文本指向 PDF p.1 的 Code 链接和 p.4 的字幕移除脚注。
- `icip-cas/MetaphorVU` 的 gh main HEAD 为 `cd46a048384a3aa17a05e24d8f442655cb381087`；递归 tree 有 `MetaphorVU.pdf`、评测脚本、`utlis/MetaphorVU_Boost.py` 和 prompt utilities，但没有 exact Figure/Table plot script、editable diagram 或 card assets。
- `YaoFANGUK/video-subtitle-remover` 的 gh main HEAD 为 `e109b9ddc1d0e8f153199dfa05c1d767546906d8`；是字幕/水印移除工具仓库，不是论文视觉源。
- 本地 `corpus/visual_sources/icml-2026-61bea4dcf717/` 与视觉源文件清单没有已取得文件；因此以下所有精确对象描述以已验证 PDF 和 180 dpi render 为主，未把 README 截图或代码仓库当作 exact source。

## 论文级视觉风格

- 主文/附录数量：7 Figure + 4 Table；附录 18 Figure + 2 Table。
- palette strategy：正文结果使用少量高饱和 categorical bar colors；流程图、taxonomy cards、qualitative atlas 和 prompt panels 使用蓝/橙/绿/灰的浅色填充区分模块或类别。颜色常与位置、标题、marker 或文字结构配合，但 qualitative/pastel objects 的灰度冗余有限。
- vector/raster mix：PDF 正文文本、表格规则和结果条形图主要是可提取的 PDF text/path；Figure 1–4、6–8 和附录 qualitative panels 混合矢量文字/图形与嵌入视频帧 JPEG；Figures 9–17 为以圆角面板承载的 prompt/annotation 版式，文字和背景在 PDF 中呈 mixed 资源。全部 34 页已以 180 dpi 渲染检查。
- style consistency：正文和 captions 以 Nimbus Roman No9 L/Computer Modern 为主，嵌入图形和 prompt/qualitative card 使用 Times New Roman、Arial、NimbusMonL；表格稳定采用 booktabs、无竖线、单/双栏切换。全篇圆角浅色卡片、filmstrip 边框、直接标签和方法/结果的短 caption 形成统一视觉语言，但 appendix prompt 与 qualitative atlas 的文字密度明显高于正文结果图。

## 逐 Figure 审计

### Figure 1 — p.1，introduction，single_column

- **类型与目的**：`conceptual_diagram, pipeline`；purpose=`headline, method_interface`。
- **设计**：Conceptual illustration only. It contains thumbnail examples and an element→concept→meaning mapping, but no sample denominator, uncertainty, or quantitative estimate. The introduction frames metaphor understanding as a transformation from perceptual signals to deeper semantics; the figure makes that cross-domain mapping visible before the benchmark and method sections.
- **图面与编码**：x=left-to-right conceptual stages from video to meaning；y=stacked semantic levels and card lanes；color=platform/source, literal recognition, event description, and metaphor mapping roles；shape=video thumbnails, rounded cards, icons, and directional arrows；line=orange/gray flow arrows；facet=one right-column conceptual panel；text=Recognition, Event Description, Visual Elements, Underlying Concepts, Implicit Meanings, and example phrases。
- **绘图语法**：rendering=`mixed`；x/y scale=`none`/`none`；grid=`none`；legend=False（无）；direct_labels=True；marker_types=None；line_styles=2；reference_lines=0；uncertainty=`none`；provenance=`mixed`。
- **复杂度**：score=4/5；panels=1；series=None；legend_items=0；annotations=17；data_marks_estimate=30。
- **字体/颜色**：字体 `NimbusRomNo9L-Regu, NimbusRomNo9L-Medi, TimesNewRomanPSMT, TimesNewRomanPS-BoldMT, ArialMT`，约 6.0–11.0 pt，中位约 8.5 pt，provenance=`mixed`；palette=`mixed`，约 8 色，Video/platform thumbnails establish the source; pale gray cards encode recognition and event description; orange accents mark the metaphor-understanding focus; blue/peach cards separate visual elements, underlying concepts, and implicit meanings. grayscale_safe=False。
- **caption/header**：`Figure 1. Metaphorical videos are prevalent across various real-world scenarios to convey many complex ideas, and metaphorical video understanding requires high-order cognitive capabilities.`（23 词；moves=title, setup；self-contained=true；main_finding_stated=false）。
- **优点**：The three semantic levels are aligned in one reading path.；Direct labels and arrows remove the need for a separate legend.；The small video strip and platform column anchor the claim in real-world short-video inputs.
- **弱点**：The right-column width makes several labels and thumbnails small at 180 dpi.；Pastel colors carry role distinctions but are not fully robust in grayscale.；The conceptual examples are illustrative rather than evidence of prevalence or performance.
- **可复用模式**：Use a compact element→concept→meaning pipeline as the opening visual anchor, then reserve quantitative objects for prevalence and capability claims.
- **证据**：p.1 Figure 1 visual and caption；basis=`rendered_observation`；bbox=None。

### Figure 2 — p.3，method，page_width

- **类型与目的**：`qualitative_grid, image_montage`；purpose=`dataset, method_interface`。
- **设计**：Qualitative taxonomy display: eight dominant metaphor types, one example card per type, and no statistical aggregation or uncertainty encoding. The taxonomy in §2.1 is instantiated with one dominant example per type; the figure supplies the visual vocabulary used by Table 1 and the later category-wise evaluation.
- **图面与编码**：x=2-column × 4-row taxonomy ordering；y=filmstrip above interpretation text within each card；color=taxonomy type via pastel header and card background；shape=six-frame filmstrips and rounded qualitative cards；line=filmstrip borders and dotted outer frame；facet=eight named taxonomy cards；text=type labels, titles, and bullet interpretations。
- **绘图语法**：rendering=`mixed`；x/y scale=`none`/`none`；grid=`none`；legend=False（无）；direct_labels=True；marker_types=None；line_styles=1；reference_lines=0；uncertainty=`none`；provenance=`rendered_estimate`。
- **复杂度**：score=5/5；panels=8；series=8；legend_items=0；annotations=32；data_marks_estimate=48。
- **字体/颜色**：字体 `NimbusRomNo9L-Regu, NimbusRomNo9L-Medi, TimesNewRomanPSMT, TimesNewRomanPS-BoldMT, ArialMT`，约 6.0–12.0 pt，中位约 8.0 pt，provenance=`mixed`；palette=`categorical`，约 8 色，Eight pastel card headers/backgrounds distinguish Body Language, Atmosphere Language, Cultural Symbol, Naturalistic Symbol, Causal Montage, Analogical Montage, Surreal Narrative, and Performative Narrative. grayscale_safe=False。
- **caption/header**：`Figure 2. MetaphorVU-Bench contains 8 types of video metaphor, enabling systematic evaluation of metaphorical video understanding. Note that most videos simultaneously contain multiple types of metaphor, we only show the dominant one in each case for illustration.`（37 词；moves=title, setup, encoding_key；self-contained=true；main_finding_stated=false）。
- **优点**：Eight classes are visible at a glance in a stable 2×4 grid.；Each filmstrip is paired with an interpretation, preserving the task’s visual-to-semantic relation.；The repeated card grammar supports comparison without a legend.
- **弱点**：The grid is text dense and its bullet text is small at page scale.；The category palette is decorative more than redundant; similar pastel values can merge in grayscale.；One example cannot represent within-type diversity, which the caption acknowledges only briefly.
- **可复用模式**：Define a taxonomy with repeated filmstrip-plus-interpretation cards so readers can map category names to concrete visual semantics before reading scores.
- **证据**：p.3 Figure 2 visual and caption；basis=`rendered_observation`；bbox=None。

### Figure 3 — p.4，method，page_width

- **类型与目的**：`pipeline, conceptual_diagram`；purpose=`experimental_design, dataset`。
- **设计**：The funnel labels approximate intermediate pool sizes (billions, ~70K, ~16K, ~4K) and the final 860 videos across eight types; no uncertainty or repeated-run variation is shown. The construction section claims a comment/LLM/MLLM/human sequence with strict quality control; the visual provides the process evidence behind the benchmark’s provenance and filtering boundaries.
- **图面与编码**：x=left-to-right source→filtration→annotation sequence；y=one pipeline lane with internal vertical stages；color=stage ownership and filtering modality；shape=rounded stage boxes, icons, and arrows；line=gray/blue/peach connectors；facet=three major process blocks；text=billions, ~70K, ~16K, ~4K, 860 data, and eight types。
- **绘图语法**：rendering=`mixed`；x/y scale=`none`/`none`；grid=`none`；legend=False（无）；direct_labels=True；marker_types=None；line_styles=1；reference_lines=0；uncertainty=`none`；provenance=`rendered_estimate`。
- **复杂度**：score=4/5；panels=3；series=None；legend_items=0；annotations=14；data_marks_estimate=16。
- **字体/颜色**：字体 `NimbusRomNo9L-Regu, NimbusRomNo9L-Medi, TimesNewRomanPSMT, TimesNewRomanPS-BoldMT, ArialMT`，约 6.0–12.0 pt，中位约 8.0 pt，provenance=`mixed`；palette=`mixed`，约 6 色，Gray represents the real-world source, blue the multi-stage filtration block, peach manual annotation, and colored icons distinguish comments/LLM/MLLM/human roles. grayscale_safe=False。
- **caption/header**：`Figure 3. We construct MetaphorVU-Bench by using a real-world short-video platform as source, selecting metaphorical videos from a large-scale video pool through multi-stage filtration, and manually annotating video metaphor interpretations with rigorous quality control. MetaphorVU-Bench can effectively support systematic and comprehensive evaluation of metaphorical video understanding.`（46 词；moves=title, setup, main_finding；self-contained=true；main_finding_stated=true）。
- **优点**：The three ownership stages and numeric attrition points are easy to follow.；Icons and labels redundantly identify comment, LLM, MLLM, and human filtering.；Page-width placement gives enough room for process labels and arrows.
- **弱点**：Intermediate counts use approximate tildes and are not tied to a denominator definition.；The diagram does not expose exclusion rates or reviewer agreement.；Icon colors are not a formal encoding key and may be ambiguous when isolated.
- **可复用模式**：Show data provenance as a small number of owned stages with explicit attrition counts and a final annotation contract.
- **证据**：p.4 Figure 3 visual and caption；basis=`rendered_observation`；bbox=None。

### Figure 4 — p.4，method，single_column

- **类型与目的**：`other`；purpose=`dataset, headline`。
- **设计**：Descriptive corpus overview: a word cloud and a qualitative topic distribution. The figure provides no visible percentages, counts, or uncertainty intervals for the topic sectors. The benchmark construction section argues for thematic diversity; this object visually supports that claim and complements the exact type counts in Table 1.
- **图面与编码**：x=none; word placement and radial sector order are spatial only；y=none; term size and radial position are visual emphasis；color=topic groups and decorative word-cloud emphasis；shape=word glyphs, radial sectors, and dashed callout boxes；line=radial/dashed connectors；facet=word cloud and topic distribution side by side；text=interpretation terms, five central topics, and subtopic labels。
- **绘图语法**：rendering=`mixed`；x/y scale=`none`/`none`；grid=`none`；legend=False（无）；direct_labels=True；marker_types=None；line_styles=0；reference_lines=0；uncertainty=`none`；provenance=`rendered_estimate`。
- **复杂度**：score=4/5；panels=2；series=None；legend_items=0；annotations=28；data_marks_estimate=40。
- **字体/颜色**：字体 `NimbusRomNo9L-Regu, NimbusRomNo9L-Medi, TimesNewRomanPSMT, TimesNewRomanPS-BoldMT, ArialMT`，约 6.0–11.0 pt，中位约 8.0 pt，provenance=`mixed`；palette=`mixed`，约 12 色，Word-cloud terms use many colors and sizes; the right radial topic graphic uses five central topic sectors plus colored dashed callout groups for subtopics. grayscale_safe=False。
- **caption/header**：`Figure 4. Benchmark covers diverse video topics, enabling accurate evaluation of real-world metaphorical video understanding.`（15 词；moves=title, setup, main_finding；self-contained=true；main_finding_stated=true）。
- **优点**：The word cloud and topic map give complementary lexical and thematic views.；Direct topic labels and dashed callouts make the radial structure readable without a legend.；The compact two-panel arrangement fits the method column beside Table 1.
- **弱点**：Topic sectors lack numeric proportions, so coverage cannot be quantitatively audited from the figure.；Word-cloud font size is a visual emphasis cue without an explicit frequency key.；Many hues and tiny labels reduce grayscale and small-screen readability.
- **可复用模式**：Pair a qualitative lexical overview with a directly labeled topic map, while putting exact corpus counts in an adjacent table.
- **证据**：p.4 Figure 4 visual and caption；basis=`rendered_observation`；bbox=None。

### Figure 5 — p.6，results，single_column

- **类型与目的**：`bar`；purpose=`main_comparison, mechanism`。
- **设计**：Category-wise mean evaluation scores for three close-source and three open-source models. Bars have no error bars, confidence intervals, sample-size labels, or value annotations. The detailed-analysis claim that the latter four types demand more cross-domain mapping is directly visualized by lower bars for Causal/Analogical Montage and the two Narrative categories; Table 3 supplies the complementary error taxonomy.
- **图面与编码**：x=eight categorical metaphor types, abbreviated on rotated labels；y=average score on the 0–100 evaluation scale；color=model series within close-source and open-source panels；shape=grouped rectangular bars；line=bar edges and category baselines；facet=close-source MLLMs vs open-source MLLMs；text=model names, category abbreviations, and y ticks。
- **绘图语法**：rendering=`vector`；x/y scale=`categorical`/`linear`；grid=`none`；legend=True（inside upper-right of each panel）；direct_labels=False；marker_types=None；line_styles=0；reference_lines=0；uncertainty=`none`；provenance=`mixed`。
- **复杂度**：score=4/5；panels=2；series=3；legend_items=3；annotations=20；data_marks_estimate=48。
- **字体/颜色**：字体 `NimbusRomNo9L-Regu, NimbusRomNo9L-Medi, TimesNewRomanPSMT, TimesNewRomanPS-BoldMT, ArialMT`，约 6.0–10.0 pt，中位约 7.5 pt，provenance=`mixed`；palette=`categorical`，约 6 色，Within each chart, three colored bars identify the close-source or open-source MLLM series; panel title identifies the model family. grayscale_safe=False。
- **caption/header**：`Figure 5. Performing worse on subsets requiring more cross-domain mapping, supports importance of mapping augmentation.`（15 词；moves=title, comparison, main_finding；self-contained=true；main_finding_stated=true）。
- **优点**：The paired panels hold the eight categories constant, making type difficulty comparable.；Grouped bars preserve within-category model comparisons.；The short caption states the intended mechanistic interpretation.
- **弱点**：Rotated abbreviated labels and small legends are difficult to decode in the narrow column.；No numerical labels or uncertainty make exact differences unavailable.；The claim about mapping difficulty is associative; duration, topic, and cultural knowledge are not controlled here.
- **可复用模式**：Use matched category bars in paired source-family panels to expose where a capability breaks, then connect the pattern to a diagnosis table.
- **证据**：p.6 Figure 5 visual and caption；basis=`rendered_observation`；bbox=None。

### Figure 6 — p.7，method，single_column

- **类型与目的**：`pipeline, architecture`；purpose=`method_interface, theory_mechanism`。
- **设计**：Mechanism diagram with no statistical marks. The surrounding text reports a graph of 54,687 nodes and 200,268 edges; those totals are not plotted in the diagram itself. The method section defines the extraction/connection graph and inference-time keyword→query→generation sequence; this figure is the interface bridge to the ablations in Table 4 and the case evidence in Figure 7/Appendix Figure 8.
- **图面与编码**：x=left-to-right operations within the upper and lower rows；y=upper graph-construction row versus lower inference-time augmentation row；color=offline graph preparation versus online video reasoning；shape=rounded boxes with concept pills, arrows, and small modality icons；line=row connectors and dashed row separator；facet=offline graph row and inference-time row；text=Texts, Extraction, Connection, Identification, KG Querying, Generation, and example concept nodes。
- **绘图语法**：rendering=`mixed`；x/y scale=`none`/`none`；grid=`none`；legend=False（无）；direct_labels=True；marker_types=None；line_styles=1；reference_lines=0；uncertainty=`none`；provenance=`rendered_estimate`。
- **复杂度**：score=4/5；panels=2；series=None；legend_items=0；annotations=22；data_marks_estimate=25。
- **字体/颜色**：字体 `NimbusRomNo9L-Regu, NimbusRomNo9L-Medi, TimesNewRomanPSMT, TimesNewRomanPS-BoldMT, ArialMT`，约 6.0–11.0 pt，中位约 8.0 pt，provenance=`mixed`；palette=`mixed`，约 8 色，Blue upper blocks encode text extraction and metaphorical graph construction; peach lower blocks encode video/title identification, KG querying, and interpretation generation. grayscale_safe=False。
- **caption/header**：`Figure 6. We construct a metaphorical knowledge graph and then propose MetaphorBoost, improving MLLMs performance on metaphorical video understanding via mapping augmentation.`（22 词；moves=title, setup, main_finding；self-contained=true；main_finding_stated=true）。
- **优点**：The upper/lower separation distinguishes offline resource construction from online inference.；Example concept pills make the cross-domain link concrete.；Arrows and modality labels communicate the data flow without a separate legend.
- **弱点**：The narrow panel forces dense labels and tiny concept pills.；The diagram does not expose query cost, hop count, or retained-node count.；Blue/peach roles are visually clear but not formally redundant in grayscale.
- **可复用模式**：Separate resource construction and inference-time use into two aligned rows, using the same concept vocabulary at the interface.
- **证据**：p.7 Figure 6 visual and caption；basis=`rendered_observation`；bbox=None。

### Figure 7 — p.8，ablation，single_column

- **类型与目的**：`bar`；purpose=`ablation, failure`。
- **设计**：Counts of three manually coded deficiency types for two base models versus MetaphorBoost. Exact bar values, denominator, and uncertainty are not printed. The detailed-analysis subsection claims reduced bad mapping; the paired bars operationalize that mechanism-level claim after Table 2 reports aggregate score improvements.
- **图面与编码**：x=Missing Mapping, Superficial Mapping, Improper Mapping；y=count of observed bad-mapping instances；color=Base Model versus Our Method；shape=paired rectangular bars；line=light horizontal guide lines；facet=Gemini-3-Pro and Qwen3-VL-8B-Thinking；text=model panel titles, legend, category labels, and sparse y ticks。
- **绘图语法**：rendering=`vector`；x/y scale=`categorical`/`linear`；grid=`y`；legend=True（inside upper-left of each panel）；direct_labels=False；marker_types=None；line_styles=0；reference_lines=0；uncertainty=`none`；provenance=`mixed`。
- **复杂度**：score=3/5；panels=2；series=2；legend_items=2；annotations=10；data_marks_estimate=12。
- **字体/颜色**：字体 `NimbusRomNo9L-Regu, NimbusRomNo9L-Medi, TimesNewRomanPSMT, TimesNewRomanPS-BoldMT, ArialMT`，约 6.0–10.0 pt，中位约 7.5 pt，provenance=`mixed`；palette=`categorical`，约 4 色，Blue bars are Base Model and orange bars Our Method in each of the Gemini-3-Pro and Qwen3-VL-8B-Thinking panels; gray gridlines are structural only. grayscale_safe=True。
- **caption/header**：`Figure 7. Amount of three kinds of bad mapping reduces, proving MetaphorBoost can effectively enhance cross-domain mapping.`（17 词；moves=title, comparison, main_finding；self-contained=true；main_finding_stated=true）。
- **优点**：Direct base/ours pairing makes the direction of change immediate.；The two-model repetition tests whether the pattern is tied to one base model.；A small chart preserves the link between categories and the error taxonomy.
- **弱点**：Exact heights are hard to recover without labels or a tabulated denominator.；Y-axis ranges differ between panels, complicating absolute comparison.；The caption says “proving” although the display is descriptive and lacks uncertainty or independent repetitions.
- **可复用模式**：Translate a qualitative error taxonomy into matched count bars for base and augmented systems, keeping one panel per representative model.
- **证据**：p.8 Figure 7 visual and caption；basis=`rendered_observation`；bbox=None。

### Figure 8 — p.18，appendix H，page_width

- **类型与目的**：`qualitative_grid, image_montage`；purpose=`qualitative_evidence, failure`。
- **设计**：Qualitative case study with one six-frame video strip and text outputs for each model; no numerical score or uncertainty is shown. Highlight colors provide an error annotation key. Appendix H’s case study makes the three Table 3 deficiency types concrete and visually connects their mitigation to the aggregate and count results in Table 2/Figure 7.
- **图面与编码**：x=left/right model comparison；y=filmstrip above thinking process and final interpretation；color=error-type highlights and model headers；shape=six-frame filmstrip plus rounded text cards；line=filmstrip frame borders and card outlines；facet=Base Model versus MetaphorBoost；text=# Thinking Process, # Video Metaphor Interpretation, and highlighted spans。
- **绘图语法**：rendering=`mixed`；x/y scale=`none`/`none`；grid=`none`；legend=False（无）；direct_labels=True；marker_types=None；line_styles=0；reference_lines=0；uncertainty=`none`；provenance=`rendered_estimate`。
- **复杂度**：score=5/5；panels=2；series=2；legend_items=0；annotations=14；data_marks_estimate=14。
- **字体/颜色**：字体 `TimesNewRomanPSMT, TimesNewRomanPS-BoldMT, ArialMT, Arial-BoldMT`，约 7.0–14.0 pt，中位约 9.0 pt，provenance=`mixed`；palette=`mixed`，约 5 色，Gray Base Model and peach MetaphorBoost headers establish the comparison; green, orange, and blue text highlights encode missing, superficial, and improper mapping. grayscale_safe=False。
- **caption/header**：`Figure 8. The green, orange, and blue highlights indicate missing mapping, superficial mapping, and improper mapping respectively, these deficiencies collectively lead to poor metaphorical video interpretation. MetaphorBoost effectively mitigates the three types of deficiencies, thereby improving MLLMs performance on metaphorical video understanding.`（42 词；moves=title, encoding_key, comparison, main_finding；self-contained=true；main_finding_stated=true）。
- **优点**：The same video and aligned text sections enable a direct before/after comparison.；Highlight colors expose the failure categories inside otherwise long prose.；The filmstrip preserves temporal evidence rather than reducing the case to one still.
- **弱点**：Long thinking-process paragraphs require substantial reading and are small at page scale.；The color key is caption-based and highlights are not paired with symbols or underlines.；One hand-selected case cannot establish general efficacy.
- **可复用模式**：Use a synchronized video strip and output transcript, with error-type highlights, to explain why an aggregate improvement occurs.
- **证据**：p.18 Figure 8 visual and caption；basis=`rendered_observation`；bbox=None。

### Figure 9 — p.19，B.1 Prompt for LLM Filtration，page_width

- **类型与目的**：`screenshot`；purpose=`experimental_design, reproduction`。
- **设计**：It is a procedural prompt screenshot; there are no plotted quantities, sample counts, or uncertainty displays. The first filtration stage in the construction funnel is made reproducible by showing the exact comment-only decision interface.
- **图面与编码**：x=none；y=none；color=section hierarchy and code-like emphasis；shape=rounded prompt container and header strip；line=box borders and Markdown separators；facet=single procedural prompt panel；text=prompt headings, requirements, placeholders, examples, and JSON schema。
- **绘图语法**：rendering=`mixed`；x/y scale=`none`/`none`；grid=`none`；legend=False（无）；direct_labels=True；marker_types=None；line_styles=0；reference_lines=0；uncertainty=`none`；provenance=`mixed`。
- **复杂度**：score=4/5；panels=1；series=None；legend_items=0；annotations=8；data_marks_estimate=0。
- **字体/颜色**：字体 `TimesNewRomanPSMT, TimesNewRomanPS-BoldMT, ArialMT, Arial-BoldMT, NimbusMonL-Regu`，约 6.0–12.0 pt，中位约 8.0 pt，provenance=`mixed`；palette=`grayscale`，约 3 色，Light gray rounded panel, darker centered header, and black/gray text organize the prompt; syntax highlighting is minimal and semantic color is not relied upon. grayscale_safe=True。
- **caption/header**：`Figure 9. Prompt for LLM filtration.`（6 词；moves=title, setup, encoding_key；self-contained=true；main_finding_stated=false）。
- **优点**：Explicit section headings make the input boundary auditable.；The JSON schema is shown in the same panel as the analysis rules.；The final placeholders expose the data fields used by the filter.
- **弱点**：The full prompt is dense and must be read line by line.；The screenshot does not show threshold calibration or inter-rater validation.；Monospace/serif mixing and small text create a high reading burden.
- **可复用模式**：Present each procedural prompt as a self-contained, headed panel with its input contract and output schema visible.
- **证据**：p.19 Figure 9 visual and caption；basis=`rendered_observation`；bbox=None。

### Figure 10 — p.20，B.2 Prompt for MLLM Filtration，page_width

- **类型与目的**：`screenshot`；purpose=`experimental_design, reproduction`。
- **设计**：Procedural text only; no plotted data or uncertainty. The second filtration stage directly addresses the risk that comment-based LLM reasoning does not match the original video.
- **图面与编码**：x=none；y=none；color=section hierarchy and code-like emphasis；shape=rounded prompt container and header strip；line=box borders and Markdown separators；facet=single procedural prompt panel；text=prompt headings, requirements, placeholders, examples, and JSON schema。
- **绘图语法**：rendering=`mixed`；x/y scale=`none`/`none`；grid=`none`；legend=False（无）；direct_labels=True；marker_types=None；line_styles=0；reference_lines=0；uncertainty=`none`；provenance=`mixed`。
- **复杂度**：score=4/5；panels=1；series=None；legend_items=0；annotations=8；data_marks_estimate=0。
- **字体/颜色**：字体 `TimesNewRomanPSMT, TimesNewRomanPS-BoldMT, ArialMT, Arial-BoldMT, NimbusMonL-Regu`，约 6.0–12.0 pt，中位约 8.0 pt，provenance=`mixed`；palette=`grayscale`，约 3 色，Light gray rounded panel, darker centered header, and black/gray text organize the prompt; syntax highlighting is minimal and semantic color is not relied upon. grayscale_safe=True。
- **caption/header**：`Figure 10. Prompt for MLLM filtration.`（6 词；moves=title, setup, encoding_key；self-contained=true；main_finding_stated=false）。
- **优点**：The prompt explicitly narrows evidence to visual content and first-stage reasoning.；The output field is machine-readable and visible.；Visual consistency with Figure 9 supports a coherent filtration protocol.
- **弱点**：Dense prose and tiny body type limit quick auditability.；No examples of accepted/rejected outcomes are included.；The notion of “reasonable” remains qualitative in the screenshot.
- **可复用模式**：Keep multi-stage quality-control prompts visually parallel while changing only the evidence modality and output field.
- **证据**：p.20 Figure 10 visual and caption；basis=`rendered_observation`；bbox=None。

### Figure 11 — p.21，B.3 Prompt for Human Filtration，page_width

- **类型与目的**：`screenshot`；purpose=`experimental_design, reproduction`。
- **设计**：Procedural guideline only; no counts or uncertainty inside the panel. This object supplies the human gate and taxonomy-selection instructions that complete Figure 3’s filtration funnel.
- **图面与编码**：x=none；y=none；color=section hierarchy and code-like emphasis；shape=rounded prompt container and header strip；line=box borders and Markdown separators；facet=single procedural prompt panel；text=prompt headings, requirements, placeholders, examples, and JSON schema。
- **绘图语法**：rendering=`mixed`；x/y scale=`none`/`none`；grid=`none`；legend=False（无）；direct_labels=True；marker_types=None；line_styles=0；reference_lines=0；uncertainty=`none`；provenance=`mixed`。
- **复杂度**：score=4/5；panels=1；series=None；legend_items=0；annotations=12；data_marks_estimate=0。
- **字体/颜色**：字体 `TimesNewRomanPSMT, TimesNewRomanPS-BoldMT, ArialMT, Arial-BoldMT, NimbusMonL-Regu`，约 6.0–12.0 pt，中位约 8.0 pt，provenance=`mixed`；palette=`grayscale`，约 3 色，Light gray rounded panel, darker centered header, and black/gray text organize the prompt; syntax highlighting is minimal and semantic color is not relied upon. grayscale_safe=True。
- **caption/header**：`Figure 11. Prompt for Human filtration.`（6 词；moves=title, setup, encoding_key；self-contained=true；main_finding_stated=false）。
- **优点**：The input fields and annotator decisions are explicit.；All eight type definitions are colocated with the selection question.；The prompt preserves the rationale for using original video plus contextual metadata.
- **弱点**：The long option table is hard to scan in a single panel.；No adjudication or agreement statistic is encoded.；The examples may bias annotators toward relationship and life-philosophy metaphors.
- **可复用模式**：Place human decision rules and category definitions in one reproducible form so the final gate is not implicit.
- **证据**：p.21 Figure 11 visual and caption；basis=`rendered_observation`；bbox=None。

### Figure 12 — p.22，B.4 Manual Annotation Guideline，page_width

- **类型与目的**：`screenshot`；purpose=`experimental_design, reproduction`。
- **设计**：Annotation template only; no statistical marks or uncertainty. The guideline implements the fixed visual-element-to-meaning format described in §2.2 and provides the contract behind the golden interpretations.
- **图面与编码**：x=none；y=none；color=section hierarchy and code-like emphasis；shape=rounded prompt container and header strip；line=box borders and Markdown separators；facet=single procedural prompt panel；text=prompt headings, requirements, placeholders, examples, and JSON schema。
- **绘图语法**：rendering=`mixed`；x/y scale=`none`/`none`；grid=`none`；legend=False（无）；direct_labels=True；marker_types=None；line_styles=0；reference_lines=0；uncertainty=`none`；provenance=`mixed`。
- **复杂度**：score=3/5；panels=1；series=None；legend_items=0；annotations=6；data_marks_estimate=0。
- **字体/颜色**：字体 `TimesNewRomanPSMT, TimesNewRomanPS-BoldMT, ArialMT, Arial-BoldMT, NimbusMonL-Regu`，约 6.0–12.0 pt，中位约 8.0 pt，provenance=`mixed`；palette=`grayscale`，约 3 色，Light gray rounded panel, darker centered header, and black/gray text organize the prompt; syntax highlighting is minimal and semantic color is not relied upon. grayscale_safe=True。
- **caption/header**：`Figure 12. Manual annotation guideline.`（5 词；moves=title, setup, encoding_key；self-contained=true；main_finding_stated=false）。
- **优点**：The two-paragraph structure directly encodes source content before meaning.；The example context fields make the annotation evidence boundary concrete.；The compact panel is less dense than the judge prompt.
- **弱点**：The template does not show reviewer revisions or a worked gold example.；Its text still depends on external video/title/comments not visible in the figure.；No annotation agreement or quality-control metric is provided.
- **可复用模式**：Separate observation and interpretation into fixed fields/paragraphs to reduce free-form annotation variance.
- **证据**：p.22 Figure 12 visual and caption；basis=`rendered_observation`；bbox=None。

### Figure 13 — p.23，C.1 Prompt for Evaluation，page_width

- **类型与目的**：`screenshot`；purpose=`experimental_design, reproduction`。
- **设计**：Evaluation procedure only; no observed scores or uncertainty. The prompt is the executable interface for the task formalized in Eq. (1), connecting video/title input to structured interpretation output.
- **图面与编码**：x=none；y=none；color=section hierarchy and code-like emphasis；shape=rounded prompt container and header strip；line=box borders and Markdown separators；facet=single procedural prompt panel；text=prompt headings, requirements, placeholders, examples, and JSON schema。
- **绘图语法**：rendering=`mixed`；x/y scale=`none`/`none`；grid=`none`；legend=False（无）；direct_labels=True；marker_types=None；line_styles=0；reference_lines=0；uncertainty=`none`；provenance=`mixed`。
- **复杂度**：score=4/5；panels=1；series=None；legend_items=0；annotations=8；data_marks_estimate=0。
- **字体/颜色**：字体 `TimesNewRomanPSMT, TimesNewRomanPS-BoldMT, ArialMT, Arial-BoldMT, NimbusMonL-Regu`，约 6.0–12.0 pt，中位约 8.0 pt，provenance=`mixed`；palette=`grayscale`，约 3 色，Light gray rounded panel, darker centered header, and black/gray text organize the prompt; syntax highlighting is minimal and semantic color is not relied upon. grayscale_safe=True。
- **caption/header**：`Figure 13. Prompt for evaluation.`（5 词；moves=title, setup, encoding_key；self-contained=true；main_finding_stated=false）。
- **优点**：The input, evidence restrictions, and output keys are explicit.；The sentence template preserves visual-content-to-implicit-idea relation.；The prompt is short enough to reproduce without splitting across panels.
- **弱点**：The “avoid forced interpretations” requirement is not operationalized with a rejection rule.；The JSON example does not define a maximum number of entries.；The screenshot cannot reveal how malformed outputs were handled.
- **可复用模式**：Show task prompts as input→requirements→machine-readable output, with the required semantic relation encoded in the output template.
- **证据**：p.23 Figure 13 visual and caption；basis=`rendered_observation`；bbox=None。

### Figure 14 — p.24，C.2 Prompt for LLM Judge，page_width

- **类型与目的**：`screenshot`；purpose=`experimental_design, reproduction`。
- **设计**：Judge rubric only; there are no plotted observations, calibration statistics, or uncertainty marks. The judge prompt defines the scoring semantics used for Table 2 and Appendix C.3’s human/LLM consistency check.
- **图面与编码**：x=none；y=none；color=section hierarchy and code-like emphasis；shape=rounded prompt container and header strip；line=box borders and Markdown separators；facet=single procedural prompt panel；text=prompt headings, requirements, placeholders, examples, and JSON schema。
- **绘图语法**：rendering=`mixed`；x/y scale=`none`/`none`；grid=`none`；legend=False（无）；direct_labels=True；marker_types=None；line_styles=0；reference_lines=0；uncertainty=`none`；provenance=`mixed`。
- **复杂度**：score=4/5；panels=1；series=None；legend_items=0；annotations=12；data_marks_estimate=0。
- **字体/颜色**：字体 `TimesNewRomanPSMT, TimesNewRomanPS-BoldMT, ArialMT, Arial-BoldMT, NimbusMonL-Regu`，约 6.0–12.0 pt，中位约 8.0 pt，provenance=`mixed`；palette=`grayscale`，约 3 色，Light gray rounded panel, darker centered header, and black/gray text organize the prompt; syntax highlighting is minimal and semantic color is not relied upon. grayscale_safe=True。
- **caption/header**：`Figure 14. Prompt for LLM judge.`（6 词；moves=title, setup, encoding_key；self-contained=true；main_finding_stated=false）。
- **优点**：Strict and loose scores are separated with explicit criteria.；The scoring procedure links completeness, error, and core-meaning correctness to outputs.；The output schema is visible at the bottom.
- **弱点**：This is the most text-dense prompt and is difficult to audit at a glance.；The caption only says “Prompt for LLM judge,” leaving score design to the panel.；The rubric does not display examples of borderline cases or judge bias checks.
- **可复用模式**：Use layered rubric blocks for free-form evaluation, keeping binary coverage, graded correctness, procedure, and output schema distinct.
- **证据**：p.24 Figure 14 visual and caption；basis=`rendered_observation`；bbox=None。

### Figure 15 — p.25，D.2 Prompt for Extracting Metaphorical Concept Pairs，page_width

- **类型与目的**：`screenshot`；purpose=`method_interface, reproduction`。
- **设计**：Extraction prompt only; no graph-size or extraction-quality statistic is plotted. This is the reproducibility interface for converting textual metaphor datasets into source/target concept pairs used by the graph in Figure 6.
- **图面与编码**：x=none；y=none；color=section hierarchy and code-like emphasis；shape=rounded prompt container and header strip；line=box borders and Markdown separators；facet=single procedural prompt panel；text=prompt headings, requirements, placeholders, examples, and JSON schema。
- **绘图语法**：rendering=`mixed`；x/y scale=`none`/`none`；grid=`none`；legend=False（无）；direct_labels=True；marker_types=None；line_styles=0；reference_lines=0；uncertainty=`none`；provenance=`mixed`。
- **复杂度**：score=4/5；panels=1；series=None；legend_items=0；annotations=10；data_marks_estimate=0。
- **字体/颜色**：字体 `TimesNewRomanPSMT, TimesNewRomanPS-BoldMT, ArialMT, Arial-BoldMT, NimbusMonL-Regu`，约 6.0–12.0 pt，中位约 8.0 pt，provenance=`mixed`；palette=`grayscale`，约 3 色，Light gray rounded panel, darker centered header, and black/gray text organize the prompt; syntax highlighting is minimal and semantic color is not relied upon. grayscale_safe=True。
- **caption/header**：`Figure 15. Prompt for extracting metaphorical concept pairs.`（8 词；moves=title, setup, encoding_key；self-contained=true；main_finding_stated=false）。
- **优点**：Examples demonstrate correct and empty-list outputs.；The cross-domain association rule is stated before processing steps.；The output keys match the later graph-construction narrative.
- **弱点**：Examples and rules occupy most of the page, leaving little visual separation between policy and demonstrations.；No extraction precision/recall or deduplication behavior is shown.；The prompt relies on inferred semantic judgments for implicit concepts.
- **可复用模式**：Put rules, positive/negative examples, and exact output format together when a prompt creates graph data.
- **证据**：p.25 Figure 15 visual and caption；basis=`rendered_observation`；bbox=None。

### Figure 16 — p.26，E.1 Prompt for Identifying Visual Elements，page_width

- **类型与目的**：`screenshot`；purpose=`method_interface, reproduction`。
- **设计**：Keyword extraction prompt only; no count of keywords or uncertainty. It defines the source-node extraction step K=Identify(v⊕t) in the MetaphorBoost inference pipeline.
- **图面与编码**：x=none；y=none；color=section hierarchy and code-like emphasis；shape=rounded prompt container and header strip；line=box borders and Markdown separators；facet=single procedural prompt panel；text=prompt headings, requirements, placeholders, examples, and JSON schema。
- **绘图语法**：rendering=`mixed`；x/y scale=`none`/`none`；grid=`none`；legend=False（无）；direct_labels=True；marker_types=None；line_styles=0；reference_lines=0；uncertainty=`none`；provenance=`mixed`。
- **复杂度**：score=3/5；panels=1；series=None；legend_items=0；annotations=5；data_marks_estimate=0。
- **字体/颜色**：字体 `TimesNewRomanPSMT, TimesNewRomanPS-BoldMT, ArialMT, Arial-BoldMT, NimbusMonL-Regu`，约 6.5–12.0 pt，中位约 8.0 pt，provenance=`mixed`；palette=`grayscale`，约 3 色，Light gray rounded panel, darker centered header, and black/gray text organize the prompt; syntax highlighting is minimal and semantic color is not relied upon. grayscale_safe=True。
- **caption/header**：`Figure 16. Prompt for identifying visual elements.`（7 词；moves=title, setup, encoding_key；self-contained=true；main_finding_stated=false）。
- **优点**：The evidence source is explicitly the video.；The requirements demand comprehensive but concise elements.；The JSON list is minimal and implementation-friendly.
- **弱点**：The prompt gives no keyword budget, temporal sampling policy, or handling of abstract concepts beyond a sentence.；There is no failure example or validation metric.；The small box leaves semantic edge cases to the model.
- **可复用模式**：Make the first inference-time extraction interface small and explicit, then pass its machine-readable output to the graph query.
- **证据**：p.26 Figure 16 visual and caption；basis=`rendered_observation`；bbox=None。

### Figure 17 — p.26，E.2 Prompt for Generating Video Metaphor Interpretation，page_width

- **类型与目的**：`screenshot`；purpose=`method_interface, reproduction`。
- **设计**：Generation prompt only; no output scores or uncertainty. This is the final generation interface after KG retrieval and is the executable counterpart of Figure 6’s Generation block and Eq. (6).
- **图面与编码**：x=none；y=none；color=section hierarchy and code-like emphasis；shape=rounded prompt container and header strip；line=box borders and Markdown separators；facet=single procedural prompt panel；text=prompt headings, requirements, placeholders, examples, and JSON schema。
- **绘图语法**：rendering=`mixed`；x/y scale=`none`/`none`；grid=`none`；legend=False（无）；direct_labels=True；marker_types=None；line_styles=0；reference_lines=0；uncertainty=`none`；provenance=`mixed`。
- **复杂度**：score=4/5；panels=1；series=None；legend_items=0；annotations=8；data_marks_estimate=0。
- **字体/颜色**：字体 `TimesNewRomanPSMT, TimesNewRomanPS-BoldMT, ArialMT, Arial-BoldMT, NimbusMonL-Regu`，约 6.0–12.0 pt，中位约 8.0 pt，provenance=`mixed`；palette=`grayscale`，约 3 色，Light gray rounded panel, darker centered header, and black/gray text organize the prompt; syntax highlighting is minimal and semantic color is not relied upon. grayscale_safe=True。
- **caption/header**：`Figure 17. Prompt for generating video metaphor interpretation.`（8 词；moves=title, setup, encoding_key；self-contained=true；main_finding_stated=false）。
- **优点**：The prompt explicitly says external references are advisory rather than a replacement for video evidence.；The output sentence format preserves the benchmark’s semantic relation.；Placement beneath Figure 16 makes the inference-time handoff visible.
- **弱点**：The examples and retrieved-reference formats are placeholders, so exact context length is unspecified.；No guard against hallucinated mapping is encoded beyond prose instructions.；The lower panel is dense enough that the output contract is easy to miss.
- **可复用模式**：Stack extraction and generation prompts to expose the inference-time handoff, with external knowledge marked as reference rather than ground truth.
- **证据**：p.26 Figure 17 visual and caption；basis=`rendered_observation`；bbox=None。

### Figure 18 — p.27，appendix H，page_width

- **类型与目的**：`qualitative_grid, image_montage`；purpose=`qualitative_evidence, dataset`。
- **设计**：Qualitative examples only: two cases with six sampled frames each (12 visible frame marks) and free-form metaphor interpretations; no score, denominator, or uncertainty. Appendix H expands the eight-type taxonomy with additional Body Language examples, giving qualitative support for the category semantics used in Figure 2 and the per-type columns in Tables 1–4.
- **图面与编码**：x=two vertically stacked qualitative cases；y=six-frame temporal strip above title and interpretation text；color=category header/background and natural image content；shape=six-frame filmstrips plus rounded interpretation cards；line=filmstrip frames and outer dotted boundary；facet=two Body Language examples；text=category header, Title, Metaphor Interpretation, and generated interpretation sentences。
- **绘图语法**：rendering=`mixed`；x/y scale=`none`/`none`；grid=`none`；legend=False（无）；direct_labels=True；marker_types=None；line_styles=1；reference_lines=0；uncertainty=`none`；provenance=`rendered_estimate`。
- **复杂度**：score=5/5；panels=2；series=2；legend_items=0；annotations=8；data_marks_estimate=12。
- **字体/颜色**：字体 `TimesNewRomanPSMT, TimesNewRomanPS-BoldMT, ArialMT, Arial-BoldMT`，约 7.0–14.0 pt，中位约 9.0 pt，provenance=`mixed`；palette=`categorical`，约 4 色，Body Language uses a colored category header and matching pale card background (#F9DD78/#FFF0C6); gray filmstrip borders and white frame perforations separate temporal samples. grayscale_safe=False。
- **caption/header**：`Figure 18. Examples of Body Language. Note that most videos simultaneously contain multiple types of metaphor, we only show the dominant one in each case for convenient illustration.`（28 词；moves=title, setup, encoding_key；self-contained=true；main_finding_stated=false）。
- **优点**：Repeated six-frame strips preserve temporal progression.；The category header and interpretation card make visual evidence and semantic reading adjacent.；The same layout across all eight pages creates a coherent appendix atlas.
- **弱点**：Long free-form interpretations dominate the frame strip and are slow to compare.；The dominant-type label is categorical rather than a measured annotation confidence.；Pastel backgrounds and natural-image colors are not grayscale robust.
- **可复用模式**：Build a taxonomy appendix as a repeated two-case atlas: temporal strip first, then title and interpretation, with only the category header changing.
- **证据**：p.27 Figure 18 visual and caption；basis=`rendered_observation`；bbox=None。

### Figure 19 — p.28，appendix H，page_width

- **类型与目的**：`qualitative_grid, image_montage`；purpose=`qualitative_evidence, dataset`。
- **设计**：Qualitative examples only: two cases with six sampled frames each (12 visible frame marks) and free-form metaphor interpretations; no score, denominator, or uncertainty. Appendix H expands the eight-type taxonomy with additional Atmosphere Language examples, giving qualitative support for the category semantics used in Figure 2 and the per-type columns in Tables 1–4.
- **图面与编码**：x=two vertically stacked qualitative cases；y=six-frame temporal strip above title and interpretation text；color=category header/background and natural image content；shape=six-frame filmstrips plus rounded interpretation cards；line=filmstrip frames and outer dotted boundary；facet=two Atmosphere Language examples；text=category header, Title, Metaphor Interpretation, and generated interpretation sentences。
- **绘图语法**：rendering=`mixed`；x/y scale=`none`/`none`；grid=`none`；legend=False（无）；direct_labels=True；marker_types=None；line_styles=1；reference_lines=0；uncertainty=`none`；provenance=`rendered_estimate`。
- **复杂度**：score=5/5；panels=2；series=2；legend_items=0；annotations=8；data_marks_estimate=12。
- **字体/颜色**：字体 `TimesNewRomanPSMT, TimesNewRomanPS-BoldMT, ArialMT, Arial-BoldMT`，约 7.0–14.0 pt，中位约 9.0 pt，provenance=`mixed`；palette=`categorical`，约 4 色，Atmosphere Language uses a colored category header and matching pale card background (#F8DD7A/#FFF0C7); gray filmstrip borders and white frame perforations separate temporal samples. grayscale_safe=False。
- **caption/header**：`Figure 19. Examples of Atmosphere Language. Note that most videos simultaneously contain multiple types of metaphor, we only show the dominant one in each case for convenient illustration.`（28 词；moves=title, setup, encoding_key；self-contained=true；main_finding_stated=false）。
- **优点**：Repeated six-frame strips preserve temporal progression.；The category header and interpretation card make visual evidence and semantic reading adjacent.；The same layout across all eight pages creates a coherent appendix atlas.
- **弱点**：Long free-form interpretations dominate the frame strip and are slow to compare.；The dominant-type label is categorical rather than a measured annotation confidence.；Pastel backgrounds and natural-image colors are not grayscale robust.
- **可复用模式**：Build a taxonomy appendix as a repeated two-case atlas: temporal strip first, then title and interpretation, with only the category header changing.
- **证据**：p.28 Figure 19 visual and caption；basis=`rendered_observation`；bbox=None。

### Figure 20 — p.29，appendix H，page_width

- **类型与目的**：`qualitative_grid, image_montage`；purpose=`qualitative_evidence, dataset`。
- **设计**：Qualitative examples only: two cases with six sampled frames each (12 visible frame marks) and free-form metaphor interpretations; no score, denominator, or uncertainty. Appendix H expands the eight-type taxonomy with additional Cultural Symbol examples, giving qualitative support for the category semantics used in Figure 2 and the per-type columns in Tables 1–4.
- **图面与编码**：x=two vertically stacked qualitative cases；y=six-frame temporal strip above title and interpretation text；color=category header/background and natural image content；shape=six-frame filmstrips plus rounded interpretation cards；line=filmstrip frames and outer dotted boundary；facet=two Cultural Symbol examples；text=category header, Title, Metaphor Interpretation, and generated interpretation sentences。
- **绘图语法**：rendering=`mixed`；x/y scale=`none`/`none`；grid=`none`；legend=False（无）；direct_labels=True；marker_types=None；line_styles=1；reference_lines=0；uncertainty=`none`；provenance=`rendered_estimate`。
- **复杂度**：score=5/5；panels=2；series=2；legend_items=0；annotations=8；data_marks_estimate=12。
- **字体/颜色**：字体 `TimesNewRomanPSMT, TimesNewRomanPS-BoldMT, ArialMT, Arial-BoldMT`，约 7.0–14.0 pt，中位约 9.0 pt，provenance=`mixed`；palette=`categorical`，约 4 色，Cultural Symbol uses a colored category header and matching pale card background (#BBD7EE/#E2EFF8); gray filmstrip borders and white frame perforations separate temporal samples. grayscale_safe=False。
- **caption/header**：`Figure 20. Examples of Cultural Symbol. Note that most videos simultaneously contain multiple types of metaphor, we only show the dominant one in each case for convenient illustration.`（28 词；moves=title, setup, encoding_key；self-contained=true；main_finding_stated=false）。
- **优点**：Repeated six-frame strips preserve temporal progression.；The category header and interpretation card make visual evidence and semantic reading adjacent.；The same layout across all eight pages creates a coherent appendix atlas.
- **弱点**：Long free-form interpretations dominate the frame strip and are slow to compare.；The dominant-type label is categorical rather than a measured annotation confidence.；Pastel backgrounds and natural-image colors are not grayscale robust.
- **可复用模式**：Build a taxonomy appendix as a repeated two-case atlas: temporal strip first, then title and interpretation, with only the category header changing.
- **证据**：p.29 Figure 20 visual and caption；basis=`rendered_observation`；bbox=None。

### Figure 21 — p.30，appendix H，page_width

- **类型与目的**：`qualitative_grid, image_montage`；purpose=`qualitative_evidence, dataset`。
- **设计**：Qualitative examples only: two cases with six sampled frames each (12 visible frame marks) and free-form metaphor interpretations; no score, denominator, or uncertainty. Appendix H expands the eight-type taxonomy with additional Naturalistic Symbol examples, giving qualitative support for the category semantics used in Figure 2 and the per-type columns in Tables 1–4.
- **图面与编码**：x=two vertically stacked qualitative cases；y=six-frame temporal strip above title and interpretation text；color=category header/background and natural image content；shape=six-frame filmstrips plus rounded interpretation cards；line=filmstrip frames and outer dotted boundary；facet=two Naturalistic Symbol examples；text=category header, Title, Metaphor Interpretation, and generated interpretation sentences。
- **绘图语法**：rendering=`mixed`；x/y scale=`none`/`none`；grid=`none`；legend=False（无）；direct_labels=True；marker_types=None；line_styles=1；reference_lines=0；uncertainty=`none`；provenance=`rendered_estimate`。
- **复杂度**：score=5/5；panels=2；series=2；legend_items=0；annotations=8；data_marks_estimate=12。
- **字体/颜色**：字体 `TimesNewRomanPSMT, TimesNewRomanPS-BoldMT, ArialMT, Arial-BoldMT`，约 7.0–14.0 pt，中位约 9.0 pt，provenance=`mixed`；palette=`categorical`，约 4 色，Naturalistic Symbol uses a colored category header and matching pale card background (#BBD7EE/#E2EFF8); gray filmstrip borders and white frame perforations separate temporal samples. grayscale_safe=False。
- **caption/header**：`Figure 21. Examples of Naturalistic Symbol. Note that most videos simultaneously contain multiple types of metaphor, we only show the dominant one in each case for convenient illustration.`（28 词；moves=title, setup, encoding_key；self-contained=true；main_finding_stated=false）。
- **优点**：Repeated six-frame strips preserve temporal progression.；The category header and interpretation card make visual evidence and semantic reading adjacent.；The same layout across all eight pages creates a coherent appendix atlas.
- **弱点**：Long free-form interpretations dominate the frame strip and are slow to compare.；The dominant-type label is categorical rather than a measured annotation confidence.；Pastel backgrounds and natural-image colors are not grayscale robust.
- **可复用模式**：Build a taxonomy appendix as a repeated two-case atlas: temporal strip first, then title and interpretation, with only the category header changing.
- **证据**：p.30 Figure 21 visual and caption；basis=`rendered_observation`；bbox=None。

### Figure 22 — p.31，appendix H，page_width

- **类型与目的**：`qualitative_grid, image_montage`；purpose=`qualitative_evidence, dataset`。
- **设计**：Qualitative examples only: two cases with six sampled frames each (12 visible frame marks) and free-form metaphor interpretations; no score, denominator, or uncertainty. Appendix H expands the eight-type taxonomy with additional Causal Montage examples, giving qualitative support for the category semantics used in Figure 2 and the per-type columns in Tables 1–4.
- **图面与编码**：x=two vertically stacked qualitative cases；y=six-frame temporal strip above title and interpretation text；color=category header/background and natural image content；shape=six-frame filmstrips plus rounded interpretation cards；line=filmstrip frames and outer dotted boundary；facet=two Causal Montage examples；text=category header, Title, Metaphor Interpretation, and generated interpretation sentences。
- **绘图语法**：rendering=`mixed`；x/y scale=`none`/`none`；grid=`none`；legend=False（无）；direct_labels=True；marker_types=None；line_styles=1；reference_lines=0；uncertainty=`none`；provenance=`rendered_estimate`。
- **复杂度**：score=5/5；panels=2；series=2；legend_items=0；annotations=8；data_marks_estimate=12。
- **字体/颜色**：字体 `TimesNewRomanPSMT, TimesNewRomanPS-BoldMT, ArialMT, Arial-BoldMT`，约 7.0–14.0 pt，中位约 9.0 pt，provenance=`mixed`；palette=`categorical`，约 4 色，Causal Montage uses a colored category header and matching pale card background (#F6C6A7/#FCE4D6); gray filmstrip borders and white frame perforations separate temporal samples. grayscale_safe=False。
- **caption/header**：`Figure 22. Examples of Causal Montage. Note that most videos simultaneously contain multiple types of metaphor, we only show the dominant one in each case for convenient illustration.`（28 词；moves=title, setup, encoding_key；self-contained=true；main_finding_stated=false）。
- **优点**：Repeated six-frame strips preserve temporal progression.；The category header and interpretation card make visual evidence and semantic reading adjacent.；The same layout across all eight pages creates a coherent appendix atlas.
- **弱点**：Long free-form interpretations dominate the frame strip and are slow to compare.；The dominant-type label is categorical rather than a measured annotation confidence.；Pastel backgrounds and natural-image colors are not grayscale robust.
- **可复用模式**：Build a taxonomy appendix as a repeated two-case atlas: temporal strip first, then title and interpretation, with only the category header changing.
- **证据**：p.31 Figure 22 visual and caption；basis=`rendered_observation`；bbox=None。

### Figure 23 — p.32，appendix H，page_width

- **类型与目的**：`qualitative_grid, image_montage`；purpose=`qualitative_evidence, dataset`。
- **设计**：Qualitative examples only: two cases with six sampled frames each (12 visible frame marks) and free-form metaphor interpretations; no score, denominator, or uncertainty. Appendix H expands the eight-type taxonomy with additional Analogical Montage examples, giving qualitative support for the category semantics used in Figure 2 and the per-type columns in Tables 1–4.
- **图面与编码**：x=two vertically stacked qualitative cases；y=six-frame temporal strip above title and interpretation text；color=category header/background and natural image content；shape=six-frame filmstrips plus rounded interpretation cards；line=filmstrip frames and outer dotted boundary；facet=two Analogical Montage examples；text=category header, Title, Metaphor Interpretation, and generated interpretation sentences。
- **绘图语法**：rendering=`mixed`；x/y scale=`none`/`none`；grid=`none`；legend=False（无）；direct_labels=True；marker_types=None；line_styles=1；reference_lines=0；uncertainty=`none`；provenance=`rendered_estimate`。
- **复杂度**：score=5/5；panels=2；series=2；legend_items=0；annotations=8；data_marks_estimate=12。
- **字体/颜色**：字体 `TimesNewRomanPSMT, TimesNewRomanPS-BoldMT, ArialMT, Arial-BoldMT`，约 7.0–14.0 pt，中位约 9.0 pt，provenance=`mixed`；palette=`categorical`，约 4 色，Analogical Montage uses a colored category header and matching pale card background (#F6C6A7/#FCE4D6); gray filmstrip borders and white frame perforations separate temporal samples. grayscale_safe=False。
- **caption/header**：`Figure 23. Examples of Analogical Montage. Note that most videos simultaneously contain multiple types of metaphor, we only show the dominant one in each case for convenient illustration.`（28 词；moves=title, setup, encoding_key；self-contained=true；main_finding_stated=false）。
- **优点**：Repeated six-frame strips preserve temporal progression.；The category header and interpretation card make visual evidence and semantic reading adjacent.；The same layout across all eight pages creates a coherent appendix atlas.
- **弱点**：Long free-form interpretations dominate the frame strip and are slow to compare.；The dominant-type label is categorical rather than a measured annotation confidence.；Pastel backgrounds and natural-image colors are not grayscale robust.
- **可复用模式**：Build a taxonomy appendix as a repeated two-case atlas: temporal strip first, then title and interpretation, with only the category header changing.
- **证据**：p.32 Figure 23 visual and caption；basis=`rendered_observation`；bbox=None。

### Figure 24 — p.33，appendix H，page_width

- **类型与目的**：`qualitative_grid, image_montage`；purpose=`qualitative_evidence, dataset`。
- **设计**：Qualitative examples only: two cases with six sampled frames each (12 visible frame marks) and free-form metaphor interpretations; no score, denominator, or uncertainty. Appendix H expands the eight-type taxonomy with additional Surreal Narrative examples, giving qualitative support for the category semantics used in Figure 2 and the per-type columns in Tables 1–4.
- **图面与编码**：x=two vertically stacked qualitative cases；y=six-frame temporal strip above title and interpretation text；color=category header/background and natural image content；shape=six-frame filmstrips plus rounded interpretation cards；line=filmstrip frames and outer dotted boundary；facet=two Surreal Narrative examples；text=category header, Title, Metaphor Interpretation, and generated interpretation sentences。
- **绘图语法**：rendering=`mixed`；x/y scale=`none`/`none`；grid=`none`；legend=False（无）；direct_labels=True；marker_types=None；line_styles=1；reference_lines=0；uncertainty=`none`；provenance=`rendered_estimate`。
- **复杂度**：score=5/5；panels=2；series=2；legend_items=0；annotations=8；data_marks_estimate=12。
- **字体/颜色**：字体 `TimesNewRomanPSMT, TimesNewRomanPS-BoldMT, ArialMT, Arial-BoldMT`，约 7.0–14.0 pt，中位约 9.0 pt，provenance=`mixed`；palette=`categorical`，约 4 色，Surreal Narrative uses a colored category header and matching pale card background (#C9E5B5/#E5F2DD); gray filmstrip borders and white frame perforations separate temporal samples. grayscale_safe=False。
- **caption/header**：`Figure 24. Examples of Surreal Narrative. Note that most videos simultaneously contain multiple types of metaphor, we only show the dominant one in each case for convenient illustration.`（28 词；moves=title, setup, encoding_key；self-contained=true；main_finding_stated=false）。
- **优点**：Repeated six-frame strips preserve temporal progression.；The category header and interpretation card make visual evidence and semantic reading adjacent.；The same layout across all eight pages creates a coherent appendix atlas.
- **弱点**：Long free-form interpretations dominate the frame strip and are slow to compare.；The dominant-type label is categorical rather than a measured annotation confidence.；Pastel backgrounds and natural-image colors are not grayscale robust.
- **可复用模式**：Build a taxonomy appendix as a repeated two-case atlas: temporal strip first, then title and interpretation, with only the category header changing.
- **证据**：p.33 Figure 24 visual and caption；basis=`rendered_observation`；bbox=None。

### Figure 25 — p.34，appendix H，page_width

- **类型与目的**：`qualitative_grid, image_montage`；purpose=`qualitative_evidence, dataset`。
- **设计**：Qualitative examples only: two cases with six sampled frames each (12 visible frame marks) and free-form metaphor interpretations; no score, denominator, or uncertainty. Appendix H expands the eight-type taxonomy with additional Performative Narrative examples, giving qualitative support for the category semantics used in Figure 2 and the per-type columns in Tables 1–4.
- **图面与编码**：x=two vertically stacked qualitative cases；y=six-frame temporal strip above title and interpretation text；color=category header/background and natural image content；shape=six-frame filmstrips plus rounded interpretation cards；line=filmstrip frames and outer dotted boundary；facet=two Performative Narrative examples；text=category header, Title, Metaphor Interpretation, and generated interpretation sentences。
- **绘图语法**：rendering=`mixed`；x/y scale=`none`/`none`；grid=`none`；legend=False（无）；direct_labels=True；marker_types=None；line_styles=1；reference_lines=0；uncertainty=`none`；provenance=`rendered_estimate`。
- **复杂度**：score=5/5；panels=2；series=2；legend_items=0；annotations=8；data_marks_estimate=12。
- **字体/颜色**：字体 `TimesNewRomanPSMT, TimesNewRomanPS-BoldMT, ArialMT, Arial-BoldMT`，约 7.0–14.0 pt，中位约 9.0 pt，provenance=`mixed`；palette=`categorical`，约 4 色，Performative Narrative uses a colored category header and matching pale card background (#C9E5B5/#E5F2DD); gray filmstrip borders and white frame perforations separate temporal samples. grayscale_safe=False。
- **caption/header**：`Figure 25. Examples of Performative Narrative. Note that most videos simultaneously contain multiple types of metaphor, we only show the dominant one in each case for convenient illustration.`（28 词；moves=title, setup, encoding_key；self-contained=true；main_finding_stated=false）。
- **优点**：Repeated six-frame strips preserve temporal progression.；The category header and interpretation card make visual evidence and semantic reading adjacent.；The same layout across all eight pages creates a coherent appendix atlas.
- **弱点**：Long free-form interpretations dominate the frame strip and are slow to compare.；The dominant-type label is categorical rather than a measured annotation confidence.；Pastel backgrounds and natural-image colors are not grayscale robust.
- **可复用模式**：Build a taxonomy appendix as a repeated two-case atlas: temporal strip first, then title and interpretation, with only the category header changing.
- **证据**：p.34 Figure 25 visual and caption；basis=`rendered_observation`；bbox=None。

## 逐 Table 审计

### Table 1 — p.4，method，single_column

- **目的**：purpose=`dataset`；Eight taxonomy rows plus the MetaphorVU-Bench total; sample counts are 136, 150, 62, 113, 54, 171, 112, 62, and 860; average duration and average tokens are reported to one decimal place.
- **结构**：rows=9；columns=4；header_levels=1；row_groups=0；decimal_precision=None；rules=`booktabs`。
- **高亮/不确定性**：highlighting=`none`；none; the table reports averages and counts without intervals, standard errors, or repeated-run uncertainty.
- **证据关系**：Exact per-type composition and total benchmark size support the construction funnel and explain the category columns in all main results.
- **caption/header**：`Table 1. Benchmark statistics of sample number, average video duration and average token number of golden interpretations.`（17 词；moves=title, setup；self-contained=true；main_finding_stated=false）。
- **优点**：Compact four-column structure links category, volume, duration, and interpretation length.；The total row makes the 860-instance benchmark boundary explicit.；Booktabs rules and no vertical lines preserve a clean single-column table.
- **弱点**：Mixed integer and one-decimal values make a single precision policy impossible.；No dispersion or range accompanies average duration/token values.；Long taxonomy names require abbreviations in the header/body.
- **可复用模式**：Put exact composition next to a qualitative coverage figure, using a total row as the benchmark boundary.
- **证据**：p.4 Table 1 table and caption；basis=`rendered_observation`；bbox=None。

### Table 2 — p.5，results，page_width

- **目的**：purpose=`main_comparison`；Twenty-five body rows: Human*; six close-source MLLMs; five open-source MLLMs; seven reasoning-enhanced methods; and six MetaphorBoost/Δ rows. Eight type columns plus Average; all displayed scores use one decimal place.
- **结构**：rows=25；columns=10；header_levels=1；row_groups=5；decimal_precision=1；rules=`booktabs`。
- **高亮/不确定性**：highlighting=`bold, cell_color, text_color`；none reported; scores are point means on a 0–100 rescaled judge scale, with no error bars or confidence intervals.
- **证据关系**：This is the central benchmark comparison: it quantifies the human gap, limited reasoning-baseline gains, and same-base MetaphorBoost improvements before Table 4 ablations.
- **caption/header**：`Table 2. Overall results on MetaphorVU-Bench. To intuitively demonstrate gap between MLLMs and human*, we sample 100 instances and collect human-written metaphor interpretations as upper-bound. The table shows that current MLLMs exhibit limited capability, and existing reasoning-enhanced methods fail to achieve effective improvements. In contrast, our method proves to be more effective.`（52 词；moves=title, setup, comparison, main_finding；self-contained=true；main_finding_stated=true）。
- **优点**：Multi-level row grouping makes model families and the upper-bound visible in one scan.；Per-type columns expose where gains occur rather than hiding behind Average.；Blue method rows and teal Δ rows make the proposed comparisons easy to locate.
- **弱点**：Twenty-five rows and ten columns create high visual density even at page width.；The human upper bound is a 100-instance sample, not full-benchmark human scoring.；No variance, sample denominator for model rows, or evaluation-cost normalization is shown.
- **可复用模式**：Use a page-width comparison table with method-family bands, per-category metrics, an average column, and explicit same-base deltas.
- **证据**：p.5 Table 2 table and caption；basis=`rendered_observation`；bbox=None。

### Table 3 — p.6，results，single_column

- **目的**：purpose=`failure`；Two models × four manually coded thinking-process deficiency categories: Wrong Recognition, Missing Mapping, Superficial Mapping, and Improper Mapping. Percentages are shown to one decimal place.
- **结构**：rows=2；columns=5；header_levels=1；row_groups=0；decimal_precision=1；rules=`booktabs`。
- **高亮/不确定性**：highlighting=`none`；none reported; each row gives four point proportions summing to 100%, without denominator or inter-rater uncertainty.
- **证据关系**：The compact diagnosis table supports the detailed-analysis interpretation of Figure 5 and motivates the graph-based mapping augmentation in Figure 6.
- **caption/header**：`Table 3. Proportion of each deficiency type, reveals that enhancing cross-domain mapping is key to improving performance.`（17 词；moves=title, setup, main_finding；self-contained=true；main_finding_stated=true）。
- **优点**：The four mutually interpretable error columns align with the later case-study highlights.；Percentages sum by row, making the relative error composition easy to inspect.；The table fits beside the two-panel type-difficulty bars.
- **弱点**：The coding denominator and sampling protocol are not visible.；No inter-annotator agreement or uncertainty is reported.；Long wrapped headers consume most of the narrow table width.
- **可复用模式**：Translate a mechanism hypothesis into a small error-composition table whose categories reappear in qualitative evidence.
- **证据**：p.6 Table 3 table and caption；basis=`rendered_observation`；bbox=None。

### Table 4 — p.8，ablation，page_width

- **目的**：purpose=`ablation, mechanism`；Four Qwen3-VL-8B-Thinking rows (full MetaphorBoost, w/o external augmentation, w/o graph-structure augmentation, w/o metaphor-oriented augmentation) × eight type columns plus Average; one decimal place.
- **结构**：rows=4；columns=10；header_levels=1；row_groups=0；decimal_precision=1；rules=`booktabs`。
- **高亮/不确定性**：highlighting=`bold`；none reported; rows are single point averages by type and no repeated-run variation is given.
- **证据关系**：Component-removal comparisons test the external knowledge, graph structure, and metaphor-oriented knowledge claims introduced by Figure 6.
- **caption/header**：`Table 4. Ablation results show that external knowledge is important for mapping augmentation, structured knowledge graph provides more effective augmentation than plain text, and augmentation by metaphor-oriented knowledge outperforms commonsense knowledge.`（31 词；moves=title, comparison, main_finding；self-contained=true；main_finding_stated=true）。
- **优点**：Each row removes one named component while retaining the same base model.；The Average column gives a concise mechanism summary and per-type columns expose heterogeneous effects.；Bold values visually anchor the proposed/full configuration.
- **弱点**：A single base model and point estimate limit generality.；The table does not report cost, graph size, seed, or uncertainty changes across ablations.；No explicit reference marker indicates which row is the control beyond the row name.
- **可复用模式**：Keep component-removal rows adjacent to the full method and align the same per-type columns as the headline comparison.
- **证据**：p.8 Table 4 table and caption；basis=`rendered_observation`；bbox=None。

### Table 5 — p.15，appendix D.1，page_width

- **目的**：purpose=`dataset, reproduction`；Four datasets—Manual Metaphors (718), Metaphor Novelty (200), Metaphor Explanation (28,000), and Metaphor Dataset (8,030)—with Name, URL, and # Samples columns.
- **结构**：rows=4；columns=3；header_levels=1；row_groups=0；decimal_precision=None；rules=`booktabs`。
- **高亮/不确定性**：highlighting=`none`；none; dataset sample counts are point counts, and URLs are identifiers rather than uncertain estimates.
- **证据关系**：The source table documents the textual inputs used to build the metaphorical KG and complements Figure 15’s extraction prompt and Figure 6’s graph-construction row.
- **caption/header**：`Table 5. Details of metaphorical textual datasets.`（7 词；moves=title, setup；self-contained=true；main_finding_stated=false）。
- **优点**：URLs make the external dataset provenance actionable.；A dedicated sample-count column exposes the scale imbalance.；The short three-column structure remains readable across the page.
- **弱点**：No license, language, deduplication, or translation-quality field is included.；The table is an input inventory, not evidence that all samples become unique graph edges.；Long URLs dominate horizontal space and are small in the render.
- **可复用模式**：Record external data sources with an explicit URL and sample-count column before showing the transformation prompt.
- **证据**：p.15 Table 5 table and caption；basis=`rendered_observation`；bbox=None。

### Table 6 — p.17，appendix G，page_width

- **目的**：purpose=`robustness, experimental_design`；Four Qwen3-VL-8B-Thinking rows: default MetaphorBoost, w/o common connection, h=1,z=10, and h=2,z=5; eight abbreviated type columns plus Average, one decimal place.
- **结构**：rows=4；columns=10；header_levels=1；row_groups=0；decimal_precision=1；rules=`booktabs`。
- **高亮/不确定性**：highlighting=`bold`；none reported; values are one-decimal point averages by type and Average, without uncertainty or query-cost measurements.
- **证据关系**：The appendix sensitivity table checks the common-connection query strategy and h/z settings that are hidden behind the default configuration used in Table 2 and Table 4.
- **caption/header**：`Table 6. Experiments about query strategy and hyperparameters.`（8 词；moves=title, setup, comparison, main_finding；self-contained=true；main_finding_stated=true）。
- **优点**：The baseline/default row is adjacent to the query variants.；The same type columns as the main results preserve cross-table alignment.；Bold cells draw attention to local best values without adding a second visual grammar.
- **弱点**：The caption does not define h and z; that meaning is supplied by the surrounding text.；No compute, latency, or graph-retrieval noise measure accompanies the sensitivity scores.；The visible “MwtaphorBoost” typo weakens polish and searchability.
- **可复用模式**：Use a compact per-category sensitivity table to expose query defaults and retain the same column semantics as the main result.
- **证据**：p.17 Table 6 table and caption；basis=`rendered_observation`；bbox=None。

## 跨对象系统判断

- **visual_narrative**：Figure 1 先定义视频元素到隐含意义的跨域映射；Figure 2 固定八类 taxonomy；Figure 3/4 说明数据构建与主题覆盖；Tables 1–3 和 Figure 5 将覆盖、总体能力、类型难度和错误组成量化；Figure 6、Table 4、Figure 7/Appendix Figure 8 再将 mapping diagnosis 接到 MetaphorBoost、消融和 case study；Appendix Tables 5–6 与 Figures 9–17 提供数据、提示词和查询设置，Figures 18–25 扩展 qualitative atlas。
- **caption_system**：Caption 统一采用斜体 Figure/Table label 加 sentence-case 英文句子。正文图表 captions 多包含 setup、比较对象或主方向；结果 captions（Tables 2–4、Figures 5–7）直接写结论，但常省略分母、误差定义和重复运行信息。附录 prompt/atlas captions 简短，将细节交给图内文字或 surrounding section。
- **table_header_system**：表格以 booktabs 顶/中/底线和无竖线为主。主结果表统一 Method + 八类缩写 + Average；Table 2 额外使用五个浅灰 row-group bands，Table 1/5 用数据描述列，Table 3 用四类错误列，Table 6 复用主结果列语义。
- **method_result_ablation_link**：Figure 6 的 offline KG→online keyword/query/generation 机制与 Table 4 的 external/graph/metaphor-oriented ablations逐项对应；Table 2 的 same-base Δ、Figure 5 的类型困难、Table 3 的 mapping error composition 和 Figure 7 的 bad-mapping counts 构成从结果到机制的证据链；Appendix Figure 8 提供一例文本级解释。
- **main_appendix_link**：正文 Figure 2 指向 Appendix A 的理论基础，Figure 3 指向 B 的 filtration/annotation prompts，evaluation metric 指向 C，KG 与 inference pipeline 指向 D/E，baselines 指向 F，默认 h=2,z=10 指向 G，taxonomy qualitative evidence 指向 H。附录 Table 5–6 和 Figures 9–25 分别承担输入来源、查询敏感性、可复现提示词和类型示例。
- **typography_consistency**：结果表和正文共享 Nimbus Roman/Computer Modern 体系；小图和卡片在同一 PDF 中引入 Arial/Times New Roman/NimbusMonL，造成混合但可辨识的层级。正文轴/图内文字偏小，附录 prompt 和 qualitative cards 的标题字号更大而 body text 仍密集。
- **color_consistency**：蓝/橙是最稳定的流程与方法角色对比，绿/黄/浅蓝/桃色承担 taxonomy 类型，灰代表基线或结构。Figure 8 明确定义绿/橙/蓝为三类 mapping error；结果 bar charts 主要依赖颜色和分组位置，pastel atlas 主要依赖 header/background，跨对象的颜色语义并非完全全局固定。

## 最终判断

- **最可复用模式**：用 element→concept→meaning 的全宽/列内流程固定抽象任务接口。；用 taxonomy card/filmstrip atlas 将类别定义与真实视觉例子相邻呈现。；用 same-column main comparison、error composition、component ablation 和 qualitative case 形成结果→机制→解释闭环。；把每个不可见的筛选/生成操作写成带输入字段与 JSON 输出的 prompt panel，置于附录复现。
- **最高价值对象**：Figure 1：把视频理解的跨域映射问题变成可读的视觉接口。；Figure 3：明确平台来源、三阶段筛选和人工质量控制边界。；Table 2：以八类和 Average 同时展示人类差距、基线和 same-base MetaphorBoost 增益。；Figure 6 + Table 4：把 KG mapping augmentation 机制与组件贡献对齐。；Table 3 + Figure 7 + Appendix Figure 8：将 mapping failure taxonomy 从比例延伸到计数和文本案例。
- **主要失败模式**：主结果与 bad-mapping 图表多为点估计或无标签柱，缺少分母、重复运行、误差和成本信息。；Figure 5、Table 2、Table 4、Table 6 的窄/宽表图文字密度较高，缩放后模型名、类别缩写和长 row label 解码成本大。；Pastel taxonomy/qualitative colors 主要靠颜色与背景承载，灰度和色觉异常下冗余不足。；Appendix prompts、judge rubric 和 free-form interpretations 提供复现内容但文本过密，且没有显示审计例、协议一致性或异常输出处理。；公开 GitHub 仓库提供评测代码和 prompt utilities，但未提供 exact figure/table source；本审计不能把相邻 README 截图或工具仓库冒充视觉源。
- **一句话视觉策略**：论文用浅色卡片、filmstrip 和直接标签先定义视频隐喻的八类与跨域映射，再以分组结果表、类型难度、错误组成、KG pipeline、消融和 qualitative case 逐层证明 MetaphorBoost 的 mapping augmentation，最后把筛选/评测/生成 prompt 与更多类别例子收纳到附录。
