# Visual audit | icml-2026-4d6c3bd9ff61

## 审计范围与事实源

- 论文 ID 为 `icml-2026-4d6c3bd9ff61`，标题为 *UniPercept: Towards Unified Perceptual-Level Image Understanding across Aesthetics, Quality, Structure, and Texture*。
- PDF：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`；`pdfinfo` 显示 27 页、Letter 612×792 pt；PDF 已验证且无加密。
- 覆盖：主文 p.1–10、参考文献 p.11–14、同一 PDF 内的补充材料 p.15–27；所有 27 页已按 200 dpi（1700×2200 px，超过 180 dpi 要求）渲染并逐页检查。
- PDF 清单核对结果：14 幅 Figure、17 张 Table；reading 的 `visual_inventory` 与 PDF 标签/页码一致。p.1 为标题/overview 页，p.15 为补充材料起点。
- 视觉证据：`/tmp/4d6_audit_render_200/page-01.png` 至 `page-27.png` 及对应 contact sheets；PDF 为最终对象清单与视觉事实源。

## Source acquisition

- PDF 首页给出 Website `https://thunderbolt215.github.io/Unipercept-project`、Code `https://github.com/thunderbolt215/UniPercept` 和 Benchmark/Checkpoint `https://hf.co/collections/Thunderbolt215215/unipercept`。reading inventory 的候选仓库为 `thunderbolt215/UniPercept`，本地 `corpus/visual_sources/icml-2026-4d6c3bd9ff61/` 不存在。
- GitHub API 对 `thunderbolt215/UniPercept` 的 `main` 递归树检查到 commit `8ca4c216687f71973842472568c0c9ccdadd5458`。仓库有 `asserts/img/` 的 Git LFS PNG/JPG 导出资产、benchmark JSON 与评测代码；没有 `plot*`、`figure*`、`table*`、`.ipynb`、`.tex`、`.tikz`、`.pgf` 或 style/plot 参数文件。README 的图像引用确认这些资产属于项目，但不能从导出图反推 PDF 对象的精确字号、颜色和线宽。
- 因此源状态为 `partial_visual_source`：以下资产可作为部分视觉参照，精确对象属性均标记为 `rendered_estimate` 或 PDF 字体对象，未把 GitHub 导出图误报为完整绘图源。

  - `asserts/img/teaser_v9.png`：README hero image; likely exported overview asset corresponding to the benchmark teaser/Figure 1 family.
  - `asserts/img/teaser_v4.jpg`：Repository teaser variant in the GitHub tree; no paper-figure mapping or generation source is provided.
  - `asserts/img/unipercetp-bench.png`：README benchmark image; exported benchmark distribution/overview asset.
  - `asserts/img/vr.png`：README performance asset for UniPercept-Bench-VR; exported table/summary image.
  - `asserts/img/vqa-iaa.png`：README performance asset for UniPercept-Bench-VQA (IAA); exported table/summary image.
  - `asserts/img/vqa-iqa.png`：README performance asset for UniPercept-Bench-VQA (IQA); exported table/summary image.
  - `asserts/img/vqa-ista.png`：README performance asset for UniPercept-Bench-VQA (ISTA); exported table/summary image.
  - `asserts/img/reward.png`：README reward-performance asset; exported application result image.
  - `asserts/img/metrics_dpg.png`：README DPG metric asset; exported T2I metric image.
  - `asserts/img/metric_geneval.png`：README GenEval metric asset; exported T2I metric image.
  - `asserts/img/profile1.png`：README image-profile asset; exported qualitative profile image.
  - `asserts/img/profile2.png`：README image-profile asset; exported qualitative profile image.
  - `asserts/img/profile3.png`：README image-profile asset; exported qualitative profile image.

## Whole-paper style

- **版式与渲染**：双栏 Letter；正文表格和线稿主要是矢量/字体对象，示意图为混合渲染，照片、截图和 profile 主要为栅格嵌入。
- **字体**：PDF 对象包含 CMR6/8、CMMI、CMSY、Nimbus Roman/Mono；图内另见 Georgia、Calibri-BoldItalic、Arial Rounded MT Bold、Times New Roman。图/表估计字号约 5.5–24 pt 与 6–8 pt。
- **颜色**：IAA 粉红、IQA 蓝、ISTA 淡紫跨对象稳定；表格深蓝/浅蓝表示 best/second-best。大部分彩色对象不能在灰度下可靠保持语义，域名、分组和直接标签提供部分冗余。
- **一致性判断**：对象层级、caption 位置和颜色语义统一，但图内字体混杂，密集卡片/截图/profile 小字是全篇主要可读性瓶颈。

## Figure 1 | p.1 | method

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 1, bbox `[55, 250, 555, 690]` PDF points, basis `rendered_observation`；placement `main`, width `page_width`.

- **类型/职责**：conceptual_diagram, qualitative_grid, other；purpose = headline, method_interface, dataset。

- **设计与布局**：Qualitative overview rather than a statistical plot. The central three-domain wheel enumerates 7 IAA, 3 IQA and 4 ISTA category families; surrounding cards show illustrative images, VR scores and VQA prompts. No sample denominator or aggregate statistic is encoded.

- **Plot grammar**：rendering `mixed`；x/y scale `none` / `none`；grid `none`；legend `False` (none)；direct labels `True`；markers `0`；line styles `0`；hatching `False`；reference lines `0`；uncertainty `none`；line width `1.0` pt；provenance `rendered_estimate`.

- **Encodings**：x=none；y=none；color=domain and task role；shape=card/image type；line=arrows and radial hierarchy；facet=IAA/IQA/ISTA domains and VR/VQA tasks；text=domain names, category names, question snippets and scores。

- **Typography**：family Nimbus Roman No9 L, Georgia, Computer Modern math, Arial Rounded MT Bold；size 5.5 / 8.0 / 24.0 pt (min/median/max)；weight regular, bold；style roman, italic；provenance `mixed`, confidence `high`.

- **Color**：mode `mixed`, approximate HEX #F2A3A8, #F7C4C4, #A9D8F5, #D3E9FA, #C7B0F2, #E9DDFB, #E53935, #1769AA, #6A1B9A, #FFFFFF, #111111; Pink, blue and lavender encode IAA, IQA and ISTA; white cards and dark ink carry examples and labels. Redundant encoding `True`, grayscale safe `False`, provenance `rendered_estimate`.

- **Complexity**：score 5/5; panels 20; series None; legend items 0; annotations 24; estimated marks None.

- **Caption (PDF exact)**：
```text
Figure 1. Overview of UniPercept-Bench. We propose a unified benchmark for perceptual-level image understanding, covering three perceptual domains: Image Aesthetics Assessment (IAA), Image Quality Assessment (IQA), and Image Structure and Texture Assessment (ISTA). Each domain is organized hierarchically into Domain–Category–Criterion levels, enabling fine-grained perceptual evaluation. The benchmark contains both Visual Rating (VR) and Visual Question Answering (VQA) tasks, facilitating comprehensive assessment of models’ abilities to understand and reason about perceptual-level image understanding.
```
  - word_count=74; moves=title, setup, encoding_key, comparison, main_finding; headline_bold=True; self_contained=True; main_finding_stated=True.

- **Evidence relation**：Converts the introduction claim of a unified perceptual layer into the benchmark interface, then points to Figure 3’s QA examples, Figure 4’s construction and Figures 5–6’s distribution/training views.

- **Strengths**：
  - The central wheel establishes taxonomy and reading order in one glance.
  - Examples connect abstract domain names to actual prompts and rating outputs.

- **Weaknesses**：
  - Many cards and very small text compete with the taxonomy; examples are illustrative rather than sampled evidence.
  - The wheel has no explicit legend for every card and is not accessible in grayscale.

- **Reusable pattern**：Use a central taxonomy with peripheral task examples when a benchmark must communicate both scope and interface.

## Figure 2 | p.2 | introduction

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 2, bbox `[325, 260, 560, 470]` PDF points, basis `rendered_observation`；placement `main`, width `single_column`.

- **类型/职责**：conceptual_diagram, image_montage；purpose = headline, experimental_design。

- **设计与布局**：Two qualitative panels juxtapose image captioning/object visual reasoning with aesthetics/structure assessment. No quantitative sample, score or uncertainty is encoded.

- **Plot grammar**：rendering `mixed`；x/y scale `none` / `none`；grid `none`；legend `False` (none)；direct labels `True`；markers `0`；line styles `0`；hatching `False`；reference lines `0`；uncertainty `none`；line width `1.0` pt；provenance `rendered_estimate`.

- **Encodings**：x=none；y=none；color=semantic versus perceptual level；shape=example image type；line=none；facet=two rounded panels；text=panel titles and task names。

- **Typography**：family Nimbus Roman No9 L, Georgia, Computer Modern math, Arial Rounded MT Bold；size 5.5 / 8.0 / 24.0 pt (min/median/max)；weight regular, bold；style roman, italic；provenance `mixed`, confidence `high`.

- **Color**：mode `mixed`, approximate HEX #B7D5F0, #F9DCC8, #D95F5F, #3B78B5, #FFFFFF, #222222, #777777; Blue semantic panel and peach perceptual panel distinguish the two levels; panel titles and example labels repeat the mapping. Redundant encoding `True`, grayscale safe `False`, provenance `rendered_estimate`.

- **Complexity**：score 2/5; panels 2; series 2; legend items 0; annotations 8; estimated marks 2.

- **Caption (PDF exact)**：
```text
Figure 2. Semantic-level vs. Perceptual-level understanding.
```
  - word_count=6; moves=title; headline_bold=True; self_contained=False; main_finding_stated=False.

- **Evidence relation**：Operationalizes the introduction’s semantic-versus-perceptual distinction and motivates the benchmark taxonomy in Figures 1 and 3.

- **Strengths**：
  - The contrast is legible because panel color, headings and task names agree.
  - Small image pairs make the difference concrete without requiring axes.

- **Weaknesses**：
  - The semantic and perceptual examples have no controlled matching or quantitative criterion.
  - The right-column inset leaves limited room for explanatory text.

- **Reusable pattern**：Use parallel panels with matched image examples and explicit task labels to introduce a new evaluation layer.

## Figure 3 | p.3 | method

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 3, bbox `[55, 65, 555, 380]` PDF points, basis `rendered_observation`；placement `main`, width `page_width`.

- **类型/职责**：qualitative_grid, image_montage；purpose = method_interface, dataset, experimental_design。

- **设计与布局**：Nine representative QA cards are arranged as three domain rows by three examples. They expose the Domain–Category–Criterion hierarchy and qualitative answer options, without reporting prevalence or accuracy.

- **Plot grammar**：rendering `mixed`；x/y scale `none` / `none`；grid `none`；legend `False` (none)；direct labels `True`；markers `0`；line styles `0`；hatching `False`；reference lines `0`；uncertainty `none`；line width `0.8` pt；provenance `rendered_estimate`.

- **Encodings**：x=none；y=none；color=domain row and answer emphasis；shape=question type；line=none；facet=IAA/IQA/ISTA rows by example category；text=question, options, criterion and QA template text。

- **Typography**：family Nimbus Roman No9 L, Georgia, Computer Modern math, Arial Rounded MT Bold；size 5.5 / 8.0 / 24.0 pt (min/median/max)；weight regular, bold；style roman, italic；provenance `mixed`, confidence `high`.

- **Color**：mode `mixed`, approximate HEX #F2B1B7, #F8D7D9, #A9D8F5, #DCEEFF, #C8B4F0, #ECE4FF, #1769AA, #8A3D90, #D95F5F, #222222; Pastel bands map rows to IAA, IQA and ISTA; answer highlights and domain labels repeat the grouping. Redundant encoding `True`, grayscale safe `False`, provenance `rendered_estimate`.

- **Complexity**：score 5/5; panels 9; series 3; legend items 0; annotations 45; estimated marks None.

- **Caption (PDF exact)**：
```text
Figure 3. Representative QA examples in UniPercept-Bench. Questions follow a three-level hierarchy of Domain–Category–Criterion, defining perceptual scope, specific visual aspects, and fine-grained criteria for constructing diverse, perception-oriented VQA tasks.
```
  - word_count=31; moves=title, setup, encoding_key; headline_bold=True; self_contained=True; main_finding_stated=False.

- **Evidence relation**：Makes the benchmark’s question interface inspectable after Figure 1 and before the construction pipeline; later VQA Tables 2–4 aggregate the same domain and template axes.

- **Strengths**：
  - Every card names its criterion and QA template, so the fine-grained interface is reproducible.
  - Consistent row colors make the three domains easy to scan.

- **Weaknesses**：
  - The nine examples are not identified as a random sample and text is dense at page scale.
  - Answer highlighting is color-dependent and does not quantify category coverage.

- **Reusable pattern**：Pair each illustrative question with its criterion and template so a taxonomy becomes an executable annotation interface.

## Figure 4 | p.4 | method

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 4, bbox `[55, 60, 555, 205]` PDF points, basis `rendered_observation`；placement `main`, width `page_width`.

- **类型/职责**：pipeline, conceptual_diagram；purpose = method_interface, reproduction。

- **设计与布局**：A three-stage process is shown with initial QA generation, reject sampling with four judger criteria, and human refinement. It specifies components and quality checks but gives no retention rate, sample count or annotator agreement.

- **Plot grammar**：rendering `mixed`；x/y scale `none` / `none`；grid `none`；legend `False` (none)；direct labels `True`；markers `0`；line styles `0`；hatching `False`；reference lines `0`；uncertainty `none`；line width `1.2` pt；provenance `rendered_estimate`.

- **Encodings**：x=none；y=none；color=stage identity；shape=generator/judger/human role；line=directional arrows；facet=three construction stages；text=stage labels, criterion checks and output labels。

- **Typography**：family Nimbus Roman No9 L, Georgia, Computer Modern math, Arial Rounded MT Bold；size 5.5 / 8.0 / 24.0 pt (min/median/max)；weight regular, bold；style roman, italic；provenance `mixed`, confidence `high`.

- **Color**：mode `mixed`, approximate HEX #B9DCF4, #E6F2FA, #F7C8C8, #F3B4B4, #D8C9F2, #C2A9EA, #D9534F, #3678B5, #FFFFFF, #222222; Pastel stage boxes and red, blue and purple arrows encode generation, rejection and human refinement. Redundant encoding `True`, grayscale safe `False`, provenance `rendered_estimate`.

- **Complexity**：score 4/5; panels 3; series None; legend items 0; annotations 30; estimated marks None.

- **Caption (PDF exact)**：
```text
Figure 4. Constuction pipeline of UniPercept-Bench. A three-stage process initial QA generation, reject sampling, and human refinement to produce high-quality perceptual-level QA pairs across aesthetics, quality, structure, and texture.
```
  - word_count=29; moves=title, setup, encoding_key; headline_bold=True; self_contained=True; main_finding_stated=False.

- **Evidence relation**：Connects the benchmark definition and QA examples to the resulting data used by Tables 1–4 and the training data summaries in Tables 14–15.

- **Strengths**：
  - Large arrows and numbered stages give a clear left-to-right procedure.
  - Reject-sampling criteria and human-refinement roles are visible at the point of use.

- **Weaknesses**：
  - The caption contains the PDF typo “Constuction” and a missing verb in “A three-stage process initial QA generation”.
  - The pipeline implies quality control without reporting acceptance statistics or agreement.

- **Reusable pattern**：Show data construction as a staged pipeline with named generators, rejection criteria and final human ownership.

## Figure 5 | p.4 | method

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 4, bbox `[55, 245, 330, 475]` PDF points, basis `rendered_observation`；placement `main`, width `single_column`.

- **类型/职责**：bar, other；purpose = dataset, experimental_design。

- **设计与布局**：The composite reports domain shares of 32.3% IAA, 34.6% IQA and 33.0% ISTA, plus category and criterion distributions. The criterion panel annotates 3 domains, 17 categories and 43 criteria; values are point shares/counts with no uncertainty.

- **Plot grammar**：rendering `mixed`；x/y scale `categorical` / `linear`；grid `none`；legend `False` (none)；direct labels `True`；markers `0`；line styles `0`；hatching `False`；reference lines `0`；uncertainty `none`；line width `0.8` pt；provenance `rendered_estimate`.

- **Encodings**：x=domain/category/criterion；y=share or count；color=domain/category/criterion；shape=none；line=none；facet=three distribution panels；text=percentages, labels and annotations。

- **Typography**：family Nimbus Roman No9 L, Georgia, Computer Modern math, Arial Rounded MT Bold；size 5.5 / 8.0 / 24.0 pt (min/median/max)；weight regular, bold；style roman, italic；provenance `mixed`, confidence `high`.

- **Color**：mode `mixed`, approximate HEX #EF9CC5, #77B9EF, #B8A7E9, #5B8CCB, #F3C0D8, #9BCBF1, #FFFFFF, #333333; Pink, blue and lavender encode domains; bar hues and direct percentage/category labels carry the distribution. Redundant encoding `True`, grayscale safe `False`, provenance `rendered_estimate`.

- **Complexity**：score 4/5; panels 3; series 3; legend items 0; annotations 35; estimated marks 45.

- **Caption (PDF exact)**：
```text
Figure 5. Distribution of UniPercept-Bench across (a) Domain, (b) Category, and (c) Criterion. Zoom in for best view.
```
  - word_count=18; moves=title, setup, encoding_key; headline_bold=True; self_contained=True; main_finding_stated=False.

- **Evidence relation**：Provides the coverage evidence promised by Figure 1 and supports the dataset-size and taxonomy claims before Tables 1–4.

- **Strengths**：
  - Three views answer domain, category and criterion coverage in a compact footprint.
  - Direct percentages and counts reduce dependence on a separate legend.

- **Weaknesses**：
  - The horizontal and vertical bar labels are crowded and require zooming.
  - Pie/donut and bar encodings mix shares and counts without a common scale or uncertainty.

- **Reusable pattern**：Use a three-panel distribution figure to expose both top-level balance and fine-grained taxonomy coverage.

## Figure 6 | p.5 | method

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 5, bbox `[55, 60, 555, 235]` PDF points, basis `rendered_observation`；placement `main`, width `page_width`.

- **类型/职责**：pipeline, architecture, line, conceptual_diagram；purpose = method_interface, theory_mechanism, experimental_design。

- **设计与布局**：The training schematic combines DAPT examples, a structured ISTA annotation, a VR score, an adaptive Gaussian soft-reward curve and a VQA answer-matching reward. The inset curve is illustrative; it does not report a fitted dataset or uncertainty.

- **Plot grammar**：rendering `mixed`；x/y scale `linear` / `linear`；grid `none`；legend `False` (none)；direct labels `True`；markers `1`；line styles `2`；hatching `False`；reference lines `0`；uncertainty `none`；line width `1.0` pt；provenance `rendered_estimate`.

- **Encodings**：x=reward deviation in inset curve；y=score/reward；color=stage and task role；shape=reward type；line=flow from DAPT to RL；facet=DAPT and task-aligned RL stages；text=structured JSON, GT/prediction, reward and score labels。

- **Typography**：family Nimbus Roman No9 L, Georgia, Computer Modern math, Arial Rounded MT Bold；size 5.5 / 8.0 / 24.0 pt (min/median/max)；weight regular, bold；style roman, italic；provenance `mixed`, confidence `high`.

- **Color**：mode `mixed`, approximate HEX #B9DCF4, #D7EAF7, #E7E7E7, #BFD3E8, #F4A7A7, #E8C5C5, #C8B4F0, #1769AA, #D9534F, #FFFFFF, #222222; Blue DAPT and gray/blue RL containers separate stages; red reward curve, lavender ISTA and blue IQA/IAA annotations encode task roles. Redundant encoding `True`, grayscale safe `False`, provenance `rendered_estimate`.

- **Complexity**：score 4/5; panels 4; series 3; legend items 0; annotations 20; estimated marks 4.

- **Caption (PDF exact)**：
```text
Figure 6. Training pipeline of UniPercept. A two-stage framework combining domain-adaptive pre-training for perceptual understanding and task-aligned RL to jointly optimize Visual Rating and Visual Question Answering.
```
  - word_count=27; moves=title, setup, encoding_key; headline_bold=True; self_contained=True; main_finding_stated=False.

- **Evidence relation**：Bridges the construction interface to the model mechanism; the DAPT/RL stages motivate Tables 1–4 and the ablation Tables 16–17, while the reward branch connects to Table 5 and Figure 8.

- **Strengths**：
  - The same diagram shows inputs, structured output, reward mechanics and optimization stages.
  - The VR/VQA branches visibly align with the paper’s two-task claim.

- **Weaknesses**：
  - Several miniature screenshots and formulas are difficult to read.
  - The reward curve has no numeric parameter annotation or empirical validation in the graphic.

- **Reusable pattern**：Expose training stages together with one concrete sample, output contract and task-specific reward path.

## Figure 7 | p.8 | results

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 8, bbox `[420, 75, 575, 290]` PDF points, basis `rendered_observation`；placement `main`, width `single_column`.

- **类型/职责**：other, bar；purpose = headline, main_comparison。

- **设计与布局**：A radial summary selects UniPercept, ArtiMuse, DeQA, LLaVA-OneVision-1.5-Instruct-8B, InternVL3-78B, InternVL3.5-38B, Q-Insight* and GPT-4o. VQA uses accuracy and VR uses (SRCC + PLCC)/2, as stated in the caption; values are derived point summaries with no error bars.

- **Plot grammar**：rendering `raster`；x/y scale `none` / `none`；grid `none`；legend `True` (around radial chart)；direct labels `True`；markers `0`；line styles `0`；hatching `False`；reference lines `0`；uncertainty `none`；line width `1.0` pt；provenance `rendered_estimate`.

- **Encodings**：x=none；y=none；color=model identity and task/domain；shape=none；line=none；facet=VQA and VR radial summaries；text=model labels and radial score annotations。

- **Typography**：family Nimbus Roman No9 L, Georgia, Computer Modern math, Arial Rounded MT Bold；size 5.5 / 8.0 / 24.0 pt (min/median/max)；weight regular, bold；style roman, italic；provenance `mixed`, confidence `high`.

- **Color**：mode `categorical`, approximate HEX #9270D7, #E99BA5, #6DB9D7, #6CC7A6, #F0B178, #5B91D0, #E4A3DC, #D95F5F, #FFFFFF, #333333; Each radial trace is a selected model; center and perimeter labels separate VQA accuracy from VR summary and domains. Redundant encoding `True`, grayscale safe `False`, provenance `rendered_estimate`.

- **Complexity**：score 4/5; panels 1; series 12; legend items 8; annotations 12; estimated marks 12.

- **Caption (PDF exact)**：
```text
Figure 7. Results on UniPercept-Bench. VQA and VR are evaluated by Acc. and (SRCC + PLCC)/2.
```
  - word_count=16; moves=title, encoding_key; headline_bold=True; self_contained=True; main_finding_stated=False.

- **Evidence relation**：Compresses Tables 1–4 into a visual headline and supports the results discussion before the reward/application evidence in Figures 8 and Tables 5–8.

- **Strengths**：
  - A single compact chart exposes cross-task and cross-domain comparisons.
  - The legend names selected models and the caption defines the two metric families.

- **Weaknesses**：
  - Radial geometry makes precise cross-model comparison harder than the source tables.
  - Different task metrics share one visual surface without a common scale or uncertainty display.

- **Reusable pattern**：Use a radial overview only as a navigation layer, with full metric tables retained for exact comparison.

## Figure 8 | p.9 | results

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 9, bbox `[55, 230, 555, 620]` PDF points, basis `rendered_observation`；placement `main`, width `page_width`.

- **类型/职责**：qualitative_grid, image_montage；purpose = qualitative_evidence, mechanism, headline。

- **设计与布局**：Four prompts are each rendered under baseline FLUX.1-dev, IAA reward, IQA reward, ISTA reward and the combined reward. It is qualitative evidence; Table 5 supplies point metrics such as baseline 65.18/73.59/46.64 and combined 74.24/77.04/59.08 for IAA/IQA/ISTA, while no replicate or human preference uncertainty is shown in the grid.

- **Plot grammar**：rendering `raster`；x/y scale `none` / `none`；grid `none`；legend `False` (none)；direct labels `True`；markers `0`；line styles `0`；hatching `False`；reference lines `0`；uncertainty `none`；line width `None` pt；provenance `rendered_estimate`.

- **Encodings**：x=reward condition columns；y=prompt/output image；color=reward condition；shape=image content；line=none；facet=four prompt rows by five columns；text=prompt text, condition headers and generated images。

- **Typography**：family Nimbus Roman No9 L, Georgia, Computer Modern math, Arial Rounded MT Bold；size 5.5 / 8.0 / 24.0 pt (min/median/max)；weight regular, bold；style roman, italic；provenance `mixed`, confidence `high`.

- **Color**：mode `mixed`, approximate HEX #BCD4F7, #E6E6E6, #D8E1F6, #F2F2F2, #FFFFFF, #444444, #111111, #9E9E9E; Column headers identify baseline and each reward; light blue header bands and natural image colors distinguish conditions. Redundant encoding `True`, grayscale safe `False`, provenance `rendered_estimate`.

- **Complexity**：score 5/5; panels 20; series 5; legend items 0; annotations 20; estimated marks None.

- **Caption (PDF exact)**：
```text
Figure 8. More results of FLUX.1-dev w/ UniPercept Reward. Different reward signals emphasize distinct perceptual attributes, while UniPercept Reward (All) achieves the best overall performance by integrating complementary perceptual cues.
```
  - word_count=31; moves=title, setup, comparison, main_finding; headline_bold=True; self_contained=True; main_finding_stated=True.

- **Evidence relation**：Tests the application claim that UniPercept can act as a T2I reward. It follows Table 5 and visually instantiates the dimension-specific trade-offs; Tables 6–8 extend the metric use beyond FLUX.

- **Strengths**：
  - Prompt text is repeated by row and condition headers are explicit.
  - The visual grid lets readers inspect attribute trade-offs that a scalar table hides.

- **Weaknesses**：
  - Generated image cells are small and qualitative judgments are not blinded or independently rated.
  - The caption says “best overall” although Table 5’s All row does not dominate every individual dimension.

- **Reusable pattern**：Align each qualitative output row to one prompt and place reward conditions in fixed columns so changes are directly inspectable.

## Figure 9 | p.16 | appendix

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 16, bbox `[325, 50, 575, 470]` PDF points, basis `rendered_observation`；placement `appendix`, width `single_column`.

- **类型/职责**：screenshot, other；purpose = method_interface, reproduction。

- **设计与布局**：A code-like screenshot presents the ISTA Structural Annotation JSON template, including component names and nested PhysicalStructure, MaterialRepresentation, GeometricComposition and SemanticPerception fields. It is an interface example, not a statistical display.

- **Plot grammar**：rendering `raster`；x/y scale `none` / `none`；grid `none`；legend `False` (none)；direct labels `True`；markers `0`；line styles `0`；hatching `False`；reference lines `0`；uncertainty `none`；line width `None` pt；provenance `rendered_estimate`.

- **Encodings**：x=none；y=none；color=JSON nesting/syntax；shape=key/value type；line=indentation；facet=single code template；text=component names and field values。

- **Typography**：family Nimbus Mono L, Computer Modern math, Nimbus Roman No9 L；size 5.5 / 7.0 / 9.0 pt (min/median/max)；weight regular, bold；style monospace, roman；provenance `mixed`, confidence `high`.

- **Color**：mode `grayscale`, approximate HEX #FFFFFF, #F0F0F0, #555555, #000000, #E6E6E6; Monospace code colors encode JSON nesting and syntax only; indentation and keys provide redundant structure. Redundant encoding `True`, grayscale safe `True`, provenance `rendered_estimate`.

- **Complexity**：score 2/5; panels 1; series None; legend items 0; annotations 15; estimated marks None.

- **Caption (PDF exact)**：
```text
Figure 9. Template of ISTA Structural Annotation.
```
  - word_count=7; moves=title; headline_bold=True; self_contained=False; main_finding_stated=False.

- **Evidence relation**：Expands the structured annotation branch in Figure 6 and points to the full ISTA definitions in Tables 9 and 12.

- **Strengths**：
  - The nested keys make the output contract concrete.
  - Monospace indentation preserves the hierarchy for readers implementing the schema.

- **Weaknesses**：
  - The screenshot is raster text and is hard to copy or search.
  - The caption does not identify required versus optional fields or link the template to a machine-readable file.

- **Reusable pattern**：Show one complete structured-output contract beside the prose definition, while publishing the schema as selectable text.

## Figure 10 | p.22 | appendix

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 22, bbox `[55, 300, 330, 510]` PDF points, basis `rendered_observation`；placement `appendix`, width `single_column`.

- **类型/职责**：image_montage, qualitative_grid；purpose = qualitative_evidence, robustness。

- **设计与布局**：Four image profiles illustrate low/high combinations across domains, including high IQA with lower IAA, high IAA with lower ISTA, and intermediate examples. Ratings are UniPercept outputs, not human ground truth; the page shows point scores without sample size or uncertainty.

- **Plot grammar**：rendering `raster`；x/y scale `none` / `none`；grid `none`；legend `False` (none)；direct labels `True`；markers `0`；line styles `0`；hatching `False`；reference lines `0`；uncertainty `none`；line width `None` pt；provenance `rendered_estimate`.

- **Encodings**：x=none；y=rating 0–100；color=IAA/IQA/ISTA bars；shape=image/profile type；line=none；facet=2×2 image profiles；text=domain score labels and qualitative descriptions。

- **Typography**：family Nimbus Roman No9 L, Georgia, Computer Modern math, Arial Rounded MT Bold；size 5.5 / 8.0 / 24.0 pt (min/median/max)；weight regular, bold；style roman, italic；provenance `mixed`, confidence `high`.

- **Color**：mode `mixed`, approximate HEX #C40000, #0756AD, #6F1D8E, #F4C6C6, #DDEEFF, #E9DEFF, #FFFFFF, #111111; Red, blue and purple rating bars identify IAA, IQA and ISTA for every profile; text headings repeat the domain labels. Redundant encoding `True`, grayscale safe `False`, provenance `rendered_estimate`.

- **Complexity**：score 4/5; panels 4; series 3; legend items 0; annotations 20; estimated marks 12.

- **Caption (PDF exact)**：
```text
Figure 10. Relationship between different perceptual domains. Ratings are provided by UniPercept.
```
  - word_count=12; moves=title, setup; headline_bold=True; self_contained=False; main_finding_stated=False.

- **Evidence relation**：Qualifies the claim that IAA, IQA and ISTA measure distinct dimensions and complements the correlation-free main tables with concrete cases.

- **Strengths**：
  - Score bars and domain labels allow quick profile comparison.
  - Contrasting qualitative descriptions explain why dimensions can diverge.

- **Weaknesses**：
  - Four hand-picked examples cannot establish independence or calibration.
  - The profile text is dense and the three colored bars are not scaled with tick marks.

- **Reusable pattern**：Use small profile cards to demonstrate that a multidimensional metric can separate otherwise plausible image qualities.

## Figure 11 | p.24 | appendix

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 24, bbox `[55, 60, 555, 680]` PDF points, basis `rendered_observation`；placement `appendix`, width `page_width`.

- **类型/职责**：qualitative_grid, image_montage；purpose = qualitative_evidence, dataset。

- **设计与布局**：Twelve additional QA cards cover IAA, IQA and ISTA, with four examples per domain. They are qualitative examples of the same domain/category/criterion and template interface; no sampling rule or frequency statistic is encoded.

- **Plot grammar**：rendering `mixed`；x/y scale `none` / `none`；grid `none`；legend `False` (none)；direct labels `True`；markers `0`；line styles `0`；hatching `False`；reference lines `0`；uncertainty `none`；line width `0.8` pt；provenance `rendered_estimate`.

- **Encodings**：x=none；y=none；color=domain row；shape=question/template type；line=answer emphasis；facet=IAA/IQA/ISTA rows and four example columns；text=questions, options, criterion and template。

- **Typography**：family Nimbus Roman No9 L, Georgia, Computer Modern math, Arial Rounded MT Bold；size 5.5 / 8.0 / 24.0 pt (min/median/max)；weight regular, bold；style roman, italic；provenance `mixed`, confidence `high`.

- **Color**：mode `mixed`, approximate HEX #F2B1B7, #F8D7D9, #A9D8F5, #DCEEFF, #C8B4F0, #ECE4FF, #D9534F, #1769AA, #FFFFFF, #222222; Pastel row bands encode IAA, IQA and ISTA, with answer marks and row labels providing redundant grouping. Redundant encoding `True`, grayscale safe `False`, provenance `rendered_estimate`.

- **Complexity**：score 5/5; panels 12; series 3; legend items 0; annotations 36; estimated marks None.

- **Caption (PDF exact)**：
```text
Figure 11. More Examples in UniPercept-Bench.
```
  - word_count=6; moves=title, setup; headline_bold=True; self_contained=False; main_finding_stated=False.

- **Evidence relation**：Provides supplementary coverage after the main nine examples in Figure 3 and makes the breadth claim more inspectable alongside Tables 10–12.

- **Strengths**：
  - The repeated 3×4 layout makes domain coverage comparable.
  - Criterion and template labels retain the annotation contract in every card.

- **Weaknesses**：
  - Dense cards and small type limit accessibility at page scale.
  - The examples remain illustrative and do not demonstrate annotator agreement or class balance.

- **Reusable pattern**：Use a repeated domain-by-example grid for supplementary qualitative coverage, keeping metadata on each tile.

## Figure 12 | p.25 | appendix

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 25, bbox `[55, 55, 555, 700]` PDF points, basis `rendered_observation`；placement `appendix`, width `page_width`.

- **类型/职责**：image_montage, screenshot, other；purpose = qualitative_evidence, other。

- **设计与布局**：A blurred garden/pergola image receives IAA 29, IQA 35 and ISTA 46. The profile contains three score bars and long domain-specific qualitative text; these are UniPercept outputs without uncertainty or external validation.

- **Plot grammar**：rendering `raster`；x/y scale `none` / `none`；grid `none`；legend `False` (none)；direct labels `True`；markers `0`；line styles `0`；hatching `False`；reference lines `0`；uncertainty `none`；line width `None` pt；provenance `rendered_estimate`.

- **Encodings**：x=none；y=score 0–100；color=IAA/IQA/ISTA domain；shape=image/profile type；line=none；facet=single profile card；text=score bars, colored section headings and generated analysis text。

- **Typography**：family Nimbus Roman No9 L, Georgia, Computer Modern math, Arial Rounded MT Bold；size 5.5 / 8.0 / 24.0 pt (min/median/max)；weight regular, bold；style roman, italic；provenance `mixed`, confidence `high`.

- **Color**：mode `mixed`, approximate HEX #C40000, #0756AD, #6F1D8E, #F4C6C6, #DDEEFF, #E9DEFF, #FFFFFF, #111111; Red, blue and purple rating bars identify IAA, IQA and ISTA for every profile; text headings repeat the domain labels. Redundant encoding `True`, grayscale safe `False`, provenance `rendered_estimate`.

- **Complexity**：score 4/5; panels 1; series 3; legend items 0; annotations 24; estimated marks None.

- **Caption (PDF exact)**：
```text
Figure 12. Example of UniPercept-Constructed Image Profiles.
```
  - word_count=7; moves=title, setup; headline_bold=True; self_contained=False; main_finding_stated=False.

- **Evidence relation**：Extends Figure 10 from comparative examples to the claimed end-user image-profile output and illustrates the lower-score case.

- **Strengths**：
  - Score bars, image and explanatory text form one self-contained report.
  - The three domains remain visually consistent with Figure 10 and the main taxonomy.

- **Weaknesses**：
  - The score bars have no tick marks or uncertainty and the prose is too small for comfortable reading.
  - A single selected image cannot validate score accuracy or profile completeness.

- **Reusable pattern**：Package a multidimensional score and its textual rationale into a stable profile-card template.

## Figure 13 | p.26 | appendix

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 26, bbox `[55, 55, 555, 700]` PDF points, basis `rendered_observation`；placement `appendix`, width `page_width`.

- **类型/职责**：image_montage, screenshot, other；purpose = qualitative_evidence, other。

- **设计与布局**：A historical-house image receives IAA 87, IQA 75 and ISTA 56. The report separates aesthetic, quality and structure/texture narratives; values are point model outputs without repeat or human-reference statistics.

- **Plot grammar**：rendering `raster`；x/y scale `none` / `none`；grid `none`；legend `False` (none)；direct labels `True`；markers `0`；line styles `0`；hatching `False`；reference lines `0`；uncertainty `none`；line width `None` pt；provenance `rendered_estimate`.

- **Encodings**：x=none；y=score 0–100；color=IAA/IQA/ISTA domain；shape=image/profile type；line=none；facet=single profile card；text=score bars, colored section headings and generated analysis text。

- **Typography**：family Nimbus Roman No9 L, Georgia, Computer Modern math, Arial Rounded MT Bold；size 5.5 / 8.0 / 24.0 pt (min/median/max)；weight regular, bold；style roman, italic；provenance `mixed`, confidence `high`.

- **Color**：mode `mixed`, approximate HEX #C40000, #0756AD, #6F1D8E, #F4C6C6, #DDEEFF, #E9DEFF, #FFFFFF, #111111; Red, blue and purple rating bars identify IAA, IQA and ISTA for every profile; text headings repeat the domain labels. Redundant encoding `True`, grayscale safe `False`, provenance `rendered_estimate`.

- **Complexity**：score 4/5; panels 1; series 3; legend items 0; annotations 24; estimated marks None.

- **Caption (PDF exact)**：
```text
Figure 13. Example of UniPercept-Constructed Image Profiles.
```
  - word_count=7; moves=title, setup; headline_bold=True; self_contained=False; main_finding_stated=False.

- **Evidence relation**：Shows a high-aesthetic but lower-structure profile and supports Figure 10’s domain-separation illustration.

- **Strengths**：
  - The fixed card geometry supports side-by-side comparison with Figures 12 and 14.
  - Domain-specific text gives a rationale for each score.

- **Weaknesses**：
  - Long text blocks and colored backgrounds reduce scan speed and grayscale fidelity.
  - No external rater or calibration evidence accompanies the scores.

- **Reusable pattern**：Keep score-card geometry invariant across examples so the reader compares profiles rather than layout changes.

## Figure 14 | p.27 | appendix

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 27, bbox `[55, 55, 555, 700]` PDF points, basis `rendered_observation`；placement `appendix`, width `page_width`.

- **类型/职责**：image_montage, screenshot, other；purpose = qualitative_evidence, other。

- **设计与布局**：A LEGO jungle scene receives IAA 75, IQA 81 and ISTA 82. The profile emphasizes high quality and structural richness with a separate aesthetic score; all values are point model outputs without uncertainty.

- **Plot grammar**：rendering `raster`；x/y scale `none` / `none`；grid `none`；legend `False` (none)；direct labels `True`；markers `0`；line styles `0`；hatching `False`；reference lines `0`；uncertainty `none`；line width `None` pt；provenance `rendered_estimate`.

- **Encodings**：x=none；y=score 0–100；color=IAA/IQA/ISTA domain；shape=image/profile type；line=none；facet=single profile card；text=score bars, colored section headings and generated analysis text。

- **Typography**：family Nimbus Roman No9 L, Georgia, Computer Modern math, Arial Rounded MT Bold；size 5.5 / 8.0 / 24.0 pt (min/median/max)；weight regular, bold；style roman, italic；provenance `mixed`, confidence `high`.

- **Color**：mode `mixed`, approximate HEX #C40000, #0756AD, #6F1D8E, #F4C6C6, #DDEEFF, #E9DEFF, #FFFFFF, #111111; Red, blue and purple rating bars identify IAA, IQA and ISTA for every profile; text headings repeat the domain labels. Redundant encoding `True`, grayscale safe `False`, provenance `rendered_estimate`.

- **Complexity**：score 4/5; panels 1; series 3; legend items 0; annotations 24; estimated marks None.

- **Caption (PDF exact)**：
```text
Figure 14. Example of UniPercept-Constructed Image Profiles.
```
  - word_count=7; moves=title, setup; headline_bold=True; self_contained=False; main_finding_stated=False.

- **Evidence relation**：Completes the three-profile qualitative series and supplies a high-IQA/high-ISTA case for the multidimensional application claim.

- **Strengths**：
  - The same template makes the score differences across Figures 12–14 easy to inspect.
  - The image, numeric bars and domain explanations remain co-located.

- **Weaknesses**：
  - The profile is a selected example and cannot establish generalization.
  - Color carries domain identity strongly, with limited grayscale support and no scale ticks.

- **Reusable pattern**：Reuse a fixed profile layout for contrasting examples, while pairing it with quantitative calibration and sampling evidence.

## Table 1 | p.6 | results

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 6, bbox `[55, 65, 555, 310]` PDF points, basis `rendered_observation`；placement `main`, width `page_width`.

- **职责**：main_comparison, robustness, failure。

- **结构**：21 data rows, 12 columns, 2 header levels, 3 row groups, rules `partial_grid`; decimal precision `None`.

- **Header/data**：Rows are grouped into proprietary, open-source and specialized models. Columns are Model; VR-IAA (ArtiMuse-10K, AVA, TAD66K, FLICKR-AES, Avg.); VR-IQA (KonIQ-10K, SPAQ, KADID, PIPAL, Avg.); and VR-ISTA (ISTA-10K). UniPercept reports IAA Avg. 0.590/0.586, IQA Avg. 0.824/0.827 and ISTA 0.778/0.767; per-dataset IAA is 0.746/0.738, 0.589/0.577, 0.336/0.346, 0.688/0.681 and IQA is 0.940/0.949, 0.904/0.895, 0.872/0.870, 0.581/0.594.

- **Typography**：family Nimbus Roman No9 L, Computer Modern math；body/header 6.0/7.0 pt；header weight `bold`；provenance `mixed`, confidence `high`.

- **Highlighting**：cell_color, text_color, best_second_best。
- **Uncertainty/failure**：SRCC/PLCC cells are point estimates across four IAA datasets, four IQA datasets and ISTA-10K. No repeats, confidence intervals or error bars are provided; “-/-” marks refusal or invalid output. The PDF contains 21 model rows including retrained variants and UniPercept, whereas the prose says 18 models.

- **Caption (PDF exact)**：
```text
Table 1. Performance comparison of different models on UniPercept-Bench-VR. The best results are highlighted with dark blue cells, and the second-best results with light blue cells. Models with * indicate those retrained on ArtiMuse-10K, KonIQ-10K, and ISTA-10K. “-/-” denotes cases where the model refuse to respond or fail to provide valid answers due to functional limitations. Metrics: SRCC/PLCC.
```
  - word_count=57; moves=title, setup, encoding_key, comparison, uncertainty_definition; headline_bold=True; self_contained=True; main_finding_stated=False.

- **Evidence relation**：Provides the primary VR comparison after the evaluation protocol and feeds the aggregate in Figure 7 and the reward baseline in Table 5.

- **Strengths**：
  - Grouped headers keep three domains and their datasets in one comparison surface.
  - Dark/light blue explicitly distinguish best and second-best cells.

- **Weaknesses**：
  - The 21 displayed model rows conflict with the stated total of 18.
  - Mixed failures and point estimates make cross-model ranking sensitive to missingness; no uncertainty is shown.

- **Reusable pattern**：Use two-level metric headers and explicit failure notation for a wide multi-domain benchmark table.

## Table 2 | p.7 | results

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 7, bbox `[55, 65, 555, 290]` PDF points, basis `rendered_observation`；placement `main`, width `page_width`.

- **职责**：main_comparison, dataset。

- **结构**：18 data rows, 16 columns, 2 header levels, 3 row groups, rules `partial_grid`; decimal precision `2`.

- **Header/data**：Columns are Model; IAA categories Comp., VisStr., Tech., Creat., Theme., Emo., Gest., CompEv.; QA templates Lv.Pred, How, What, Which, Why, Yes-No; Overall. UniPercept is 76.55% overall with category scores 80.00, 77.54, 69.70, 80.56, 79.26, 80.95, 67.53 and 69.77, and template scores 63.71, 92.20, 81.88, 75.32, 86.67 and 84.62%.

- **Typography**：family Nimbus Roman No9 L, Computer Modern math；body/header 6.5/7.5 pt；header weight `bold`；provenance `mixed`, confidence `high`.

- **Highlighting**：cell_color, bold, best_second_best。
- **Uncertainty/failure**：All entries are accuracy percentages and point estimates; random-guess row gives 23.08–32.56% by category/template and 25.17% overall. No uncertainty, class-weighting rule or repeat count is shown.

- **Caption (PDF exact)**：
```text
Table 2. Performance comparison of different models on UniPercept-Bench-VQA (IAA). Category names are abbreviated; full definitions are provided in the Appendix. Results follow the same notation throughout the paper.
```
  - word_count=29; moves=title, setup, abbreviation_definition; headline_bold=True; self_contained=True; main_finding_stated=False.

- **Evidence relation**：Extends Table 1 from continuous VR to IAA explanatory VQA and is summarized with IQA/ISTA in Figure 7; full category definitions are in Table 10.

- **Strengths**：
  - Two-level headers separate semantic category and QA-template axes.
  - The random-guess baseline makes percentage scale interpretable.

- **Weaknesses**：
  - Abbreviations require an appendix lookup and the wide row is hard to scan.
  - Category/template aggregation and class balance are unspecified.

- **Reusable pattern**：Keep category and question-template columns side by side so diagnostic accuracy patterns remain visible.

## Table 3 | p.8 | results

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 8, bbox `[55, 60, 410, 270]` PDF points, basis `rendered_observation`；placement `main`, width `page_width`.

- **职责**：main_comparison, dataset。

- **结构**：17 data rows, 11 columns, 2 header levels, 2 row groups, rules `partial_grid`; decimal precision `2`.

- **Header/data**：Columns are Model; IQA categories Loc., Sev., Type.; templates Lv.Pred, How, What, Which, Why, Yes-No; Overall. UniPercept scores 81.07% overall, 77.43/79.60/90.98% for the three categories, 79.60/87.03/80.86/75.60/83.42/79.31% for templates. The displayed rows include random guess, five proprietary models, ten open-source models and UniPercept.

- **Typography**：family Nimbus Roman No9 L, Computer Modern math；body/header 6.5/7.5 pt；header weight `bold`；provenance `mixed`, confidence `high`.

- **Highlighting**：cell_color, bold, best_second_best。
- **Uncertainty/failure**：Accuracy percentages are point estimates with random-guess overall 23.16%; no confidence intervals, repeats or category weighting are shown.

- **Caption (PDF exact)**：
```text
Table 3. Performance comparison of different models on UniPercept-Bench-VQA (IQA).
```
  - word_count=10; moves=title; headline_bold=True; self_contained=False; main_finding_stated=False.

- **Evidence relation**：Supplies the IQA VQA slice referenced by Figure 7 and pairs with the IQA definition table and the VR IQA columns in Table 1.

- **Strengths**：
  - Compact category set leaves room for template diagnostics.
  - The table reports both overall and decomposed accuracy in one place.

- **Weaknesses**：
  - The IQA table has a different row composition from Table 2 and no specialized-model row.
  - No denominator or per-class uncertainty accompanies percentages.

- **Reusable pattern**：Use a consistent model row order across domain-specific VQA tables and reserve columns for template failure modes.

## Table 4 | p.8 | results

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 8, bbox `[55, 300, 555, 515]` PDF points, basis `rendered_observation`；placement `main`, width `page_width`.

- **职责**：main_comparison, dataset。

- **结构**：17 data rows, 13 columns, 2 header levels, 2 row groups, rules `partial_grid`; decimal precision `2`.

- **Header/data**：Columns are Model; ISTA categories Scene., Phys., Mat., Geo., Sem.; templates How, What, Which, Why, Yes-No; Overall. UniPercept is 84.23% overall, with category scores 89.74, 85.71, 82.44, 93.94 and 78.51%, and template scores 82.69, 89.24, 78.54, 83.12 and 85.51%.

- **Typography**：family Nimbus Roman No9 L, Computer Modern math；body/header 6.5/7.5 pt；header weight `bold`；provenance `mixed`, confidence `high`.

- **Highlighting**：cell_color, bold, best_second_best。
- **Uncertainty/failure**：Accuracy percentages are point estimates with random-guess overall 26.60%; no uncertainty, repeats or class-weighting rule are shown.

- **Caption (PDF exact)**：
```text
Table 4. Performance comparison of different models on UniPercept-Bench-VQA (ISTA).
```
  - word_count=10; moves=title; headline_bold=True; self_contained=False; main_finding_stated=False.

- **Evidence relation**：Completes the three-domain VQA comparison after Tables 2–3 and feeds Figure 7’s cross-domain summary; definitions are in Table 12.

- **Strengths**：
  - The five-category decomposition exposes geometric and semantic strengths.
  - The same percentage/highlight grammar as Table 2 makes domain comparison possible.

- **Weaknesses**：
  - The wide header and dense rows reduce legibility.
  - Accuracy is not accompanied by class counts or uncertainty.

- **Reusable pattern**：Repeat one metric grammar across domains while allowing each domain its own criterion columns.

## Table 5 | p.9 | results

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 9, bbox `[55, 60, 555, 220]` PDF points, basis `rendered_observation`；placement `main`, width `page_width`.

- **职责**：main_comparison, mechanism, qualitative_evidence。

- **结构**：5 data rows, 9 columns, 2 header levels, 0 row groups, rules `booktabs`; decimal precision `2`.

- **Header/data**：Columns group Preference Score (PickScore, HPSv3), Image Quality (DeQA), Image Aesthetics (LAION-Aes, ArtiMuse) and UniPercept Score (IAA, IQA, ISTA). Baseline is 22.46/10.71/4.32/5.77/59.02 and 65.18/73.59/46.64 on the grouped metrics; Reward (All) is 22.67/10.93/4.33/6.19/65.52 and 74.24/77.04/59.08. Single rewards exceed All on some dimensions, such as IAA-only IAA 76.20 and ISTA-only ISTA 59.61.

- **Typography**：family Nimbus Roman No9 L, Computer Modern math；body/header 7.0/8.0 pt；header weight `bold`；provenance `mixed`, confidence `high`.

- **Highlighting**：cell_color, bold, best_second_best。
- **Uncertainty/failure**：All cells are point metrics for FLUX.1-dev reward variants. There are no repeats, error bars, confidence intervals or composite-objective definition.

- **Caption (PDF exact)**：
```text
Table 5. More Performance Results of FLUX.1-dev w/ UniPercept Reward.
```
  - word_count=11; moves=title; headline_bold=True; self_contained=False; main_finding_stated=False.

- **Evidence relation**：Quantifies the reward mechanism introduced in Figure 6 and supplies exact counterparts for the qualitative grid in Figure 8; the per-dimension trade-off should temper the caption’s “best overall” wording.

- **Strengths**：
  - Grouped headers put external preference, quality, aesthetics and proposed metrics on one row.
  - Fixed baseline and reward variants make direction of change easy to inspect.

- **Weaknesses**：
  - The combined reward has no declared composite utility, so “overall” is not reproducible.
  - Point estimates hide variance and the table mixes metric scales without units.

- **Reusable pattern**：Place baseline, single-dimension interventions and combined intervention in fixed rows with grouped outcome headers.

## Table 6 | p.10 | results

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 10, bbox `[55, 60, 555, 260]` PDF points, basis `rendered_observation`；placement `main`, width `page_width`.

- **职责**：main_comparison, robustness。

- **结构**：8 data rows, 11 columns, 2 header levels, 0 row groups, rules `booktabs`; decimal precision `2`.

- **Header/data**：Columns group DPG Global, Entity, Attribute, Relation, Other, Overall and UniPercept IAA, IQA, ISTA, Avg. Eight T2I models are listed. UniPercept Avg. values in row order are 60.04, 57.90, 58.83, 41.87, 58.99, 61.80, 60.00 and 60.76; the proposed metrics expose lower IAA/ISTA for several models.

- **Typography**：family Nimbus Roman No9 L, Computer Modern math；body/header 6.5/7.5 pt；header weight `bold`；provenance `mixed`, confidence `high`.

- **Highlighting**：cell_color, bold, best_second_best。
- **Uncertainty/failure**：DPG and UniPercept values are point estimates at fixed 1024×1024 output for most models; GPT-Image-1 is a stated special case. No uncertainty or repeated generation protocol is reported.

- **Caption (PDF exact)**：
```text
Table 6. Evaluation of T2I Models on DPG [16] Metrics and UniPercept Metrics.
```
  - word_count=13; moves=title, abbreviation_definition; headline_bold=True; self_contained=True; main_finding_stated=False.

- **Evidence relation**：Extends the reward experiment from FLUX.1-dev to cross-model diagnostics and is paired with Table 7’s GenEval comparison and Table 8’s dataset profiles.

- **Strengths**：
  - Two metric families are visibly grouped and best cells are highlighted.
  - The fixed output-resolution note in surrounding prose provides an important comparability boundary.

- **Weaknesses**：
  - Metric scales and semantics differ between DPG and UniPercept.
  - The table does not report generation seeds, sample counts or uncertainty.

- **Reusable pattern**：Use a shared model order and grouped external/proposed metric columns for application-level diagnostics.

## Table 7 | p.10 | results

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 10, bbox `[55, 270, 555, 470]` PDF points, basis `rendered_observation`；placement `main`, width `page_width`.

- **职责**：main_comparison, robustness。

- **结构**：8 data rows, 12 columns, 2 header levels, 0 row groups, rules `booktabs`; decimal precision `2`.

- **Header/data**：Columns group GenEval Single Obj., Two Obj., Counting, Colors, Position, Attr. Bind., Overall and UniPercept IAA, IQA, ISTA, Avg. Eight T2I models are listed. UniPercept Avg. values are 58.49, 54.61, 56.09, 33.56, 51.59, 60.11, 65.69 and 53.53 in row order.

- **Typography**：family Nimbus Roman No9 L, Computer Modern math；body/header 6.5/7.5 pt；header weight `bold`；provenance `mixed`, confidence `high`.

- **Highlighting**：cell_color, bold, best_second_best。
- **Uncertainty/failure**：GenEval and UniPercept values are point estimates with no uncertainty, repeats or sampling details; most outputs use 1024×1024 except the documented GPT-Image-1 case.

- **Caption (PDF exact)**：
```text
Table 7. Evaluation of T2I Models on GenEval [12] Metrics and UniPercept Metrics.
```
  - word_count=13; moves=title, abbreviation_definition; headline_bold=True; self_contained=True; main_finding_stated=False.

- **Evidence relation**：Provides a second external alignment axis for the same UniPercept diagnostic claim and complements Table 6.

- **Strengths**：
  - The seven GenEval dimensions remain visible beside the four perceptual metrics.
  - The matched model order enables row-wise inspection.

- **Weaknesses**：
  - The wide table is dense and the differing metric ranges invite over-reading of rank.
  - No correlation or calibration analysis links the two metric families.

- **Reusable pattern**：Pair external instruction-following metrics with explicitly labeled perceptual metrics, while preserving their separate scales.

## Table 8 | p.10 | results

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 10, bbox `[350, 485, 575, 610]` PDF points, basis `rendered_observation`；placement `main`, width `single_column`.

- **职责**：dataset, main_comparison。

- **结构**：6 data rows, 5 columns, 1 header levels, 2 row groups, rules `booktabs`; decimal precision `2`.

- **Header/data**：Columns are Dataset, UniPercept-IAA, UniPercept-IQA, UniPercept-ISTA and Avg. Natural-image rows are ImageNet (53.88,61.90,36.79,50.85), Unsplash (62.49,69.19,43.32,58.33), DF2K (45.99,52.92,34.78,44.56), LAION-5B (60.56,69.21,38.85,56.21); AIGC rows are Blip3o-60K (63.81,73.88,49.38,62.36) and ImgEdit (55.83,59.77,36.88,50.83).

- **Typography**：family Nimbus Roman No9 L, Computer Modern math；body/header 7.0/8.0 pt；header weight `bold`；provenance `mixed`, confidence `high`.

- **Highlighting**：bold。
- **Uncertainty/failure**：Each row is a point average of three UniPercept scores; dataset sample sizes and sampling rules are not reported, and no uncertainty is shown.

- **Caption (PDF exact)**：
```text
Table 8. UniPercept Metrics on Various Datasets.
```
  - word_count=7; moves=title; headline_bold=True; self_contained=False; main_finding_stated=False.

- **Evidence relation**：Shows the proposed metric as a dataset-level diagnostic after Tables 6–7; the surrounding text highlights Unsplash and Blip3o-60K, while the appendix gives training-source tables.

- **Strengths**：
  - Natural/AIGC row groups make the intended distributional comparison explicit.
  - The average is colocated with the three component scores.

- **Weaknesses**：
  - Average ranking lacks dataset size and sampling context.
  - The compact inset is readable only after zooming.

- **Reusable pattern**：Keep component metrics and their average in one short dataset-profile table, with explicit source and sample metadata.

## Table 9 | p.18 | appendix

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 18, bbox `[325, 330, 575, 575]` PDF points, basis `rendered_observation`；placement `appendix`, width `single_column`.

- **职责**：method_interface, dataset。

- **结构**：3 data rows, 2 columns, 1 header levels, 0 row groups, rules `booktabs`; decimal precision `None`.

- **Header/data**：Rows are Weak (1), Medium (2) and Strong (3), each with texture-term lists. Weak terms include smooth/plain/uniform and regular patterns; Medium includes braided/woven/crosshatched/meshed and related terms; Strong includes bumpy/blotchy/bubbly/cracked through veined/wrinkled/zigzagged.

- **Typography**：family Nimbus Roman No9 L, Computer Modern math；body/header 7.0/8.0 pt；header weight `bold`；provenance `mixed`, confidence `high`.

- **Highlighting**：bold。
- **Uncertainty/failure**：Weights are deterministic category labels 1, 2 and 3; there is no sampling uncertainty because the table defines the lexicon rather than estimates it.

- **Caption (PDF exact)**：
```text
Table 9. Texture categories used for texture intensity mapping. Higher groups indicate richer and more complex morphology patterns.
```
  - word_count=18; moves=title, setup; headline_bold=True; self_contained=True; main_finding_stated=False.

- **Evidence relation**：Defines the texture-intensity mapping used by the ISTA equations and structured annotation in Figure 9, feeding Table 12’s criterion system.

- **Strengths**：
  - The three ordinal groups are immediately readable.
  - Long term lists make the lexical mapping concrete.

- **Weaknesses**：
  - The ordered weights are a design convention with no validation or annotator agreement.
  - The long Strong row is visually unbalanced against the short Weak row.

- **Reusable pattern**：Publish the controlled vocabulary and its ordinal weights next to the scoring formula.

## Table 10 | p.20 | appendix

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 20, bbox `[55, 50, 555, 690]` PDF points, basis `rendered_observation`；placement `appendix`, width `page_width`.

- **职责**：dataset, method_interface。

- **结构**：31 data rows, 4 columns, 1 header levels, 8 row groups, rules `booktabs`; decimal precision `None`.

- **Header/data**：Columns are No., Category, Criterion and Description. The 31 IAA criteria are grouped across Composition & Design, Visual Elements & Structure, Technical Execution, Originality & Creativity, Theme & Communication, Emotion & Viewer Response, Overall Gestalt and Comprehensive Evaluation; the category groups contain 6, 6, 5, 3, 4, 4, 2 and 1 rows respectively.

- **Typography**：family Nimbus Roman No9 L, Computer Modern math；body/header 6.5/7.5 pt；header weight `bold`；provenance `mixed`, confidence `high`.

- **Highlighting**：bold。
- **Uncertainty/failure**：The table is a definition inventory, not a statistical estimate; descriptions have no uncertainty, count denominator beyond the 31 listed criteria, or annotator agreement.

- **Caption (PDF exact)**：
```text
Table 10. Definition details of IAA domain.
```
  - word_count=7; moves=title; headline_bold=True; self_contained=False; main_finding_stated=False.

- **Evidence relation**：Provides the complete IAA taxonomy behind Figure 3 and Table 2, and supplies the appendix pointer named in Table 2’s caption.

- **Strengths**：
  - Numbering and category group labels make the hierarchy auditable.
  - Descriptions are colocated with each criterion rather than deferred to prose.

- **Weaknesses**：
  - Thirty-one dense rows require small type and wrap long descriptions.
  - The table documents definitions but does not show label prevalence or agreement.

- **Reusable pattern**：Use one row per criterion with category group rules to make a hierarchical annotation ontology implementable.

## Table 11 | p.21 | appendix

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 21, bbox `[55, 50, 555, 275]` PDF points, basis `rendered_observation`；placement `appendix`, width `page_width`.

- **职责**：dataset, method_interface。

- **结构**：4 data rows, 4 columns, 1 header levels, 3 row groups, rules `booktabs`; decimal precision `None`.

- **Header/data**：Columns are No., Category, Criterion and Description. Rows cover two Distortion Location criteria (Location Description and Object Association), Distortion Severity (Severity Level) and Distortion Type (Distortion Types Present).

- **Typography**：family Nimbus Roman No9 L, Computer Modern math；body/header 7.0/8.0 pt；header weight `bold`；provenance `mixed`, confidence `high`.

- **Highlighting**：bold。
- **Uncertainty/failure**：The four IQA criterion definitions are deterministic descriptions; no uncertainty or validation statistic is included.

- **Caption (PDF exact)**：
```text
Table 11. Definition details of IQA domain.
```
  - word_count=7; moves=title; headline_bold=True; self_contained=False; main_finding_stated=False.

- **Evidence relation**：Completes the IQA ontology used by Figure 3 and Table 3, making category abbreviations and question scopes explicit.

- **Strengths**：
  - Short table supports quick lookup from abbreviated Table 3 headers.
  - Descriptions specify concrete region/object constraints for location questions.

- **Weaknesses**：
  - The table cannot reveal class frequency or difficulty.
  - Long descriptions wrap within a wide float and the caption is not self-contained.

- **Reusable pattern**：Define each metric category with an operational criterion and concrete question scope.

## Table 12 | p.21 | appendix

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 21, bbox `[55, 300, 555, 650]` PDF points, basis `rendered_observation`；placement `appendix`, width `page_width`.

- **职责**：dataset, method_interface。

- **结构**：9 data rows, 4 columns, 1 header levels, 4 row groups, rules `booktabs`; decimal precision `None`.

- **Header/data**：Columns are No., Category, Criterion and Description. Rows cover Scene Decomposition Principles (one), Physical Structure (two), Material Representation (two), Geometric Composition (two) and Semantic Perception (two).

- **Typography**：family Nimbus Roman No9 L, Computer Modern math；body/header 6.5/7.5 pt；header weight `bold`；provenance `mixed`, confidence `high`.

- **Highlighting**：bold。
- **Uncertainty/failure**：The nine ISTA criterion definitions are descriptive and deterministic; no uncertainty, prevalence or inter-rater validation is reported.

- **Caption (PDF exact)**：
```text
Table 12. Definition details of ISTA domain.
```
  - word_count=7; moves=title; headline_bold=True; self_contained=False; main_finding_stated=False.

- **Evidence relation**：Completes the ISTA ontology used by Figure 3, Figure 9 and Table 4, and clarifies the five abbreviated Table 4 categories.

- **Strengths**：
  - Category grouping mirrors the structured JSON fields.
  - Descriptions tie visual concepts to controlled classifications and forms.

- **Weaknesses**：
  - Several criterion labels are semantically close and require cross-reading of prose.
  - The table records the ontology but not annotation ambiguity or agreement.

- **Reusable pattern**：Align ontology table groups with the keys of the structured output so data and evaluation use the same vocabulary.

## Table 13 | p.22 | appendix

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 22, bbox `[55, 50, 555, 300]` PDF points, basis `rendered_observation`；placement `appendix`, width `page_width`.

- **职责**：dataset, experimental_design, main_comparison。

- **结构**：13 data rows, 11 columns, 2 header levels, 3 row groups, rules `partial_grid`; decimal precision `None`.

- **Header/data**：Two-level headers group Perceptual-Level Domain (IAA, IQA, ISTA) and Task (VQA, VR). Other columns are Benchmark, # Test, # QA Category, Data Format, Annotation Level and Annotator. Six VQA benchmarks, six VR datasets and UniPercept-Bench are listed; UniPercept reports ~6K tests, 44 QA categories, Rating & Text, Example-Level, Human & MLLM, all three domains and both tasks.

- **Typography**：family Nimbus Roman No9 L, Computer Modern math；body/header 6.5/7.5 pt；header weight `bold`；provenance `mixed`, confidence `high`.

- **Highlighting**：bold。
- **Uncertainty/failure**：Counts are approximate test sizes or dashes for unreported values; no uncertainty is applicable. Domain/task checkmarks are categorical coverage indicators.

- **Caption (PDF exact)**：
```text
Table 13. Comparison with existing benchmarks & datasets.
```
  - word_count=7; moves=title; headline_bold=True; self_contained=False; main_finding_stated=False.

- **Evidence relation**：Supports the appendix claim of broader benchmark coverage and contrasts the construction choices introduced in Figures 1, 3 and 4.

- **Strengths**：
  - Multi-level headers place coverage, format and annotation provenance together.
  - Checkmarks let readers inspect domain/task gaps quickly.

- **Weaknesses**：
  - The comparison includes selected prior resources only and does not define a completeness criterion.
  - Many dashes and approximate sizes limit numeric comparability.

- **Reusable pattern**：Use an explicit coverage matrix to compare benchmark scope, annotation level and task availability.

## Table 14 | p.22 | appendix

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 22, bbox `[345, 300, 575, 650]` PDF points, basis `rendered_observation`；placement `appendix`, width `single_column`.

- **职责**：dataset, reproduction。

- **结构**：6 data rows, 4 columns, 1 header levels, 3 row groups, rules `partial_grid`; decimal precision `None`.

- **Header/data**：Columns are Domain, Type, Size and Source. IAA has Text ~360K from APDDv2, Impressions, AVA, TAD66K and FLICKR-AES plus Rating ~9K from ArtiMuse-10K; IQA has Text ~380K and Rating ~7K; ISTA has Text ~40K and Structure ~40K from the listed texture/scene datasets.

- **Typography**：family Nimbus Roman No9 L, Computer Modern math；body/header 6.2/7.2 pt；header weight `bold`；provenance `mixed`, confidence `high`.

- **Highlighting**：bold。
- **Uncertainty/failure**：Sizes are approximate (about 360K/9K/380K/7K/40K/40K) and sources are lists, not uncertainty intervals. The table reports composition, not independently audited counts.

- **Caption (PDF exact)**：
```text
Table 14. Data Overview for Domain-Adaptive Pre-Training.
```
  - word_count=7; moves=title; headline_bold=True; self_contained=False; main_finding_stated=False.

- **Evidence relation**：Makes the approximately 800K DAPT total in the main text reproducible and connects source composition to Figure 6’s DAPT stage.

- **Strengths**：
  - Grouped domains and type rows expose the data recipe compactly.
  - Source lists preserve dataset provenance beside each size.

- **Weaknesses**：
  - Approximate counts and “among others” wording prevent exact reconstruction.
  - The narrow inset and long source lists are hard to scan.

- **Reusable pattern**：Report domain, data type, approximate size and source in one table when a training stage aggregates heterogeneous corpora.

## Table 15 | p.23 | appendix

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 23, bbox `[55, 50, 330, 230]` PDF points, basis `rendered_observation`；placement `appendix`, width `single_column`.

- **职责**：dataset, reproduction。

- **结构**：6 data rows, 4 columns, 1 header levels, 3 row groups, rules `partial_grid`; decimal precision `None`.

- **Header/data**：Columns are Domain, Type, Size and Source. Each of IAA, IQA and ISTA has a VR row from ArtiMuse-10K, KonIQ-10K or ISTA-10K and a VQA row from UniPercept Data-VQA (train).

- **Typography**：family Nimbus Roman No9 L, Computer Modern math；body/header 6.5/7.5 pt；header weight `bold`；provenance `mixed`, confidence `high`.

- **Highlighting**：bold。
- **Uncertainty/failure**：VR/VQA sizes are approximate; IAA ~9K/~10K, IQA ~7K/~10K and ISTA ~10K/~10K. No split, repeat or uncertainty information is supplied.

- **Caption (PDF exact)**：
```text
Table 15. Data Overview for Task-Aligned RL.
```
  - word_count=7; moves=title; headline_bold=True; self_contained=False; main_finding_stated=False.

- **Evidence relation**：Details the RL data branch in Figure 6 and gives the dataset composition used for ablations Tables 16–17.

- **Strengths**：
  - The matched VR/VQA rows make task mixing explicit.
  - Domain grouping mirrors the main benchmark taxonomy.

- **Weaknesses**：
  - “~” counts and a shared VQA source omit split and filtering details.
  - Inset placement beside prose reduces available width for source names.

- **Reusable pattern**：Mirror the training-stage table across domains and tasks so the data mixture is inspectable.

## Table 16 | p.23 | ablation

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 23, bbox `[345, 250, 575, 455]` PDF points, basis `rendered_observation`；placement `appendix`, width `single_column`.

- **职责**：ablation, main_comparison。

- **结构**：8 data rows, 6 columns, 2 header levels, 3 row groups, rules `partial_grid`; decimal precision `None`.

- **Header/data**：Rows are grouped into Training Strategy (Threshold Reward; w/o Adaptive Pre-Training), Training Tasks (VQA-Only; VR-Only) and Training Domains (IAA-Only; IQA-Only; ISTA-Only), followed by UniPercept. Notable cells include adaptive/ours IQA Avg. 0.824/0.827 and ISTA 0.778/0.767 versus threshold 0.801/0.790 and 0.303/0.334; threshold IAA Avg. 0.617/0.596 exceeds the full model’s 0.590/0.586.

- **Typography**：family Nimbus Roman No9 L, Computer Modern math；body/header 6.4/7.2 pt；header weight `bold`；provenance `mixed`, confidence `high`.

- **Highlighting**：cell_color, text_color, best_second_best。
- **Uncertainty/failure**：SRCC/PLCC point estimates are shown for IAA ArtiMuse-10K and Avg., IQA KonIQ-10K and Avg., and ISTA-10K. No repeats, uncertainty or tests are reported. The table contains threshold/adaptive, DAPT, task and domain ablations.

- **Caption (PDF exact)**：
```text
Table 16. Ablation studies on UniPercept-Bench-VR. The best results are highlighted with dark blue cells, and the second-best results with light blue cells. Metrics: SRCC/PLCC.
```
  - word_count=26; moves=title, setup, encoding_key, uncertainty_definition; headline_bold=True; self_contained=True; main_finding_stated=False.

- **Evidence relation**：Tests the DAPT/RL mechanism in Figure 6 after the main results. It is the primary evidence for joint-task and multi-domain claims, while exposing counter-cells that the prose does not discuss.

- **Strengths**：
  - Grouped ablation rows map directly to training design choices.
  - Dark/light blue best/second highlights permit quick within-column comparison.

- **Weaknesses**：
  - Ablation factors are not fully factorial and there is no seed variance.
  - The threshold row’s higher IAA average and task-specific counter-cells complicate the “consistently outperforms” narrative.

- **Reusable pattern**：Keep one ablation table organized by training strategy, task mixture and domain mixture, with the full model as an explicit anchor row.

## Table 17 | p.23 | ablation

- **对象证据**：`corpus/preprints/icml-2026-4d6c3bd9ff61.pdf`, page 23, bbox `[345, 460, 575, 665]` PDF points, basis `rendered_observation`；placement `appendix`, width `single_column`.

- **职责**：ablation, main_comparison。

- **结构**：8 data rows, 5 columns, 1 header levels, 3 row groups, rules `partial_grid`; decimal precision `2`.

- **Header/data**：Rows and groups match Table 16; there are two Training Strategy rows, two Training Tasks rows, three Training Domains rows and UniPercept. UniPercept reports 76.55/81.07/84.23% and 80.62% Avg.; threshold reward reports 72.32/76.29/81.65% and 76.75%; removing DAPT reports 69.16/75.09/80.00% and 74.75%.

- **Typography**：family Nimbus Roman No9 L, Computer Modern math；body/header 6.8/7.5 pt；header weight `bold`；provenance `mixed`, confidence `high`.

- **Highlighting**：cell_color, bold, best_second_best。
- **Uncertainty/failure**：Accuracy percentages are point estimates for IAA, IQA, ISTA and Avg.; no repeat, uncertainty or significance test is reported.

- **Caption (PDF exact)**：
```text
Table 17. Ablation studies on UniPercept-Bench-VQA.
```
  - word_count=6; moves=title; headline_bold=True; self_contained=False; main_finding_stated=False.

- **Evidence relation**：Provides the VQA counterpart to Table 16 and closes the joint VR/VQA training argument, while remaining supplementary rather than main-body evidence.

- **Strengths**：
  - The same ablation grouping makes the VR and VQA tables directly comparable.
  - The average column gives a compact summary after domain scores.

- **Weaknesses**：
  - The caption does not define the average or task/sample weighting.
  - The table is outside the main results, so the central training claims require appendix lookup.

- **Reusable pattern**：Repeat ablation factors across outcome tables and retain a full-model anchor plus an explicit average.

## Cross-object system

- **visual_narrative**：主线从 Figure 1 的三域 taxonomy 与 VR/VQA 接口开始，经 Figures 3–5 的 QA/构建/分布，Figure 6 的 DAPT+RL 机制，Tables 1–4 的逐域基准比较，Figure 7 的摘要，再到 Table 5/Figure 8 的 T2I reward 应用和 Tables 6–8 的诊断扩展。附录 Figures 9–14、Tables 9–17 将结构 schema、定义、数据配方、域关系和消融补齐。

- **caption_system**：caption 统一采用“Figure/Table N.”标题格式并置于对象下方。Figure 1 和 Figure 8 在 caption 中写出主发现；大多数方法图只给设置与接口，表格 caption 多数只给比较对象和高亮规则。Figure 4 保留 PDF 原文拼写/语法错误 “Constuction” 与 “A three-stage process initial…”。

- **table_header_system**：主结果表使用两级分组表头，模型在左、域/任务或外部指标在右；附录定义表采用 No.–Category–Criterion–Description，数据表采用 Domain–Type–Size–Source。主比较表保留百分比、SRCC/PLCC、平均值和失败标记，但没有统一 uncertainty 或分母表头。

- **method_result_ablation_link**：Figure 6 的 DAPT、VR/VQA reward 和三域设计由 Tables 1–4、5 及 Tables 16–17 检验；Table 16 的 threshold IAA Avg. 反例和 Table 5 的 single-reward dimension trade-off 说明方法叙事需要按指标逐项回收。

- **main_appendix_link**：正文 Figures 1–8 与 Tables 1–8 建立 taxonomy、评测、reward 和应用主线；附录 Figure 9/Tables 9–12 定义结构词典，Tables 13–15 给比较与数据配方，Figure 10–14 和 Tables 16–17 提供域关系、profiles 与消融。主文 caption 对附录定义有指针，但消融只在附录出现。

- **typography_consistency**：Nimbus Roman/Computer Modern 构成表格与正文主字体，图内另嵌 Georgia、Calibri、Arial Rounded、Nimbus Mono 等，表格字号约 6–8 pt，图内卡片和 profile 小字常接近 5.5–7 pt。层级和粗体稳定，跨对象字族不完全一致。

- **color_consistency**：粉/蓝/紫始终分别指向 IAA/IQA/ISTA，深/浅蓝始终表示表格 best/second-best；红色多用于 reward/IAA，紫色用于 ISTA。该映射跨对象稳定，但自然图像、截图和多色 radial 图会引入额外颜色，且没有统一色盲或灰度验证。

## Final judgment

- **most_reusable_patterns**：
  - 用 Domain–Category–Criterion 三层 taxonomy 同时组织示意图、问题模板、表头和结构 JSON。
  - 让定性图保持固定行列或 profile 卡片几何，将每个 tile 的 prompt、criterion、template 或 score 放在对象旁边。
  - 宽表采用两级分组表头，把逐域指标、QA 模板、平均值和失败标记放入同一决策面，并用深/浅蓝区分 best/second-best。
  - 以 Figure 6 的阶段、输入、输出和 reward 分支把训练机制与 Tables 1–4、5、16–17 的证据相连。

- **highest_value_objects**：
  - Figure 1：最有效的范围与接口总览。
  - Figure 6：把 DAPT、VR/VQA 和 reward 机制放在同一可读接口。
  - Tables 1–4：逐域、逐模板的精确主比较。
  - Table 5 + Figure 8：将 reward 的数值 trade-off 与生成结果并置。

- **failure_patterns**：
  - 大量小字卡片、截图和 profile 文本在双栏页面上可读性不足。
  - 多数结果只有点估计，没有重复、误差、分母或显著性；表格高亮容易被读作稳健排序。
  - Figure 7 的 radial 摘要牺牲精确可比性；Figure 5 混合 donut、bar 和 count/share 但没有统一尺度。
  - caption/正文的“best overall”与 Table 5 的逐维 trade-off、Table 16 的 IAA 反例没有完全对齐。
  - GitHub 只有部分导出视觉资产，没有可复现绘图脚本、TeX/TikZ 或参数文件。

- **one_sentence_visual_strategy**：以粉/蓝/紫三域 taxonomy 为视觉骨架，用操作化 QA/训练 pipeline 接上逐域宽表，再用固定 profile 与 T2I qualitative grid 展示多维感知的应用价值。


## Verification

- JSON must validate against `schemas/visual-audit.schema.json`; object counts must be 14 figures and 17 tables. This Markdown is written first; JSON is emitted atomically only after Markdown review.
