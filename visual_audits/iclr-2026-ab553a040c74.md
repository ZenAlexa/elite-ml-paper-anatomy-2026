# Visual audit — `iclr-2026-ab553a040c74`

## 审计边界与对象清单

- **论文**：Matthew Finlayson、Xiang Ren、Swabha Swayamdipta，*Every Language Model Has a Forgery-Resistant Signature*。
- **PDF 事实源**：`corpus/pdfs/iclr-2026-ab553a040c74.pdf`；17 个物理页，PDF 版本 1.7，页面尺寸 612 × 792 pt。
- **渲染与检查**：使用 `pdftoppm -r 220` 渲染 p.1–p.17（每页 1870 × 2420 px）。先用全页 contact sheet 核对页级对象，再以原始分辨率检查含对象的 p.2–p.4、p.6–p.9、p.13–p.17。`pdfimages -list` 只有表头、没有嵌入位图；图形、文字和表格按 PDF 观察均为矢量对象。
- **PDF 对象清单**：Figure 1–7（p.2–p.9）、Table 1（p.7）、Table 2（p.13）、Figure 8–11（p.14–p.17）均逐一在页面中出现，共 11 Figures、2 Tables。p.6 的 Algorithm 1 与 p.16 的 Algorithm 2 是伪代码对象，按 schema 不放入 `figures`/`tables`，在此 Markdown 单独记录。p.1、p.5、p.10–p.12 没有 Figure/Table；p.10–p.12 为致谢和参考文献页。
- **正文/附录边界**：正文 Figure 1–7、Table 1；附录 B 的 Table 2，附录 C 的 Figure 8，附录 E 的 Figure 9–10，附录 G 的 Figure 11。附录 A、D、F、H 提供文字和公式支撑，但没有额外 Figure/Table。

|对象|物理页|模块|版面位置与宽度|
|---|---:|---|---|
|Figure 1|2|§1 Introduction|页面宽度，上部居中|
|Figure 2|3|§2.1 normalization-to-ellipse geometry|页面宽度，上部居中|
|Figure 3|4|§2.2 cross-model identification|页面宽度，上部居中|
|Figure 4|6|§3.1 smoothing/fitting ablation|页面宽度，上半部|
|Figure 5|7|§3.1 sample-count ablation|页面宽度，上半部|
|Table 1|7|§3.2 sample cost|居中窄表（inset）|
|Figure 6|8|§3.3 fitting runtime|页面宽度，上部|
|Figure 7|9|§4 verification protocol|页面宽度，上部|
|Table 2|13|Appendix B comparison|页面宽度，上部|
|Figure 8|14|Appendix C reparameterization|页面宽度，上部|
|Figure 9|15|Appendix E layer norm geometry|页面宽度，上部|
|Figure 10|16|Appendix E layer norm recovery|页面宽度，上部|
|Figure 11|17|Appendix G smoothing severity|页面宽度，上部|

## 来源核查

`reports/tables/visual_source_inventory.csv` 的目标行是 `no_public_source_found`，自动候选为 `negrinho/sane_tikz`；`corpus/visual_sources/iclr-2026-ab553a040c74/` 不存在。PDF 正文没有作者代码仓库链接；参考文献 p.12 只包含 OpenReview/论文链接和 `https://github.com/negrinho/sane_tikz`，Figure 2 caption 明确写有 “Transformer diagram credit Vaswani et al. (2017); Negrinho (2020)”。

按论文标题、作者和方法名执行 `gh search repos` 的严格检索没有得到论文专属仓库。随后只读查看了 `https://github.com/negrinho/sane_tikz`（master 当前提交 `fd6f291d9815613594d724678cb91ac9d412fbb7`）。其 README 与 `examples/transformer.py`、`examples/transformer.tex` 是通用 Sane TikZ Transformer 示例；Figure 2 左侧的 Transformer tower、节点、配色与这些文件直接相符，且论文 caption 给出同一作者/工具信用，因此可作为 Figure 2 左侧子图的部分视觉源。该仓库没有本文 Figure 2 右侧最终层示意、Figure 1/3–11 的绘图代码、表格生成器或本文 LaTeX 工程。来源状态为 **partial_visual_source**；只把下列两个 Transformer 示例记录为 `diagram_source`，不把它们扩展为整篇论文的 source-exact 代码。

- `examples/transformer.py`：Sane TikZ Python builder；其中定义了 `emb_color=(252,224,225)`、`multi_head_attention_color=(252,226,187)`、`add_norm_color=(242,243,193)`、`ff_color=(194,232,247)`、`softmax_color=(203,231,207)`、`linear_color=(220,223,240)` 和 `gray_bbox_color=(243,243,244)`，与 PDF Figure 2 左侧渲染色一致。
- `examples/transformer.tex`：由上述 builder 导出的 TikZ；包含左侧 tower 的节点、残差箭头、`N×` 标签和 0.046875 cm 线宽。
- `README.md`：说明仓库是通用 TikZ 工具并链接 transformer 示例；没有本文标题、作者列表或 OpenReview ID，因此仅作为仓库性质证据。

## Figure 审计

### Figure 1（p.2，Introduction）

**Caption（逐字）**：

> Figure 1: Language model logits are subject to constraints that force them to lie on a high-dimensional ellipse. This constitutes a signature because we can identify which model generated an output by checking which ellipse it lies on. Among other unique properties, we show that ellipse signatures are forgery-resistant because it is computationally hard in practice to generate signed logits without access to the model parameters.

- **布局与几何**：页面宽度的单一概念图。左侧为 `Language model A` 与蓝色圆角 `LM`；中间为灰色椭圆 `Model A ellipse`；椭圆上用空心圆表示 A 的 logit outputs，旁侧用叉号表示来自 `Language model B` 的输出；右侧为粉色圆角 `LM` 与 `Language model B`。多条细灰色曲线箭头从两个模型汇入输出点。无坐标轴、面板、inset、legend 或网格；复杂度 2/5。
- **类型与 plot grammar**：`conceptual_diagram`。PDF 图形为 `vector`；x/y=`none`，grid=`none`；无 legend（`legend_present=false`、`shared_legend=null`），`direct_labels=true`；marker 类型 2（空心圆、叉号），线型 1（实线箭头），无 hatch、reference line 或 uncertainty；线宽约 0.9 pt；`provenance=rendered_estimate`。
- **Typography**：图内主标签为 TeX Gyre Termes X/数学斜体，约 9–14 pt，regular，`A/B` 为数学 italic；PDF 字体对象可由 `pdffonts` 确认，字号来自渲染估计，`provenance=pdf_object`、置信度 high。
- **颜色**：categorical，代表色 `#CCCCFF`（Model A/蓝色 LM）、`#FFCCCC`（Model B/粉色 LM）、`#666666`（椭圆、箭头和线稿）。颜色表达模型来源，位置、文字和 marker 提供冗余；灰度下两种浅色接近，`grayscale_safe=false`，`provenance=rendered_estimate`。
- **数据与编码**：x 表示从两个模型到输出空间的关系，y 仅承担概念布局；颜色/圆角框表示模型身份，形状表示“在椭圆上”或“偏离椭圆”，曲线箭头表示生成关系，文字是对象名。没有统计数据、聚合、不确定性或数值单位。
- **证据关系**：连接引言关于“logits 受高维椭圆约束”的主张、§2 的 normalization→ellipse 推导、§2.2 的 inverse-affine distance 与 Figure 3 识别实验，并预览 §3 的 forgery-resistance 论证。Caption 直接给出 source identification 与 forgery resistance 的 headline 结论。
- **优点**：把“模型—签名椭圆—输出点”压缩成一个可扫读的空间关系；空心圆/叉号与标签共同承担来源区分；没有引入无关统计装饰。
- **缺点**：高维椭圆被二维示意替代，维度和 distance 定义未在图内说明；浅蓝/浅粉在灰度打印中近似；caption 没有定义空心圆和叉号的具体编码。
- **可复用模式**：使用单个几何约束、同源点与异源点的对照，把抽象 signature 主张在引言中先变成可检查的视觉判据。

### Figure 2（p.3，§2.1）

**Caption（逐字）**：

> Figure 2: A typical language model’s final layers consist of normalization followed by a linear (or affine) transformation. Normalization has the effect of mapping the representations onto the surface of a sphere, while the linear layer stretches and rotates this sphere, resulting in an ellipse. Transformer diagram credit Vaswani et al. (2017); Negrinho (2020).

- **布局与几何**：页面宽度的左右双区复合示意图。左区是带 `N×`、输入/输出 embedding、positional encoding、masked/multi-head attention、feed-forward、Add & Norm、Linear、Softmax 的 Transformer tower；右区是纵向 `Input → Neural net → Norm → Linear → Softmax → Output probabilities`，并在 Norm/Linear 旁直接标注 “Maps to sphere surface”“Stretches & rotates”。无统计坐标轴或 legend；复杂度 4/5（两个逻辑区、十余个模块、残差箭头与层级嵌套）。
- **类型与 plot grammar**：`architecture`、`pipeline`、`conceptual_diagram`；PDF 对象为 `vector`，但图左区与公开 TikZ 源的关系只覆盖部分图，故 `provenance=mixed`；x/y=`none`，grid=`none`；无 legend，`direct_labels=true`；marker=0，line_styles=1，hatching=false，reference_lines=0，uncertainty=`none`；线宽约 1.33 pt（左区源文件的 0.046875 cm）和右区的相近线宽，`line_width_pt=1.33`。
- **Typography**：TeX Gyre Termes X 与 NewTX 数学字体，约 5–12 pt，regular/italic，模块名与箭头说明为 roman，`N×` 等为数学体；字体家族来自 PDF object，大小为渲染估计，`provenance=pdf_object`、置信度 high。
- **颜色**：categorical，代表色 `#FCE0E1`（embedding/neural-net）、`#FCE2BB`（multi-head attention/Add & Norm 的暖色）、`#F2F3C1`（Add & Norm）、`#C2E8F7`（Feed Forward）、`#CBE7CF`（Softmax）、`#DC DFF0`（Linear，规范化写作 `#DCDFF0`）、`#F3F3F4`（左侧 tower 背景），以及右区同色/浅色模块填充。左区 RGB 值由 `examples/transformer.py/.tex` 精确读取，右区颜色由 PDF 渲染核对；整体记录 `provenance=rendered_estimate`，`color_count=7`。颜色编码层类型，模块文字、垂直顺序与箭头提供冗余；灰度下黑色边界仍清楚但浅色类别会合并，`grayscale_safe=false`。
- **数据与编码**：x 是网络到输出的处理方向，y 是层级顺序；颜色是模块角色，圆角框是操作节点，箭头是数据流；右区是对左区末端的语义展开。无数据、统计估计或 uncertainty。
- **PDF 可见残留**：图左区和附近可见灰色 `000`–`017` 样式的序号文本，且未在 caption 或正文定义；本审计按 PDF 实际渲染记录为排版残留/调试标记，降低左区局部可读性。
- **证据关系**：方法入口图，把 Transformer 最终 normalization 的球面约束与线性层的 stretch/rotate 连接到 §2 的 ellipse 定义、Eq. (1) 与 Figure 3 的识别实验；caption 还明确给出外部 Transformer 图 credit。它是 `method_interface`/`theory_mechanism` 的核心对象。
- **优点**：右区把复杂 backbone 的末端步骤抽象成短流程，直接解释 sphere→ellipse；左区保留架构上下文，使末端操作不脱离 LM；颜色、箭头和模块名有多重编码。
- **缺点**：左区信息密度高，`000`–`017` 残留覆盖图形附近；caption 没有解释右区与左区的对应边界，也未说明 learned affine `γ, β` 在右区的省略方式。
- **可复用模式**：把完整架构缩略图与末端机制放在同一图中，通过第二个极简流程将统计/几何主张绑定到具体层序列；公开源可复用时保留颜色 token 和线宽 token。

### Figure 3（p.4，§2.2）

**Caption（逐字）**：

> Figure 3: Mean distance to the model ellipse for logprobs generated from several open-weight models, projected onto each other’s output spaces. Small distances indicate that the outputs are on the target model ellipse, and therefore came from the target model. Standard errors, shown as shaded regions, are mostly too narrow to see.

- **布局与几何**：单一折线图，五个 categorical target model 位于 x 轴：`Olmo 2`、`Olmo 2 (300)`、`Llama 3.1`、`Qwen 3`、`GPT OSS`。五个生成模型各为一条系列，图外右侧有 boxed legend。y 轴标题 `Avg. distance to ellipse`，刻度为 `10^-6`、`10^-2`、`10^2`，为 log 轴；x 为 categorical；浅色阴影围绕折线表示 standard error。复杂度 2/5，约 25 个点。
- **类型与 plot grammar**：`line`、`scatter`；`vector`；x=`categorical`、y=`log`、grid=`none`（仅有轻微浅灰轴内参考线，不形成显式网格）；legend 存在、位置 `right_outside`，为全图共享（`shared_legend=true`）；direct_labels=false；marker_types=5（填充圆、空心圆、三角、方形、菱形），line_styles=1，hatching=false，reference_lines=0，uncertainty=`band`；线宽约 0.8 pt；`provenance=rendered_estimate`。
- **Typography**：TeX Gyre Termes X/NewTX 数学字体，约 7–13 pt，regular/italic；legend 与 tick 较小，y/x 标题较大；PDF object 确认字体，字号为 rendered estimate，`provenance=pdf_object`、置信度 high。
- **颜色**：categorical 五色：`#800080`（Olmo 2 7B）、`#FF8000`（Olmo 2 (300) 7B）、`#FF0000`（Llama 3.1 8B）、`#008000`（Qwen 3 8B）、`#0000FF`（GPT OSS 20B）。颜色与 marker/legend 联合编码生成模型；两个圆 marker 仍需要颜色区分，故 `grayscale_safe=false`、`redundant_encoding=true`、`provenance=rendered_estimate`。
- **数据与统计**：每个点是生成模型 logprob 经 shared-token、target column-space projection 后到 target ellipse 的 mean distance；五个 open-weight models 加 Olmo 2 的 300 checkpoint；阴影是 standard error，caption 说多数窄到不可见。对角位置的生成模型在自身 target ellipse 上最低，跨模型距离高出多个数量级。没有显著性检验、重复数或额外区间。
- **证据关系**：这是从“ellipse 是 signature”到跨模型 source attribution 的 headline/main-comparison 证据，直接承接 §2.2 的 common-token projection 与 inverse affine transform，支持单个 logprob output 可识别来源的主张；后续 Table 2 将这一属性放入方法比较矩阵。
- **优点**：对数 y 轴揭示数量级差异；矩阵式 x×series 安排让“自身目标最低”的 diagonal pattern 一眼可见；marker 与 legend 使五个模型可追踪，caption 交代 projection 与 uncertainty。
- **缺点**：右侧 legend 占用横向空间；跨模型 projection、common-token 选择和 mean 的分母仍依赖正文；两种圆 marker 在灰度输出中容易混淆，standard-error band 几乎不可见。
- **可复用模式**：用 categorical target 轴和生成来源系列构成 source×target 矩阵，将身份判别主张转译成可读的 diagonal minimum pattern，并在 caption 说明投影与误差带。

### Figure 4（p.6，§3.1）

**Caption（逐字）**：

> Figure 4: The predicted and true values for bias and singular values, and the angles between columns of the predicted and true rotation matrices for a 1 million-parameter model. The angles must lie in the range [0, π], and the true rotation matrix columns have angle 0 with themselves. Our predictions are highly accurate, demonstrating robustness to normalization smoothing in ellipse fitting algorithms.

- **布局与几何**：页面宽度、三个并排 panel：`Bias`、`Singular values`、`Rotation`。每个 panel x 轴显示 0、20、40、60 的参数/列索引；Bias 与 Rotation 为线性 y 轴，Singular values 为 log y 轴，后者右上有 zoom inset 与连线。Bias panel 左上有 `True`/`Predicted` legend；两条线分别为蓝色圆点与红色方点。Rotation panel 的角度点靠近 0；图中还出现灰色 `000`–`005` 样式的序号残留。复杂度 3/5。
- **类型与 plot grammar**：`line`、`scatter`；`vector`；x=`linear`，y=`unknown`（同一 Figure 同时包含 linear 与 log panel）；grid=`none`；legend 存在于 Bias panel 左上，跨 panel 解释两系列（`shared_legend=true`）；direct_labels=false；marker_types=2，line_styles=1，hatching=false，reference_lines=0，uncertainty=`none`；线宽约 0.8 pt；`provenance=rendered_estimate`。
- **Typography**：TeX Gyre Termes X/NewTX，约 6–12 pt，regular/italic；panel 标题和轴刻度分级明显，数学 `π` 为 math font；`provenance=pdf_object`、置信度 high。
- **颜色**：categorical，`#0000FF`（True）、`#FF0000`（Predicted），浅色反锯齿/线段约为 `#7F7FFF` 与 `#FF7F7F`；颜色与圆/方 marker 冗余，灰度下仍可能把同一 y 值的两系列混在一起，`grayscale_safe=false`、`provenance=rendered_estimate`。
- **数据与统计**：一百万参数模型，embedding size 64、normalization smoothing `ε=1×10^-5`；Bias、singular values/stretch、rotation column-angle 的 predicted/true 参数序列。图中没有 seed 聚合、误差条或区间。Caption 定义 rotation angle 的范围和 true self-angle=0，但没有解释 x 轴为何是参数索引。
- **证据关系**：对应 §3.1 将 SVD-based quadric fitting 替换为 ellipse-specific SDP fit 的机制诊断，和 Appendix F 的 `cvxpy`/`mosek` implementation sanity check 相连；它承担 robustness/ablation，支持 smoothing 下参数回收近似准确，同时暴露 singular-value 略低估的限制。
- **优点**：三种可回收参数并列展示，预测/真值的逐点贴合比单一汇总误差更直观；rotation 单列显示角度范围；singular-value inset 保留了高值段细节。
- **缺点**：三 panel 的 y scale 不同且 x 轴语义未写出；legend 只在第一 panel；`000`–`005` 残留覆盖坐标下方；没有不确定性表达，无法判断贴合误差是否跨重复稳定。
- **可复用模式**：将不同参数族拆为共享 x 索引的多 panel，并保留 predicted/true 的同形 marker，配一个局部 zoom 来同时呈现大范围与尾部误差。

### Figure 5（p.7，§3.1）

**Caption（逐字）**：

> Figure 5: Distance between predicted and true parameters for bias, stretch, and rotation (lower is better). Fitting the ellipsoid to more samples generally improves the predictions, though with diminishing returns.

- **布局与几何**：三个并排 panel，标题分别为 `Stretch (MSE)`、`Bias (MSE)`、`Rotation (geodesic distance)`；x 轴均为 `Samples`，线性刻度约 0、20,000、40,000；y 轴标题 `Distance`，log 刻度；每 panel 一条蓝色圆点折线和浅蓝色 band。末端有直接数值标签 `0.0157`、`0.146`、`0.312`。复杂度 3/5。
- **类型与 plot grammar**：`line`、`scatter`；`vector`；x=`linear`，y=`log`，grid=`none`；无 legend，`shared_legend=false`，`direct_labels=true`（panel 标题和端点标注）；marker_types=1，line_styles=1，hatching=false，reference_lines=0，uncertainty=`band`；线宽约 0.9 pt；`provenance=rendered_estimate`。
- **Typography**：TeX Gyre Termes X/NewTX，约 6–11 pt，regular/italic；panel title、axis label 和 endpoint annotation 分层，`provenance=pdf_object`、置信度 high。
- **颜色**：mixed，`#0000FF`（点估计线）和 `#E5E5FF`/`#C8C8FF`（浅蓝 band）；颜色表示 estimate/可见变异的层级，曲线与端点数值提供冗余，`grayscale_safe=true`，`provenance=rendered_estimate`。
- **数据与统计**：改变用于 ellipse fit 的 output samples，bias/stretch 使用 MSE，rotation 使用 `tr(UᵀU*)` 所称 geodesic distance；曲线整体随样本增加下降并趋平，末端标注三项数值。论文正文把 irreducible `ε` error 作为收益受限因素；图内没有重复数、seed、误差定义或每个样本点的数值表。
- **证据关系**：是 Figure 4 的 sample-count extension，连接 §3.1 的“更多输出通常改善参数但 diminishing returns”主张，并把 normalization smoothing 的不可约误差引入实验结论；它承担 ablation/robustness。
- **优点**：三个参数族共享 Samples 轴，端点直接给出可比数值；log y 使早期快速改善与后期平台同时可见；浅色 band 不遮盖主线。
- **缺点**：caption 没定义 band 的统计含义；geodesic distance 的公式只在正文，y 轴单位抽象；没有显示原始 sample sequence 与重复变异。
- **可复用模式**：把一个拟合预算拆为同轴多 panel，在每 panel 保留指标名和末端数值，从而同时展示收益曲线、指标语义和 diminishing-return 形态。

### Figure 6（p.8，§3.3）

**Caption（逐字）**：

> Figure 6: Extrapolated running time of our implementation of the ellipse extraction algorithm from Ying et al. (2012). We extract ellipses from several models with different hidden sizes (blue). Fitting a degree-6 polynomial to these points, we can extrapolate to guess at the time required to extract the parameters of larger, more popular models.

- **布局与几何**：一个主图加左上 inset。主图 x=`Dimension d`，线性 0–9000；y=`Time (years)`，线性 0–15,000。蓝色实测点靠近原点，黑/灰色 degree-6 拟合曲线向上弯曲；三个空心圆直接标注 `OpenAI Babbage 002 (4 years)`、`Llama 2 8B (254 years)`、`Llama 3 70B (16,167 years)`。Inset x 约 0–250、y=`Time (minutes)` 0–15，蓝色点显示实测区间。无 legend，复杂度 3/5。
- **类型与 plot grammar**：`line`、`scatter`；`vector`；x=`linear`、y=`linear`；grid=`none`；无 legend，`direct_labels=true`；marker_types=2（蓝色实测点、黑色空心外推标记），line_styles=1，hatching=false，reference_lines=0，uncertainty=`none`；线宽约 0.8 pt；`provenance=rendered_estimate`。
- **Typography**：TeX Gyre Termes X/NewTX，约 6–12 pt，regular/italic；主轴标题与注释较大，inset ticks 较小，`provenance=pdf_object`、置信度 high。
- **颜色**：categorical，`#0000FF`（实测点）、`#4D4D4D`（拟合/外推曲线与空心标记）；颜色与 marker、主图/inset 位置共同编码 measured vs extrapolated，`grayscale_safe=true`，`provenance=rendered_estimate`。
- **数据与统计**：使用 64 CPU（脚注给出 AMD EPYC 7643 48-core）在 hidden dimension 8–256 上记录 solver time，再用 degree-6 polynomial 外推到 Babbage、Llama 2 8B、Llama 3 70B。图中无误差带、拟合区间或重复方差；脚注要求将年份数字视为 intuition，外推远超测量范围。
- **证据关系**：将 §3.2 的 sample/query cost 与 §3.3 的 `O(d^6)` fitting time 转成决策量，和 Table 1 的样本数/美元成本共同支撑 production-scale extraction barrier；不是实测生产攻击。
- **优点**：inset 放大实测低维区，主图保留大尺度外推；直接标注模型与年份，读者无需从额外表格查找。
- **缺点**：degree-6 曲线在测量范围外极不确定，图中没有 uncertainty 或敏感性比较；主图将真实测量点压在零附近；年份标注容易被读成保证值，caption 只写 “guess”。
- **可复用模式**：对测量区间与外推区间使用主图/inset 双尺度，并用不同 marker 明确 measured/extrapolated，caption 同时交代拟合阶数和外推边界。

### Figure 7（p.9，§4）

**Caption（逐字）**：

> Figure 7: A cryptographic message authentication system (left) uses a shared secret key to validate messages. The message author, Alice, signs the message by generating a tag from the message and a secret key. After receiving the message and tag, Bob verifies the message by generating another tag with his copy of the key. If the received and generated tags match, the message is authentic. In our proposed verification system (right), the ellipse functions as a secret key, and logprobs take the role of messages. The tag is encoded in the position of the logprobs in R^v, and can be verified by checking that they lie on the secret ellipse.

- **布局与几何**：页面宽度的左右并置流程图。左区标准 MAC：Alice/Bob 两个圆角框，分别包含 Key、Message、Tag、Same? 及 Generates/Sends 箭头；右区 ellipse protocol：Alice 的 `LM → Inference → Logprobs` 发送给 Bob 的 `Ellipse → Logprobs → On ellipse?`。上方 `Secret key`/`Secret LM` 箭头连接双方。无坐标轴、legend 或网格；复杂度 3/5。
- **类型与 plot grammar**：`pipeline`、`conceptual_diagram`；`vector`；x/y=`none`、grid=`none`；无 legend，direct_labels=true；marker=0，line_styles=1，hatching=false，reference_lines=0，uncertainty=`none`；线宽约 0.9 pt；`provenance=rendered_estimate`。
- **Typography**：TeX Gyre Termes X/NewTX，约 8–12 pt，regular/italic；Alice、Bob、Key 等为直接标签，数学 `R^v` 使用 math font；`provenance=pdf_object`、置信度 high。
- **颜色**：grayscale，`#000000` 文字、边框、箭头，`color_count=1`；角色由左右位置、框结构和标签编码，`redundant_encoding=true`、`grayscale_safe=true`、`provenance=rendered_estimate`。
- **数据与编码**：x 是 signer→verifier 的消息方向，y 为框内步骤；shape 是参与方/验证状态容器，line/arrow 是 key/message/tag 依赖，text 是协议变量和判定问题。无数据统计；Figure 7 传达 ellipse 作为 secret key、logprob 作为 message、椭圆上的位置作为 tag 的抽象映射。
- **证据关系**：连接引言的 MAC-like proposal、§4 的 key/message/tag 定义、§3 的 ellipse forgery resistance 和后文 replay/splicing attack 讨论；承担 method_interface/experimental-design 前的协议语义入口。Caption 独立说明两侧流程和验证判据。
- **优点**：左右严格对齐传统 MAC 与 ellipse system，映射关系无需颜色；Alice/Bob 与箭头让 signer/verifier 角色明确；`On ellipse?` 将几何检测转成协议判定。
- **缺点**：右侧 `Secret LM` 与 `Ellipse` 的信任/泄露边界仍需正文解释；没有标出单步 logprob、top-k、temperature 等限制；箭头较细，缩小后标签间距有限。
- **可复用模式**：对新协议同时展示熟悉的基准协议与提出的替代协议，保持参与方、消息和验证终点的同构布局。

## Table 审计

### Table 1（p.7，§3.2）

**Caption（逐字）**：

> Table 1: Models, their sizes, the number of samples required to ascertain their output ellipse, and the cost of the samples, based on OpenAI inference pricing on September 16, 2025. The number of samples required grows quadratically with the hidden size of the model, and the price grows cubically.

- **结构**：4 个数据行、5 列、单级表头：`Model`、`Hidden size d`、`Vocab size`、`Samples for Ellipsoid`、`Ellipse ($)`。表头/数据均为横向对齐，模型名使用等宽体；没有 row group，没有合并表头。顶部、表头下方和底部为三条 booktabs 风格横线，无竖线。复杂度低。
- **数据与精度**：行依次为 `pythia-70m`（512，50,304，131,327，成本空缺）、`babbage-002`（1536^a，101,281，1,180,415，1056）、`gpt-3.5-turbo`（4650^b，101,281，10,813,574，150,699）、`llama-3-70b-instruct`（8192，128,256，33,558,527，16,487,421^c）。整数列使用千位逗号，成本为美元整数；没有 mean±SD、区间或失败值。脚注 a/b/c 分别说明 confirmed size、estimated upper limit、hypothetical gpt-4-turbo inference cost。
- **Typography/颜色**：TeX Gyre Termes X、t1xtt（模型名）与 NewTX 数学字体；body/header 约 9 pt，header weight=`regular`，模型名 monospace，数学 d italic；`provenance=pdf_object`、置信度 high。黑色文字和规则，`mode=grayscale`、`#000000`、灰度安全。
- **证据关系**：把 Eq. (2) 的 quadratic sample lower bound、API token-prefix cost 和 §3.2 的 cubic price argument 放到一个决策面，承接 Figure 5 的 sample effect，和 Figure 6 的 fitting-time extrapolation 共同支撑 production-scale infeasibility。数据是解析/定价估计，不是生产攻击测量。
- **优点**：hidden/vocab/sample/cost 四个量在同一行对齐，读者可直接看到模型规模到成本的增长；脚注把 confirmed、estimated、hypothetical 状态分开。
- **缺点**：`Samples for Ellipsoid` 与正文 ellipse 术语不一致；pythia 行成本空缺，caption 没说明空缺原因；Table 1 的 pythia `131,327` 与 Eq. (2) 直接算出的 `131,840` 存在 513 的差异，论文未解释；价格日期和 gpt-4-turbo 假设必须结合脚注读取。
- **可复用模式**：将抽象维度增长转换为样本数、美元与模型行并列，并用脚注明确 observed/estimated/hypothetical 的事实状态。

### Table 2（p.13，Appendix B）

**Caption（逐字）**：

> Table 2: Summary of different model attribution methods and their properties. Some method classes may include both methods with and methods without specific properties, in which case we assign the label “-”.

- **结构**：6 个数据行、6 列、单级表头：`Method`、`Examples`、`Naturally occurring`、`Self-contained`、`Compact`、`Forgery resistant`。Method 与 Examples 较宽；Naturally occurring、Self-contained、Forgery resistant 在表头换行；方法名 `Text-based watermark`、`Backdoor fingerprint`、`Natural fingerprint`、`Cryptographic`、`Linear signature`、`Ellipse signature` 也有换行。无 row group、无竖线；顶部、表头下方、底部为 booktabs 横线。
- **数据与不确定性**：单元格为 Yes/No/`-` 的分类属性；Examples 列给出引用，Ellipse signature 行无具体示例。`-` 是 caption 定义的类内属性不确定/不统一标签；没有数值精度、均值、区间或统计重复。`rows=6`、`columns=6`、`header_levels=1`、`row_groups=0`、`decimal_precision=null`。
- **Typography/颜色**：TeX Gyre Termes X 与 NewTX 数学/符号字体，body/header 约 9 pt，header weight=`regular`，roman；黑白 booktabs，`mode=grayscale`、`#000000`、灰度安全，`highlighting=none`、`provenance=pdf_object`、置信度 high。
- **证据关系**：附录 B 将 §2.3 的 watermark、backdoor、natural fingerprint、cryptographic、linear signature 与 ellipse signature 放在同一属性矩阵，直接支撑 naturally occurring、self-contained、compact、forgery-resistant 四性质的定位。它为正文 Figure 1/3 的身份判别主张提供方法类对照，但没有新增实验测量。
- **优点**：六类方法和四项属性适合快速扫描；Examples 列保留文献锚点；`-` 避免把混合方法类强行二值化。
- **缺点**：属性判定的操作化标准未在表内给出；换行较多且 caption 语法略拗口；Ellipse signature 没有 examples cell，读者需回到本文正文。
- **可复用模式**：用短属性矩阵把方法定位与性质边界放在附录入口，并在 caption 明确类内混合值的编码。

## Appendix Figure 审计

### Figure 8（p.14，Appendix C）

**Caption（逐字）**：

> Figure 8: An overview of the intermediate representations in both traditional (gray) and our own (yellow) parameterization of the final LM layers. Representations (boxes) are annotated the functions that map between them (arrows), as well as the space in which they reside (blue tags). Our method recovers the parameters Σ, U, b, and A^-, which in turn give us access to the cannonical logit, ellipse, and rotated representations. Notably, the rotated representation that we recover is a pure rotation of the normalized representation.

- **布局与几何**：页面宽度、两行流程。上行灰色 `Typical form`：`Pre-norm R^d → Normalized R^d → Hidden R^d → Logit R^v → Probability R^v`；下行黄色 `Isolated operations`：`Rotated R^d → Ellipse R^d → Cannonical logit R^v`。箭头标注 Normalize、Stretch γ & bias β、Linear W、Softmax、Rotate V^T、Stretch Σ/rotate U/& bias b、Linear A^-、Center、Log & center 等；蓝色 tag 表示所在空间。复杂度 4/5。
- **类型与 plot grammar**：`pipeline`、`architecture`、`conceptual_diagram`；`vector`；x/y=`none`、grid=`none`；无 legend，direct_labels=true；marker=0、line_styles=1、hatching=false、reference_lines=0、uncertainty=`none`；线宽约 0.8 pt；`provenance=rendered_estimate`。
- **Typography**：TeX Gyre Termes X/NewTX，约 6–11 pt，regular/italic；蓝色空间 tag 使用数学字体，caption 中 `cannonical` 保留 PDF 原拼写；`provenance=pdf_object`、置信度 high。
- **颜色**：categorical，`#EEEEEE`（traditional row）、`#FFFEE6`（our row）、`#E5E5FF`（space tags）、`#000000`（文字/箭头/边框）；颜色编码参数化来源和空间，行位置与直接标签提供冗余，`grayscale_safe=true`、`provenance=rendered_estimate`。
- **数据与编码**：流程位置表示表示空间转换，色块表示传统/新参数化，蓝 tag 表示 R^d/R^v，箭头文本对应 operator，文本框对应 intermediate representation；无统计数据。图将 Eq. (3) 的 W(γ⊙x̂+β) 与 Eq. (4) 的 A^- (UΣV^T x̂+b) 显式拆开。
- **证据关系**：Appendix C 的 method_interface 图，承接 Algorithm 1、Eq. (3)–(4) 与 Appendix D 等价重参数化证明；为正文 Figure 2 的 sphere→ellipse 直觉补充可抽取参数 `Σ,U,b,A^-`。
- **优点**：两行保持同一表示语义，参数和空间在箭头/标签处就地标注；传统路径与可恢复路径的对应关系清楚。
- **缺点**：下行的 `Cannonical` 拼写错误会传播术语；箭头标签密集，线交叉要求读者沿行回溯；caption 没说明 V^T 的不可识别性边界。
- **可复用模式**：把隐式计算图拆成“原始路径/可识别操作”两行，以相同节点语义和空间 tag 建立一一映射。

### Figure 9（p.15，Appendix E）

**Caption（逐字）**：

> Figure 9: An illustration of the layer norm output space (left), which is shown as the thick ring at the intersection of the plane perpendicular to 1 (due to centering) and a sphere (due to normalization). To reduce the dimension of the representation, we can apply an isometric transform that rotates 1 to align with an axis (right), then drop that axis.

- **布局与几何**：两个并置的灰度三维示意。左 panel 是带斜面、球体和粗黑交圈的 sphere-plane intersection，旁边标注 `Σ_i x_i^2=d` 与 `Σ_i x_i=0`；右 panel 将向量 1 旋至坐标轴方向并展示水平交圈，随后说明可 drop axis。无坐标轴、legend、网格或数值序列；复杂度 2/5。
- **类型与 plot grammar**：`conceptual_diagram`；`vector`；x/y=`none`、grid=`none`；无 legend，direct_labels=true；marker=0、line_styles=2（球/平面轮廓与粗交圈）、hatching=false、reference_lines=0、uncertainty=`none`；线宽约 1.0 pt；`provenance=rendered_estimate`。
- **Typography**：TeX Gyre Termes X/NewTX math，约 8–12 pt，regular/italic；公式标签为数学体，`provenance=pdf_object`、置信度 high。
- **颜色**：grayscale，`#CFCFCF`（球体）、`#EEEEEE`（平面/阴影）、`#000000`（轮廓、交圈、公式）；空间关系由形状和线宽编码，`grayscale_safe=true`、`provenance=rendered_estimate`。
- **数据与编码**：平面表示 centering 约束，球面表示 normalization，粗环表示 layer norm 输出空间；右侧旋转后的轴与删除操作表示从 d 维到 d−1 维的 isometry。无统计数据或 uncertainty。
- **证据关系**：把 Appendix E 的 layer norm centering/normalization 公式和 Figure 10 的 d−1 ellipse 流程连起来，解释为何 bias 会把 ellipse lift 回 d 维；属于 theory_mechanism/method_interface。
- **优点**：交集用粗环突出，球面和约束面可分；左右变换直接回应“旋转并删除一个轴”的算法步骤；全灰度仍可读。
- **缺点**：三维投影没有坐标刻度，平面/球面深度关系依赖视觉推断；caption 使用 `1` 但未在图内给出向量维度；右图的 drop-axis 动作只由 caption 说明。
- **可复用模式**：用几何交集和变换前后并置，解释维数约束，再把视觉对象连接到后续线性代数算法。

### Figure 10（p.16，Appendix E）

**Caption（逐字）**：

> Figure 10: Model representations and mappings between them for LMs with a layer norm. Compared to Figure 8, this model has an ellipse in d − 1 dimensions.

- **布局与几何**：页面宽度、两行表示流程。上行为常规 `Pre-norm R^d → Normalized R^d → Hidden R^d → Logit R^v → Probability R^v`；下行为 `Sphere R^{d−1} → Rotated R^{d−1} → Ellipse R^{d−1} → Biased ellipse R^d → Cannonical logit R^v`。箭头标注 Isometric、Rotate V^T、Stretch Σ & rotate U、Linear Y & bias b、Linear A/B、Center、softmax；蓝色 tags 标示空间。复杂度 4/5。
- **类型与 plot grammar**：`pipeline`、`architecture`、`conceptual_diagram`；`vector`；x/y=`none`、grid=`none`；无 legend、direct_labels=true；marker=0、line_styles=1、hatching=false、reference_lines=0、uncertainty=`none`；线宽约 0.8 pt；`provenance=rendered_estimate`。
- **Typography**：TeX Gyre Termes X/NewTX，约 6–11 pt，regular/italic；空间 tag 和 d−1 为数学字体，`provenance=pdf_object`、置信度 high。
- **颜色**：categorical，`#E5E5FF`（空间 tags）、`#000000`（文字/箭头/边框）及白色框背景；颜色数量 2，位置、流程和文字承担主要编码，`grayscale_safe=true`、`provenance=rendered_estimate`。
- **数据与编码**：x 是表示变换方向，y 分离 traditional 与 layer-norm-specific route；shape 是表示框，arrow label 是变换算子，blue tag 是维度，`Biased ellipse R^d` 显示 bias lifting。无统计数据和不确定性。
- **证据关系**：是 Figure 8 的 layer-norm 变体，承接 Figure 9 的 d−1 geometry、Algorithm 2 的 `Y` up-projection 和 Appendix E 文字；它把 centered plane、ellipse fit、bias recovery 串成 method_interface/theory_mechanism。
- **优点**：与 Figure 8 保持节点语义，只有 layer-norm 必要分支发生变化；`R^{d−1}` 到 `R^d` 的维度跃迁直接可见；下行把 `Y` 与 bias 放在正确位置。
- **缺点**：箭头较多且有回路，Linear A/B、Center、softmax 的方向需要结合正文；`Cannonical` 拼写错误延续 Figure 8；caption 只指出 d−1 ellipse，没有解释 biased ellipse 的 lifting。
- **可复用模式**：复用基础架构图的节点词汇，只高亮架构变体的额外投影/升维步骤，避免为每个 normalization 重新设计视觉语言。

### Figure 11（p.17，Appendix G）

**Caption（逐字）**：

> Figure 11: The distribution of L2 norms of hidden states x̂ from two pre-trained language models with hidden sizes 64 (GPT NeoX) and 4096 (Llama 3). Both distributions are concentrated near the maximum norm of 1, more so for the larger model.

- **布局与几何**：两个并排 histogram panel，标题 `GPT NeoX 1M` 与 `Llama 3 8B`。左 panel 主窗口放大 0.988–1 的 norm 区域，并在左上嵌入 0–1 的全范围 `Plot area` inset，矩形连线指向放大区；右 panel 放大到约 0.999985–1。直方柱为黑色，无 y 轴标题/刻度，x 轴为 norm；复杂度 3/5。
- **类型与 plot grammar**：`histogram`；`vector`；x=`linear`，y=`linear`（计数轴未标注）；grid=`none`；无 legend，direct_labels=true（panel titles/inset label）；marker/line styles=null，hatching=false，reference_lines=0，uncertainty=`distribution`；line_width_pt=null；`provenance=rendered_estimate`。
- **Typography**：TeX Gyre Termes X/NewTX，约 7–12 pt，regular/italic；panel title 和 x tick 分层，`provenance=pdf_object`、置信度 high。
- **颜色**：grayscale，`#000000`（histogram bars/边框）与 `#EEEEEE`（全范围 inset 背景），`color_count=2`；zoom connector、panel title 和 x 轴提供冗余，`grayscale_safe=true`、`provenance=rendered_estimate`。
- **数据与编码**：x 为 normalized hidden-state L2 norm，bar height 为分布频数/密度；panel 是两个模型，inset 解释左图全范围到窄区间的 zoom。模型 hidden sizes 为 64 与 4096，论文正文说明 smoothing `ε=1×10^-5`；未给 bin width、样本数、y 轴单位或汇总统计。
- **证据关系**：Appendix G 的 robustness/mechanism 诊断，支撑“大模型的 smoothing 影响更小、忽略 ε 时 scaling 轻微低估”的解释；与 Figure 4 的 smoothing 参数贴合、Appendix F 的 solver sanity check 相连。
- **优点**：全范围 inset 与窄区间主窗口兼顾支持范围和集中峰形；两个模型并置后，Llama 3 的更紧分布易于比较；黑色柱在灰度复制中稳定。
- **缺点**：y 轴没有标度，分布高度无法定量比较；两个 panel 的 x 窗口不同，视觉比较需先读刻度；binning 和样本规模未交代，`Plot area` 文本更像内部标记。
- **可复用模式**：对接近边界的分布使用“全范围 inset + 局部 zoom”并置，保留边界数值刻度，同时在 caption 补充 binning、样本量和 y 轴定义。

## 跨对象系统判断

- **视觉叙事**：Figure 1 先给出 ellipse-as-signature 的空间判据；Figure 2 把判据落到 normalization→sphere→linear→ellipse；Figure 3 用 cross-model distance 的 diagonal minimum 完成 source identification；Figure 4–5 依次测试 smoothing 与 sample budget；Table 1/Figure 6 将恢复难度转成样本、美元和 solver years；Figure 7 再把 ellipse 映射到 MAC key/message/tag；Table 2 与 Figure 8–11 在附录补全方法类比较、可恢复参数、layer-norm 维度与 smoothing 边界。
- **Caption 系统**：正文 caption 多为完整句，Figure 1、3–7 同时交代设置、编码和主要发现；Figure 2 补 credit；附录 Figure 8–11 更偏结构/机制，Table 1 给出定价日期与增长结论，Table 2 定义 `-`。Figure 4 的 x 轴索引和 Figure 11 的 y 轴/直方图定义仍需要正文补充。
- **表头系统**：两张表均为单级、黑白 booktabs。Table 1 以模型为行、规模/样本/成本为列；Table 2 以方法类为行、性质为列。没有跨表共享色彩 token，脚注承担事实状态和混合类别说明。
- **方法—结果—消融连接**：Figure 2/8/9/10 与 Algorithm 1/2 是方法接口；Figure 3 是主比较；Figure 4/5 与 Figure 11 是 smoothing/sample robustness；Table 1/Figure 6 是 efficiency/cost；Figure 7 是协议应用。主张链在对象顺序上闭合，外推和缺少重复/seed 是主要证据边界。
- **正文—附录连接**：正文保留 ellipse 几何、跨模型识别、拟合误差、样本成本、运行时间和协议；附录把 centered logits、方法比较、可识别重参数化、layer norm 版本、实现 sanity check、smoothing histogram、logit transformation 限制展开。Figure 8–10 复用 Figure 2 的 sphere/ellipse 词汇，Figure 11 回应 Figure 4 的 smoothing 误差来源。
- **Typography 一致性**：主文与附录以 TeX Gyre Termes X/NewTX 数学体为主，diagram labels、axis labels、captions 分级稳定；模型名和算法符号使用 monospace/math 的局部差异。PDF 对象字体稳定，字号在 Figure 2/8/10 的密集箭头处偏小。
- **Color 一致性**：Figure 3–6 使用高饱和蓝/红/紫/橙/绿线条，Figure 1/2/8/10 使用淡色模块填充和蓝色空间 tag，Figure 7/9/11 与表格为灰度。颜色在对象内有语义，跨对象没有统一 palette token；Figure 1、2、3、4 的灰度可读性弱于 Figure 7、9、11。
- **矢量/栅格**：`pdfimages -list` 没有位图对象；PDF 页面中的图表、文字和规则均按矢量观察记录。Figure 2 左侧有可验证的公开 TikZ builder，但 PDF 右侧和其余对象仍没有直接 source-exact 文件。

## 最终判断

- **最可复用模式**：
  1. 用 Figure 1 的“约束几何 + 同源/异源点”在引言给出签名判据。
  2. 用 Figure 2、8、10 的双路径流程把抽象公式拆成表示、算子、空间和可恢复参数。
  3. 用 Figure 3 的 source×target 矩阵和 Figure 5 的预算—误差曲线把主张变成结构性视觉模式。
  4. 用 Figure 6 的实测区间/inset 与外推主图分离 measured 和 extrapolated，Table 1 再把复杂度转换为成本单位。
- **最高价值对象**：Figure 3（跨模型身份判别主证据）、Table 1 + Figure 6（样本/成本/时间的可决策化证据）、Figure 2（几何方法入口）、Figure 7（协议语义映射）。
- **失败模式**：Figure 2 与 Figure 4 出现未解释的灰色序号残留；Figure 3–5 的误差带/颜色在灰度和缩小后不稳定；Figure 6 的 degree-6 长程外推没有不确定性；Table 1 的 pythia 样本数与 Eq. (2) 算术不一致；Figure 11 缺少 y 轴量纲和 bin/sample 说明；Figure 8/10 的 `Cannonical` 拼写错误。
- **一句话视觉策略**：论文用“几何直觉 → 跨模型对角识别 → 拟合/成本压力 → MAC 协议 → 附录参数化边界”的顺序，将 ellipse signature 从概念判据推进到可验证但生产规模难恢复的证据链。

## 复核记录

- PDF 物理页与 caption anchor 计数：11 Figures、2 Tables、2 Algorithms；对象数量与 PDF 对齐。
- 视觉检查分辨率：220 dpi（高于协议要求的 180 dpi）。
- 只写入本论文的 `visual_audits/iclr-2026-ab553a040c74.md` 与 `visual_audits/iclr-2026-ab553a040c74.json`；其他并行工作区文件未纳入修改。
