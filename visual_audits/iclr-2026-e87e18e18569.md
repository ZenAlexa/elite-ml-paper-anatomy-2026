# SWINGARENA 视觉审计

- `paper_id`: `iclr-2026-e87e18e18569`
- 论文：*SWINGARENA: Adversarial Programming Arena for Long-context GitHub Issue Solving*
- PDF：`corpus/pdfs/iclr-2026-e87e18e18569.pdf`，29 页，Letter，612×792 pt。
- 对象事实源：PDF；以下页码是 PDF 物理页码，bbox 采用 PDF 点坐标 `[x0, y0, x1, y1]`，原点在左上角，向右/向下为正。
- 审计范围：完整读取正文、参考文献和附录 A–H（PDF p.1–29）；Algorithm 1 是算法对象而非 Figure/Table，按任务范围排除。
- 视觉核查：全部页面以 200 dpi（高于 180 dpi）渲染并逐页查看；对象页为 p.3、p.5、p.8、p.9、p.14、p.15、p.16、p.17、p.19。

## 1. 来源核查

先核查 `reports/tables/visual_source_inventory.csv`：该行把来源状态记为 `no_public_source_found`，候选字符串为 `davecgh/go-spew`、`ollama/ollama`、`spf13/cobra`、`NY1024/SecAI_Radar`；它们分别是 Go 调试库、Ollama 模型运行时、Cobra CLI 库和论文雷达项目，仓库描述与 SWINGARENA 论文不匹配。`corpus/visual_sources/iclr-2026-e87e18e18569/` 不存在。

随后以 `gh` 只读检索：完整标题和 `iclr-2026-e87e18e18569` 没有额外严格命中；`gh search repos SWINGARENA` 命中 `https://github.com/menik1126/Swing-Bench`。仓库元数据描述为 `[ICLR2026 Oral] SwingArena: Competitive Programming Arena for Long-context GitHub Issue Solving`；README 明确写有 SwingArena 论文标题、论文 arXiv 链接、代码和数据说明。只读递归树（main 当前 commit `4ccbb5c54f6f7a6bbf9a83ecb2f83e6d391e2439`）中可见 `docs/images/main_pot.png` 与 `docs/images/github_logo_pot.png`，未见论文 `.tex`、`.tikz`、`.pgf`、绘图脚本、表格生成器或对应 Figure 3–7 的可复用视觉源。README 的 `main_pot.png` 构图与 PDF Figure 2 的检索、双代理、验证器、角色切换和结果面板逐项匹配；`github_logo_pot.png` 只是品牌图，不纳入视觉源。因此来源状态为 `partial_visual_source`：Figure 2 有经论文 README 与 PDF 构图验证的公开渲染资产，其余对象以 PDF 为事实源。

## 2. PDF 对象清单

| 对象 | PDF 页 | 位置 | 类型/事实 |
|---|---:|---|---|
| Figure 1 | 3 | 正文 §3 前 | 数据构建 pipeline 栅格图 |
| Figure 2 | 5 | 正文 §3.1 | 对抗评测架构栅格图；公开 `main_pot.png` 对应 |
| Table 1 | 8 | 正文 §4.2 | 专有模型 16 行配对结果 |
| Table 2 | 8 | 正文 §4.2 | 4×5 Best@3 语言结果（含 Model 列） |
| Figure 3 | 9 | 正文 §4.2 | Best@k 两条曲线和面积填充 |
| Table 3 | 9 | 正文 §4.3 | RACG/检索基线 12 行消融 |
| Table 4 | 14 | 附录 A | 开源模型 8 行配对结果 |
| Figure 4 | 14 | 附录 B | 四个 3D token 柱状小图及连续色条 |
| Figure 5 | 15 | 附录 B.1 | 4 个 clarity 饼图 + 4 个 difficulty 饼图 |
| Figure 6 | 16 | 附录 B.2 | 2×4 长度直方图 |
| Figure 7 | 16 | 附录 C | 语言×长度类型分组柱状图 |
| Table 5 | 17 | 附录 C.1 | 检索方法文件命中率 |
| Table 6 | 19 | 附录 E | 与 Table 5 完全重复的文件命中率表 |

## 3. 论文级风格

- 正文 Figure 1–2 使用高饱和语义色与浅色圆角框；Figure 3 使用两种蓝色和浅蓝面积；附录 Figure 4–7 改用蓝色连续/分类配色。表格沿用 Nimbus Roman No9 L，表头为 medium/bold，Win Rate 列或结果列使用浅 lavender 填充并以粗体标出极值。
- PDF 内部图表是混合生产：Figure 1–2 为嵌入栅格；Figure 4 的 3D 绘图和文字为 PDF 向量、色条为嵌入窄栅格；Figure 3、5、6、7 与所有表格主要是向量对象。正文 caption 与表格正文使用 Nimbus Roman No9 L；Figure 3 使用 Times New Roman 图内文本，Figure 4 使用 DejaVuSerif，Figure 5–7 使用 DejaVuSans，存在跨图字体切换。
- 主要缺陷是 caption 多为标题式短句，未在 caption 中给出样本数、统计不确定性或缩写定义；Figure 4 将 3D 柱状小图称为 heatmap；Figure 5 的多饼图难以做精确跨语言比较；Table 5/6 重复且正文交叉引用存在编号错位。

## 4. Figure 审计

### Figure 1 — 数据构建 pipeline

- **位置与用途**：PDF p.3，正文 §3 前，`double_column`；类型 `pipeline`、`conceptual_diagram`；用途 `method_interface`、`dataset`。图从左到右并在下方回流，展示 Repo Mining、CI Test Filtering、LLM Filtering、Expert Filtering 四阶段，最终输出 SwingArena Dataset。
- **Caption**：`Figure 1: Overview of SWINGARENA data construction pipeline, including repository collection, pull request extraction, task instance creation, quality filtering, and multiple CI-based validation.`（21 词；moves=`title`,`setup`；不加粗；基本自洽但没有定量结果；未陈述主发现。）
- **字体**：图内为栅格化的 Arial/Calibri-like 无衬线体，阶段标题与框内标签为粗体，流程说明为常规体；估计 6–17 pt，中位约 10 pt；`rendered_estimate`，中等置信度。
- **颜色**：混合语义配色，约 10 个主要颜色：灰 `#E7E6E6`、粉 `#FFDEEF`、黄 `#FFF2CC`、绿 `#E2F0D9`、近白 `#F9FAFD`、蓝 `#5A8DB8`、红 `#C00000`、绿 `#70AD47`、金 `#FFC000`、状态绿 `#00FF00`。颜色编码阶段容器、通过/失败状态和最终数据集；箭头、图标、文字同时提供冗余线索；灰度下语义色不可完全安全区分。
- **几何与复杂度**：4 个阶段面板，外层圆角容器、内层虚线框、GitHub/PR/CI/人物图标和回流箭头；约 4 个主要输出节点、10 个以上文字/图标标注；复杂度 5/5。
- **Plot grammar**：栅格；x=`none`、y=`none`（流程位置而非数值轴）；无网格、无标准 legend、无 shared legend、无不确定性；direct labels=`true`；marker=`none`；线型约 2 种（实线/虚线箭头）；无 hatch、无 reference line；线宽约 1.5 pt；`rendered_estimate`。
- **编码与数据**：x 为阶段顺序和回流方向，y 为阶段/质量筛选层，color 为阶段与 CI/筛选状态，shape 为圆角框/图标，line 为箭头与虚线连接，facet 为四阶段，text 为阶段名、Repo/PR/Instance 标签。它是概念流程，没有绘制数值统计。
- **证据关系**：与正文 §3 的 Repository Mining、CI Test Filtering、LLM Filtering、Expert Filtering 一一对应，解释 2,300 个 GitHub issue/solution 实例如何经 CI、LLM 和专家筛选进入数据集；是 dataset construction 的方法接口，不是效果证据。
- **优点**：阶段顺序和最终输出明确；通过/失败状态用图标、颜色和文字重复编码；回流方向提示筛选闭环；跨列居中后仍可从 caption 读出主题。
- **缺陷**：内部字体偏大且色彩/图标风格偏演示稿；阶段内信息密度高，虚线框和多个箭头交叉；没有给各阶段保留/删除数量，无法从图判断筛选率；颜色的语义需要图内文字才能恢复。
- **可复用范式**：用四段有向 pipeline 展开“采集→自动验证→语义筛选→专家筛选”，每段保留输入、筛选条件和输出，并用回流箭头表达质量闭环；定量漏斗应另加样本数标签。
- **PDF 证据**：bbox `[167.3,82.0,444.7,211.3]`，为 PDF embedded image object，`pdf_object`。

### Figure 2 — 对抗评测框架

- **位置与用途**：PDF p.5，正文 §3.1 Interactive Environment/Battle Protocol/Verification，`double_column`；类型 `architecture`、`conceptual_diagram`；用途 `method_interface`、`experimental_design`。左侧为 RACG 检索/重排，右侧为 submitter/reviewer 双代理和 patch/test verifier，中下部为 role switch、最终 verifier 与胜者状态。
- **Caption**：`Figure 2: Illustration of the SWINGARENA adversarial evaluation framework. The framework simulates an adversarial software engineering workflow between two agents—a submitter and a reviewer—who alternate roles and iteratively refine their solutions based on CI feedback.`（33 词；moves=`title`,`setup`,`comparison`；不加粗；说明角色和迭代关系，但未给数值发现。）
- **字体**：公开 `docs/images/main_pot.png` 与 PDF 栅格图均显示黑色粗体无衬线标签，代码片段采用等宽视觉；估计 6–12 pt，中位约 8 pt；PDF 图内为栅格估计，`rendered_estimate`，中等置信度。README 公开图与 PDF 构图匹配，来源文件为 `source_exact` 级别的对应 rendered asset，但 PDF 重采样使像素不作逐点断言。
- **颜色**：混合语义配色，主要为灰 `#E7E6E6`、粉 `#FFDEEF`、浅粉 `#E7D1CF`、蓝 `#DAE3F3`/`#DEEBF7`、绿 `#E2F0D9`/`#A9D18E`、红 `#EC002C`、浅蓝虚线 `#8FAADD`、状态绿 `#3BAF4C`。粉表示 Participant A/submitter，蓝表示 Participant B/reviewer，绿表示 verifier/valid，红表示 invalid/failed；图标、文字和箭头提供冗余编码；依赖颜色的角色识别在灰度下不完全安全。
- **几何与复杂度**：约 5 个逻辑面板（检索、patch、test、role switch、outcome），圆角卡片、虚线状态框、实线/虚线方向箭头和代码片段；约 20 个文字/图标标注；复杂度 5/5。
- **Plot grammar**：栅格；x/y=`none`；无网格；右上 Code Snippet/Issue 图例式 key，`legend_present=true`、placement=`upper right`、非 shared；direct labels=`true`；无 marker；线型 2 种（实线/虚线箭头）；无 hatch、无 reference line、无不确定性；线宽约 1.5 pt；`mixed` 图内渲染但本 PDF 对象为 raster，grammar provenance=`rendered_estimate`。
- **编码与数据**：x 为工作流方向，y 为代理与验证器泳道，color 为角色/状态，shape 为 agent/verifier/codebase 卡片和图标，line 为生成、验证、失败、角色切换箭头，facet 为检索/patch/test/循环/结果面板，text 为角色、文件和状态标签；没有数值数据。
- **证据关系**：直接对应正文 Battle Protocol：submitter 生成 patch，reviewer 生成 test，patch/test verifier 通过 CI 联合验证，模型交换角色并由 final verifier 决定胜负；是实验设计的核心接口图。
- **优点**：把检索、双角色、CI 验证和角色交替放在一个闭环；valid/invalid 与 patch/test outcome 有明确路径；代码片段让抽象接口具象化；公开仓库 README 的同图资产可复用。
- **缺陷**：面板和箭头较多，阅读路径需从左上检索再跳到右侧；胜负评分公式、轮数和 baseline/golden patch 没有在图内出现；彩色状态框在缩小后文字很小；图例 key 不是独立的标准 legend。
- **可复用范式**：将“输入/检索→双代理生成→独立验证→共同判定→角色交换”组织为带反馈箭头的 architecture map；在图旁补充轮数、评分和失败条件，避免把可执行协议留给正文猜测。
- **PDF 证据**：bbox `[108.0,82.0,504.0,255.3]`，PDF image object，`pdf_object`；仓库验证文件为 `https://github.com/menik1126/Swing-Bench/blob/main/docs/images/main_pot.png`。

### Figure 3 — Best@k win rate

- **位置与用途**：PDF p.9，正文 §4.2 Best@k Sampling for Win Rate，`single_column`；类型 `line`、`area`；用途 `main_comparison`、`efficiency_cost`。横轴为 retries `k`（1、2、4、6、8、10、12、14、16），比较 submitter 与 reviewer 的 Best@k win rate。
- **Caption**：`Figure 3: Best@k win rate.`（3 词；moves=`title`；不加粗；不自洽、不定义 k 或两条线、不写主发现。）
- **字体**：图内 Times New Roman（坐标/点值/legend）与页面 Nimbus Roman No9 L caption；约 4.7–9.3 pt，中位约 7.3 pt；数值标注和轴文字为粗体；`pdf_object`/`rendered_estimate` 混合观察，中等置信度。
- **颜色**：约 3 个主要颜色：submitter 蓝 `#5A8AF3`、reviewer 蓝灰 `#557FAE`、浅蓝面积 `#DEE7F4`。颜色编码角色/曲线，圆形与方形 marker 及 legend 重复编码；两种蓝色及浅填充在灰度下区分度有限。
- **几何与复杂度**：1 个面板、2 条单调上升曲线、约 18 个 marker/点值标签、右下角 2 项 legend；复杂度 3/5。
- **Plot grammar**：向量；x=`linear`、y=`linear`，网格=`both`（浅灰点划网格）；legend=`true`，placement=`lower right inside plot`，shared=`false`；direct labels=`true`；marker types=2（circle/square）；line styles=2（均为实线）；无 hatch、无 reference line；不确定性=`none`（浅色区域是趋势填充，不是置信带）；线宽约 1.2 pt；`rendered_estimate`。
- **编码与数据**：x 为 retry 数 k，y 为 win rate，color 为角色，shape 为角色 marker，line 为两条 Best@k 曲线，facet 无，text 为每个点的数值与 legend。读取到的点值为 submitter `0.43, 0.45, 0.49, 0.52, 0.55, 0.57, 0.59, 0.61, 0.64`，reviewer `0.57, 0.59, 0.61, 0.62, 0.64, 0.64, 0.66, 0.67, 0.69`。
- **证据关系**：正文定义温度 0.25、每个角色最多 k 次独立尝试、至少一次通过即成功；图把 test-time scaling 与 adversarial win-rate 联系起来，但不提供方差、任务数或失败调用处理信息。
- **优点**：直接标值，k 轴和两角色关系清晰；曲线单调趋势容易读取；marker、颜色、legend 三重说明角色。
- **缺陷**：caption 过短；y 轴从 0.40 起而非 0，浅色填充可能被误读为不确定性；k=1 的点与偶数 tick 混排；没有样本量或重复实验稳定性。
- **可复用范式**：对 Best@k 使用共享线性轴、每点直接标注和角色形状 marker；应把 `k` 定义、重复种子/不确定性和面积填充语义写进 caption。
- **PDF 证据**：bbox 约 `[312.0,83.0,506.0,183.0]`，图内 PDF vector objects，`pdf_object`。

### Figure 4 — Token usage heatmap（实际为 3D 柱状小图）

- **位置与用途**：PDF p.14，附录 B Data Feature Distribution，`double_column`；类型 `bar`、`heatmap`（caption 称 heatmap，但实际为 3D bars）；用途 `dataset`、`efficiency_cost`。四个语言面板 C++、Python、Rust、Go，每个面板为 submitter×reviewer 的 4×4 模型配对。
- **Caption**：`Figure 4: Token usage heatmap for SWINGARENA. The darker the blue color, the higher the number of request tokens. The taller the bars, the higher the number of response tokens.`（28 词；moves=`title`,`encoding_key`；不加粗；说明两个视觉通道但没有样本/不确定性。）
- **字体**：图内 DejaVuSerif 向量文字和 Nimbus Roman No9 L caption；约 5.3–10 pt，中位约 6 pt；`pdf_object`，高置信度（p.14 的 pdffonts 明确含 DejaVuSerif；色条为窄栅格）。
- **颜色**：连续蓝色约 9 个代表端点/采样色（浅 `#D9E4EF` 至深 `#2279B5`）；色深表示 request tokens 的 log scale，柱高表示 response tokens；二者是互补而非重复编码；蓝色明度单调，灰度基本可读。
- **几何与复杂度**：4 个 3D small multiples，4×4=16 柱/面板、约 64 柱；每面板有 submitter/reviewer 分类轴和 response-token z 轴，右侧共享 request-token 色条；复杂度 5/5。
- **Plot grammar**：混合（3D 图形/文字为 vector，色条为 raster）；x=`categorical`（submitter）、y=`categorical`（reviewer），两轴都有 3D 参考网格；legend/colorbar=`true`，placement=`right`，shared=`true`；direct labels=`false`；marker=`none`；line styles=0；无 hatch、无 reference line；不确定性=`none`；线宽约 0.5 pt；`rendered_estimate`。
- **编码与数据**：x 为 submitter 模型，y 为 reviewer 模型，z/柱高为 response token，color 为 request token（log scale），shape 为 3D prism/bar，facet 为语言，text 为模型名、token ticks 和色条。正文/附录说明柱高/颜色分别编码 response/request tokens，坐标 z 约到 10,000。
- **证据关系**：附录 B 和正文 §5 的 token 讨论说明代码上下文占用主要输入预算；图支持效率/成本诊断，不直接支撑模型胜负效果。
- **优点**：共享色条和四语言面板使 token 负载可作横向检查；同一 4×4 角色配对结构保持一致；双通道编码能同时显示请求量和响应量。
- **缺陷**：3D 透视带来遮挡和高度比较困难；caption 的 heatmap 名称与柱状实现不一致；log 色条刻度小，request token 数值难反查；无汇总统计、不确定性或样本数。
- **可复用范式**：在多语言/多角色资源诊断中用共享尺度的 small multiples；若用颜色和高度编码两个量，应使用 2D grouped bars/annotated matrix 替代透视 3D，并明确 log 变换。
- **PDF 证据**：bbox 约 `[112.0,412.0,492.0,490.0]`，p.14 色条 image object 位于 XML `[712,641,720,717]`（PDF 约 `[474.7,427.3,480.0,478.0]`）；主体为 `pdf_object`。

### Figure 5 — Clarity and Difficulty Distribution

- **位置与用途**：PDF p.15，附录 B.1，`double_column`；类型 `other`（8 个 pie charts）；用途 `dataset`。上排为 Go/Python/Cpp/Rust clarity 二分类，下排为四语言 difficulty 分布。
- **Caption**：`Figure 5: Clarity and Difficulty Distribution.`（4 词；moves=`title`；不加粗；不在 caption 中定义评分或过滤规则，未陈述主发现。）
- **字体**：图内 DejaVuSans 向量文本，caption 为 Nimbus Roman No9 L；约 6.7–10 pt，中位约 8 pt，常规体；`pdf_object`，高置信度。
- **颜色**：8 色 categorical palette：`#4C72B0`、`#DD8452`、`#55A868`、`#C44E52`、`#8172B3`、`#937860`、`#DA8BC3`、`#CCB974`。颜色对应 clarity/difficulty bucket，扇区角度和百分比文字提供部分冗余；多色 palette 灰度不安全。
- **几何与复杂度**：2×4=8 个面板；clarity 每图 2 扇区，difficulty 每图 8 扇区（含 `other`），约 40 个扇区和 40 组外部类别/内部百分比标注；复杂度 5/5。
- **Plot grammar**：向量；x/y=`none`，无网格；无标准 legend、无 shared legend；direct labels=`true`；marker=`none`；line styles=0；无 hatch/reference line；不确定性=`none`；分隔线约 0.5 pt；`rendered_estimate`。
- **编码与数据**：x/y 无轴，color/sector 为 rating bucket，facet 为语言×维度，text 为 bucket 和百分比。clarity：Go 2/3=`94.7%/5.3%`，Python=`93.7%/6.3%`，Cpp=`97.1%/2.9%`，Rust=`94.2%/5.8%`。difficulty（按图中顺序的主要百分比）：Go `0.65 16.1%, 0.35 14.7%, 0.25 13.9%, 0.3 11.6%, 0.45 11.1%, 0.15 9.5%, 0.55 8.9%, 0.75 7.1%, other 7.1%`；Python `0.25 16.2%, 0.45 14.5%, 0.35 13.7%, 0.65 12.0%, 0.15 11.3%, 0.75 9.5%, 0.55 7.8%, 0.3 8.8%, other 6.3%`；Cpp `0.65 24.4%, 0.35 15.5%, 0.55 13.0%, 0.45 12.2%, 0.25 10.5%, 0.75 8.4%, 0.3 7.1%, other 8.8%`；Rust `0.25 19.3%, 0.35 16.8%, 0.45 13.3%, 0.65 12.6%, 0.55 9.5%, 0.3 8.6%, 0.15 7.7%, other 12.4%`。数据已过滤不完整/无效记录。
- **证据关系**：附录 B.1 的正文解释 clarity 为 2/3 两级、difficulty 为 0.15–0.75 的准连续评分并含 `other`；图支持数据集质量与任务难度描述，不是模型效果证据。
- **优点**：8 个面板行列语义一致；百分比直接写入扇区；clarity 的极端偏向和 difficulty 的跨语言差异可快速发现。
- **缺陷**：饼图角度不利于精确跨语言比较；difficulty 标签和百分比拥挤，`other` 需要依赖正文解释；没有每种语言的 n 或不确定性；色彩依赖强。
- **可复用范式**：若保留饼图，应限制扇区数并共享排序/颜色；对于跨语言分布比较，优先用 100% stacked bars 或 dot plot，并在 caption 说明过滤和样本数。
- **PDF 证据**：bbox 约 `[108.0,79.0,507.0,274.0]`，图内 DejaVuSans/路径为 `pdf_object`。

### Figure 6 — Length distributions in different languages

- **位置与用途**：PDF p.16，附录 B.2，`double_column`；类型 `histogram`；用途 `dataset`。上排为 Go/Python/Cpp/Rust problem-statement length，下排为对应 patch length。
- **Caption**：`Figure 6: Length distributions in different languages.`（5 词；moves=`title`；不加粗；未定义两行含义、长度单位、样本量或不确定性。）
- **字体**：图内 DejaVuSans，caption 为 Nimbus Roman No9 L；约 6.7–10 pt，中位约 7.3 pt，常规体；`pdf_object`，高置信度。
- **颜色**：单一蓝 `#5B8DB8`，用于所有柱；color_count=1，颜色不是数据变量，灰度安全；语言与指标由 panel title/位置编码。
- **几何与复杂度**：8 个 panel、每图一组左偏柱，约 100 个 bin bars（各语言 bin 数不同），无 legend；复杂度 5/5。
- **Plot grammar**：向量；x=`categorical`（长度 bucket），y=`linear`（Count）；无网格、无 legend/shared legend、无 direct point labels；marker=`none`；line styles=0；无 hatch/reference line；不确定性=`none`；线宽约 0.5 pt；`rendered_estimate`。
- **编码与数据**：x 为 0–100/0–200 等长度 bucket，y 为频数，color 为固定单色，facet 为语言×problem/patch，text 为标题和旋转 bucket 标签。正文报告所有分布左偏、最频繁区间集中在 0–200；Python problem statement 峰值超过 250，Rust/C++ 尾部相对更长。
- **证据关系**：附录 B.2 及正文对数据长度/patch 复杂度的讨论；用于解释数据集任务规模与语言差异，不直接给出模型正确率。
- **优点**：上下两行保持 problem/patch 对照；每个语言独立显示长尾，能看出短样本占主导；同一蓝色减少无意义分类。
- **缺陷**：各 panel y 轴上限不同，跨语言柱高不可直接比较；旋转 x 标签密集且缩小后难读；caption 没有说明两行和单位；无 n、误差或分箱规则的统计不确定性。
- **可复用范式**：使用固定行语义的 small-multiple histogram，并在每 panel 标注 n、单位和共享/独立 y-scale；需要跨语言比较时同步 y 轴或改用比例密度。
- **PDF 证据**：bbox 约 `[108.0,80.0,506.0,310.0]`，图形和文字为 `pdf_object`。

### Figure 7 — Average problem statement and patch length

- **位置与用途**：PDF p.16，附录 C Data Analysis 右栏，`single_column`；类型 `bar`；用途 `dataset`、`efficiency_cost`。四个语言类别各有 problem-statement 与 patch 两根柱。
- **Caption**：`Figure 7: Average problem statement and patch length of different languages.`（9 词；moves=`title`；不加粗；正文随后补充 context 长度，但 caption 本身未给单位/样本数。）
- **字体**：图内 DejaVuSans 向量文本，caption 为 Nimbus Roman No9 L；约 6.7–10 pt，中位约 8 pt，常规体；`pdf_object`，高置信度。
- **颜色**：两类蓝：problem statement `#5B8DB8`，patch `#AED6F1`；类别由颜色、并列位置、legend 和直接数值标签冗余编码；明度差使灰度基本可读。
- **几何与复杂度**：1 个面板、4 个语言类别、2 个系列、8 根柱和 8 个顶端数值标注；复杂度 3/5。
- **Plot grammar**：向量；x=`categorical`（Go/Python/Cpp/Rust），y=`linear`（Average Length）；无网格；legend=`true`、placement=`upper right`、非 shared；direct labels=`true`；marker=`none`；line styles=0；无 hatch/reference line；不确定性=`none`；线宽约 0.5 pt；`rendered_estimate`。
- **编码与数据**：x 为语言，y 为平均 token 长度，color/shape 为 problem statement vs patch，facet 无，text 为 8 个数值和 legend。读取到的值为 Go `286.7/287.6`、Python `2369.9/852.6`、Cpp `205.4/631.2`、Rust `216.1/1059.9`。正文还报告检索 20 files 时上下文平均为 Go 8,232、Python 29,258、C++ 54,483、Rust 36,875 tokens，这些量级因超出图的可视化尺度而未画入图。
- **证据关系**：附录 C 将图连接到 token/context 成本讨论，说明短问题也可能对应长 patch，并指出代码上下文是主要输入瓶颈；它是 dataset/efficiency 诊断。
- **优点**：零基线分组柱适合平均量比较；所有柱直接标值；两系列 legend 简单，语言顺序稳定。
- **缺陷**：上下文长度被文字说明但未进入图，容易把平均问题/patch 长度误读为完整输入成本；无误差或 n；两种浅/深蓝在低质量打印中区分度下降；caption 与正文的 token 单位不一致地分散。
- **可复用范式**：对每语言同时显示 prompt 与 patch 平均量，用 direct labels 保留精确值；若讨论成本，应加第三类 context 或用断轴/并列 panel，并在 caption 说明 token 单位和过滤规则。
- **PDF 证据**：bbox 约 `[343.0,480.0,505.0,595.0]`，图内为 `pdf_object`。

## 5. Table 审计

### Table 1 — 专有模型配对结果

- **位置与用途**：PDF p.8，正文 §4.2，`double_column`；用途 `main_comparison`。6 列：Matchup、Submitter、Reviewer、RPR、SPR、Win Rate；16 行，按四个 submitter 分为 4 组。
- **Caption**：`Table 1: Evaluation of Code Submission vs. Test Submission Capabilities Among Proprietary LLMs.`（11 词；标题式，未定义 RPR/SPR/Win Rate，未写结论或不确定性。）
- **排版**：NimbusRomNo9L-Regu/Medium，body/header 约 8.7 pt，header medium/bold；1 层表头、4 个行组；booktabs 风格的上/表头下/组间/下规则，无竖线。RPR/SPR/Win Rate 两位小数；Win Rate 整列浅 lavender，部分列内极值粗体；无 uncertainty。
- **数据与统计**：报告 GPT-4o、Claude、Gemini、DeepSeek 的 16 个 submitter×reviewer 配对，RPR/SPR 为 patch/test CI pass rates，Win Rate 为 adversarial outcome。PDF 值包括 GPT-4o self-play `0.71/0.68/0.97`、Claude self-play `0.62/0.62/1.00`、Gemini self-play `0.72/0.63/0.91`、DeepSeek self-play `0.70/0.66/0.96`；交叉配对的 Win Rate 范围约 0.89–1.00。表中没有任务数、重复种子、误差区间或失败调用处理。
- **证据关系**：正文用该表支持“GPT-4o 作为 submitter 更具攻击性、DeepSeek/Gemini CI 稳定、reviewer 造成轻微非对称”的主结果；应与正文指标定义一起解释，不能单独把 Win Rate 当作代码正确性。
- **优点**：配对矩阵完整，submitter/reviewer 角色显式分列；4 组水平线帮助浏览；Win Rate 聚焦列、极值加粗。
- **缺陷**：caption 没有定义缩写或 score 方向；点估计没有 n/不确定性；整列着色强调 Win Rate 但可能掩盖 RPR/SPR 的解释边界；16 行和长模型名在缩小后偏密。
- **可复用范式**：用 submitter×reviewer 明确角色列、按 submitter 分组并以规则分隔；将 outcome 列单独着色，同时在 caption 给出指标定义、任务数和不确定性。
- **PDF 证据**：bbox 约 `[152.0,101.0,456.0,286.0]`，表格文字/规则为 `pdf_object`。

### Table 2 — 语言 Best@3

- **位置与用途**：PDF p.8，正文 §4.2 Language-Specific Evaluation 右栏，`single_column`；用途 `main_comparison`。6 列（Model、Average、C++、Go、Rust、Python），4 行模型。
- **Caption**：`Table 2: Best@3 across Models and Languages.`（5 词；仅标题，不定义 Best@3、不写样本或主发现。）
- **排版**：NimbusRomNo9L-Regu/Medium，body/header 约 8.7 pt；1 层表头、无显式行组；booktabs 规则；平均值和语言值两位小数；以粗体突出每列最高值，无 cell color、无 uncertainty。
- **数据与统计**：Gemini `.57/.64/.58/.51/.57`、DeepSeek `.59/.64/.61/.58/.52`、GPT-4o `.57/.63/.53/.56/.54`、Claude `.55/.63/.55/.52/.50`，列顺序为 Average/C++/Go/Rust/Python。无 n、误差或显著性信息。
- **证据关系**：支持正文关于 DeepSeek 平均 Best@3=0.59、C++ 最强及 Rust/Python 较弱的语言异质性描述，并补充 Table 1 的总体配对结果。
- **优点**：紧凑地把总体与四语言结果放在同一表；粗体最大值使列内比较快。
- **缺陷**：caption 不解释 Best@3 和评测协议；窄栏长模型/列标题拥挤；平均值与语言值均为点估计，无任务数或不确定性；并列最高值的规则未写明。
- **可复用范式**：保留一列整体指标加语言列的宽表；在表注中说明 Best@k、聚合方式、并列规则和不确定性。
- **PDF 证据**：bbox 约 `[282.7,540.7,481.3,610.0]`，`pdf_object`。

### Table 3 — RACG 消融

- **位置与用途**：PDF p.9，正文 §4.3，`single_column`；用途 `ablation`。3 列 Method、Best@3、Win Rate；12 行，前 8 行为四语言 w/ vs w/o RACG，后 4 行为 BM25/Top-k Related 检索基线。
- **Caption**：`Table 3: RACG Ablation Comparison.`（3 词；仅标题；未展开 RACG、未定义基线或不确定性。）
- **排版**：NimbusRomNo9L-Regu/Medium，body/header 约 8.7 pt；1 层表头、2 个逻辑行组；booktabs 规则，组间水平线；两位小数；Win Rate 列浅 lavender，列内最大值粗体；无 uncertainty。
- **数据与统计**：RACG 行：C++ `.42/.84` vs `.38/.77`，Python `.46/.84` vs `.44/.71`，Rust `.58/.75` vs `.49/.72`，Go `.45/.80` vs `.37/.71`（Best@3/Win Rate）；检索基线 BM25 `.38/.62`、Top-2 `.42/.69`、Top-10 `.43/.72`、Top-20 `.43/.73`。无 n、误差或重复种子。
- **证据关系**：直接检验方法中的 RACG 模块及检索粒度，支撑正文关于 RACG 提升 Best@3/Win Rate、Top-20 为最强检索基线以及固定 Top-5 文件限制的讨论。
- **优点**：w/ 与 w/o 成对排列；检索 baseline 单独分组；两项关键指标并列，适合消融阅读；突出列和极值明显。
- **缺陷**：语言消融和跨语言检索基线放在同一表，比较对象层级不完全一致；caption 过短；没有说明 submitter/reviewer 固定设置、数据量和不确定性；浅色列可能让读者忽略 Best@3。
- **可复用范式**：以水平规则分隔“机制消融”和“外部基线”，每对配置相邻，并在 caption 说明固定角色、数据切分和指标方向。
- **PDF 证据**：bbox 约 `[325.3,526.0,481.3,681.0]`，`pdf_object`。

### Table 4 — 开源模型配对结果

- **位置与用途**：PDF p.14，附录 A，`double_column`；用途 `main_comparison`。6 列同 Table 1，8 行，分为 7/8B 与 14/16B 两个行组。
- **Caption**：`Table 4: Evaluation of Code Submission vs. Test Submission Capabilities Among Open Source LLMs.`（12 词；仅标题，未定义缩写、参数组或不确定性。）
- **排版**：NimbusRomNo9L-Regu/Medium，body/header 约 8.7 pt，header medium/bold；1 层表头、2 个行组；booktabs 规则；两位小数；Win Rate 整列浅 lavender，最佳 RPR/SPR/Win Rate 粗体；无 uncertainty。
- **数据与统计**：7/8B 组四行：Qwen2.5-7B/Seed-8B 与 Seed-8B/Qwen2.5-7B，RPR `.55–.61`、SPR `.48–.52`、Win Rate `.87–.90`；14/16B 组四行：Qwen2.5-14B/DeepSeek 及反向配对，RPR `.58–.62`、SPR `.52–.55`、Win Rate `.91–.95`。无 n/误差。
- **证据关系**：附录 A 将主要结论扩展到开源模型，并按参数规模分组；正文 p.8/9 引用 Table 4 作为 open-source matchups 补充。
- **优点**：同 Table 1 的列系统便于专有/开源结果对照；参数规模水平线清楚；角色方向与 Win Rate 同时保留。
- **缺陷**：模型简称在正文另行解释，caption 不自洽；仅 8 行且未说明每个配对的任务数/温度/重复；Win Rate 高于 CI pass rate 的解释仍需正文指标边界。
- **可复用范式**：复用主体结果表的列语法和配对方向，在附录按参数规模分组扩展模型；表注应同步给出简称和实验协议。
- **PDF 证据**：bbox 约 `[132.0,628.7,478.0,727.0]`，`pdf_object`。

### Table 5 — 检索文件命中率

- **位置与用途**：PDF p.17，附录 C.1 Failure Typology 右栏，`single_column`；用途 `mechanism`。4 列 Method、Top-2、Top-10、Top-20，4 行检索方法。
- **Caption**：`Table 5: File hit rates of different retrieval methods. Each value gives the fraction of queries whose correct file appears within the Top-2, Top-10, or Top-20 retrieved results.`（26 词；moves=`title`,`setup`,`encoding_key`；定义了指标但不提供 n/不确定性/主发现。）
- **排版**：NimbusRomNo9L-Regu/Medium，body/header 约 8.7 pt；1 层表头、无行组；booktabs 规则；三位小数；Class Chunks 三列最高值加粗；无 cell color、无 uncertainty。
- **数据与统计**：BM25 `.182/.207/.195`，Block Chunks `.207/.341/.329`，Function Chunks `.277/.371/.368`，Class Chunks `.363/.487/.461`，顺序为 Top-2/Top-10/Top-20。正文据此称 class-level Top-10 从 20.7% 增至 48.7%，并说明粒度/上下文窗口权衡。
- **证据关系**：测量 RACG 的 patch-file localization/retrieval mechanism，为正文消融和附录失败类型 F4 提供机制证据；它不是最终模型胜率。
- **优点**：caption 直接定义命中率和 k；三位小数保留检索差异；方法行短、列对齐清晰。
- **缺陷**：没有查询总数、置信区间或重复划分；三位小数可能造成超出数据支持的精确感；没有说明 chunk 命中如何映射回 parent file。
- **可复用范式**：用 Top-k 并列列展示检索 recall，并在 caption 写明“正确文件”定义、查询数、映射规则和不确定性。
- **PDF 证据**：bbox 约 `[318.7,206.0,491.3,260.0]`，`pdf_object`。

### Table 6 — 重复的检索文件命中率

- **位置与用途**：PDF p.19，附录 E Annotator Demographics 右栏，`single_column`；用途 `mechanism`。4 列、4 行，内容与 Table 5 完全相同。
- **Caption**：与 Table 5 完全相同（26 词；moves=`title`,`setup`,`encoding_key`；未说明这是复制表）。
- **排版**：NimbusRomNo9L-Regu/Medium，body/header 约 8.7 pt；1 层表头、无行组；booktabs 规则；三位小数；Class Chunks 三列最高值加粗；无 uncertainty。
- **数据与统计**：BM25 `.182/.207/.195`，Block Chunks `.207/.341/.329`，Function Chunks `.277/.371/.368`，Class Chunks `.363/.487/.461`。PDF p.19 同时用左栏正文介绍 12 名 annotator（8 Ph.D.、2 Master、1 undergraduate、1 assistant professor），但该表不是 demographics 表。
- **证据关系**：数值机制证据仍是检索命中率；它在 annotator appendix 中重复出现，不能被当作 annotator 统计。正文 p.9 对 patch localization 的交叉引用也指向 Table 6，造成与 p.17 Table 5 的编号/位置混淆。
- **优点**：单独出现时 caption 对 Top-k 指标定义完整；版式与 Table 5 一致。
- **缺陷**：与 Table 5 逐值重复却没有 duplicate 注记，浪费附录空间并降低对象清单可靠性；位置标题与内容主题不一致；同样缺少 n/不确定性/映射规则。
- **可复用范式**：同一数值表只保留一个权威位置；若需在多个附录引用，使用交叉引用而非复制，或明确标注“重复/复现”。
- **PDF 证据**：bbox 约 `[318.7,401.3,491.3,455.0]`，`pdf_object`。

## 6. 跨对象判断

- **视觉叙事**：Figure 1 先交代数据构建，Figure 2 定义双代理/CI arena；Table 1–2 与 Figure 3 给出专有模型、语言与 Best@k 结果；Table 3 把主结果连接到 RACG 消融；Table 4、Figure 4–7、Table 5–6 在附录扩展开源模型、token/长度、检索机制和失败边界。
- **Caption 系统**：统一使用 `Figure/Table n:` 加短标题；Figure 1/2/4 和 Table 5/6 有一定 setup/encoding 信息，其余多数只给标题。缺少统一的 n、指标方向、过滤规则和不确定性模板。
- **表头系统**：全部表为单层 bold/medium 表头，角色列与指标列固定；行组靠 booktabs 水平线，不使用 spanning headers。RPR/SPR/Best@k 等缩写依赖正文定义。
- **方法—结果—消融**：F1/F2 说明采集与 arena 接口，T1/T2/F3 测结果和 test-time scaling，T3 测 RACG，T5/T6 测检索定位；链条存在，但 T5/T6 复制和正文 Table 6 引用错位削弱了可追溯性。
- **正文—附录**：正文 Figure 3/Tables 1–3 仅保留核心对比，附录 Table 4 与 Figures 4–7、Tables 5–6 承担开源、数据特征、token 成本和失败机制；附录提供了额外解释，但表重复与 caption 过短影响导航。
- **字体一致性**：caption/表格的 Nimbus Roman No9 L 稳定；图内 Figure 3 的 Times New Roman、Figure 4 的 DejaVuSerif、Figures 5–7 的 DejaVuSans 和 Figure 1–2 的栅格无衬线体不完全统一。
- **颜色一致性**：同一图内语义保持一致，表格 Win Rate lavender 也一致；跨模态没有统一色板（流程图语义色、曲线蓝、饼图 seaborn、附录柱状蓝），但不影响单图解释。灰度安全性在 Figures 1/2/5 和 Figure 3 的双蓝上较弱。

## 7. 最终判断

- **最可复用模式**：四段式方法 pipeline；双代理生成—验证—角色切换闭环；Best@k 曲线的直接点值标签与 marker 冗余；表格按角色显式分列、按行组用水平规则分隔并突出结果列。
- **最高价值对象**：Figure 2 最清楚地定义实验接口；Table 1 与 Table 3 分别承载主配对结果和 RACG 因果对照；Figure 3 以最小面积解释 retry scaling。
- **失败模式**：短 caption 没有自洽定义；Figure 4 的“heatmap”命名与 3D bars 不符；Figure 5 饼图比较困难且无 n/不确定性；Figure 6 独立 y 轴与密集 bin label 限制横向比较；Table 5/6 重复并造成引用歧义。
- **一句话视觉策略**：论文用演示式彩色流程图建立 SWINGARENA 的数据与双代理机制，再用紧凑表格、Best@k 曲线和附录分布图支撑结果与检索/成本机制，但需要统一图内字体、补充统计上下文并消除重复表与编号错位。
