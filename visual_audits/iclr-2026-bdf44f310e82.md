# Differentiable Model Predictive Control on the GPU：视觉审计

## 审计边界与事实源

- **paper_id**：`iclr-2026-bdf44f310e82`
- **论文**：Emre Adabag、Marcus Greiff、John Subosits、Thomas Lew，ICLR 2026 Oral。
- **PDF**：`corpus/pdfs/iclr-2026-bdf44f310e82.pdf`；19 个物理页，p.1–10 为正文/混合页，p.10–12 为 references，p.13–19 为 appendix。
- **复核方式**：完整读取 PDF 正文、references 与附录；所有含 Figure/Table 的页按 200 dpi 渲染后逐对象检查。PDF 负责对象清单、标签、页码、caption 和表内文字；截图中由像素推断的颜色、线宽、字号、几何和栅格类型均在 JSON 中标为 `rendered_estimate`。
- **清单结果**：PDF 含 Figure 1–13（13 幅）、Table 1–6（6 张）以及 Algorithm 1–4。算法已逐页检查，但 schema 只收录 Figure/Table，因此不作为 JSON 对象。
- **版面**：正文是单栏 ICLR conference layout；Figure 2、3、5–8、10、13 为接近页面宽度的复合对象，Figure 1、4、9、11、12 为窄栏或右侧对象。附录表格沿用 booktabs 风格，Table 3、5、6 接近页宽。
- **字体对象**：`pdffonts` 显示 Nimbus Roman No9 L、Computer Modern 数学字体、Nimbus Mono L 等；嵌入图像中的图内字体只能从渲染结果估计。PDF metadata 为 pdfTeX 1.40.26、PDF 1.5。

## Figure/Table 清单核对

|对象|PDF 页|模块|版面宽度|视觉职责|
|---|---:|---|---|---|
|Figure 1|3|theory|inset|DO forward/backward 概念入口|
|Figure 2|6|method|page width|DiffMPC forward/backward 数据流与矩阵复用|
|Figure 3|7|results|page width|RL solver CPU/GPU forward/backward 时间比较|
|Figure 4|7|results|single column|cart-pole IL 的 imitation/model loss 收敛|
|Figure 5|8|results|page width|domain-randomized drifting RL pipeline|
|Figure 6|8|results|page width|figure-8 水坑中的状态、轨迹与 spinout|
|Figure 7|9|results|page width|Toyota Supra 水坑漂移照片与速度/侧滑标注|
|Figure 8|9|results|page width|donut 水坑 baseline/learned 状态和 top-view 轨迹|
|Figure 9|15|ablation|single column|PCG tolerance 与 gradient cosine similarity|
|Figure 10|15|ablation|page width|warm-start speedup 与 PCG tolerance|
|Figure 11|18|appendix|single column|3 seeds 的 wall-clock training loss|
|Figure 12|19|appendix|single column|学习后 MPC 参数相对 nominal 的变化|
|Figure 13|19|appendix|page width|figure-8 第一弯的单次 on-vehicle closed-loop response|
|Table 1|14|appendix|single column|mpc.pytorch 原版/修改版 timing|
|Table 2|15|appendix|single column|六个 RL benchmark 配置|
|Table 3|16|appendix|page width|六 problem、CPU/GPU、Fwd/Bwd timing|
|Table 4|16|appendix|single column|nonlinear attitude stabilization timing|
|Table 5|17|appendix|page width|nominal vehicle model 与 controller gains|
|Table 6|18|appendix|page width|domain-randomization 参数范围|

## 公开视觉源核查

PDF p.10 的 reproducibility statement 明确链接 `https://github.com/ToyotaResearchInstitute/diffmpc`。GitHub `gh repo view` 返回的 README 标题和描述均为 **Differentiable Model Predictive Control on the GPU**；main 分支递归树确认了 inventory 中的 notebook/script，并额外发现两个直接复用的公开图像资产。`corpus/visual_sources/iclr-2026-bdf44f310e82/` 当前有 `plot_expert_data.py` 与 `plot_IL_results.ipynb` 的紧凑副本；`warm_start_plot.py` 及 `figs/*.jpg` 通过 GitHub API 只读核查，未拉取数据集、checkpoint 或完整仓库。

本审计将自动 inventory 的 `exact_visual_source` 调整为 **`partial_visual_source`**：`figs/diffmpc.jpg` 与 Figure 2 图像部分一致，`figs/supra_puddle.jpg` 与 Figure 7 center-view 面板一致，`plot_IL_results.ipynb` 提供 Figure 4 的曲线/带状处理，`warm_start_plot.py` 提供 Figure 10 的双 panel、log x 轴和带状处理；其余 Figure 1、3、5、6、8、9、11、12、13 及六张表没有可确认的同编号绘图/表格源。Figure 10 源代码使用 2.5/97.5 percentile，而 PDF caption 写成 “±2 standard deviation intervals”，因此该统计语义按 PDF 记录并标出源/PDF 差异。

## Figure 逐对象审计

### Figure 1（p.3，theory，inset）

- **图型与布局**：单个三维 loss-surface 概念图；轴标为 `ℓ`、`z`、`θ`，蓝色 forward path 从左上落向黑色解点，红色 backward path 沿参数方向回传。图与 §2.2 的 KKT/IFT primer 并置在右侧窄栏。
- **绘图语法**：`raster`；x/y 均无数据尺度；无网格、无 legend；直接标签；两种箭头/曲线线型；无 marker 数据序列、hatch、reference line 或 uncertainty。颜色和几何为渲染估计。
- **编码与数据**：位置/轴表达优化变量与参数方向；蓝/红表达 forward/backward 角色；曲面、黑点和箭头表达优化过程。纯机制示意，无样本、聚合、分母或不确定性。
- **Caption**：`Figure 1: DO solves optimization problems in z (the forward pass) and computes sensitivities with respect to parameters θ (the backward pass), moving along the loss surface ℓ defined by the optimization problem.` 说明标题、两条动作和 loss surface，但未在 caption 展开 DO 或定义黑点，脱离正文后部分自足；没有直接写主发现。
- **证据关系**：DO/KKT/IFT 背景（§2.2）→ OCP、SQP 和 IFT 公式（§3）→ Figure 2 的 forward/backward solver 接口 → Figure 3–4 的效率与学习结果。
- **优点**：用单个曲面和两条有方向路径把抽象的 forward/backward 分工压缩成可扫读的入口；颜色、箭头、文字标签和黑点形成冗余线索。
- **弱点**：曲面没有数值地形、步长或收敛判据；窄栏 raster 小图中蓝/红和斜向轴容易损失；caption 依赖正文术语。
- **可复用模式**：在公式前用相同解点连接正向求解路径与反向敏感度路径，随后让数据流图给出可实现的组件。
- **证据锚点**：PDF p.3 Figure 1 图面、caption 与 §2.2 段落；`rendered_observation`。

### Figure 2（p.6，method，page width）

- **图型与布局**：架构/管线图，左右信息区合并为一个页面宽对象。左侧以蓝色虚线框表示 forward pass，以红色虚线框表示 backward pass；每个框内有 `Evaluate`、`Linesearch`、Schur/PCG 矩阵块和输入/输出箭头。右侧并列 Algorithm 1/2，列出对应主步骤和公式/算法引用。
- **绘图语法**：`raster`（公开 `figs/diffmpc.jpg` 为 1600×1008 JPEG，PDF p.6 的图像与算法版面一致）；x/y 无尺度；无 legend；直接标签；虚线框、黑色流程箭头、蓝/红数据流；无不确定性或 hatch。图内文字与边界来自 raster，PDF caption 为向量文字。
- **编码与数据**：x 方向为输入→forward solve→backward sensitivity→输出；y 方向分隔 forward/backward 阶段；蓝/红箭头表示梯度与解的方向；矩阵块和淡灰色 stair 结构表示 Schur/PCG 复用；Algorithm 1/2 文字绑定 Eq. 4、8、11、3 和 Alg. 3。没有经验数据。
- **Caption**：`Figure 2: DiffMPC architecture: forward and backward passes, data flows, and main steps.` 只说明对象类型和三种内容，未定义符号、颜色、复用关系或输出含义；不能独立恢复完整流程，也没有主发现句。
- **证据关系**：§3.1–§3.3 的 SQP、Schur complement、PCG、IFT → Figure 2 的 forward/backward 接口 → Algorithm 1/2 细节 → Figure 3、Table 3、Figure 9/10 的效率、梯度和 warm-start 证据。
- **优点**：把算法伪代码和实际数据流放在同一版面，forward/backward 的蓝红方向和矩阵复用一眼可见；公开 JPEG 是可复用的直接视觉资产。
- **弱点**：算法字号很小，多个公式编号需要正文交叉阅读；raster 在窄显示器或打印后会模糊；caption 没有说明为什么 backward 可复用 forward matrices。
- **可复用模式**：将“阶段框—共享中间矩阵—伪代码行号”并排绑定，用颜色区分数据流而不是单独绘制装饰箭头。
- **证据锚点**：PDF p.6 Figure 2 图面/caption/Algorithm 1–2；GitHub `figs/diffmpc.jpg` 的直接资产核对；`rendered_observation` + `source_exact`。

### Figure 3（p.7，results，page width）

- **图型与布局**：单 panel grouped bar chart；横轴按 `Theseus/mpc.pt/trajax/DiffMPC` 与 CPU/GPU 组合排列，共 8 个设备-求解器组，每组 Forward（蓝）和 Backward（红）两根柱；柱顶印出数值，带竖向 error bars。纵轴为 `Time (ms)`，左上有 `×10^4`。
- **绘图语法**：`vector`；x 为 categorical、y 为 linear；无明显网格；右上 legend；两种颜色对应 Forward/Backward；无 marker、line series 或 hatch；error bar 是唯一不确定性编码；无 reference line。几何、色值和线宽为 `rendered_estimate`，字体/文字为 `pdf_object`。
- **编码与数据**：x 为 solver×device；y 为毫秒时间并用 `×10^4` 缩放；颜色为 pass；error bars 为 caption 所说的 `2σ confidence intervals`；图内数字是对应聚合时间的直接标签。p.6 说明 RL reward 由 50 个环境步、batch 64 的 rollout 聚合，每个 problem 平均 10 seeds，backward 含一次 forward。
- **Caption**：`Figure 3: RL computation times on one of the test problems. Error bars indicate 2σ confidence intervals. Each backward pass also includes one forward pass (to evaluate the inputs of Algorithm 2).` 说明问题范围、比较单位和误差定义，基本可独立解释；未在 caption 写出代表 problem 的配置或主 speedup 数字。
- **证据关系**：引言“GPU speedup >4×”→ §4.1 benchmark → Figure 3 的 Problem 1 主比较 → Appendix Table 3 的六问题完整 timing → Figure 10 的 warm-start 分析。
- **优点**：设备与求解器组合横向并列，柱顶数字避免从柱高反推；Forward/Backward 分组和 2σ error bars 直接支撑效率主张。
- **弱点**：Theseus CPU 的高值超过纵轴上限（Table 3 给出 49.35/81.47 s，而图轴上限约 `1×10^4 ms`），导致柱形被截断；一个 problem 的图面容易被误读为全局结果；红蓝在灰度下区分弱。
- **可复用模式**：把设备、实现和 pass 组合成单一比较轴，保留原始数值标签与误差条，同时用附录宽表承载完整 problem grid。
- **证据锚点**：PDF p.7 Figure 3、caption；p.6 §4.1 timing protocol；p.16 Table 3；`rendered_observation` + `pdf_object`。

### Figure 4（p.7，results，single column）

- **图型与布局**：右侧窄栏内两幅上下排列的 loss curve：上图 `Imitation Loss`，下图 `Model Loss`，共享 epoch 横轴。Trajax 为红色实线，DiffMPC 为蓝色虚线；两者各带半透明红/蓝带；上图右上方 legend。
- **绘图语法**：`vector`；x/y 均 linear；浅色横纵网格；共享 legend 位于上 panel 右上；无 marker；两种 line style；band 表达跨运行 spread；无 reference line。Notebook cell 2 精确给出红/蓝、`linewidth=2` 和 10th–90th percentile band，但 PDF 中蓝线为 dotted，故整体 provenance 为 `mixed`。
- **编码与数据**：x 为 0–200 epochs；y 分别是 imitation loss 与 `||θ−θ*||₂` model loss；颜色/线型区分 Trajax 与 DiffMPC，透明带承载运行间变化；两个 panel 是指标 facet。论文正文称 GPU 上 DiffMPC 约快 2× 且保持收敛。
- **Caption**：`Figure 4: Losses over 200 epochs for the pendulum cart-pole IL benchmark.` 只有标题、训练轮数和 benchmark 设置；没有颜色/线型、带状统计或两 panel 指标说明，不能脱离正文完全解释；没有直接主发现。
- **证据关系**：§4.2 cart-pole IL end-to-end training claim → Figure 4 的双指标收敛曲线 → Appendix B.2 的 dynamics、32 initial conditions、学习率和 warm-start 设置；与 Figure 3 的 solver timing 互补。
- **优点**：把任务损失和参数恢复误差分开，避免仅凭单一 loss 判断收敛；共享坐标和 uncertainty band 使两方法趋势可比。
- **弱点**：caption 未定义 band、颜色、线型、样本数或 wall-clock metric；正文的“2× faster”没有在图内给出秒数；窄栏中浅色带和蓝虚线辨识成本较高。
- **可复用模式**：用共享 x 的任务损失/参数误差双 panel，保留方法颜色、线型和分布带，并在 caption 写清 band 的统计定义。
- **证据锚点**：PDF p.7 Figure 4 与 caption；GitHub `benchmarking/imitation-learning/plot_IL_results.ipynb` cells 0–2；`rendered_observation` + `source_exact`。

### Figure 5（p.8，results，page width）

- **图型与布局**：中心是浅灰色 `RL training` 框，左侧输入 `{E^i}`、`θ`、`{x_0^i}`，堆叠环境进入 `π^θ(x)` 与 `E^i` rollout，再到右侧 loss `L`；蓝色正向、红色反向箭头。左、右各有带车辆轨迹的 `Baseline`/`Post RL` 窄照片，形成 pipeline 两端的 qualitative before/after。
- **绘图语法**：`raster`；无 x/y 数据尺度、无网格和 legend；阶段名、符号、箭头均为 direct labels；黑色框、灰色批次卡片、蓝/红方向箭头和虚线 inset；无 uncertainty/hatch/reference line。
- **编码与数据**：x 是 baseline→RL training→post-RL 的阶段顺序，y 是 environment/state、policy rollout、loss/gradient 三条 lane；堆叠卡片表示 batched environments；蓝/红箭头表示 forward/backward；两张照片是 qualitative endpoints。图本身没有数值，p.8 正文另给 `B=32`、`H=200`、1000 steps 和 14 hours。
- **Caption**：`Figure 5: Proposed RL training pipeline to robustify an MPC policy πθ(x) for drifting.` 说明 pipeline 目标和 policy，但未解释 `E^i`、`x_0^i`、蓝红箭头或照片，脱离正文不自足，也没有主发现。
- **证据关系**：§5 domain randomization 定义 → Figure 5 的环境/初始状态/政策/损失闭环 → Figure 6 模拟鲁棒性与 Figure 12 参数变化 → Figure 7–8 车辆部署。
- **优点**：把 domain randomization、可微 policy rollout 和 gradient update 收在一条流程，before/after 照片给流程增加应用锚点；方向箭头具有冗余语义。
- **弱点**：照片更像装饰性端点，没有 trial 数或成功条件；批次和 episode 长度未画出；符号密集且 caption 未给关键变量定义。
- **可复用模式**：在一个中心闭环框中同时画环境采样、可微 policy、loss 和反向更新，两侧用 matched qualitative endpoint 表示训练前后。
- **证据锚点**：PDF p.8 Figure 5–caption 与 §5 rollout 描述；`rendered_observation`。

### Figure 6（p.8，results，page width）

- **图型与布局**：左侧四个上下排列的状态/控制轨迹 panel，依次为 `β (rad)`、`ω_r (rad/s)`、`δ (rad)`、`τ (kNm)`，横轴 Timestep 0–1000；右侧为 `Baseline` 与 `Learned` 两个 N/E top-view 轨迹 panel。红/蓝主线表示 baseline/learned，细浅色线表示多次 rollout；黑色圆点为起始位置，彩色 `×` 标出 Baseline/Learned Spinout。
- **绘图语法**：`raster`；左、右 x/y 均 linear；浅色网格同时存在于方向；顶部共享 legend；直接 panel/title 与 spinout labels；solid lines、circle/x markers；无 hatch、reference line 或 uncertainty band（多条细线是 rollout，不是带状区间）。
- **编码与数据**：时间/状态与位置坐标分别承载动态响应和路径；颜色区分 baseline/learned；圆点与 `×` 区分起点和 spinout；facet 为 4 个状态/控制量加 2 个位置 panel。正文给出 20 次 randomized roll-outs，learned 成功率 100%，baseline 70%。
- **Caption**：`Figure 6: Vehicle states (left) and position trajectories (right) when drifting a figure 8 with puddles.` 说明左右区域和场景，图例补充方法与 spinout；未在 caption 写出 20 次、成功率或浅色轨迹的统计含义，因此自足性有限；没有主结论句。
- **证据关系**：Figure 5 domain-randomized pipeline → Figure 6 的模拟鲁棒性主比较 → Figure 12 学到的参数变化 → Figure 7–8 硬件转移与 Figure 13 额外硬件边界。
- **优点**：将 action/state safety buffer 与最终路径结果放在一个对象中；失败标记和初始点使 spinout 的时刻/位置可追踪；多 rollout 线展示环境随机性。
- **弱点**：六 panel 的状态量和位置量单位不同，窄线叠加后阅读密度高；成功分母和失败判据在 caption 中缺失；红蓝和浅色轨迹在灰度下不稳健。
- **可复用模式**：用“状态/动作 stack + top-view outcome”成对展示控制机制与任务结果，并用显式终止 marker 表达 failure。
- **证据锚点**：PDF p.8 Figure 6 图面/caption；§5 模拟结果段；`rendered_observation`。

### Figure 7（p.9，results，page width）

- **图型与布局**：两张照片横排：左为 top view，右为 center view。左图标出 `Water Puddle`、速度向量 `v` 与侧滑角 `β`；右图将 Supra 放在带白色虚线参考轨迹的湿滑场地，并标注 puddle、`v`、`β`。
- **绘图语法**：`raster`；无数值 x/y 轴、网格或 legend；direct labels；黑色速度/侧滑箭头、白色虚线轨迹；无 marker 数据序列、hatch、reference line 或 uncertainty。公开 `figs/supra_puddle.jpg` 1600×397 JPEG 与右侧 center-view 面板直接匹配。
- **编码与数据**：空间场景与相机视角是主要编码；自然图像颜色承载车辆、道路和水坑，蓝字标出 puddle，黑箭头标出 `v` 与 `β`，白虚线表示运动/参考路径；没有试验次数、尺度或统计聚合。
- **Caption**：`Figure 7: Vehicle drifting through a water puddle: top view (left) and center view (right). The Supra robustly drifts despite variations in friction, which requires carefully selecting actions and sideslip.` 说明视角、车辆、现象和控制含义，能理解图像职责；仍没有重复次数或成功定义，主结论以文字直接陈述。
- **证据关系**：§5 的 figure-8 domain-randomized training → donut 车辆部署 → Figure 7 的硬件 qualitative anchor → Figure 8 的状态/轨迹定量化展示。
- **优点**：把空间 puddle、车辆姿态和侧滑几何直接标在照片上；top/center 两视角互补，公开原图便于复用。
- **弱点**：照片无法单独验证“robustly”或 friction variation；水坑边界、尺度和时间序列未量化；蓝色标注依赖颜色和小字。
- **可复用模式**：用 top/center 双视角照片固定物理场景，再以少量向量箭头连接可测的运动量，避免让照片承担统计证据。
- **证据锚点**：PDF p.9 Figure 7/caption；GitHub `figs/supra_puddle.jpg`；`rendered_observation` + `source_exact`。

### Figure 8（p.9，results，page width）

- **图型与布局**：左侧 2×3 小 panel 画 `r`、`v`、`β`、`ω_r`、`δ`、`τ` 随 `Path distance (m)` 的轨迹，右侧一幅 N/E top-view 轨迹图。Baseline 红、Learned 蓝；右图标出灰色 `Water Patch`、起始圆点和失败 `×`，legend 位于右上。
- **绘图语法**：`raster`；左侧 path distance 和右侧 Position E/N 均 linear；浅色 x/y 网格；右图 legend 同时服务左右方法语义，direct label 为 Water Patch；circle/x markers、solid lines；无 uncertainty band/hatch/reference line。
- **编码与数据**：x 分别为 path distance 和 Position E，y 为六个状态/控制量和 Position N；颜色区分 baseline/learned；起始圆点和 spinout `×` 表达事件；facet 为 six state panels + top-view。对象是 donut 水坑中的两次 run，不是多次汇总。
- **Caption**：`Figure 8: Drifting a donut with a water puddle with a Toyota Supra. Left: State trajectories of two runs with the baseline vs the learned policy. Right: top view trajectories of the runs.` 说明车辆、场景、左右区域和两种 policy，图例/轴补齐含义；未在 caption 写出 baseline spinout、learned completion 或样本限制。
- **证据关系**：Figure 7 物理场景 → §5 donut transfer claim → Figure 8 的 matched state/trajectory comparison → Appendix Figure 13 的 figure-8 on-vehicle response。
- **优点**：把控制量变化和 top-view 结果对齐；灰色 puddle、起始点和 `×` 将 failure 位置绑定到轨迹；方法颜色跨 panel 稳定。
- **弱点**：只展示两次 run，不能替代硬件重复统计；小 panel 标签和浅色网格较细；caption 没有说明终止/失败标记。
- **可复用模式**：将状态 stack 与空间路径共享 baseline/proposed 颜色，并在空间 panel 放置环境干扰区域和事件 marker。
- **证据锚点**：PDF p.9 Figure 8/caption 与 §5 deployment 段；`rendered_observation`。

### Figure 9（p.15，ablation，single column）

- **图型与布局**：单 panel boxplot；x 为六个 PCG tolerance 类别 `1e-12`、`1e-10`、`1e-8`、`1e-6`、`1e-4`、`1e-2`，y 为 `Cosine Similarity`。箱体和须为黑色，median 线为绿色，离群点以小圆点显示。
- **绘图语法**：`raster`；x 视觉上为 categorical（log-spaced labels 等距排列），y linear；浅色 x/y 网格；无 legend、无 direct series labels；一个 outlier marker 类型、一个 box/whisker line style；`uncertainty_display=box`；无 hatch/reference line。
- **编码与数据**：横轴 tolerance，纵轴 computed gradient 与 finite-difference gradient 的 cosine similarity；箱体表达分布、须和离群点表达 spread/outliers；100 problem instances per tolerance。正文观察到 tolerance 到 `10^-6` 前一致性较高，其余结果使用 `10^-12`。
- **Caption**：`Figure 9: Cosine similarity between computed gradients and finite differencing across PCG exit tolerances.` 说明比较量和横轴；未定义 boxplot 统计、样本量或 cosine interpretation，独立可读性不足；没有主发现句。
- **证据关系**：§3.4 对 gradient accuracy 的限制 → Figure 9 tolerance diagnostic → `10^-12` 的主实验选择 → Figure 10 warm-start/tolerance trade-off。
- **优点**：用完整分布而非均值展示 tolerance 对梯度一致性的影响；outlier 暴露高 tolerance 的退化。
- **弱点**：x 标签按 log 值但视觉等距，容易误读间隔；caption 未写 100 instances、箱体定义和阈值；只覆盖一个 QP 问题族。
- **可复用模式**：当 solver tolerance 是机制旋钮时，用分布图展示 accuracy degradation，并在 caption 写清实例数、箱体统计和选择阈值。
- **证据锚点**：PDF p.15 Figure 9/caption 与 B.1 accuracy 段；`rendered_observation`。

### Figure 10（p.15，ablation，page width）

- **图型与布局**：两幅共享 y 轴的 line chart，上方标题为 `Forward Pass`、`Backward Pass`。x 为 log-scaled `PCG Tolerance ε`，y 为 `Speedup (%)`；每幅一条蓝线并带半透明蓝 band，网格同时贯穿方向。
- **绘图语法**：`raster`；x log、y linear；x/y grid；无 legend、无 marker、无 direct label；一个 solid line style；band 表达 interval；无 hatch/reference line；公开 `benchmarking/reinforcement-learning/warm_start_plot.py` 精确设置 `set_xscale("log")`、three tolerance ticks、`linewidth=2`、blue `fill_between` 和两个 shared-y panels。
- **编码与数据**：x 是 PCG tolerance，y 是 `(cold−warm)/cold` 的百分比；facet 区分 forward/backward；蓝线是均值，band 是 interval。论文写明每个 tolerance 100 independent runs，`10^-12` 约 4%/4%，`10^-4` 约 11%/9%。
- **Caption**：`Figure 10: RL with DiffMPC: Speedups from warm-starting (SolveTime_cold − SolveTime_warm) / SolveTime_cold for different PCG exit tolerances ε, with ±2 standard deviation intervals.` 定义 speedup、横轴和误差，基本自足；没有在 caption 写 100 runs 或具体数值。
- **证据关系**：PCG 可 warm-start 的方法主张 → p.7 warm-start 段 → Figure 10 tolerance response → 其他结果固定 `ε=10^-12` 的配置边界。
- **优点**：共享 y 轴和 log x 轴使两种 pass 的 tolerance 趋势直接可比；带状区间不遮挡主线，图面简洁。
- **弱点**：源代码的 `np.percentile(..., 2.5/97.5)` 与 PDF caption 的“±2 standard deviation”不是同一统计定义；没有 raw timing 或 sample count 在图内；速度提升幅度较小，需结合正文数字阅读。
- **可复用模式**：对一个 solver knob 用 shared-y 双 panel，同时画均值和 spread，并把 speedup 定义写入 caption；源代码与 caption 应采用同一误差术语。
- **证据锚点**：PDF p.15 Figure 10/caption 与 p.7 warm-start 段；GitHub `benchmarking/reinforcement-learning/warm_start_plot.py`；`rendered_observation` + `source_exact`。

### Figure 11（p.18，appendix，single column）

- **图型与布局**：单 panel noisy line chart；横轴 `Time (Minutes)`，纵轴 `Loss`；三条细实线代表 3 seeds，使用绿色、红色和蓝色；没有 legend、均值线或 band。曲线大约在 40 分钟后围绕低值波动，仍有尖峰。
- **绘图语法**：`raster`；x/y linear；浅色网格；无 legend、无 direct labels、无 marker；一条 solid line style；无 uncertainty display、hatch 或 reference line；所有样式为 rendered estimate。
- **编码与数据**：x 是 wall-clock minutes，y 是 training loss，颜色只区分 seed；3 条曲线和尖峰展示 run-to-run noise。B.3 文字定义 first 100 steps、batch `B=32`、RTX 3090、18 GB VRAM，并称约 40 minutes 后稳定。
- **Caption**：`Figure 11: Wall-clock training loss curves across 3 seeds.` 说明时间、loss 和 seed 数，但未定义颜色到 seed 的映射、硬件、band/mean 或稳定标准；没有主发现句。
- **证据关系**：B.3 drifting RL training procedure → Figure 11 的运行噪声/时间诊断 → Figure 12 的参数变化 → Figure 13 的硬件响应。
- **优点**：保留三条 seed 曲线而非只画平滑均值，能看到尖峰和稳定时间；时间轴与训练成本直接相关。
- **弱点**：无 legend，颜色与具体 seed 无法追踪；“stabilizes”没有预定义阈值或汇总统计；caption 未写 first 100 steps 和 memory footprint。
- **可复用模式**：将少量 seed 的原始 wall-clock traces 作为训练动态补充图，并在图注给出时间窗、硬件和 seed 聚合规则。
- **证据锚点**：PDF p.18 Figure 11/caption 与 B.3 training dynamics 段；`rendered_observation`。

### Figure 12（p.19，appendix，single column）

- **图型与布局**：两幅横向 bar chart。上 panel `Dynamics variable` 有 `I_w,C_f,C_r,μ_f,μ_r` 五行；下 panel `Cost variable` 有 `ω_r,r,s,v,δ,Δφ,τ,e,β` 九行。横轴均为 `Percent change vs nominal`，0% 虚线作为 reference；每根浅紫色 bar 右侧印出百分比。
- **绘图语法**：`raster`；x linear，y categorical；竖向 x grid；无 legend；百分比 direct labels；横向 bars、一个 solid outline；无 uncertainty/hatch；一条零值 reference line。颜色与精确线宽为 rendered estimate。
- **编码与数据**：横向长度和正负方向表达相对 Table 5 nominal 的参数变化；上下 facet 分开 dynamics 与 cost；标签承载变量名和百分比。可见数值为 dynamics `I_w +3.58%`、`C_f +0.37%`、`C_r +0.11%`、`μ_f −4.54%`、`μ_r −13.38%`；cost `ω_r +7.38%`、`r 0.00%`、`s 0.00%`、`v −0.44%`、`δ −0.46%`、`Δφ −0.62%`、`τ −1.94%`、`e −7.50%`、`β −58.00%`。
- **Caption**：`Figure 12: Change in MPC policy parameters after learning θ by domain randomization that includes water puddles.` 说明相对变化、学习对象和 puddle 条件；未在 caption 定义 nominal reference、上下分组或数值标签，不能完全脱离 Table 5。
- **证据关系**：Table 5 nominal vehicle/cost parameters → Figure 12 learned-minus-nominal diagnostic → §5 对 rear friction `−13%` 与 sideslip cost `−58%` 的解释 → Figure 6、8、13 robustness response。
- **优点**：0% reference、signed labels 和 horizontal bars 同时展示方向与大小；把 dynamics 与 cost 变量拆成两个语义组，直接暴露不对称学习变化。
- **弱点**：近零变化的 bars 在窄图中几乎不可见；变量数量多且 Greek/下标标签细小；没有 parameter uncertainty、seed spread 或 learned absolute values。
- **可复用模式**：对学习前后参数使用带零线的 signed horizontal bars，按机制角色分组，并在标签中保留相对百分比。
- **证据锚点**：PDF p.19 Figure 12/caption、B.3 additional simulation 段；`rendered_observation`。

### Figure 13（p.19，appendix，page width）

- **图型与布局**：与 Figure 8 相似的复合图：左侧 2×3 状态/控制 panel（`r`、`v`、`β`、`ω_r`、`δ`、`τ` 对 path distance），右侧 N/E top-view trajectory。Baseline 红、learned 蓝；起始黑圆点、终止/失效 `×`，右图灰色 `Water Patch`，legend 在右上。
- **绘图语法**：`raster`；x/y linear；浅色 x/y grid；共享方法 legend；direct label 为 Water Patch；circle/x 两类 marker、solid lines；无 uncertainty band/hatch/reference line。样式为 rendered estimate。
- **编码与数据**：横轴是路径距离或位置 E，纵轴是状态量或位置 N；颜色表示 baseline/learned；圆点和 `×` 表示起点/结束状态；facet 为 six states + top view。正文明确每种方法只进行一次 hardware run。
- **Caption**：`Figure 13: On-vehicle test for drifting a figure 8 trajectory with a water puddle in the first turn. Closed-loop response task with the baseline (red) and the learned controller (blue).` 说明任务、puddle、闭环和颜色，基本可读；未写一次试验的限制。
- **证据关系**：Figure 6 的模拟 figure-8 robustness → Figure 13 的额外 on-vehicle response → Figure 12 learned parameters 和 p.19 对单次硬件试验的限制声明。
- **优点**：将 baseline spinout 与 learned completion 在状态和空间轨迹上对齐；caption 明确红/蓝含义；作者在相邻正文主动披露单次试验边界。
- **弱点**：单次 run 不能支持通用硬件成功率；spinout 后的 baseline 曲线提前终止，比较区间不完全相同；无重复/误差或时间成本编码。
- **可复用模式**：对 closed-loop hardware 结果同时画状态响应和空间轨迹，并用显式 failure marker 和 caption 限定试验分母。
- **证据锚点**：PDF p.19 Figure 13/caption 与 additional hardware 段；`rendered_observation`。

## Table 逐对象审计

### Table 1（p.14，single column）

- **结构**：2 个数据行（Fwd、Bwd），3 列（Pass、Original、Modified），1 层表头，0 个 row group；秒为单位，1 位小数；booktabs 顶/中/底横线，无竖线。
- **表头与编码**：`Pass | Original | Modified`；Modified 的两项数值加粗，表示针对 Problem 1 GPU 的修改后 timing。数据为 Fwd `67.9→1.9`、Bwd `136.4→4.5`。
- **不确定性与统计**：单个 Problem 1 的 GPU timing；B.1 说明是 first seed，表内没有重复、误差、设备细节或失败列。
- **Caption**：`Table 1: Computation times (s) with mpc.pytorch for Problem 1 on the GPU.` 只有指标、单位、实现、problem 和设备，未写“修改显著降低”的结论。
- **证据关系**：mpc.pytorch serial list-comprehension 修改 → Table 1 → Figure 3 公平 baseline → Table 3 六问题 timing。
- **优点**：小表直接呈现 modification ablation，单位和 pass 方向明确，加粗突出了变化。
- **弱点**：first seed 单点容易被当作普遍 speedup；没有原始 timing protocol、variance 或修改成本。
- **可复用模式**：对 baseline implementation change 使用最小双行 paired table，并把重复/统计范围写进 caption 或紧邻正文。
- **证据锚点**：PDF p.14 Table 1、B.1 Setup 段；`pdf_object` + `rendered_observation`。

### Table 2（p.15，single column）

- **结构**：6 个 problem 行、6 列（Problem、`n_x`、`n_u`、`T`、`H`、`B`），1 层表头、0 个 row group；全部整数，0 位小数；booktabs 横线，无竖线和高亮。
- **表头与编码**：state/control dimensions、MPC horizon、RL episode length、batch size 均显式命名；Problem 列保持 1–6 的配置索引。
- **不确定性与统计**：配置表，不承载抽样不确定性；没有 seed、分布参数、solver tolerance 或硬件列。
- **Caption**：`Table 2: Parameters in the RL benchmark: state dimension n_x, control dimension n_u, MPC horizon T, RL episodes length H, batch size B.` 定义列和符号，足以恢复配置，但 “episodes length” 语法略不自然。
- **证据关系**：§4.1 随机 QP → Table 2 problem grid → Figure 3 Problem 1 → Table 3 全部 timing。
- **优点**：用一张小表让 problem size、episode horizon 和 batch 组合可复现；列语义比正文散列数字更清楚。
- **弱点**：没有把 `A,B,b,Q` 的随机化范围或 10 seeds 放进表中；没有标注每个配置对应的设备/solver。
- **可复用模式**：将影响计算成本的 state/control/horizon/batch 作为同行配置表，随后用结果表引用同一 Problem ID。
- **证据锚点**：PDF p.15 Table 2/caption 与 B.1 QP problems 段；`pdf_object` + `rendered_observation`。

### Table 3（p.16，page width）

- **结构**：16 个数据行（4 methods × 2 GPU flags × 2 passes）、9 列（Method、GPU、Pass、Problem 1–6），1 层表头，8 个 method/device row groups；booktabs/partial group rules，无竖线。数值均为 mean `(2σ)`，均保留 2 位小数。
- **表头与编码**：GPU 列用 `✗/✓`，Pass 用 Bwd/Fwd，Problem 1–6 横向铺开；GPU 下的 DiffMPC Fwd/Bwd 单元加粗，表示相对表内比较的低 timing，而没有 underline/颜色。
- **不确定性与统计**：caption 写 mean time 与 `2σ (s)`；B.1 说明每个 problem/method 10 independent runs。括号是 2σ，不是第二个指标；表内未列失败、speedup 或置信检验。
- **Caption**：`Table 3: Timing results on CPU and GPU across 6 problem instances with mean time and 2σ (s).` 定义设备、problem 数、聚合和单位；没有解释 GPU checkmark 或 pass 缩写。
- **证据关系**：Figure 3 单个 representative problem → Table 3 六 problem 的完整 CPU/GPU 结果 → Table 4 非线性 attitude 扩展 → Figure 10 tolerance/warm-start 机制。
- **优点**：设备、pass 和 problem 维度一次展开，保留 CPU 上 DiffMPC 较慢的反例；row group 让四个 solver 的 paired Fwd/Bwd 容易扫读。
- **弱点**：9 列宽表迫使字体变小；2σ 的实验分母在 caption 之外；正文主张的 speedup 需要读者自行跨列计算。
- **可复用模式**：将实现×设备×pass 组织为分组行、将 problem instances 组织为列，并以 mean (spread) 保持完整性；不要只保留最佳 GPU 行。
- **证据锚点**：PDF p.16 Table 3/caption、B.1 timing protocol；`pdf_object` + `rendered_observation`。

### Table 4（p.16，single column）

- **结构**：4 个数据行（trajax/DiffMPC × Fwd/Bwd）、3 列（Method、Pass、Compute time (ms)），1 层表头、2 个 method row groups；booktabs 横线、无竖线；1 位小数，括号内 spread 同为 1 位。
- **表头与编码**：method、pass 和毫秒单位明确；trajax 与 DiffMPC 由分组横线区分，没有加粗或 best marker。
- **不确定性与统计**：`310.6 (24.1)`、`505.0 (27.4)`、`39.8 (8.0)`、`69.3 (16.1)` 的括号数值存在，但 caption 没有定义是 SD、2σ 还是其他 spread；正文给 batch 16 和随机初值/惯性范围，却没有明确重复数。
- **Caption**：`Table 4: Compute times on the nonlinear attitude stabilization problem.` 只说明任务与指标，未说明设备、spread 语义或配置。
- **证据关系**：B.1 nonlinear attitude stabilization → Table 4 对比 Trajax/DiffMPC → 支持“约 7.3× backward speedup”→ 与 Table 3 的线性 QP timing 互补。
- **优点**：任务、pass、单位和两种方法紧凑呈现；分组横线足以表达 paired comparison。
- **弱点**：括号统计含义未定义，设备列缺失；没有把 horizon、batch 或随机化分母放入表头/表注。
- **可复用模式**：为新增非线性任务复用相同 Method×Pass 表，但必须在 caption 中声明设备、重复数和括号统计定义。
- **证据锚点**：PDF p.16 Table 4/caption 与 B.1 段落；`pdf_object` + `rendered_observation`。

### Table 5（p.17，page width）

- **结构**：1 个 `Value` 数据行，19 列（左侧 stub 加 18 个参数列），1 层表头，0 个 row group；booktabs 顶/底横线，无竖线或高亮；精度混合（科学记数法、整数、1–3 位小数），因此 `decimal_precision=null`。
- **表头与编码**：列依次为 `Q11…Q77`、`R11,R22`、`a,b,m,rw`、`μf,μr`、`Cf,Cr`、`Iw`；value 行给出 nominal model/gain。表头采用数学字体，正文参数与单位解释放在 B.3。
- **不确定性与统计**：纯配置点值，无 seed、分布或不确定性。表中值包括 `μf=0.99`、`μr=0.90`、`Cf=54000`、`Cr=220000`、`Iw=11.28` 等。
- **Caption**：`Table 5: Nominal vehicle model parameters and controller gains for the baseline MPC policy.` 说明表的角色，但不定义每个符号或单位，依赖紧邻 vehicle model 段。
- **证据关系**：B.3 vehicle model/MPC → Table 5 nominal reference → Figure 12 relative parameter changes → Figure 6/8/13 learned policy response。
- **优点**：把高维 nominal vector 固定成可复现基线，并与 Figure 12 的相对变化形成直接键值关系。
- **弱点**：18 个参数压在单行，字体和符号很小；列没有单位，变量含义分散在正文；没有 learned absolute values 或 uncertainty。
- **可复用模式**：对高维 controller/model configuration 使用宽表保存一行 nominal vector，同时为后续 change plot 保留完全一致的列顺序。
- **证据锚点**：PDF p.17 Table 5/caption 与 B.3 model 段；`pdf_object` + `rendered_observation`。

### Table 6（p.18，page width）

- **结构**：1 个 `range` 数据行，9 列（stub 加 8 个参数列），1 层表头，0 个 row group；booktabs 横线、无竖线和高亮；区间含整数和小数，精度混合。
- **表头与编码**：`μf`、`μr`、`10^{-3}Cf`、`10^{-3}Cr`、`Iw`、`s0`、`ℓpuddle`、`spuddle`；range 行分别为 `[0.94,1.04]`、`[0.85,0.95]`、`[52,56]`、`[200,240]`、`[8,14]`、`[185,700]`、`[0,5]`、`[-1,1]`。
- **不确定性与统计**：这些是 uniform domain-randomization ranges，不是统计误差区间；caption 另写 water puddle tire friction drops to `μ=0.6`。随机化分布和符号解释由 p.18 正文补充。
- **Caption**：`Table 6: Parameters used for domain randomization. On water puddles, tire friction coefficients drop to μ = 0.6.` 同时给出表用途和关键 puddle 条件，但没有说明 `s0`、puddle length/spacing 的单位。
- **证据关系**：Figure 5 采样环境 → Table 6 randomization ranges → Figure 6 20 rollout robustness → Figure 12 learned parameter shift。
- **优点**：一行覆盖 friction、stiffness、inertia 和 puddle geometry，便于复现实验抽样；caption 把水坑摩擦变化直接暴露。
- **弱点**：单位、uniform 分布和 `s0/ℓpuddle/spuddle` 语义依赖正文；没有 random seed、实际样本或逐因素 ablation。
- **可复用模式**：把 domain-randomization 参数与范围集中成一行表，并在 caption 明确特殊环境值，正文再定义单位和抽样分布。
- **证据锚点**：PDF p.18 Table 6/caption 与 B.3 Randomization 段；`pdf_object` + `rendered_observation`。

## 跨对象系统判断

- **视觉叙事**：Figure 1/2 先固定 DO 与 DiffMPC 的机制接口，Figure 3/4 给出 solver/learning efficiency，Figure 5/6 将机制嵌入 domain-randomized drifting，Figure 7/8 以真实车辆和转移任务收束；Figure 9/10 与 Tables 1–4 在 appendix 提供 accuracy、warm-start、规模和非线性扩展，Figures 11–13 与 Tables 5–6 补充训练动态、参数机制和硬件边界。
- **Caption 系统**：所有 caption 采用 `Figure/Table n:`，正文 Figure caption 大多只给对象标题和设置；Figure 3、10 明确 uncertainty，Table 3 明确 mean/2σ，其他 caption 对颜色、panel、样本数和 failure marker 的定义不足。caption 没有粗体结论标题。
- **表头系统**：表格普遍是 1 层数学表头、booktabs 横线和无竖线；Method/Pass/Problem 等分组列靠 row rules 或上下文表达，Table 3 以 GPU checkmark 与方法分组承载多维比较，Table 5/6 以宽向量/范围行服务复现。
- **方法—结果—消融**：Figure 2 的流程对应 Eq. 4、8、11 和 Algorithm 1/2；Figure 3/4 直接承接正文效率与 IL 结果；Figure 9/10 是最清晰的独立消融（tolerance 与 warm-start），Table 1 的实现修改是 baseline ablation，Table 3/4 负责跨 problem/非线性外推。
- **正文—附录链接**：正文主图 3–8 用 caption 和正文段落给出 headline；Table 3、Figure 9/10、Figures 11–13、Tables 1/2/4/5/6 提供对应完整设置或边界。Figure 13 的单次硬件限制是与 Figure 7/8 qualitative strength 同时出现的明确边界。
- **字体一致性**：caption、表格和正文由 Nimbus Roman No9 L 与 Computer Modern math 保持一致；Algorithm/方法名使用等宽 Nimbus Mono L；嵌入 raster 图的 sans/数学字形和字号存在估计误差，Figure 2 与车辆照片的图内字形不完全统一。
- **颜色一致性**：主比较反复使用红=baseline/Trajax、蓝=learned/DiffMPC，附录 Figure 10/12 使用蓝/紫单色；Figure 11 以三色区分 seed，表格维持黑白。红蓝语义稳定，但未在所有 caption 重复定义，灰度可读性依赖线型、marker 或面板位置。

## 最终判断

- **最可复用模式**：
  1. 用 Figure 2 的“阶段框—共享矩阵—算法步骤”把方法图直接绑定到公式和伪代码。
  2. 用 Figure 6/8/13 的“状态 stack + top-view trajectory + failure marker”同时承载机制、任务结果和失败边界。
  3. 用 Table 3 的实现×设备×pass 行分组与 problem 列展开保留完整效率证据，而不是只展示最佳 GPU 单元。
  4. 用 Figure 10 的 shared-y、log tolerance 诊断图展示单一 solver knob 的效率响应，并同步定义 uncertainty。
- **最高价值对象**：Figure 2（方法接口）、Figure 3（GPU efficiency headline）、Figure 6（20-rollout simulation robustness）、Figure 8（硬件 transfer qualitative comparison）、Table 3（六问题完整 timing）。Figure 13 的价值主要在于主动限定 hardware evidence，而非提供稳定统计估计。
- **失败模式**：Theseus CPU 柱被 Figure 3 的纵轴截断；Figure 4/9/11/12/13 的 caption 缺少样本/统计或颜色定义；Figure 9 把 log tolerance 等距显示；Figure 10 源代码 percentile band 与 caption 的 ±2σ 术语不一致；Table 4 括号统计、Table 5/6 单位依赖正文；窄栏 raster 与宽表小字体影响可读性。
- **一句话视觉策略**：论文以红蓝方法对照和“求解器机制→GPU/IL效率→随机化漂移→车辆证据”的连续路径组织图表，再把 tolerance、配置、参数和硬件重复限制下沉到附录。
