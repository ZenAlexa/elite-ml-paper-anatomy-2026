# `iclr-2026-7833e39d5371` 视觉审计

## 审计边界与对象清单

- 论文：*Taming Momentum: Rethinking Optimizer States Through Low-Rank Approximation*。
- PDF：`corpus/pdfs/iclr-2026-7833e39d5371.pdf`，官方 proceedings 版本，24 个物理页，Letter `612 × 792 pt`。
- 阅读范围：逐页读取 pp.1–24，包括正文、Conclusion、References、Appendix A–E；没有 supplementary PDF。
- 渲染：使用 `pdftoppm -r 200 -png` 将 pp.1–24 渲染为 200 dpi PNG；先用全页接触表核对所有页，再对 p.4、p.7–p.9、p.23 的对象使用原始渲染图复核。PDF 对象、文字和图像清单用 `pdfinfo`、`pdffonts`、`pdfimages -list`、`pdftohtml -xml` 交叉核对。
- bbox 约定：下文 `bbox` 使用 PDF point，原点在页面左上角；范围是对象（含 caption）的大致外接框。
- PDF 实际对象：3 幅 Figure（Figure 1–3）和 4 张 Table（Table 1–4）。Appendix 的 Algorithm 1（p.15）和 Algorithm 2（p.17）是算法伪代码，不属于 Figure/Table，因此不放入 schema 的 `figures`/`tables` 数组；其存在已在清单中单独注明。References（pp.10–12）和 Appendix E（p.24）没有新增图表。

| 对象 | 物理页 | 版面 | 视觉职责 |
|---|---:|---|---|
| Figure 1 | 4 | 正文、双栏宽 | EMA 等价、低秩压缩和闭式更新的理论/方法接口 |
| Table 1 | 7 | 正文、双栏宽 | C4 预训练跨模型规模的主比较 |
| Table 2 | 8 | 正文、双栏宽 | 两个数学任务、两个模型和两个优化器族的微调主比较 |
| Figure 2 | 8 | 正文、双栏宽，两面板 | rank efficiency 消融 |
| Figure 3 | 9 | 正文、右栏单栏 | 不同 rank 的训练轨迹与收敛机制 |
| Table 3 | 9 | 正文、跨栏居中 | Muon 优化器迁移/低秩消融 |
| Table 4 | 23 | 附录、跨栏居中 | β₁/β₂–γ coupling 的敏感性和失败值 |

## 公开视觉源获取

PDF 首页在摘要末明确给出 `https://github.com/mrflogs/LoRA-Pre`。使用 `gh repo view mrflogs/LoRA-Pre`、`gh api repos/mrflogs/LoRA-Pre/contents` 和 `gh api 'repos/mrflogs/LoRA-Pre/git/trees/main?recursive=1'` 只读查看了 `main`；仓库描述与 README 标题严格匹配论文，但递归树只有 `LICENSE` 和 `README.md`。README 明确写明代码和训练脚本仍在准备中。以论文完整标题执行 `gh search repos` 后，唯一严格相关命中仍为该仓库；未把同名清单或优化器合集当作来源。仓库没有 `plot*`、`figure*`、`visual*`、`table*`、`.ipynb`、`.tex`、`.tikz`、`.pgf`、`.svg` 或 style/data 文件可对应本论文对象。

因此 `source_acquisition.status` 为 `repository_without_visual_source`，保留该明确仓库作为候选/选中仓库，但 `visual_source_files` 为空。Figure 2 和 Figure 3 在 PDF 中是栅格 XObject（p.8：`1860×1380`、`1859×1380`；p.9：`2340×1433`，均带 soft mask），Figure 1 和表格为 PDF 矢量/文字对象；这些 PDF 内部资产不能替代公开源文件。

## 全文风格

- 正文和表格主要使用嵌入的 Nimbus Roman No9 L/Computer Modern；正文约 10 pt，表格主体约 10 pt。数学符号由 Computer Modern Math 系列承载，图 1 的示意图还嵌入 Cambria Math 与 Comic Sans MS。Figure 2–3 的文字嵌在栅格图像内，只能按渲染估计为 DejaVu Sans 风格。
- 表格采用黑色文字、Royal Blue 引用链接 `#4169E1`，LoRA-Pre 行使用浅青色 `#CCE6E6`；图 2–3 使用 Matplotlib 风格的蓝/橙/绿/红四色。颜色不是所有对象的唯一判别方式：表格另用粗体/下划线，图 2 用红色虚线区分 Adam；但图 2–3 的四条曲线仍缺少 marker/线型冗余，灰度下不易区分。
- 正文图表围绕“方法接口 → 跨规模主比较 → rank 消融 → optimizer transfer”推进，Table 4 后置为附录敏感性证据。主表和图都只给单次点估计，没有 seed、重复、误差条或误差带；表格 caption 对设置的说明明显比图 3 完整。

## Figure 1

- **位置与分类**：p.4，`main`，双栏宽；类型 `pipeline` + `conceptual_diagram`；用途 `method_interface` + `theory_mechanism`。bbox 约 `[90, 52, 522, 263]`。
- **版面与复杂度**：单个浅奶油色虚线框，内部按左→右组织为“EMA 更新/等价形式”→“Low-Rank Compression”→“m_B、m_A 因子及 Online Gradient Flow 更新”。有上/下两条局部数学路径和一条中心大箭头；无 legend，标签直接放在结构旁。复杂度 3/5：虽然只有一面板，但同时有多个公式、两个方向箭头、时间轴虚线和因子化结构。约 8 个直接文字/公式注释，数据标记数为 0。
- **绘图语法**：PDF 矢量；x/y 均无坐标尺度，网格无；无 legend；直接标签为真；无 marker；内部实线与时间轴/外框虚线两类线型；无 hatching、无 reference line、无不确定性；内部形状轮廓约 0.65 pt，外框约 0.98 pt（PDF 对象换算，单值字段取内部主线）。
- **字体**：图题和结构标签为 Comic Sans MS regular/bold；公式为 Cambria Math。PDF font object 显示图内约 5.3–8.7 pt，集中约 8 pt；普通/粗体并存，数学字形含斜体效果。字体数值和家族以 PDF object 为依据，高置信度。
- **颜色与编码**：背景 `#FFFBF0`；中心“Low-Rank Compression”右箭头 `#83CBEB`；“Equivalent Form”双向箭头和 m_A 梯形 `#C1E5F5`；m 方块和 m_B 梯形 `#FBE3D6`；梯度历史/时间轴灰色约 `#AEAEAE`；黑色文字和轮廓。颜色模式 `mixed`，约 5 个有语义的颜色；颜色与形状、左右位置和直接文字共同冗余编码，灰度下仍可由文字/形状读懂。
- **编码字段**：x 为左到右的计算阶段；y 为 EMA/在线梯度流的上下对应关系；color 为阶段/因子角色；shape 为箭头、矩形和上下梯形；line 为方向箭头、梯度历史虚线和外框虚线；facet 无；text 为公式、`Equivalent Form`、`Low-Rank Compression`、`Online Gradient Flow` 和 update 标签。
- **数据与统计**：没有经验数据、聚合、分母、单位或不确定性。`m`、`m_B`、`m_A`、`g_t` 和目标函数是符号对象；箭头表示 EMA 与在线回归的等价改写，以及低秩因子更新。
- **Caption**：`Figure 1: Illustration of our LoRA-Pre method. In this work, we establish a novel connection: the exponential moving average (EMA) update for optimizer momentum is mathematically equivalent to training a linear regressor using online gradient descent. Leveraging this equivalence, we propose compressing the optimizer states (i.e., the momenta) using low-rank matrices to reduce the memory footprint. Finally, the closed-form update rules for these matrices without requiring back-propagation are given by Theorem 3.1.` 共 72 个空白分词。标题为粗体；moves=`title, setup, comparison, main_finding`；脱离正文基本可理解，且明确写出 EMA 等价、压缩和 Theorem 3.1，`self_contained=true`、`main_finding_stated=true`。
- **证据关系**：Introduction 的 EMA/optimizer-state 问题 → Section 3.2 式 (7)–(9) 的在线回归等价 → Section 3.3 式 (10)–(12) 与 Theorem 3.1 的低秩 Newton 更新 → Figure 1 汇总接口 → Appendix A pp.13–14 给出证明。它承担方法接口和理论机制，不承担实验结论。
- **优点**：把目标函数、等价形式、压缩动作和最终更新放在同一阅读路径中；公式与直写标签配对，读者可以从 EMA 直接追到低秩因子；形状、方向和浅色填充构成一定的冗余提示。
- **缺陷**：Comic Sans MS、奶油背景和 pastel 填充与全文 Times/Nimbus 风格不一致；小尺寸渲染时数学式和二阶导数逆矩阵拥挤；没有单独解释每个矩阵维度、灰色历史线或箭头语义，复现仍需回看 Section 3.3/Appendix A。
- **可复用模式**：用一个左→右流程把“原始更新 → 等价优化目标 → 压缩参数化 → 可执行更新”串起来，并将关键公式直接放在对应节点旁，而不是只画抽象模块。

## Figure 2

- **位置与分类**：p.8，`main`，双栏宽；类型 `line`；用途 `ablation` + `robustness` + `main_comparison`。由两个并列子图组成：(a) 60M，(b) 130M；bbox 约 `[108, 462, 504, 607]`。
- **版面与复杂度**：两面板、各四条系列、每条四个 `x` 点；图题位于各面板顶部，legend 位于右上角，各面板各自重复一份 legend，面板下方有 `(a)`/`(b)`。复杂度 3/5，约 16 个数据标记、2 个面板注记、每面板 4 个 legend 条目。
- **绘图语法**：栅格图像；x 为 categorical rank 位置（a=`4,16,64,128`，b=`16,64,128,256`，间距视觉上等距），y 为 linear perplexity；两方向均有浅灰网格；legend 在每面板 upper-right，不共享；无 direct labels；所有系列均为 `x` marker，线型为实线和 Adam 红色虚线两类；无 hatching；每面板 1 条横向 Adam reference line；无不确定性；线宽按栅格估计约 1.5 pt。
- **字体**：图题、轴标题、tick 和 legend 为嵌入图像内的 DejaVu Sans 风格，约 6.5–11 pt（中位约 8 pt），regular/roman；这是渲染估计，中置信度。
- **颜色与编码**：四条系列分别为 GaLore `#1F77B4`、LoRA-Pre Adam `#FF7F0E`、LoRA-Pre Muon `#2CA02C`、Adam `#D62728`；categorical palette，4 色。所有曲线都用相同 `x` marker，只有 Adam 另外用红色虚线，因此颜色仍是前三条方法的主要区分方式；灰度安全性为假。
- **编码字段**：x=rank；y=perplexity（越低越好）；color=optimizer/method；shape=统一 `x` marker；line=方法实线与 Adam 基线虚线；facet=模型规模 60M/130M；text=面板标题、轴名、legend、(a)/(b)。
- **数据与统计**：每面板四个 rank 设置、四条系列，均为单次 perplexity 点估计；没有 seed、重复、误差条、误差带、区间或失败值。Adam 是每面板一条水平基线。图把 rank 当等距 categorical 位置，适合比较离散预算，但不表达 rank 比值的真实横向距离。
- **Caption**：`Figure 2: Rank efficiency comparison across efficient optimization methods. Perplexity versus rank for 60M (left) and 130M (right) models, demonstrating LoRA-Pre’s superior performance at lower ranks compared to baseline methods.` 共 30 个空白分词。标题为粗体；moves=`title, setup, comparison, main_finding`；`self_contained=true`、`main_finding_stated=true`。
- **证据关系**：Section 4.3 rank ablation 的 rank 集合和 Figure 2 → 正文 p.9 比较 LoRA-Pre Adam rank=16 对 GaLore rank=128/256 的 8×/16× 比较 → 归因于 continuous subspace adaptation。它是 headline 的 rank-efficiency 支撑和消融主比较，不是独立的机制干预。
- **优点**：两个模型规模并排、y 轴语义一致；同一颜色映射使四个方法容易横向比较；Adam 虚线把低秩方法与 full optimizer reference 放在同一图面；离散 rank 下的相对性能趋势很快可读。
- **缺陷**：实际 rank 数值等距摆放会隐藏 4→16 与 64→128 的比例差异；四条低 perplexity 曲线在底部挤在一起；legend 重复且占用图内空间；没有不确定性或实验次数，无法判断相邻曲线差异；栅格图在双栏缩放后 tick/legend 偏小。
- **可复用模式**：对同一度量用并列规模面板、固定方法 palette 和一条 full-method reference line，把“性能随资源预算变化”的消融问题压缩成可扫读的结构。

## Figure 3

- **位置与分类**：p.9，`main`，右栏单栏；类型 `line`；用途 `ablation` + `mechanism` + `robustness`。bbox 约 `[314, 279, 504, 397]`。
- **版面与复杂度**：单面板，四条 rank 系列；标题在图内顶部，legend 在 upper-right，x/y 轴和 tick 均嵌入图像；约 80 个数据标记（四条曲线各约 20 个 iteration 点），无额外 annotation。复杂度 2/5。
- **绘图语法**：栅格图像；x 为 linear iteration（约 1k–20k，tick 以 `1k`–`20k` 显示），y 为 linear perplexity；x/y 均有浅灰点状网格；legend upper-right、非共享；无 direct labels；所有系列使用 `x` marker 和实线；无 hatching/reference line/不确定性；线宽按渲染估计约 1.5 pt。
- **字体**：嵌入栅格图像内的 DejaVu Sans 风格，约 6.5–11 pt（中位约 8 pt），regular/roman；渲染估计，中置信度。
- **颜色与编码**：rank=16/64/128/256 分别为蓝 `#1F77B4`、橙 `#FF7F0E`、绿 `#2CA02C`、红 `#D62728`；四色 categorical palette，颜色是主要系列编码，marker/线型相同，灰度安全性为假。
- **编码字段**：x=training iteration；y=test/evaluation perplexity；color=rank；shape=统一 `x` marker；line=各 rank 的实线轨迹；facet 无；text=标题、轴名、tick、rank legend。
- **数据与统计**：130M LoRA-Pre Muon 的训练轨迹，约 20 个等间隔检查点/系列；只显示测试 perplexity 点估计，没有重复、误差带或误差条。图表达小 rank 初始 perplexity 较高、训练后迅速接近高 rank 曲线，但不提供逐点数据表。
- **Caption**：`Figure 3: Test perplexity for LoRA-Pre Muon with different ranks during training.` 共 12 个空白分词。标题不加粗；moves=`title, setup`；caption 没有写 130M、rank 列表或主结论，`self_contained=false`、`main_finding_stated=false`。
- **证据关系**：Figure 2 的 rank efficiency 结果 → Figure 3 展示 LoRA-Pre Muon 的动态训练轨迹 → p.9 以“初始差距快速缩小”归因于每步 subspace update → 支持 continuous adaptation 的机制解释。它是机制/定性轨迹证据，不是统计不确定性证据。
- **优点**：相比只给终点的 Figure 2，轨迹直接显示收敛速度和 rank gap 如何随训练消失；共享 y 轴语义、固定颜色和密集 iteration ticks 能表达动态过程。
- **缺陷**：单栏栅格图较小，legend 和标题挤占绘图区；caption 没有列出模型规模、rank 值或检查点定义；曲线完全没有误差表达，且多条曲线后半段很接近，无法判断差异是否超过运行噪声。
- **可复用模式**：在离散 rank 终点比较后补一张统一坐标的训练轨迹，用“初始差距—快速靠近—稳定区间”检验作者提出的动态机制。

## Table 1

- **位置与分类**：p.7，`main`，双栏宽；用途 `headline` + `main_comparison` + `efficiency_cost`。bbox 约 `[108, 83, 504, 438]`。
- **结构**：11 个数据行、5 列（Method + 60M/130M/350M/1B）；三层信息表头（Model Size、`r/d_model`、Training Tokens），3 个 row group：full optimizer（Adam、Muon）、低秩 baseline（GaLore 至 Fira*）、LoRA-Pre（Adam、Muon）。顶部/组间/底部横线，并在 Method 与数值列之间有竖分隔线，属于 `partial_grid`/booktabs 风格的混合。
- **字体**：主体 Nimbus Roman No9 L regular，数学/斜体使用 Computer Modern；body/header 约 10 pt，header weight regular；PDF object，高置信度。
- **颜色与高亮**：正文和方法名黑色；引用链接 `#4169E1`；两行 LoRA-Pre 使用 `#CCE6E6` 全行底色；低秩组内最佳值用 bold，第二佳用 underline（例如 LoRA-Pre Muon 在 60M/130M、LoRA-Pre Adam 在 350M/1B）。颜色模式 `mixed`，约 3 个可见语义色；粗体/下划线使灰度阅读安全。
- **Caption**：`Table 1: Comparison with low-rank algorithms on pre-training various sizes of Llama models on the C4 dataset. We report the validation perplexity (↓) on a hold-out C4 test set. The best and second-best performance within the low-rank optimizers are highlighted with bold and underline. ∗ denotes the results are reproduced by ourselves.` 共 52 个空白分词。标题不加粗；moves=`title, setup, encoding_key, comparison`；明确 C4、hold-out、↓、best/second-best 和 reproduced 标记，`self_contained=true`、`main_finding_stated=false`。
- **数据与统计**：C4 hold-out validation perplexity，方向为越低越好；四个模型规模及其 `r/d_model`、training tokens 元数据；每个方法/规模一个点估计，保留 Low-Rank 1B 的 142.53 和 Fira* 的复现星号；没有 seed、方差、区间、运行失败列或实测 memory/time。
- **证据关系**：Section 4.1 的 memory-efficient pre-training 设计 → Table 1 汇总 60M–1B 的 cross-scale 性能 → Section 4.1 对 0.81/2.45/1.6 perplexity 差异和 LoRA-Pre Adam/Muon 排名作解释 → Appendix B Algorithm 1/2 给出实现。该表承担正文 headline 主比较，但“memory-efficient”主要由低秩结构支撑，表内未直接测量内存。
- **优点**：把模型规模、rank/hidden size、训练 token 和结果放在一个横向表头内，读者可快速检查 scale claim；full optimizer、低秩 baseline 和 proposed row 分组清楚；粗体/下划线明确 best/second-best，失败/很差的 Low-Rank 值没有被删除。
- **缺陷**：`r/d_model` 和 Training Tokens 作为多层表头但没有单独解释每个 baseline 的 rank 是否完全可比；跨模型规模只有单次点估计，无法判断小差异；整行 cyan 加粗与下划线同时存在，视觉重点偏重；没有 memory bytes、吞吐、wall-clock 或失败率。
- **可复用模式**：将 scale、预算元数据、完整 baseline 行和 proposed 行放在同一个横向表中，以方向箭头、best/second-best 文字规则和轻量底色支持可复核的主比较。

## Table 2

- **位置与分类**：p.8，`main`，双栏宽；用途 `headline` + `main_comparison` + `robustness`。bbox 约 `[108, 83, 504, 438]`。
- **结构**：8 个数据行、7 列（Method + 两个模型组各 3 列）；两层表头：Llama-3.1-8B/Llama-2-7B，再分 GSM8K、MATH-500、Average。2 个 row group：Adam-like（LoRA、rsLoRA、DoRA、GaLore、LoRA-Pre Adam）和 Muon-like（GaLore Muon、LoRA Muon、LoRA-Pre Muon）。有顶线、表头线、组间线、底线，以及 Method/模型组间的竖分隔线，属于 `partial_grid`。
- **字体**：主体 Nimbus Roman No9 L regular，数学/引用使用 Computer Modern；body/header 约 10 pt、header regular；PDF object，高置信度。
- **颜色与高亮**：引用链接 `#4169E1`；LoRA-Pre Adam 与 LoRA-Pre Muon 两行用 `#CCE6E6`；每个 optimizer group 的最高值用 bold，未使用 underline。颜色模式 `mixed`，约 3 个语义色；bold 与 row shading 提供冗余，灰度安全。
- **Caption**：`Table 2: Results of memory-efficient fine-tuning methods. We compare our method with efficient fine-tuning methods including LoRA (Hu et al., 2022), rsLoRA (Kalajdzievski, 2023), and DoRA (Liu et al., 2024), and an efficient optimizer GaLore (Zhao et al., 2024). The models are fine-tuned on the MetaMath100k (Yu et al., 2024) dataset, and evaluated on GSM8K (Cobbe et al., 2021) and MATH-500 (Lightman et al., 2024). We highlight the best performance on Adam-like optimizer and Muon-like optimizer with bold.` 共 78 个空白分词。标题为粗体；moves=`title, setup, encoding_key, comparison`；说明模型、数据集、两个 benchmark 和 bold 规则，`self_contained=true`、`main_finding_stated=false`。
- **数据与统计**：GSM8K、MATH-500 为 accuracy/score，Average 是两项的 arithmetic average；两个模型、两个 optimizer family，各方法每个配置一个 aggregate，未报告 seed dispersion 或区间。表中可见同一配置的任务 trade-off，例如 LoRA-Pre Adam 的 MATH-500 不总是组内最高，但 Average/GSM8K 领先；Muon 组 MATH-500 存在 6.15 并列。
- **证据关系**：Section 4.2 的 MetaMath100k fine-tuning 设计 → Table 2 把模型、benchmark、optimizer family 放进同一决策面 → 正文回收 2.59/4.88/1.96/6.03 的 Average 改善 → Appendix B 算法支持跨 optimizer。它是 fine-tuning headline 和 compatibility 证据；Average 不能替代单任务结论。
- **优点**：模型和 benchmark 的两层表头清楚；Adam/Muon group 用分隔线和方法行组织；Average 将双任务结果压缩为可扫读摘要，同时保留逐任务分数；LoRA-Pre 行轻量底色、最优值 bold。
- **缺陷**：Average 未在表头说明是等权 arithmetic mean；rank=8、学习率等训练条件在 caption/表头中缺失；没有误差或样本量；粗体“best”在不同 optimizer group 中可能让跨组比较产生歧义，且并列值没有 tie 标记。
- **可复用模式**：用“模型组 × 任务子列 × optimizer family row group”的三维表头，把任务级结果与汇总指标同屏，并在 caption 中明确最佳值的比较范围。

## Table 3

- **位置与分类**：p.9，`main`，跨栏居中；用途 `ablation` + `mechanism` + `main_comparison`。bbox 约 `[151, 441, 424, 539]`。
- **结构**：5 个数据行、4 列（Method + 60M/130M/350M）；单层 `Model Size` 表头；3 个 row group：Muon/无 momentum、GaLore Muon/Fira Muon、LoRA-Pre Muon。横线分组、Method 与数值间竖分隔线，属于 `partial_grid`。
- **字体**：Nimbus Roman No9 L regular/medium，body/header 约 10 pt，header regular；PDF object，高置信度。
- **颜色与高亮**：引用链接 `#4169E1`；LoRA-Pre Muon 行以 `#CCE6E6`；其三个数值均 bold；颜色模式 `mixed`，约 3 个语义色；灰度下仍可依靠粗体和横线识别。
- **Caption**：`Table 3: Results of pre-training using different efficient Muon optimizers.` 共 10 个空白分词。标题为粗体；仅有 `title` move；没有数据集、metric direction、训练设置或主结论，`self_contained=false`、`main_finding_stated=false`。
- **数据与统计**：60M/130M/350M 的 validation perplexity 点估计，越低越好；Muon 是 full-optimizer upper/reference，Muon w/o momentum 作为 lower reference，GaLore/Fira 是改写到 Muon 的 projection baselines，LoRA-Pre Muon 是 proposed。无重复、方差、区间或内存量。
- **证据关系**：Section 4.3 的 “Ablation of Low-Rank Muon Optimizers” → Table 3 检验 projection-based 方法迁移到 Muon 后的性能 → 正文以 3.54/1.80/0.43 的 second-best gap 和 projection baseline 低于无 momentum 的反常结果解释 → Algorithm 2 提供 LoRA-Pre Muon 伪代码。该表承担 optimizer transfer 的机制性消融。
- **优点**：full Muon、无 momentum、两个迁移 baseline 和 proposed row 组成明确的 upper/lower/efficient 对照；三种规模横向可比；用保留负面结果的行暴露 projection-based transfer 的失败模式。
- **缺陷**：caption 过短，读者必须回到正文才能知道数据集、指标方向、哪些行是 upper/lower；“second-best gap”不能仅凭 caption 复核；没有把 Muon 改写实验的实现差异或成本列出来；单次点估计无法量化反常结果的稳定性。
- **可复用模式**：在跨优化器迁移表中同时放置 full、去动量和 efficient variant 三类参照，使“方法有效”与“迁移后基线失败”在同一表面上可核验。

## Table 4

- **位置与分类**：p.23，`appendix`，跨栏居中；用途 `ablation` + `robustness` + `failure` + `theory_mechanism`。bbox 约 `[120, 538, 432, 640]`。
- **结构**：6 个数据行、4 列（Parameter、Value、Perplexity、Status）；单层表头，2 个参数 row group（β₁ fix β₂、β₂ fix β₁），每组 3 个取值。粗顶线、表头线、组间线和底线，无竖线，属于 `booktabs`。β 默认值用 italic `Default`；默认 perplexity/status 与 Optimal 用 bold，Diverged 以文字直接标记。
- **字体**：Nimbus Roman No9 L regular/medium，数学 β/γ 为 Computer Modern；主体约 10 pt，header 约 10 pt、bold；PDF object，高置信度。
- **颜色与高亮**：黑白灰度，无彩色底色、无引用链接；语义由粗体、italic 和 Status 文本编码。`color_count=1`（黑色 ink），灰度安全。
- **Caption**：`Table 4: Sensitivity Analysis of Momentum Parameters β. We report the validation perplexity on the 60M model. The method exhibits stability around the default settings derived from standard Adam (β1 = 0.9, β2 = 0.95). Extreme values (e.g., β → 1) cause the implicit γ to vanish, leading to training instability.` 共 51 个空白分词。标题为粗体；moves=`title, setup, main_finding`；明确 60M、默认 β、稳定/极端值和训练不稳定，`self_contained=true`、`main_finding_stated=true`。
- **数据与统计**：60M LoRA-Pre Adam 的 validation perplexity；固定另一 β 后分别测试 β₁=`0.90,0.95,0.99`、β₂=`0.90,0.95,0.999`。数值为单次点估计；Status 为 Stable/Optimal/Diverged 的离散运行状态，没有重复、方差或阈值定义。
- **证据关系**：Appendix D.1 的解析 coupling `(1−γ₁)^2=β₁`、`(1−γ₂)^4=β₂` → Table 4 以默认/中间/极端 β 检验敏感性 → 正文没有对应主表，但附录文字解释 γ 接近 0 时 subspace adaptation 变慢。它连接理论机制与失败/鲁棒性证据。
- **优点**：Parameter row group 和 fixed-condition 文本让两个一维敏感性扫描可区分；Perplexity 与 Status 同屏，Diverged 不被删除；默认值、最优值和极端失败值以粗体/italic/文字形成不依赖颜色的编码。
- **缺陷**：只有 6 个点，无法说明“reasonable range”的边界；Status 没有给出 divergence 判据或训练步数；caption 没有说明每一行是否同一 seed/训练预算；表格后置，正文 headline 不直接回收其限制条件。
- **可复用模式**：对解析耦合超参数用“固定另一参数的短扫描 + 数值 + 离散状态”表，并同时保留稳定与发散行，避免只展示默认点。

## 跨对象系统判断

- **视觉叙事**：Figure 1 先把 EMA/online regression 和低秩因子化画成方法接口；Table 1 给跨模型规模的预训练主比较；Table 2 给跨模型/任务/optimizer 的微调主比较；Figure 2 将 rank efficiency 变成同轴消融；Figure 3 将终点现象延伸为训练轨迹机制；Table 3 检验 Muon transfer；Table 4 将 β–γ coupling 的稳定性和失败值后置到附录。顺序与正文“等价 → 压缩 → 规模结果 → rank/optimizer 消融 → 敏感性”一致。
- **Caption 系统**：Figure 1/2 和 Table 2/4 使用粗体标题并说明设置；Table 1 的标题不粗体但设置和编码最完整；Table 3 只有结论性短标题；Figure 3 caption 最弱，缺模型规模、rank 列表和机制结论。图 caption 常把主发现写出来，表 caption 多描述设置而将数值结论留给正文。
- **表头系统**：Table 1 采用规模/`r/d_model`/token 三层表头，Table 2 采用模型→任务两层表头，Table 3/4 使用单层表头；组间横线和 Method 竖分隔线保持可扫读，但表 1–3 的 cyan proposed rows 和引用蓝色未在 caption 中逐项解释色值。
- **方法—结果—消融链**：Figure 1 的方法接口对应式 (7)–(12)；Table 1/2 的主比较对应 pre-training/fine-tuning；Figure 2/3/ Table 3 对应 rank、dynamic subspace 和 Muon transfer；Table 4 对应 Appendix D coupling。Appendix A/B/C 提供证明、算法和误差收敛分析，但没有额外 Figure/Table；附录表只补敏感性，不补主结果的重复或不确定性。
- **正文—附录链接**：正文 p.4 Figure 1 的 Theorem 3.1 由 Appendix A pp.13–14 展开；正文 p.6–p.9 多次指向 Algorithm 1/2（pp.15/17）；Appendix C pp.17–23 是理论误差/收敛保证；Appendix D p.23 的 Table 4 是唯一附录表。PDF 的 cross-object 结构完整，但理论假设、算法初始化和实验方差都没有在主图表中显式出现。
- **字体一致性**：正文/表格的 Nimbus/Computer Modern 一致；Figure 2/3 的 DejaVu Sans 栅格图整体相容但字号小；Figure 1 的 Comic Sans MS 与其他对象明显不一致，是全篇最突出的字体风格断裂。
- **颜色一致性**：Figure 2/3 共享 Matplotlib 蓝/橙/绿/红方法/ rank palette；Table 1–3 共享 Royal Blue citation 与 cyan LoRA-Pre row；Figure 1 独立使用奶油/粉/蓝 pastel 语义。每个局部对象内部可读，但跨对象没有统一的 LoRA-Pre accent 色或全篇灰度冗余规范。

## 最终判断

- **最可复用模式**：
  1. Figure 1 的“符号目标—等价改写—压缩动作—闭式更新”单路径方法图。
  2. Table 1 的规模、rank/hidden-size、训练 token 与性能同屏，配合明确的 best/second-best 规则。
  3. Figure 2→Figure 3 的互补消融：先用离散 rank 曲线比较终点，再用训练轨迹检验动态机制。
  4. Table 3 的 full/无 momentum/迁移 baseline/proposed 四类对照，以及 Table 4 保留 Stable/Diverged 失败行。
- **最高价值对象**：Figure 1（方法接口与理论直觉）；Table 1（跨规模 headline）；Table 2（跨任务/optimizer 的决策面）；Figure 2（rank-efficiency 的直接视觉证据）。
- **主要失败模式**：所有经验图表都缺少 seed/重复/不确定性；Figure 2 把非等距 rank 当等距 categorical 位置；Figure 3 caption 不自足且单栏栅格过小；Table 3 caption 过短；表格用 cyan/bold 强调结果却不显示实测 memory/time；Figure 1 的 Comic Sans/pastel 风格与正文不一致。
- **一句话视觉策略**：论文用一张理论流程图建立 EMA 到低秩更新的接口，再用跨规模表格和双层 rank/optimizer 消融把“低秩而不牺牲性能”的主张变成可扫读的点估计比较，但将不确定性、成本和部分适用条件留在图表之外。
