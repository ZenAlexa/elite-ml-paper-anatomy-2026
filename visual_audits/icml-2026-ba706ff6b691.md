# Visual audit — `icml-2026-ba706ff6b691`

## 审计范围与事实边界

- **论文**：*Error Propagation Mechanisms and Compensation Strategies for Quantized Diffusion Models*，arXiv `2508.12094v4`。
- **PDF 事实源**：`corpus/preprints/icml-2026-ba706ff6b691.pdf`；物理页 21 页，letter `612 × 792 pt`。第 1–10 页为正文，第 11–12 页为参考文献，第 13–21 页为附录；无独立 supplementary PDF。
- **渲染**：使用 `pdftoppm -r 220`（高于协议要求的 180 dpi）渲染全部 21 页；逐页查看含对象的第 2、4、8、9、13、16、17、18、19、20、21 页，并以 PDF 文字对象、嵌入图像清单和渲染结果交叉核对。
- **对象对账**：PDF 中实际存在 Figure 1–4、Table 1–9，合计 4 幅 Figure、9 张 Table；与 reading inventory 一致。没有 Algorithm、跨页表或隐藏 inset。Figure 2 位于正文第 4 页顶部，但在第 9 页结果段落被调用；Figure 3/4 和 Table 4–9 属附录。
- **嵌入资产事实**：`pdfimages -list` 显示 Figure 1、Figure 2、Figure 3 为大型嵌入栅格对象，Figure 4 为 15 个 `1024 × 1024` JPEG 图块；表格文字和规则为 PDF 文字/路径对象。图形的字号、线宽和颜色因缺少绘图源均标为渲染估计；表格字体可由 PDF font objects 确认。

## 源获取核查

- `reports/tables/visual_source_inventory.csv` 的目标行（第 223 行）为 `no_public_source_found`，未给出仓库、候选仓库或视觉源文件。
- `corpus/visual_sources/icml-2026-ba706ff6b691/` 不存在。
- PDF 首页、脚注、正文和参考文献仅给出 arXiv `2508.12094`；OpenReview 元数据链接为 `https://openreview.net/forum?id=4YIY66tpvQ`，没有作者代码 URL。附录 J 明确说 TAC 尚未放出代码、QNCD 已公开代码，但 QNCD 是对比方法，不是本文 TCEC 的绘图源。
- 只读使用 `gh search repos` 检索完整题名、`2508.12094`、OpenReview ID、`TCEC quantized diffusion`、方法名/作者组合，并检索对应 code；均无严格匹配结果。故没有将 QNCD 或其他基线仓库误记为本文源。
- **结论**：`no_public_source_found`；无可记录的 `plot_script`、`table_generator`、`paper_tex`、`.tikz`、`.pgf`、`.svg`、notebook 或 style 文件。

## PDF 对象清单

| 对象 | 页码/模块 | 版面 | PDF/渲染观察 |
|---|---:|---|---|
| Figure 1 | 2 / introduction | page width | 左侧三条 PF-ODE 路径，右侧 Noise/Data 分布示意；嵌入 RGB 栅格图 |
| Figure 2 | 4 / results | page width | 2 个 prompt 行 × 5 个方法列的图像网格；嵌入 RGB 栅格图 |
| Table 1 | 8 / results | page width | 21 行 × 11 列，三层分组表头，横线分组 |
| Table 2 | 9 / results | page width | 14 行 × 10 列，W8A8/W4A8 两组，TCEC 行灰底 |
| Table 3 | 9 / results | double column | 4 行 × 5 列，视频与 Img2Vid 分组 |
| Table 4 | 13 / appendix A | double column | 3 行 × 5 列，三种 timestep 阶段 |
| Table 5 | 16 / appendix C | single column | 8 行 × 6 列，SDXL 与 PixArt-σ 各四行 |
| Figure 3 | 17 / appendix E | page width | 4 个 timestep 列 × 4 个语义行，含四个 heatmap colorbar；JPEG 栅格 |
| Table 6 | 18 / appendix F | single column | 3 行 × 6 列，λ₁ 选择消融 |
| Table 7 | 18 / appendix G | double column | 8 行 × 8 列，四个 model/step 行组 |
| Table 8 | 19 / appendix I | double column | 5 行 × 8 列，calibration data size 消融 |
| Table 9 | 20 / appendix J | double column | 6 行 × 8 列，DDIM-100/250 两层表头 |
| Figure 4 | 21 / appendix K | page width | 3 个 prompt 行 × 5 个方法列；15 个独立 JPEG 图块 |

## Figure 逐对象审计

### Figure 1（p.2，introduction；page width）

- **Caption（逐字）**：`Figure 1 Visualization of Error Propagation and Correction in Quantized Diffusion Models. The gray path represents the iterative denoising process of the full-precision model μ, while the brown-red path represents that of the quantized model μ̃. Affected by cumulative errors δt, its output gradually deviates from μ. The green path represents the denoising process after online correction of cumulative errors, with outputs better aligned with the full-precision model.`（68 词；Figure 编号加粗，标题正文常规；动作：标题、颜色/编码键、比较、主发现。）
- **结构与绘图语法**：左侧把 PF-ODE of Full Precision DM、PF-ODE of Quantized DM、Corrected PF-ODE of Quantized DM 排成三条水平时间路径，节点为菱形 μθ/μ̃θ，箭头从 `x_{t+1}` 经 `x_t` 到 `x_{t-1}`，中间用 δt、εt、`δ_{t-1}=A_tδ_t+B_tε_t` 标注传播关系；底部绿色路径在节点旁加入虚线浅绿 `K_{t+2}, K_{t+1}, K_t` 和加法圆圈 `Δ`。右侧用 Noise 与 Data 两条灰底密度曲线及三条短路径表达高/低质量输出。无数值坐标轴，x/y 均为 none；无网格；底部三色箭头是路径 key，且文本直接标注角色；黑色实线和红色虚线分别承载迭代与 Error Propagation。图形主体是单个高分辨率栅格 XObject，caption 为矢量 PDF 对象，故主体渲染为 raster、整体为 mixed 的视觉系统。
- **Typography**：图内为无衬线公式/标签视觉，含 μ、δ、ε 数学字形和粗体路径标签；caption 使用 ByteSans bold 的 Figure label、SFRM/Computer Modern Roman 正文和 Computer Modern math。图内字号约 7–10 pt，caption 约 8.7 pt；`rendered_estimate`，medium confidence。
- **颜色**：mixed，约 7 个语义色：灰 `#B9B6B7`（FP path）、棕红 `#845553`（quantized path）、橄榄绿 `#9FAA91`（corrected path）、淡紫 `#C3A5A6`（方程框）、浅绿 `#CCDAC5`（K 框）、黑 `#070507`（箭头/文字）、白 `#FFFFFF`（画布/输出边界）。颜色和箭头、路径文字、节点形状共同编码，`redundant_encoding=true`；三色在灰度下不稳定，`grayscale_safe=false`；均为 `rendered_estimate`。
- **复杂度**：score 4；2 个主要区域/面板，3 条路径，legend items 3，约 14 个公式/节点/说明标注，约 40 个箭头、节点和输出缩略图标记。
- **Plot grammar**：`rendering=raster`；`x_scale=none`、`y_scale=none`、`grid=none`；`legend_present=true`、位置 `bottom center`、`shared_legend=true`（三条路径共用）；`direct_labels=true`；`marker_types=2`（菱形模型节点、圆形加法节点）；`line_styles=2`（实线与 Error Propagation 虚线）；无 hatch、无 reference line、无不确定性；估计线宽约 1.0 pt；`provenance=rendered_estimate`。
- **数据/统计**：机制示意而非数据图；没有样本、分母、重复、聚合或不确定性。右侧 Noise/Data 曲线是概念分布，不能读取成实验密度估计。
- **证据关系**：引言“逐步误差累积导致低精度退化” → Figure 1 的三路径/在线 K 校正接口 → §3 的 Eq. (7)–(18) 误差传播、估计和 TCEC → Table 1–3 的图像、视频质量与延迟；Appendix A/Table 4 进一步检验 Jacobian 次项。
- **优点**：把基线、量化偏移和校正路径放在同一时间轴；颜色、箭头、节点和文字冗余表达方法接口；右侧将误差路径和输出质量直观相连。
- **缺陷**：右侧分布无刻度/样本含义，容易被误读为定量统计；三色在灰度和小尺寸下区分度有限；K、Δ 和传播式较密，未标注实际 timestep 窗口或数值尺度。
- **可复用范式**：用同一状态网格并排画 full precision、quantized 和 corrected 路径，把误差变量和在线校正操作放在对应时间点，再用一个概念质量空间承接输出差异。
- **证据锚点**：PDF p.2 顶部 `Figure 1 Visualization of Error Propagation and Correction in Quantized Diffusion Models`；对象为嵌入图像 XObject，观察基础 `rendered_observation`。

### Figure 2（p.4，results；page width）

- **Caption（逐字）**：`Figure 2 Qualitative visual results comparison. Prompt1: An alien octopus floats through a portal reading a newspaper. Prompt2: A middle-aged woman of Asian descent, her dark hair streaked with silver, appears fractured and splintered, intricately embedded within a sea of broken porcelain. The porcelain glistens with splatter paint patterns in a harmonious blend of glossy and matte blues, greens, oranges, and reds, capturing her dance in a surreal juxtaposition of movement and stillness. Her skin tone, a light hue like the porcelain, adds an almost mystical quality to her form.`（90 词；编号和 Prompt1/Prompt2 加粗；动作：标题、prompt 设置、定性比较。）
- **结构与绘图语法**：上/下两行分别对应两个 prompt，五列为 FP16、GGUF W4、ViDiT-Q W4A4、SVDQuant W4A4、TCEC W4A4；每个 cell 是一个方形生成图，无坐标轴、网格、marker、线型或误差显示。方法名在图像上方直接作为列标签，不是独立 legend。2×5=10 个图像面板，阅读方向为列内比较同一 prompt、行内比较同一方法。
- **Typography**：方法列标题为大号无衬线栅格文字，约 9–11 pt；caption 为 ByteSans/SFRM 组合，约 8.7 pt。图像 XObject 将图内标题栅格化，`rendered_estimate`，medium confidence。
- **颜色**：mixed、自然图像多色；代表色包括深蓝 `#1E2B32`、青灰 `#475C63`、棕橙 `#8C705F`、肤色 `#D4B893`、红色 `#C93F38`、浅纸色 `#E4E1D1`、黑色 `#181D21`、白色 `#FFFFFF`。颜色表达生成内容而非连续度量，方法身份由列标题而非色值表达；`redundant_encoding=true`（网格位置和标题重复）；自然图像不能保证灰度可读，`grayscale_safe=false`；`rendered_estimate`。
- **复杂度**：score 5；10 面板，5 个方法系列，5 个列标签，约 10 个图像 mark，无 legend 项和 annotation。
- **Plot grammar**：`rendering=raster`；`x_scale=none`、`y_scale=none`、`grid=none`；`legend_present=false`、`legend_placement=null`、`shared_legend=false`；`direct_labels=true`；`marker_types=0`、`line_styles=0`、`hatching=false`、`reference_lines=0`、`uncertainty_display=none`、`line_width_pt=null`；`provenance=rendered_estimate`。
- **数据/统计**：两个固定文本 prompt，各方法一张输出；没有 seed、生成数、聚合、质量分数、误差或失败标记。它是内容保持/视觉退化的定性证据，不是从图块估读优劣的统计图。
- **证据关系**：Table 1 的 SDXL/SDXL-Turbo/PixArt 指标比较 → 正文 p.9 对 Figure 2 的“less quality degradation and smaller changes in image content”调用 → Appendix K Figure 4 扩展到三个 prompt；支持 TCEC W4A4 的 qualitative evidence，但不替代数值质量指标。
- **优点**：相同 prompt × 方法矩阵方便横向比较；保留极端方法（GGUF/ViDiT-Q/SVDQuant）使内容变化可见；标题和列顺序简单。
- **缺陷**：caption 不说明每行/列的模型、采样步数、seed 或图像选择；没有行标签，读者需从 prompt 长文本和列标题重建实验条件；无量化指标或不确定性，视觉相似性判断主观。
- **可复用范式**：固定 prompt 行、方法列、统一尺寸和同一阅读方向的定性网格；同时在 caption 或行列头补充模型、步数、seed 和对应定量表。
- **证据锚点**：PDF p.4 顶部图像网格及其 `Figure 2 Qualitative visual results comparison` caption；`rendered_observation`。

### Figure 3（p.17，appendix E；page width）

- **Caption（逐字）**：`Figure 3 Flux visualization of quantization errors during denoising. We compare the full-precision model (FP16) and the quantized model (W4A8) under the prompt “hummingbird flying near a flower. 4k ultra realistic ray tracing dynamic lighting” with hyperparameters num_timesteps=4 and guidance_scale=3.5. The figure illustrates three key phenomena: (1) quantization errors accumulate as denoising progresses (Step 1 → 0.75 → 0.5 → 0.25), exhibiting distinct spatial structures; (2) the errors are strongly correlated with the model outputs, particularly along object boundaries and textured regions; and (3) high-frequency components such as feather edges and flower petals amplify the discrepancies, highlighting the timestep-dependent and output-correlated nature of error propagation in quantized diffusion models.`（109 词；Figure 3 及标题粗体，prompt 斜体；动作：标题、设置、比较、主发现。）
- **结构与绘图语法**：四列 `Step 1`、`Step 0.75`、`Step 0.5`、`Step 0.25`；四行 `FP16`、`W4A8`、`Error Map`、`Single-step error εt`。前两行是图像，第三行是两模型差异图，第四行是 channel 0 的热图且每列有独立色条。顶端还有 Eq. (45)，但不属于 Figure 3。全图无共同坐标轴；热图内部有像素索引刻度，x 为 timestep 类别/列，y 对主网格无共享量尺；无网格、无 marker、无线型；四个色条是局部连续尺度 key。
- **Typography**：图像内部的行/列标题、色条刻度及像素索引已栅格化，约 4–9 pt；caption 使用 ByteSans bold + SFRM/Computer Modern，约 8.7 pt，prompt 使用斜体；`rendered_estimate`，medium confidence。
- **颜色**：mixed；自然图像色、Error Map 的灰/青/红边缘色、Single-step error 的紫–青–黄连续色条。代表色 `#238F8C`（青绿背景）、`#F2E05B`（高值黄）、`#533196`（低值紫）、`#D94A50`（差异红）、`#7D7F7B`（误差底色）、`#D4B893`（图像亮部）、`#983943`（花/边缘红）、`#FFFFFF`。语义由行（FP16/W4A8/error）和色条共同编码；`redundant_encoding=true`；灰度下色阶和自然图像不安全，`grayscale_safe=false`；`rendered_estimate`。
- **复杂度**：score 5；16 个子面板，4 个 timestep 系列，4 个局部 colorbar，约 8 个行/列标签，约 16 个主要图像/热图 mark。
- **Plot grammar**：`rendering=raster`；`x_scale=categorical`（四个 timestep 列）、`y_scale=none`、`grid=none`；`legend_present=true`（色条作为局部尺度 key）、位置 `right of each Single-step error panel`、`shared_legend=false`；`direct_labels=true`；`marker_types=0`、`line_styles=0`、`hatching=false`、`reference_lines=0`、`uncertainty_display=none`、`line_width_pt=null`；`provenance=rendered_estimate`。
- **数据/统计**：单一 hummingbird/flower prompt，四个去噪阶段；FP16/W4A8 输出、视觉 Error Map 和 channel-0 的逐像素 εt 色值。caption 没有说明批量、seed、分母或重复，色条展示值域但不是置信区间。
- **证据关系**：Figure 1 的累积传播机制和 §3.3 的 `εt=K_t⊙μ̃θ(...)` → Figure 3 的 timestep-dependent、output-correlated 观察 → Appendix E 三条经验结论 → Table 1/5 的质量结果与 Table 6–8 的校准/步数消融。它是附录机制证据，而非独立的 TCEC 效果估计。
- **优点**：同一时间轴同时给出输出、差异和单步误差，能够把高频边缘与误差空间结构对齐；四个色条避免把单步误差混成一个总分；caption 详细写出 prompt、超参数和观察结论。
- **缺陷**：16 格密集且像素刻度难读；Error Map/εt 的颜色归一化、channel 选择和误差单位未在 caption 中定义；没有数值聚合、重复或不确定性，观察仍依赖一例。
- **可复用范式**：对每个时间点按“参考输出—量化输出—差异—局部误差场”成组排列，用共享时间列和可读色条把机制解释绑定到单一案例。
- **证据锚点**：PDF p.17 中部 `Figure 3 Flux visualization of quantization errors during denoising`；主体是 JPEG XObject，`rendered_observation`。

### Figure 4（p.21，appendix K；page width）

- **Caption（逐字）**：`Figure 4 Qualitative visual results comparison. Prompt1: Luffy from ONEPIECE, handsome face, fantasy. Prompt2: The image features a woman wearing a red shirt with an icon. She appears to be posing for the camera, and her outfit includes a pair of jeans. The woman seems to be in a good mood, as she is smiling. The background of the image is blurry, focusing more on the woman and her attire. Prompt3: Bright scene, aerial view, ancient city, fantasy, gorgeous light, mirror reflection, high detail, wide angle lens.`（87 词；Figure 4、Prompt1/2/3 粗体；动作：标题、prompt 设置、定性比较。）
- **结构与绘图语法**：三行 prompt × 五列 FP16、GGUF W4、ViDiT-Q W4A4、SVDQuant W4A4、TCEC W4A4，共 15 个 `1024×1024` JPEG 图块；列头为 PDF 中的 ArialMT 无衬线文字，方法列直接作为 header，不设独立 legend。无坐标、网格、marker、线型和不确定性；阅读方向是同一 prompt 的五方法横向比较。
- **Typography**：列头 ArialMT 约 10.7 pt；caption 的 Figure/Prompt 标签为 ByteSans bold，描述为 SFRM/Computer Modern Roman，约 8.7 pt；prompt 描述中无独立图内标签。字体信息来自 PDF 对象与渲染，`mixed`，high confidence。
- **颜色**：mixed，自然图像内容为主；代表色 `#1E2B32`、`#475C63`、`#C93F38`、`#D4B893`、`#8C705F`、`#E4E1D1`、`#B0907A`、`#3D2F2A`、`#96AAA9`、`#FFFFFF`。颜色不是测量尺度，方法由列位置/标题重复编码；`redundant_encoding=true`、`grayscale_safe=false`、`rendered_estimate`。
- **复杂度**：score 5；15 个子面板，5 个方法系列，5 个列标签，3 个 prompt 行，约 15 个图像 mark，无 legend 项。
- **Plot grammar**：`rendering=mixed`（15 个 JPEG 图块 + 矢量列头/caption）；`x_scale=none`、`y_scale=none`、`grid=none`；`legend_present=false`、`legend_placement=null`、`shared_legend=false`；`direct_labels=true`；`marker_types=0`、`line_styles=0`、`hatching=false`、`reference_lines=0`、`uncertainty_display=none`、`line_width_pt=null`；`provenance=rendered_estimate`。
- **数据/统计**：三个固定 prompt、五种方法各一张生成图；没有 seed、批量、质量分数、重复或置信度。作为 Appendix K 对 Figure 2 的更多内容样本，而不是新的定量实验。
- **证据关系**：正文 Figure 2 的两 prompt qualitative comparison → Appendix K 的三 prompt 扩展 → Table 1 的图像指标和正文 p.9 关于较小内容变化的结论。它支持方法在多内容上的视觉一致性，但不能独立证明数值提升。
- **优点**：比 Figure 2 覆盖更多 prompt，严格保持五列方法和统一尺寸；图像块独立嵌入，列对齐清楚。
- **缺陷**：没有行标签，Prompt 长文本只在 caption 中出现；自然图像比较缺乏放大/差异图或客观指标；GGUF/ViDiT-Q/SVDQuant 的条件差异、采样步数和随机性未说明。
- **可复用范式**：把多 prompt 放在行、基准/候选方法放在列，统一裁切和尺度；在 caption 或相邻表格提供 prompt、seed、采样器和定量核验。
- **证据锚点**：PDF p.21 上部 3×5 图像矩阵及 `Figure 4 Qualitative visual results comparison` caption；15 个 JPEG XObject + PDF 文本，`rendered_observation`。

## Table 逐对象审计

### Table 1（p.8，results；page width）

- **Caption（逐字）**：`Table 1 Quantization Performance Comparison of Different Models. SDXL and SDXL-Turbo generate at 512² resolution, while PixArt achieves 1024². Evaluation metrics include FID (distribution distance) [20], IR (human preference) [30], LPIPS (perceptual similarity) [34], and PSNR (numerical fidelity against 16-bit references) [13].`（42 词；编号粗体；动作：标题、设置、指标定义、比较；可脱离正文读表，但未写样本数/seed。）
- **表结构**：21 个数据行、11 列。三层表头：MJHQ/sDCI → Quality/Similarity → FID↓/IR↑/LPIPS↓/PSNR↑；Model、Precision、Method 为左侧行列标签。行组为 SDXL（6）、SDXL-Turbo（8）、PixArt-Σ（7），横线分隔模型和精度组；无竖线。`rules=booktabs`，`header_levels=3`，`row_groups=3`，最大显示小数位 3（FID/IR/LPIPS/PSNR 实际列精度不完全一致，异常值如 353、-2.26、6.44 保留原样）。
- **字体/高亮**：SFRM/Computer Modern Roman 与 math PDF objects，body/header 约 8.7 pt；表头为 regular/mixed，`pdf_object`、high confidence。TCEC 行的多数列最优值粗体，使用 `bold`/`best_second_best`；无色块、无下划线、无箭头图标。
- **数据/不确定性**：MJHQ 与 sDCI 上 FID、IR、LPIPS、PSNR 点估计；正文说明各 benchmark 采样 5K prompts，SDXL 50 steps、SDXL-Turbo 4 steps、PixArt 使用 DPM++，但表 caption 未写这些条件。FP16 的 LPIPS/PSNR 以 `-` 表示不可用；没有均值±SD、error bar、区间、seed 或显著性。
- **证据关系**：Figure 1 的校正机制 → Table 1 主图像质量/相似度比较 → Figure 2 qualitative 结果；Appendix Table 5 分解 Eq. (10)/(13)，Table 6–8 进一步做参数、步骤和校准规模消融。
- **优点**：把两个数据集、质量/相似度和多精度放在一个比较面；三层表头和分组横线可扫描；粗体突出每个模型/精度内的候选最好值。
- **缺陷**：列极密且精度混杂；MixDQ W4A4 的 FID 353/373 与 IR -2.26/-2.28 是失败值，但没有脚注解释；缺少样本/seed/不确定性，容易把点值当作稳定排名。
- **可复用范式**：用数据集 × 指标角色的嵌套表头，按模型/bit-width 行分组，统一方向箭头并在每组突出最佳方法；同时在 caption 给出 sampler、样本数和不确定性。
- **证据锚点**：PDF p.8 上半部 `Table 1 Quantization Performance Comparison of Different Models` 及完整 21 行表体；`pdf_object`。

### Table 2（p.9，results；page width）

- **Caption（逐字）**：`Table 2 Performance of PTQ Algorithms for OpenSora on Vbench eval Benchmark in the W4A8 configuration.`（16 词；编号粗体；动作：标题、设置、比较；caption 称 W4A8 但表中同时含 16/16 和 8/8 行，因此不完全自洽。）
- **表结构**：14 个数据行、10 列：Method、Bit-width W/A、Imaging Quality、Aesthetic Quality、Motion Smooth.、Dynamic Degree、BG. Consist.、Subject Consist.、Scene Consist.、Overall Consist.；单层表头但多个指标两行换行；行组为 16/16 baseline、8/8、4/8。`rules=booktabs`，`header_levels=1`，主要比较组可视为 2 个 PTQ bit-width 组（另有 baseline），`row_groups=2`，最大显示小数位 3（主体两位，但 4.166、9.722、3.561、4.985 等三位）。
- **字体/高亮**：表头 ByteSans bold，body SFRM/Computer Modern Roman，约 8.7 pt，`pdf_object`、high confidence。两条 `ViDiT-Q + TCEC` 行用浅灰底，列内最优值粗体；`highlighting=bold, cell_color, best_second_best`；无误差线、下划线或箭头图标。
- **数据/不确定性**：OpenSora VBench 八个点指标，W8A8 与 W4A8 对比 ViDiT-Q/TCEC 及其他 PTQ；正文说明 100-step DDIM、CFG 4.0、128 WebVid calibration samples。表中没有视频样本数、重复、error bar、区间或显著性；`-` baseline 仅表示 16-bit 未量化方法名。
- **证据关系**：正文 Video Generation Evaluation 的 PTQ 选择 → Table 2 的 frame-wise/temporal/prompt consistency → Table 3 成本；Appendix Table 9 以 CIFAR-10 对比 QNCD，补充较低 activation bitwidth 的机制结果。
- **优点**：把静态质量、运动、跨帧一致性和 prompt consistency 放在同一表；灰底和粗体让 TCEC 行易定位；方向箭头和缩写使指标含义可扫描。
- **缺陷**：caption 的 W4A8 说法与同时存在的 W8A8/16/16 行冲突；缩写 BG./Consist. 的定义主要留给正文；点值无重复/不确定性，低值失败行（如 Overall 4.985）没有失败注释。
- **可复用范式**：将 frame-wise、temporal 和 prompt-level 指标作为同一决策表，并按 bit-width 分组；修正 caption 的覆盖范围，显式给出样本/seed 和 failure semantics。
- **证据锚点**：PDF p.9 顶部完整表格与 `Table 2 Performance of PTQ Algorithms for OpenSora` caption；`pdf_object`。

### Table 3（p.9，results；double column）

- **Caption（逐字）**：`Table 3 Inference overhead comparison with TCEC. Quantization: video models use W8A8, Flux-dev uses W4A4.`（15 词；编号粗体；动作：标题、设置、比较；自包含性较好。）
- **表结构**：4 个数据行、5 列：Model、Type、FP16 (s)、Quantized Inference (s) 下的 w/o TCEC 与 w/ TCEC；两层表头；视频三行与 Img2Vid 一行由横线分组。`rules=booktabs`，`header_levels=2`，`row_groups=2`，最大显示小数位 3（例如 26.211/26.316、9.947/9.996，FP16 有 199）。
- **字体/高亮**：body/header 约 8.7 pt，SFRM/Computer Modern 与 ByteSans bold，`pdf_object`、high confidence；w/ TCEC 列加粗，`highlighting=bold`；无色块、无不确定性。
- **数据/不确定性**：Nvidia A800-40G 上端到端秒数；OpenSora1.2、CogVideoX、Wan2.1-1.3B 为 Video (W8A8)，Flux-dev 1.0 为 Img2Vid (W4A4)。只给单点时间，没有运行次数、标准差、硬件负载或置信区间；可直接计算 TCEC 增量约 0.4% 以内，但表中未给百分比列。
- **证据关系**：正文 §4.2 的质量收益 → Table 3 的 `<0.5%` overhead → Appendix H 的 O(`N C² H W`) 单步复杂度解释；与 Table 1/2 共同形成质量—成本决策面。
- **优点**：FP16、量化无校正、有校正三种时间基线同列；设备、模型规模/帧数和量化条件写在 model/type 中；加粗列直接突出成本变化。
- **缺陷**：没有重复和运行条件（batch、warm-up、I/O）；跨模型的秒数受帧数/分辨率/步数影响，不能直接作效率排序；没有专门的 overhead 百分比或误差表达。
- **可复用范式**：把绝对 latency baseline、候选 overlay 和模型/输入规模放在同一多层表头，另加相对开销列及测量协议。
- **证据锚点**：PDF p.9 下半部 `Table 3 Inference overhead comparison with TCEC`；`pdf_object`。

### Table 4（p.13，appendix A；double column）

- **Caption（逐字）**：`Table 4 Statistics of dominant and secondary terms at different SDXL timesteps.`（12 词；编号粗体；动作：标题、timestep 比较。）
- **表结构**：3 个数据行、5 列：time step (SDXL, T=50)、Dominant term、J_xt、B_t·J_xt、Secondary/Dominant；行分别为 10 High-noise、25 Intermediate、40 Low-noise。单层表头、三行 stage 分组、booktabs 极简横线；`header_levels=1`、`row_groups=3`、`decimal_precision=5`（主系数多为 3 位，B_t·J_xt 为 5 位，百分比为 3 位）。
- **字体/高亮**：SFRM/Computer Modern Roman/math，body/header 约 8.7 pt，regular/mixed，`pdf_object`、high confidence；无粗体数值、颜色或其他 highlighting。
- **数据/不确定性**：SDXL T=50 下三个 timestep 的 dominant term、Jacobian、secondary term 和相对比例点值；无样本、重复或统计不确定性。正文用 0.107%、0.194%、0.454% 支持忽略 Jacobian 次项的近似。
- **证据关系**：Appendix A 对 `J_xt≈0` 的理论/经验说明 → Table 4 的量级检查 → 简化累计误差计算可部署性；回连正文 Eq. (7) 与 TCEC 复杂度主张。
- **优点**：表头直接暴露 dominant/secondary 分解和相对比例；三个 noise stage 以语义标签配合 timestep，能快速检查近似边界。
- **缺陷**：百分比和绝对量的单位/聚合规则未说明；仅三个 timestep、单一 SDXL 设置，不能代表所有模型/调度器；caption 未写 W4A4 条件，需依赖正文。
- **可复用范式**：用“主项—敏感项—乘积—相对比例”短表给出近似的量级证据，并显式标出适用模型、bit-width、timestep 和重复方式。
- **证据锚点**：PDF p.13 Appendix A 中段 `Table 4 Statistics of dominant and secondary terms at different SDXL timesteps`；`pdf_object`。

### Table 5（p.16，appendix C；single column）

- **Caption（逐字）**：`Table 5 Quantization Performance Comparison of Different Models.`（8 词；编号粗体；动作：标题/比较；caption 本身不定义数据集、bit-width 或 Eq.10/13 的实验语境。）
- **表结构**：8 个数据行、6 列：Model、Method、FID↓、IR↑、LPIPS↓、PSNR↑；单层表头；SDXL 与 PixArt-σ 两个四行 row group，每组 FP16、SVDQuant、TCEC (Eq. 10)、TCEC (Eq. 13)。`rules=booktabs`，`header_levels=1`，`row_groups=2`，`decimal_precision=3`（IR/LPIPS 三位，FID/PSNR 混有一/两位）。
- **字体/高亮**：body/header 约 10 pt 的 SFRM/Computer Modern math，`pdf_object`、high confidence；Eq. 13 行的最佳值粗体，`highlighting=bold, best_second_best`；Eq. 10/13 引用使用蓝色链接文本，但不是数据色编码。
- **数据/不确定性**：SDXL 与 PixArt-σ 的 FID、IR、LPIPS、PSNR 点估计；FP16 的不可用 LPIPS/PSNR 为 `-`；无重复、seed、error bar 或区间。正文 Appendix C 用它比较 full-window Eq. (10) 与 two-step Eq. (13)，而 caption 未说明这一点。
- **证据关系**：Appendix C/DPM++ solver 推导和 Eq. (10)/(13) → Table 5 验证两种累计窗口 → 正文 Table 1 的主结果；表中 Eq. 13 的 SDXL FID 16.0/PSNR 27.3 与 Table 1 的 TCEC 行相连。
- **优点**：用同一四指标直接比较近似窗口；模型 row span 与横线清晰，Eq. 13 的最优值集中突出。
- **缺陷**：caption 过于泛化，读者不知道这是对 Eq.10/13 的消融；不同模型/数据条件没有写入表头；点值和缺失值语义未定义。
- **可复用范式**：对方法公式变体保持同一指标表，把模型和公式条件作为 row group，并在 caption 明确 solver、数据集、bit-width 与窗口含义。
- **证据锚点**：PDF p.16 顶部 `Table 5 Quantization Performance Comparison of Different Models`；`pdf_object`。

### Table 6（p.18，appendix F；single column）

- **Caption（逐字）**：`Table 6 Ablation experiment on the selection of regularization term λ1`（11 词；编号粗体；动作：标题/消融。）
- **表结构**：3 个数据行、6 列：Model、Method、FID↓、IR↑、LPIPS↓、PSNR↑；一个 SDXL row group，方法为 SVDQuant (W8A8)、TCEC (grid search)、TCEC (empirical rule)。`rules=booktabs`，`header_levels=1`，`row_groups=1`，`decimal_precision=3`；无竖线。
- **字体/高亮**：body/header 约 10 pt，SFRM/Computer Modern math，`pdf_object`、high confidence；empirical rule 的 FID、IR、PSNR 粗体，grid search 的 LPIPS 0.090 粗体；`highlighting=bold, best_second_best`，无色块/下划线。
- **数据/不确定性**：SDXL MJHQ 的四个质量/相似度点估计；对比 λ1 网格搜索与经验规则；无 seed、重复、误差或搜索范围/计算成本列。正文给出经验式 `λ1 = 0.01 × mean(μ̃²)/var(μ)`，表不复述公式。
- **证据关系**：Eq. (15) 的 λ1 正则化接口 → Table 6 的参数选择消融 → Appendix F 采用经验规则 → Table 1/5 主结果。它检验调参策略，不是模型结构消融。
- **优点**：基线、grid search、empirical rule 同表，指标方向箭头明确；粗体呈现“性能相近但调参更省”的结论。
- **缺陷**：表没有网格大小、候选范围、耗时或重复信息，无法量化“调参成本”；标题未写 SDXL/MJHQ/W8A8 条件；最后一列粗体不足以表达计算代价。
- **可复用范式**：把可部署默认规则与昂贵搜索并列，保留质量指标，并额外给出搜索预算/校准耗时和不确定性。
- **证据锚点**：PDF p.18 Appendix F 中段 `Table 6 Ablation experiment on the selection of regularization term λ1`；`pdf_object`。

### Table 7（p.18，appendix G；double column）

- **Caption（逐字）**：`Table 7 Comparison of SVDQuant and SVDQuant+TCEC across models and steps on MJHQ dataset.`（14 词；编号粗体；动作：标题、设置、比较。）
- **表结构**：8 个数据行、8 列：Model、Step、Method、FID↓、PSNR↑、FID↓、PSNR↑、Avg；四个 row group（SDXL/50、SDXL-Turbo/4、/3、/2），每组两行 SVDQuant/TCEC。表头没有给第二组 FID/PSNR 和 Avg 的“improvement”标签，虽正文说明它们是 gains。`rules=booktabs`，`header_levels=1`，`row_groups=4`，`decimal_precision=1`。
- **字体/高亮**：SFRM/Computer Modern，body/header 约 10 pt，`pdf_object`、high confidence；每组较优原始指标和 TCEC 结果粗体，`highlighting=bold, best_second_best`；无色块、下划线或不确定性。
- **数据/不确定性**：MJHQ 上 FID/PSNR 基线、TCEC 后值以及差值/Avg（例如 SDXL 50-step 的 0.6、0.9、1.5；SDXL-Turbo 2-step 的 0、0.2、0.2）；无 seed、重复或误差。Avg 在表中未定义，数值看起来是两项 gain 的和而非平均。
- **证据关系**：正文 Table 1 的 SDXL-Turbo 4-step 结果 → Appendix G 对 step=50/4/3/2 的敏感性 → 结论“≥3 steps 才有明显 gain”；与 Figure 1 累积误差机制相呼应。
- **优点**：把迭代步数作为显式 row group，直接展示 TCEC 收益随步数变化；差值列使改善方向可见。
- **缺陷**：重复 FID/PSNR 表头缺少“gain”语义，Avg 定义缺失；正文把 <0.2 称为实验噪声但没有噪声估计；只有四个步数设置。
- **可复用范式**：用“模型 × step × baseline/overlay”分组，原始指标与变化量相邻；明确变化列、聚合公式和测量误差。
- **证据锚点**：PDF p.18 Appendix G 中下部 `Table 7 Comparison of SVDQuant and SVDQuant+TCEC across models and steps on MJHQ dataset`；`pdf_object`。

### Table 8（p.19，appendix I；double column）

- **Caption（逐字）**：`Table 8 Performance comparison of SDXL with SVDQuant and TCEC at different data sizes.`（14 词；编号粗体；动作：标题、设置、比较。）
- **表结构**：5 个数据行、8 列：Model、Method、Data Size、FID↓、PSNR↑、FID↓、PSNR↑、Avg；一个 SDXL row group，baseline 一行、TCEC 的 2048/1024/512/256 四行；后两组 FID/PSNR 同样是变化量但未在表头标注。`rules=booktabs`，`header_levels=1`，`row_groups=1`，`decimal_precision=1`。
- **字体/高亮**：SFRM/Computer Modern，body/header 约 10 pt，`pdf_object`、high confidence；无粗体数值、无色块、无下划线，`highlighting=none`。
- **数据/不确定性**：SDXL、SVDQuant-W8A8 基线，TCEC 在 calibration size 2048/1024/512/256 的 FID/PSNR 和变化量/Avg；正文说明 COCO calibration、MJHQ evaluation，1024 为默认选择。没有 seed、重复、误差或校准时长。
- **证据关系**：Appendix I 的校准数据效率主张 → Table 8 的样本数敏感性 → Table 1 主结果的 1,024 prompts；与 Table 2 的 WebVid→VBench domain shift 论据共同支撑 robustness。
- **优点**：校准样本数和质量结果同表；平台化 row group 让性能平台期直观可读。
- **缺陷**：变化列的含义/Avg 未在 header 中定义；caption 未写 COCO/MJHQ、W8A8 或“calibration samples”；缺少效率成本和不确定性。
- **可复用范式**：把 calibration size、主指标和相对变化并列，显式标注训练/校准数据与评估数据，并增加耗时或内存代价。
- **证据锚点**：PDF p.19 Appendix I 下部 `Table 8 Performance comparison of SDXL with SVDQuant and TCEC at different data sizes`；`pdf_object`。

### Table 9（p.20，appendix J；double column）

- **Caption（逐字）**：`Table 9 Comparison of TCEC with the error compensation method QNCD.`（11 词；编号粗体；动作：标题、比较。）
- **表结构**：6 个数据行、8 列：Method、Bitwidth (W/A)，再按 DDIM(steps=100) 与 DDIM(steps=250) 分为 IS↑、FID↓、sFID↓；两层表头、4/8 与 4/6 两个 row group、booktabs 横线。`header_levels=2`，`row_groups=2`，`decimal_precision=2`。
- **字体/高亮**：SFRM/Computer Modern，body/header 约 10 pt，`pdf_object`、high confidence；每个 bitwidth 组 TCEC 行六个指标粗体，`highlighting=bold, best_second_best`；无颜色、下划线或不确定性。
- **数据/不确定性**：CIFAR-10 32×32、DDIM 100/250 steps、50,000 generated samples（由正文而非 caption 说明）；IS、FID、sFID 为点估计。没有 seed、重复、误差或区间；4/6 下 Q-Diffusion FID/sFID 的大幅退化是失败信号但未单独标注。
- **证据关系**：附录 J 为对 QNCD 的补充实验（TAC 无公开代码）→ Table 9 将 TCEC 的 cumulative correction 与单步 QNCD 对照 → 正文结论关于低 activation bitwidth 下优势增大；不把 QNCD 仓库当成 TCEC 源。
- **优点**：两个 DDIM 步数和两个 bitwidth 在同一嵌套表头下，保留 IS/FID/sFID 三个互补指标；TCEC 粗体行使方法结论易定位。
- **缺陷**：caption 未说明 CIFAR-10、样本数、DDIM 设定；没有不确定性和重复；W4A6 失败幅度大但没有单独 failure 标记。
- **可复用范式**：用 solver/step 的多层表头和 bitwidth row group 对齐基线、竞争方法和候选方法，同时在 caption 给出数据集、样本数、seed 和失败规则。
- **证据锚点**：PDF p.20 Appendix J 中上部 `Table 9 Comparison of TCEC with the error compensation method QNCD`；`pdf_object`。

## 跨对象系统判断

- **视觉叙事**：Figure 1 先建立 full precision→quantized→corrected 的误差传播接口；Table 1 给出图像主比较，Figure 2 以两 prompt 做 qualitative check；Table 2 把指标扩展到视频质量，Table 3 绑定额外成本。附录依次用 Table 4 验证 Jacobian 近似、Table 5 验证 solver/window、Figure 3 展开 timestep/error map、Table 6–8 做正则化/步数/校准规模消融，Table 9 与 QNCD 对照，Figure 4 扩展多 prompt 视觉证据。
- **Caption 系统**：所有 caption 用粗体对象编号、常规 serif 描述；Figure 3 的结论标题加粗，Figure 2/4 的 Prompt 标签加粗。Figure 1/3 caption 较自洽；定性图 caption 主要给 prompt，未给 seed/采样器；表 caption 多为短标题，关键实验条件常留在正文。
- **表头系统**：Table 1 与 Table 9 使用嵌套分组表头和方向箭头；Table 2–8 多为单层表头，通过横线和 row span 表示模型/bit-width/step。Table 7/8 的“重复指标列”实际承载变化量，但 header 未明确说明，是全篇最明显的表头语义缺口。
- **方法—结果—消融链**：Figure 1/Equation → Table 1–3；Appendix Table 4/5/6/7/8/9 分别对应近似、solver、λ1、steps、calibration、baseline comparison，链接完整，但定性图与量化表之间没有共同 ID/seed 或可复现数据键。
- **正文—附录链接**：正文明确调用 Appendix D（DPM++）、G（步数）、I（calibration）和 K（更多视觉结果）；Figure 2 的物理前置位置与 p.9 的调用形成浮动体顺序错位，但 caption 足以识别。Appendix E Figure 3 作为 mechanism case study，K Figure 4 作为 Figure 2 的 visual extension。
- **字体一致性**：主体 PDF 使用 SFRM/Computer Modern Roman 与 math，标题/对象编号使用 ByteSans；表格字号约 8.7–10 pt 且规则一致。嵌入栅格图内部使用无衬线/Arial-like 标签，Figure 4 列头的 ArialMT 为 PDF object，因而图内字体与正文存在可见断层。
- **颜色一致性**：表格整体黑白、粗体和 Table 2 的灰底形成稳定编码；Figure 1 的灰/棕红/绿是机制语义色，但 Figure 2/4 的自然图像没有可迁移的 TCEC 色值，Figure 3 又使用独立误差色图。跨 Figure 没有一个统一的 TCEC 颜色 token，需依赖列标题/row label。

## 最终判断

- **最可复用模式**：
  1. Figure 1 的 matched-path 机制图：同一时间轴同时呈现基线、误差传播和在线校正。
  2. Table 1/2/9 的嵌套条件 × 指标表头、方向箭头、row group 和粗体最佳值。
  3. Figure 3 的 timestep 列 × 输出/差异/局部误差行配对布局。
  4. Figure 2/4 的统一 prompt 行 × 方法列定性网格，但应补充 seed、采样器和量化核验。
- **最高价值对象**：Figure 1（解释核心机制）、Table 1（主图像质量比较）、Table 2（视频多维质量）、Table 3（质量收益对应的延迟成本）、Figure 3（误差传播的可视机制证据）。
- **失败模式**：caption 与表内容覆盖范围不总是一致（Table 2 的 W4A8 标题）；变化列/Avg 未在 Table 7/8 表头定义；所有表和定性图都缺少重复/不确定性；Figure 2/4 缺少行标签、seed 和实验条件；Figure 1/3 的密集栅格/颜色在灰度或小尺寸下可读性下降。
- **一句话视觉策略**：以 Figure 1 的误差传播路径建立方法接口，以分组指标表绑定质量与成本，再用 timestep 对齐的误差图和多 prompt 网格把机制解释延伸到定性证据与附录消融。

## 交付状态

本 Markdown 在 JSON 原子发布前完成；随后仅写入同名 JSON 并运行 `schemas/visual-audit.schema.json` 的 Draft 2020-12 校验。审计范围之外的文件不应被本任务修改。
