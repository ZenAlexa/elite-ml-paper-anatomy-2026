# Visual Audit: icml-2026-2801956159d6

## 审计范围与事实源
本轮只审计 `paper_id=icml-2026-2801956159d6`。事实源是 `corpus/preprints/icml-2026-2801956159d6.pdf`：27 个物理页；主文 pp.1–9，致谢/参考文献 pp.10–13，附录 A–D 为 pp.14–27。PDF 为 Figure/Table 清单事实源，逐页核对后得到 14 Figures（正文 8、附录 6）和 10 Tables（正文 8、附录 2）；p.4 的 Algorithm 1/2 明确不计入 Figure/Table。
所有 27 页先以 200 dpi（高于协议要求的 180 dpi）渲染为 PNG；用 `/tmp/icml280_contact1.jpg`、`/tmp/icml280_contact2.jpg` 检查全页，并对含对象的 p.1–3、p.5–9、p.22、p.25–27 逐对象复核几何、字体、绘图语法、caption/header、统计编码与证据关系。正文公式、参考文献和附录证明/实验叙述也完成了版面检查。
PDF 的 `pdfimages -list` 显示 p.2 概念图含多个嵌入 image objects，p.3 qualitative montage 含 24 个 JPEG tile，定量主图和附录图主要以 PNG raster 嵌入；`pdffonts` 确认正文/表格主要为 Nimbus Roman、Computer Modern math、Nimbus Mono 等 PDF 字体对象。

## 对象清单对账

|对象|PDF 页|模块|布局|
|---|---:|---|---|
|Figure 1|2|method|main / page_width|
|Figure 2|3|results|main / page_width|
|Figure 3|5|theory|main / page_width|
|Figure 4|7|results|main / page_width|
|Figure 5|8|ablation|main / inset|
|Figure 6|8|ablation|main / inset|
|Figure 7|9|results|main / page_width|
|Figure 8|9|ablation|main / inset|
|Figure 9|22|appendix C / negative guidance analysis|appendix / page_width|
|Figure 10|25|appendix D.1 efficiency analysis|appendix / inset|
|Figure 11|25|appendix D.2 robust scaling in δ|appendix / inset|
|Figure 12|25|appendix D.3 diversity analysis|appendix / inset|
|Figure 13|26|appendix D.4 SDXL DDIM-50|appendix / inset|
|Figure 14|27|appendix D.5 sample comparison|appendix / page_width|
|Table 1|1|introduction|main / single_column|
|Table 2|6|results|main / page_width|
|Table 3|7|results|main / inset|
|Table 4|7|results|main / inset|
|Table 5|8|results|main / inset|
|Table 6|8|ablation|main / inset|
|Table 7|8|results|main / inset|
|Table 8|9|results|main / single_column|
|Table 9|25|appendix D.1|appendix / inset|
|Table 10|26|appendix D.4|appendix / page_width|

reading 的 `visual_inventory` 有 26 项，其中 14 Figure、10 Table 与上述 PDF 清单逐项对齐；Algorithm 1/2 作为方法对象核对但没有写入本审计数组。没有遗漏跨页对象，也没有把普通公式、caption 内部 subfigure 标识或参考文献计为独立 Figure/Table。

## 源核查
- **作者链接与 GitHub**：论文摘要/README 明确链接 `https://github.com/aailab-kaist/Diffusion-LiDAR-Sampling`；README 标题、作者、arXiv 2602.03211、Phase 1/2 命令和三张 overview/performance rendered assets 与论文直接匹配。
- **本地 inventory**：`reports/tables/visual_source_inventory.csv` 将该 paper 标为 `no_public_source_found`，自动候选为 `zacharyhorvitz/Fk-Diffusion-Steering`；本地没有该 paper 的紧凑视觉源目录。该候选只被正文脚注用于 diversity protocol，不能代替论文 source。
- **检索路径**：按协议运行 `unicli search`、`agent-reach doctor --json`、`gh auth status`、`gh api repos/aailab-kaist/Diffusion-LiDAR-Sampling` 和 recursive tree inspection。GitHub tree 有 sampling implementation、README 和三张 rendered assets，但没有 plot/table generator、notebook、raw results 或 source figure builders。
- **最可信视觉源**：`https://arxiv.org/src/2602.03211` source package。`camera_ready.tex`、`appendix.tex`、`icml2026.sty` 和 `Figure/` 下 28 个 PNG/PDF assets 精确给出 include 顺序、figure/table captions、表头、可见数值与布局；表格值直接内嵌 TeX，没有单独 table generator。故 JSON `status=exact_visual_source`，source-exact 只用于 TeX/layout/caption/asset/字体对象事实。

## 全文视觉风格
- **结构**：主文 8 Figures、8 Tables；附录 6 Figures、2 Tables。主文 p.1–9 采用 ICML 双栏，Figure 1/2/3/4/7 与 Table 2 以页宽 float 为主，Tables 3–7、Figures 5/6/8 为 inset；附录 p.22、25–27 为单栏，复用窄图/宽表/qualitative montage。
- **字体**：定量 raster 图约 4.5–8.5 pt（median 6.5）；表体/表头约 7.5/8.0 pt。最终 PDF 的表格/正文 PDF object 主要是 Nimbus Roman No9 L、Computer Modern math；PNG/PDF assets 的图内字样为 DejaVu Sans/Arial/Aptos/Cambria 或渲染估计，Figure 1/2/14 的资产字体与表格不完全一致。
- **色彩与输出**：定量图以灰色 baseline 与蓝色 LiDAR/solver 色阶为主，红色用于 UG/DATE/Taylor，绿/紫用于 batch/metric/solver 扩展；Figure 6 为顺序色阶 heatmap，Figure 2/14 保留自然图像颜色。方法色通常与线型、marker、灰底或数字标注冗余，但小 inset 灰度可读性有限。
- **一致性**：caption 均位于对象附近并沿用 ICML 的简洁动作句；主 benchmark Table 2 与 appendix Table 10 共享多级表头。主要缺口是 captions 普遍不报告 seed/run/uncertainty，Figure 4/8/13 的混合或双轴语义依赖读者辨认，Figure 14 p.27 留白显著。

## Figure 逐对象审计
### Figure 1 (p.2 / method)
- **布局与职责**：`page_width`；类型 `pipeline, conceptual_diagram`；purpose `headline, method_interface, theory_mechanism`；复杂度 4/5（2 panels、2 series、约 18 marks、3 legend items、14 annotations）。
- **Caption（逐字）**：*Overview of the proposed sampling framework from prompt c. (a) To obtain marginal samples, we generate lookahead samples using a few-step solver and annotate them with a reward function. (b) To generate samples from the target reward-tilted distribution, LiDAR guides particles x_t toward high-reward lookahead sample x̂_0^1 and repels low-reward lookahead samples x̂_0^2, x̂_0^3. The guiding weight w_i^r − w_i provided by each lookahead sample x̂_0^i is proportional to its reward annotation value; see Eq. (16) for the definition.（79 words；moves=title, setup, encoding_key, comparison, appendix_pointer；headline_bold=False；self_contained=True；main_finding_stated=False）*。
- **Typography**：['ArialMT', 'Aptos', 'Cambria Math', 'Nimbus Roman No9 L', 'Computer Modern math']，约 5.5–10.0 pt（median 8.0），weight `regular, bold`，style `roman, italic`，provenance `mixed`。
- **Color**：`mixed`，约 9 色，HEX `#202020, #6E6E6E, #D9D9D9, #D62728, #E69F00, #56B4E9, #0072B2, #2CA02C, #FFFFFF`；黑/灰线条、圆点和容器组织两个阶段；红色/橙色权重和引导箭头区分 reward 影响；蓝色/绿色样本框区分高低 reward；白底缩略图展示样本。 grayscale_safe=`False`，redundant=`True`，provenance=`rendered_estimate`。
- **Plot grammar**：rendering `mixed`；x/y `none/none`；grid `none`；legend `True`（upper-left reward key and panel labels，shared=`False`）；direct_labels=`True`；markers/line styles `0/4`；reference lines `0`；hatching=`False`；uncertainty `none`；line width `1.2` pt；provenance `mixed`。
- **Encodings**：`{"x": null, "y": null, "color": "reward rank (high/mid/low), two sampling phases, and positive/negative guidance", "shape": "sample thumbnails, particle dots, reward markers, arrows, and boxed panels", "line": "solid reverse trajectories and arrows; red guidance vector/weight annotation; dashed or outlined containers", "facet": "(a) lookahead sampling and reward annotation; (b) LiDAR target sampling", "text": "prompt c, q(x̂0|c), p^rθ(x0|c), x_t, x̂_0^i, w_i^r−w_i, High/Mid/Low reward, phase labels, and equation callout"}`
- **Data/statistics**：概念示意图不编码样本统计或误差。Phase 1 从先验/中间状态生成多个 lookahead x̂0 并按 reward 标记；Phase 2 将高 reward 样本作为吸引、低 reward 样本作为排斥方向，解释 target reward-tilted sampling。
- **Evidence relation**：引言指出 backward rollout、Taylor bias、gradient/backprop 和 SMC collapse 的限制 → Figure 1 把 lookahead annotation 与 LiDAR target update 接成两阶段接口 → Algorithm 1/2 和 EFR 闭式推导 → Figure 3/4 及 Table 2 检验 scaling 与成本。
- **设计优点**：
  - 两阶段并置、箭头方向和高/低 reward 样本形成明确方法接口。
  - 粒子、缩略图、权重和数学标注对“吸引/排斥”提供形状与文字冗余编码。
  - 页宽布局能承载从 marginal samples 到 target distribution 的完整故事。
- **设计弱点：
  - 概念元素较多，缩小后 reward key、粒子轨迹和公式的视觉层级接近。
  - 颜色承担 reward 正负和阶段语义，caption 未逐项定义色值；灰度下弱。
  - 两个源 PDF 内含图标/缩略图，最终 PDF 混合嵌入 raster 与矢量，字体风格不完全统一。
- **可复用模式**：将 sampling preparation 与 guided target update 做成左右两阶段 schematic，并用 reward 排序、箭头方向和权重公式同时编码机制。
- **证据**：p.2；rendered_observation；p.2 Figure 1 full object, labels, caption, and surrounding layout inspected from 200 dpi render。

### Figure 2 (p.3 / results)
- **布局与职责**：`page_width`；类型 `qualitative_grid, image_montage`；purpose `qualitative_evidence, main_comparison`；复杂度 5/5（6 panels、4 series、约 24 marks、0 legend items、8 annotations）。
- **Caption（逐字）**：*Sampling results for the prompt “a photo of a yellow bird and a black motorcycle.” Among the lookahead samples, the blue boxes indicate all samples used for LiDAR (DPM-3, n=3), ordered from left to right by increasing reward values. The green box indicates the lookahead sample with the highest reward used for LiDAR (DPM-5, n=50).（55 words；moves=title, setup, encoding_key, comparison；headline_bold=False；self_contained=True；main_finding_stated=False）*。
- **Typography**：['ArialMT', 'Nimbus Roman No9 L', 'Computer Modern math']，约 5.0–9.0 pt（median 7.0），weight `regular, bold`，style `roman, italic`，provenance `mixed`。
- **Color**：`mixed`，约 8 色，HEX `#2B6CB0, #2F855A, #BDBDBD, #111111, #E7B416, #D55E00, #FFFFFF, #333333`；蓝框标记用于 LiDAR 的全部 DPM-3/n=3 lookahead samples，绿框标记 DPM-5/n=50 选取的最高 reward 样本；方法标题和图像内容承担其余语义。 grayscale_safe=`False`，redundant=`True`，provenance=`rendered_estimate`。
- **Plot grammar**：rendering `raster`；x/y `none/none`；grid `none`；legend `False`（None，shared=`False`）；direct_labels=`True`；markers/line styles `None/None`；reference lines `0`；hatching=`False`；uncertainty `none`；line width `None` pt；provenance `rendered_estimate`。
- **Encodings**：`{"x": null, "y": null, "color": "method group and blue/green selection boxes", "shape": "photographic image tiles", "line": null, "facet": "Vanilla, Gradient Guidance, Sequential Monte Carlo, Lookahead Samples/LiDAR output", "text": "method headings, prompt string, blue-box and green-box conventions"}`
- **Data/statistics**：一个 prompt 下以两行×三个方法组展示 raster samples；lookahead row 中的蓝框按 reward 从低到高排列，绿色高 reward sample 对应后续 DPM-5/n=50 LiDAR target output。无数值轴、均值、误差或重复统计。
- **Evidence relation**：正文由 SMC 高维 resampling collapse 与 gradient guidance 对比引出 → Figure 2 提供“样本多样性/语义对齐”的直观证据 → Table 2 的 multi-metric benchmark、Table 7 的 sample-based comparison 与 Figure 14 的额外 prompt 扩展。
- **设计优点**：
  - 统一 prompt 和方法分组使视觉差异可快速扫描。
  - 蓝/绿框及 reward 排序把中间 lookahead 与最终 target output 的对应关系显式化。
  - 页宽 montage 适合展示 prompt-image alignment 与 SMC collapse 这类非数值现象。
- **设计弱点：
  - 没有 seed、样本数量之外的生成重复或评测筛选说明，定性样本不构成总体性能估计。
  - 图像 tiles 很密，方法标题和框线在缩小后可读性受限；蓝绿框对色觉/灰度读者不稳。
  - caption 说明了框和排序，却没有指出每组输出的采样步、λ 或评价指标。
- **可复用模式**：用同一 prompt 的 baseline、guidance、particle method 和 proposed 四组 montage 展示机制后果；将中间样本与最终样本用边框和排序连接。
- **证据**：p.3；rendered_observation；p.3 Figure 2 full object, labels, caption, and surrounding layout inspected from 200 dpi render。

### Figure 3 (p.5 / theory)
- **布局与职责**：`page_width`；类型 `line`；purpose `theory_mechanism, headline, robustness`；复杂度 3/5（3 panels、5 series、约 75 marks、5 legend items、6 annotations）。
- **Caption（逐字）**：*Scaling behavior of the LiDAR sampler under different lookahead strategies and varying numbers of lookahead samples n. The vanilla method uses only the Stein score sθ, whereas LiDAR (w/ DPM-δ) incorporates lookahead samples with δ discretization steps. All results are obtained using SD v1.5 with ImageReward annotations. The legend is shared across all three panels.（55 words；moves=title, setup, encoding_key, comparison, abbreviation_definition；headline_bold=False；self_contained=True；main_finding_stated=False）*。
- **Typography**：['DejaVu Sans', 'DejaVu Sans Mono', 'Nimbus Roman No9 L', 'Computer Modern math']，约 4.5–8.5 pt（median 6.5），weight `regular, bold`，style `roman, italic`，provenance `rendered_estimate`。
- **Color**：`categorical`，约 5 色，HEX `#777777, #9EC5E5, #5B9BD5, #2F75B5, #1F4E79`；灰色为 Vanilla；由浅蓝到深蓝分别为 LiDAR DPM-3、DPM-4、DPM-5、DPM-10，颜色同时编码 lookahead solver δ。 grayscale_safe=`False`，redundant=`True`，provenance=`rendered_estimate`。
- **Plot grammar**：rendering `raster`；x/y `linear/linear`；grid `both`；legend `True`（inside third panel; shared semantics across panels，shared=`True`）；direct_labels=`False`；markers/line styles `1/1`；reference lines `0`；hatching=`False`；uncertainty `none`；line width `1.0` pt；provenance `rendered_estimate`。
- **Encodings**：`{"x": "number of lookahead samples n", "y": "average ImageReward, CLIP Score, or GenEval (↑)", "color": "Vanilla versus LiDAR solver discretization δ", "shape": "circular point markers", "line": "solid interpolating curves", "facet": "ImageReward average, CLIP Score average, GenEval average", "text": "panel metric labels, Vanilla/LiDAR (w/ DPM-δ) legend, red dashed low-n zoom boxes"}`
- **Data/statistics**：三个 metric panels 使用每个 lookahead-sample 数量 n 的点估计曲线；n 从低值扩展到约 400，局部红框放大低 n 区间。结果来自 SD v1.5、ImageReward annotations；没有 error bar、band、seed 或置信区间。
- **Evidence relation**：理论部分的 δ discretization 与 n finite-sample gap → Figure 3 显示随 solver steps 和 lookahead count 增长的经验 scaling → Appendix Figure 11 分解 wall-clock/δ trade-off，Table 2 将默认 DPM-5/n=50 放回主 benchmark。
- **设计优点**：
  - 三 panel 共享 n 与颜色语义，直接呈现 reward/semantic/compositional metrics 的 scaling。
  - 低 n 红色 zoom box 避免早期差异被长尾 n 范围压扁。
  - caption 明确 Vanilla、DPM-δ、SD v1.5 和共享 legend，复现入口清晰。
- **设计弱点：
  - 三项 metric 的 y 量纲不同，纵轴范围也不同，跨 panel 的线高不可直接比较。
  - 颜色仅有色阶差异，灰度打印时 DPM-3/4/5/10 难分；局部 zoom 框未在 caption 解释。
  - 单个点/线没有重复或不确定性，scaling law 的形状依赖少量 n 网格。
- **可复用模式**：用固定 baseline 加深度递进的同色系 solver 曲线，在共享 x 轴上展示准确性/样本量 scaling，并用局部 zoom 保留小 n 证据。
- **证据**：p.5；rendered_observation；p.5 Figure 3 full object, labels, caption, and surrounding layout inspected from 200 dpi render。

### Figure 4 (p.7 / results)
- **布局与职责**：`page_width`；类型 `line`；purpose `main_comparison, efficiency_cost, ablation`；复杂度 3/5（3 panels、4 series、约 55 marks、4 legend items、7 annotations）。
- **Caption（逐字）**：*(a) Performance trade-offs between UG and LiDAR. (b, c) Efficiency–performance trade-offs for test-time scaling methods. Vanilla increases sampling steps, DATE adjusts gradient update frequency, and LiDAR controls n. The cross marker indicates the performance in Table 2. The black dotted line indicates the time difference for LiDAR to reach DATE’s maximum scaling performance.（53 words；moves=title, setup, encoding_key, comparison, abbreviation_definition, appendix_pointer；headline_bold=False；self_contained=False；main_finding_stated=False）*。
- **Typography**：['DejaVu Sans', 'DejaVu Sans Mono', 'Nimbus Roman No9 L', 'Computer Modern math']，约 4.5–8.5 pt（median 6.5），weight `regular, bold`，style `roman, italic`，provenance `rendered_estimate`。
- **Color**：`categorical`，约 4 色，HEX `#777777, #D62728, #2F75B5, #8E63CE`；灰色 Vanilla、红色 UG/DATE baseline、蓝色 LiDAR；SDXL panel 另以紫色 DMD-1 区分另一 LiDAR solver；cross 为 Table 2 setting，黑色 dotted annotation 标记 time gap。 grayscale_safe=`False`，redundant=`True`，provenance=`rendered_estimate`。
- **Plot grammar**：rendering `raster`；x/y `linear/linear`；grid `both`；legend `True`（inside each panel，shared=`False`）；direct_labels=`False`；markers/line styles `2/2`；reference lines `2`；hatching=`False`；uncertainty `none`；line width `1.0` pt；provenance `rendered_estimate`。
- **Encodings**：`{"x": "guidance scale λ in (a); wall-clock time (sec.) in (b,c)", "y": "ImageReward avg/HPS avg in (a); GenEval avg in (b,c)", "color": "Vanilla, UG, DATE, LiDAR solver/backbone", "shape": "circle curves and cross marker for Table 2 point", "line": "solid performance curves and black dotted time-difference guide", "facet": "(a) SD v1.5 guidance scale; (b) SD v1.5 efficiency; (c) SDXL efficiency", "text": "local legends, 7.0×/9.5× faster callouts, cross marker, axis directions"}`
- **Data/statistics**：Panel (a) traces ImageReward–HPS trade-off under guidance scale; panels (b,c) trace GenEval against wall-clock time across sampling strategies. Crosses anchor the Table 2 configuration; dotted segments compare LiDAR time with DATE maximum scaling performance. No error bars or repeated-run summaries.
- **Evidence relation**：Table 2 baseline comparison → Figure 4 tests the performance-cost frontier and the 9.5× SDXL speed claim → Figure 8 λ/EFR ablation explains guidance quality, while Appendix Table 10 extends the SDXL target-sampler setting.
- **设计优点**：
  - Metrics and wall-clock cost share a compact three-panel decision surface.
  - Cross/Table 2 anchor and dotted time difference make the headline efficiency comparison concrete.
  - Panel (a) separates reward hacking trade-off from backbone-specific cost panels.
- **设计弱点：
  - Panel (a) and panels (b,c) use different x variables; shared visual treatment can mislead a quick reader.
  - Multiple curves and dotted guides are small in the page-width composite; 7.0×/9.5× labels are annotations rather than interval estimates.
  - Caption does not state averaging unit, GPU, λ range, or whether time includes lookahead annotation.
- **可复用模式**：Place quality-versus-guidance and quality-versus-time panels together, using a common table anchor and explicit dotted cost gap to turn efficiency into a readable claim.
- **证据**：p.7；rendered_observation；p.7 Figure 4 full object, labels, caption, and surrounding layout inspected from 200 dpi render。

### Figure 5 (p.8 / ablation)
- **布局与职责**：`inset`；类型 `scatter`；purpose `ablation, mechanism`；复杂度 2/5（1 panels、3 series、约 18 marks、3 legend items、0 annotations）。
- **Caption（逐字）**：*Results on mixed reward annotations on lookahead samples.（8 words；moves=title, setup；headline_bold=False；self_contained=False；main_finding_stated=False）*。
- **Typography**：['DejaVu Sans', 'DejaVu Sans Mono', 'Nimbus Roman No9 L', 'Computer Modern math']，约 5.0–8.5 pt（median 6.5），weight `regular, bold`，style `roman, italic`，provenance `rendered_estimate`。
- **Color**：`categorical`，约 3 色，HEX `#59A14F, #D62728, #2F75B5`；绿色 Mix、红色 CLIP Score、蓝色 ImageReward；x=ImageReward，y=CLIP Score，点的位置表达 mixed-reward operating points。 grayscale_safe=`False`，redundant=`True`，provenance=`rendered_estimate`。
- **Plot grammar**：rendering `raster`；x/y `linear/linear`；grid `both`；legend `True`（lower-left，shared=`False`）；direct_labels=`False`；markers/line styles `1/0`；reference lines `0`；hatching=`False`；uncertainty `none`；line width `1.0` pt；provenance `rendered_estimate`。
- **Encodings**：`{"x": "ImageReward average (↑)", "y": "CLIP Score average (↑)", "color": "reward annotation source/mix", "shape": "filled circular operating points", "line": null, "facet": null, "text": "Mix, CLIP Score, ImageReward legend"}`
- **Data/statistics**：散点展示不同 reward annotation 配方下的 ImageReward 与 CLIP Score 点估计，未画连接线、frontier envelope、error bar 或重复分布。
- **Evidence relation**：正文 Table 6 扫描 IR/CLIP 权重 → Figure 5 将相同 mixture 映射到二维 reward frontier → Figure 6 检验 IR、CLIP、HPS、GenEval 的相关性，支撑“少量混合 reward 形成多指标折中”的解释。
- **设计优点**：
  - 二维轴直接表达 reward mixture 的性能折中。
  - 颜色把混合配方与两个单一 reward baseline 区分，图例短且局部。
  - 小 inset 与 Table 6 相邻，读者可把点与精确表格值对应。
- **设计弱点：
  - 点没有 mixture percentage 标签，脱离 Table 6 无法知道每点的权重。
  - 颜色和点相互遮挡且灰度不可稳健区分；没有不确定性或样本数。
  - caption 极短，未定义点的聚合单位、solver 或四项指标的关系。
- **可复用模式**：把表格中的 reward-mixture sweep 投影成二维 scatter，再保留表格作为精确数值来源；每个点应附权重标签或可追溯索引。
- **证据**：p.8；rendered_observation；p.8 Figure 5 full object, labels, caption, and surrounding layout inspected from 200 dpi render。

### Figure 6 (p.8 / ablation)
- **布局与职责**：`inset`；类型 `heatmap, matrix`；purpose `ablation, mechanism, experimental_design`；复杂度 3/5（1 panels、4 series、约 16 marks、0 legend items、16 annotations）。
- **Caption（逐字）**：*Pairwise correlations between metrics on lookahead samples (DPM-5).（8 words；moves=title, setup, abbreviation_definition；headline_bold=False；self_contained=False；main_finding_stated=False）*。
- **Typography**：['DejaVu Sans', 'DejaVu Sans Mono', 'Nimbus Roman No9 L', 'Computer Modern math']，约 5.0–8.0 pt（median 6.5），weight `regular, bold`，style `roman, italic`，provenance `rendered_estimate`。
- **Color**：`sequential`，约 5 色，HEX `#F6EFA6, #B4DDB4, #4FC3C1, #2C7FB8, #0B3C5D`；颜色深浅编码指标两两相关系数，方阵 diagonal=1；数字标注提供不依赖色觉的精确读数，右侧 colorbar 给出 0–1 方向。 grayscale_safe=`False`，redundant=`True`，provenance=`rendered_estimate`。
- **Plot grammar**：rendering `raster`；x/y `categorical/categorical`；grid `none`；legend `False`（None，shared=`False`）；direct_labels=`True`；markers/line styles `0/0`；reference lines `0`；hatching=`False`；uncertainty `none`；line width `None` pt；provenance `rendered_estimate`。
- **Encodings**：`{"x": "metric categories IR, CLIP, HPS, GenEval", "y": "same metric categories", "color": "pairwise correlation magnitude", "shape": "square matrix cells", "line": null, "facet": null, "text": "numeric correlation values inside cells and colorbar"}`
- **Data/statistics**：4×4 pairwise correlation matrix for metrics evaluated on DPM-5 lookahead samples; diagonal cells equal 1 and off-diagonal numbers are shown directly. It is descriptive correlation, not a causal effect or uncertainty display.
- **Evidence relation**：正文解释 LiDAR 只用 IR 仍改善 HPS/GenEval → Figure 6 supplies metric-correlation mechanism → Table 6/Figure 5 test alternative reward mixtures and their multi-metric frontier。
- **设计优点**：
  - Numeric overlays make the heatmap auditable without extracting color.
  - Symmetric matrix and diagonal highlight the metric relation structure compactly.
  - The colorbar gives an explicit magnitude scale and the four metrics match Table 2 headers.
- **设计弱点：
  - Correlation is visually presented without sample size, prompt set, or confidence intervals.
  - Warm-to-cool colors are not fully grayscale-safe and the small inset reduces numeric legibility.
  - Caption does not state whether correlations are Pearson/Spearman or how lookahead samples were pooled.
- **可复用模式**：Combine a sequential correlation heatmap with printed coefficients; state correlation estimator and aggregation unit in the caption or adjacent text.
- **证据**：p.8；rendered_observation；p.8 Figure 6 full object, labels, caption, and surrounding layout inspected from 200 dpi render。

### Figure 7 (p.9 / results)
- **布局与职责**：`page_width`；类型 `line`；purpose `main_comparison, efficiency_cost, robustness`；复杂度 4/5（4 panels、10 series、约 70 marks、5 legend items、15 annotations）。
- **Caption（逐字）**：*(Left three) Efficiency–performance trade-offs of LiDAR applied to each method as the number of target samples N varies. (Right) Time required to generate N samples.（25 words；moves=title, setup, comparison；headline_bold=False；self_contained=False；main_finding_stated=False）*。
- **Typography**：['DejaVu Sans', 'DejaVu Sans Mono', 'Nimbus Roman No9 L', 'Computer Modern math']，约 4.5–8.5 pt（median 6.5），weight `regular, bold`，style `roman, italic`，provenance `rendered_estimate`。
- **Color**：`categorical`，约 5 色，HEX `#D62728, #2F75B5, #777777, #A65E2E, #111111`；红色是原始 SMC/BoN/SMC+BoN 或 baseline，蓝色为对应 +LiDAR；右侧时间面板再区分 Vanilla/UG/LiDAR，灰色/黑色为 reference。 grayscale_safe=`False`，redundant=`True`，provenance=`rendered_estimate`。
- **Plot grammar**：rendering `raster`；x/y `linear/linear`；grid `both`；legend `True`（inside panels; local legends，shared=`False`）；direct_labels=`False`；markers/line styles `1/2`；reference lines `0`；hatching=`False`；uncertainty `none`；line width `1.0` pt；provenance `rendered_estimate`。
- **Encodings**：`{"x": "wall-clock time (sec.) in left three; target sample count N in right", "y": "ImageReward average (↑); right panel target sampling time (sec., log-spaced display)", "color": "base method versus +LiDAR and target-sampling method", "shape": "circle points at N=4,8,16,32,64", "line": "solid trade-off curves", "facet": "SMC, BoN, SMC+BoN; right time-versus-N panel", "text": "N=4/8/16/32/64 point labels and local method legends"}`
- **Data/statistics**：Left three panels connect ImageReward to wall-clock time as target particle count N varies for SMC, BoN, and SMC+BoN; right panel reports time required to generate N samples. The right y-axis is visually logarithmic/spaced by powers of ten, while the left panels use linear time; all are point estimates without uncertainty.
- **Evidence relation**：Section 4.3 separates SMC/BoN use cases from four-image Table 2 → Figure 7 shows LiDAR’s orthogonal efficiency/performance benefit as N grows → Table 8 gives exact ImageReward values by N and Appendix Figure 10 shows target-sampling cost versus lookahead n.
- **设计优点**：
  - Four aligned panels preserve the same N labels while covering three interaction methods and a cost curve.
  - Direct N annotations remove ambiguity about which point is shared/reused.
  - Red/blue pairing makes the orthogonal LiDAR effect readable at a glance.
- **设计弱点：
  - Mixed axis semantics (right time axis and left trade-off panels) are easy to miss; the small right panel uses log-spaced time.
  - Many overlapping curves/labels and no error bars make small differences hard to assess.
  - Caption does not define whether wall-clock includes shared lookahead overhead or the exact hardware/batch protocol.
- **可复用模式**：Use matched baseline/+method pairs across interaction methods, with shared sample-count labels and a separate cost panel; explicitly mark any mixed axis transformation.
- **证据**：p.9；rendered_observation；p.9 Figure 7 full object, labels, caption, and surrounding layout inspected from 200 dpi render。

### Figure 8 (p.9 / ablation)
- **布局与职责**：`inset`；类型 `line`；purpose `ablation, theory_mechanism, efficiency_cost`；复杂度 3/5（2 panels、4 series、约 44 marks、3 legend items、4 annotations）。
- **Caption（逐字）**：*(Left) Ablation on λ for LiDAR. (Right) EFR approximation error of Taylor and LiDAR according to λ.（17 words；moves=title, setup, encoding_key, abbreviation_definition；headline_bold=False；self_contained=False；main_finding_stated=False）*。
- **Typography**：['DejaVu Sans', 'DejaVu Sans Mono', 'Nimbus Roman No9 L', 'Computer Modern math']，约 5.0–8.5 pt（median 6.5），weight `regular, bold`，style `roman, italic`，provenance `rendered_estimate`。
- **Color**：`mixed`，约 5 色，HEX `#2F75B5, #D62728, #777777, #59A14F, #8E63CE`；左图蓝色 LiDAR 曲线与灰色/彩色 vanilla reference；右图红色 Taylor、蓝色 LiDAR。λ 为 log x，颜色同时区分 approximation method 与 baseline。 grayscale_safe=`False`，redundant=`True`，provenance=`rendered_estimate`。
- **Plot grammar**：rendering `raster`；x/y `log/linear`；grid `both`；legend `True`（inside each panel，shared=`False`）；direct_labels=`False`；markers/line styles `1/2`；reference lines `2`；hatching=`False`；uncertainty `none`；line width `1.0` pt；provenance `rendered_estimate`。
- **Encodings**：`{"x": "reward strength λ (log scale)", "y": "ImageReward/CLIP Score averages in left; EFR approximation MSE (×10^6) in right", "color": "LiDAR, Taylor, and vanilla reference", "shape": "circle data markers", "line": "solid estimates and dashed horizontal vanilla references", "facet": "λ performance ablation; EFR approximation error", "text": "metric labels, method legend, λ ticks"}`
- **Data/statistics**：Left panel varies λ and plots ImageReward and CLIP Score on separate colored y axes/labels with dashed vanilla references; right panel plots accumulated EFR approximation MSE for Taylor versus LiDAR. λ is logarithmic. No uncertainty band or replicate distribution is shown; Appendix B.4 states T=100, K=20 for the MSE protocol.
- **Evidence relation**：Main analysis fixes λ=5000 after the left ablation and claims Taylor error grows with λ → Figure 8 directly tests λ and EFR approximation quality → Appendix Figure 13 repeats λ behavior for HPS/GenEval under SDXL DDIM-50.
- **设计优点**：
  - Log λ axis exposes behavior over several orders of magnitude.
  - Reference lines and paired Taylor/LiDAR traces separate absolute performance from approximation error.
  - The two panels join the hyperparameter choice to its theoretical EFR mechanism.
- **设计弱点：
  - The left panel uses two metric scales while the caption does not explicitly call out dual axes.
  - MSE is scaled (×10^6) and the caption omits this unit; small inset labels hinder exact reading.
  - No uncertainty or per-trajectory breakdown, so approximation robustness is summarized by a single aggregate curve.
- **可复用模式**：Pair a log-scale strength ablation with an approximation-error panel and explicit baseline references; state all rescaling and aggregation constants beside the axes.
- **证据**：p.9；rendered_observation；p.9 Figure 8 full object, labels, caption, and surrounding layout inspected from 200 dpi render。

### Figure 9 (p.22 / appendix C / negative guidance analysis)
- **布局与职责**：`page_width`；类型 `line`；purpose `ablation, failure, robustness`；复杂度 3/5（3 panels、3 series、约 28 marks、0 legend items、3 annotations）。
- **Caption（逐字）**：*Analysis of ImageReward trends by varying a single parameter of SR (Kirchhof et al., 2025) while others are fixed.（19 words；moves=title, setup, comparison, abbreviation_definition；headline_bold=False；self_contained=False；main_finding_stated=False）*。
- **Typography**：['DejaVu Sans', 'DejaVu Sans Mono', 'Nimbus Roman No9 L', 'Computer Modern math']，约 5.0–9.0 pt（median 7.0），weight `regular, bold`，style `roman, italic`，provenance `rendered_estimate`。
- **Color**：`categorical`，约 1 色，HEX `#2F75B5`；三 panel 均使用蓝色 ImageReward average 曲线；区分 s、K、r 依赖 x 轴标题和 facet caption，而非颜色。 grayscale_safe=`True`，redundant=`True`，provenance=`rendered_estimate`。
- **Plot grammar**：rendering `raster`；x/y `linear/linear`；grid `both`；legend `False`（None，shared=`False`）；direct_labels=`False`；markers/line styles `1/1`；reference lines `0`；hatching=`False`；uncertainty `none`；line width `1.0` pt；provenance `rendered_estimate`。
- **Encodings**：`{"x": "one SR hyperparameter per panel: s, K, or r", "y": "ImageReward average (↑)", "color": "single SR sweep trace", "shape": "circular points", "line": "solid connecting sweep values", "facet": "(a) s; (b) K; (c) r", "text": "panel labels and variable-specific ticks"}`
- **Data/statistics**：Each panel varies one SR (negative/sample-repulsion) hyperparameter while others are fixed and reports ImageReward averages. The curves are point estimates over the displayed sweep; no error bars, baseline bands, or uncertainty definition is provided.
- **Evidence relation**：Table 7 shows Safe-D/SR negative guidance is weak for reward alignment → Appendix Figure 9 sweeps SR strength, reference-set size, and radius to check that failure is not one arbitrary setting → supports LiDAR’s soft attract/repel design.
- **设计优点**：
  - One blue trace per panel minimizes legend overhead and makes non-monotone failure patterns visible.
  - The shared y metric and aligned three-panel layout support robustness checks across s/K/r.
  - Appendix text gives sweep sets and best settings, making points traceable to the protocol.
- **设计弱点：
  - Caption does not repeat the sweep ranges or best setting; reader must consult preceding text.
  - Non-monotonicity is descriptive without replicate variability or confidence information.
  - Single color and no direct value labels make small changes difficult to recover from the raster inset.
- **可复用模式**：Audit a competing guidance family by one-factor-at-a-time sweeps with common metric axes; put sweep grids and fixed settings in nearby text.
- **证据**：p.22；rendered_observation；p.22 Figure 9 full object, labels, caption, and surrounding layout inspected from 200 dpi render。

### Figure 10 (p.25 / appendix D.1 efficiency analysis)
- **布局与职责**：`inset`；类型 `line`；purpose `efficiency_cost, reproduction`；复杂度 3/5（2 panels、3 series、约 42 marks、3 legend items、0 annotations）。
- **Caption（逐字）**：*The inference cost for target sampling (Algorithm 2) according to the number of lookahead samples n for SD v1.5.（19 words；moves=title, setup, abbreviation_definition；headline_bold=False；self_contained=True；main_finding_stated=False）*。
- **Typography**：['DejaVu Sans', 'DejaVu Sans Mono', 'Nimbus Roman No9 L', 'Computer Modern math']，约 5.0–8.5 pt（median 6.5），weight `regular, bold`，style `roman, italic`，provenance `rendered_estimate`。
- **Color**：`categorical`，约 3 色，HEX `#D62728, #2F75B5, #2CA02C`；红/蓝/绿分别为 Batch 4/8/16；同一颜色跨两个 panel 表示对应 batch size。 grayscale_safe=`False`，redundant=`True`，provenance=`rendered_estimate`。
- **Plot grammar**：rendering `raster`；x/y `linear/linear`；grid `both`；legend `True`（upper-right inside each panel，shared=`True`）；direct_labels=`False`；markers/line styles `1/1`；reference lines `0`；hatching=`False`；uncertainty `none`；line width `1.0` pt；provenance `rendered_estimate`。
- **Encodings**：`{"x": "number of lookahead samples n", "y": "Memory (GiB) in left; target sampling time (sec.) in right", "color": "batch size 4/8/16", "shape": "circular markers", "line": "solid cost traces", "facet": "memory and target sampling time", "text": "Batch 4/8/16 legend and n ticks up to 800"}`
- **Data/statistics**：Two cost panels show memory and target sampling time as lookahead sample count n grows to 800 for three batch sizes. The curves are point estimates; no error bars, run count, or hardware detail appears in the caption, although surrounding text states costs are nearly unchanged for n<800.
- **Evidence relation**：Appendix D.1’s cost claim → Figure 10 tests whether lookahead sample storage changes target-stage cost → Table 9 decomposes the default DPM-5/n=50 total and Figure 7 reuses shared-lookahead logic for N target samples.
- **设计优点**：
  - Same n grid and batch color across memory/time permit cost comparison.
  - Two panels separate resource dimensions without a dual y axis.
  - Flat traces make the “nearly identical target sampling cost” claim visually inspectable.
- **设计弱点：
  - Batch colors are not grayscale-safe and legend is duplicated.
  - Caption omits hardware, units are only in axis labels, and no uncertainty/measurement repeats are shown.
  - The n<800 boundary mentioned in prose is not marked in the plot.
- **可复用模式**：Show resource scaling as matched panels with a stable batch-color mapping; annotate the budget boundary that motivates the claim.
- **证据**：p.25；rendered_observation；p.25 Figure 10 full object, labels, caption, and surrounding layout inspected from 200 dpi render。

### Figure 11 (p.25 / appendix D.2 robust scaling in δ)
- **布局与职责**：`inset`；类型 `line`；purpose `efficiency_cost, robustness, reproduction`；复杂度 3/5（1 panels、7 series、约 45 marks、7 legend items、0 annotations）。
- **Caption（逐字）**：*Wall-clock time for various lookahead solvers and numbers of lookahead samples on SD v1.5.（14 words；moves=title, setup；headline_bold=False；self_contained=False；main_finding_stated=False）*。
- **Typography**：['DejaVu Sans', 'DejaVu Sans Mono', 'Nimbus Roman No9 L', 'Computer Modern math']，约 5.0–8.5 pt（median 6.5），weight `regular, bold`，style `roman, italic`，provenance `rendered_estimate`。
- **Color**：`categorical`，约 7 色，HEX `#2CA02C, #D62728, #2F75B5, #9467BD, #8C564B, #111111, #777777`；LiDAR DPM-3/4/5/10/15 用不同颜色，黑色为 Vanilla，灰色为 Universal Guidance；颜色对应 solver δ 或 baseline。 grayscale_safe=`False`，redundant=`True`，provenance=`rendered_estimate`。
- **Plot grammar**：rendering `raster`；x/y `linear/linear`；grid `both`；legend `True`（inside panel, lower/center，shared=`False`）；direct_labels=`False`；markers/line styles `1/1`；reference lines `0`；hatching=`False`；uncertainty `none`；line width `1.0` pt；provenance `rendered_estimate`。
- **Encodings**：`{"x": "wall-clock time (sec.)", "y": "ImageReward average (↑)", "color": "lookahead solver DPM-δ and Vanilla/Universal Guidance", "shape": "circular points", "line": "solid performance–time traces", "facet": null, "text": "DPM-3/4/5/10/15, Vanilla, Universal Guidance legend"}`
- **Data/statistics**：One efficiency frontier plots ImageReward against wall-clock time for five LiDAR lookahead solvers plus Vanilla and Universal Guidance. It uses point traces with no uncertainty or repeated-run envelope; δ is represented by legend series, while n varies along each trace.
- **Evidence relation**：Appendix D.2 explains the δ–n lookahead budget trade-off → Figure 11 shows near-overlapping frontiers and motivates DPM-5 default → Figure 3 gives main-text quality scaling and Table 2 fixes DPM-5/n=50 cost.
- **设计优点**：
  - All solver choices share one frontier, making the δ trade-off compact.
  - Legend explicitly names solver step counts and baselines.
  - ImageReward and time match the paper’s main decision quantities.
- **设计弱点：
  - Seven traces are dense in a small panel; color-only solver identification is weak in grayscale.
  - No explicit n labels on points, so the solver-versus-sample trade-off is hard to recover.
  - Caption omits whether time includes reward annotation and the number of sampled prompts/images.
- **可复用模式**：Overlay solver-specific performance–cost traces, but label the sample-count progression or provide an accompanying table for exact operating points.
- **证据**：p.25；rendered_observation；p.25 Figure 11 full object, labels, caption, and surrounding layout inspected from 200 dpi render。

### Figure 12 (p.25 / appendix D.3 diversity analysis)
- **布局与职责**：`inset`；类型 `line`；purpose `robustness, failure, ablation`；复杂度 3/5（2 panels、2 series、约 40 marks、2 legend items、0 annotations）。
- **Caption（逐字）**：*Diversity as a function of λ and n for SD v1.5.（11 words；moves=title, setup；headline_bold=False；self_contained=True；main_finding_stated=False）*。
- **Typography**：['DejaVu Sans', 'DejaVu Sans Mono', 'Nimbus Roman No9 L', 'Computer Modern math']，约 5.0–8.5 pt（median 6.5），weight `regular, bold`，style `roman, italic`，provenance `rendered_estimate`。
- **Color**：`categorical`，约 2 色，HEX `#2F75B5, #777777`；蓝色为 LiDAR，灰色虚线为 SMC reference；左 panel 横扫 λ，右 panel 横扫 n。 grayscale_safe=`False`，redundant=`True`，provenance=`rendered_estimate`。
- **Plot grammar**：rendering `raster`；x/y `log/linear`；grid `both`；legend `True`（inside panels，shared=`True`）；direct_labels=`False`；markers/line styles `1/2`；reference lines `1`；hatching=`False`；uncertainty `none`；line width `1.0` pt；provenance `rendered_estimate`。
- **Encodings**：`{"x": "λ (log) in left; number of lookahead samples n (log) in right", "y": "Diversity average (↑)", "color": "LiDAR versus SMC reference", "shape": "circular markers", "line": "solid LiDAR trace and dashed SMC reference", "facet": "diversity versus λ; diversity versus n", "text": "LiDAR/SMC legend and log tick labels"}`
- **Data/statistics**：Two log-x panels report average diversity, measured in surrounding text via pairwise CLIP-embedding similarity protocol, as λ and n vary. A dashed SMC reference line is included; no uncertainty or per-prompt distribution is shown. The metric direction/interpretation is not fully disambiguated in the caption.
- **Evidence relation**：Main Figure 8 claims stronger λ improves alignment but may reduce diversity → Figure 12 quantifies λ and n diversity trade-offs and compares SMC → Figure 2/Figure 14 qualitative montages check whether diversity collapse is visible.
- **设计优点**：
  - Log axes cover wide λ/n ranges and make gradual diversity change visible.
  - SMC dashed reference supplies a concrete collapse comparator.
  - Same blue LiDAR mapping is consistent with other main figures.
- **设计弱点：
  - The paper describes pairwise similarity as “diversity,” so direction and metric definition can be confusing.
  - Only one reference level and one LiDAR trace per setting hide prompt/run heterogeneity.
  - Caption omits the FK protocol, aggregation, and sample count; color/line style remain necessary to distinguish series.
- **可复用模式**：Use matched log-scale hyperparameter sweeps with an external/reference line, but define whether the plotted quantity is similarity or diversity and its direction.
- **证据**：p.25；rendered_observation；p.25 Figure 12 full object, labels, caption, and surrounding layout inspected from 200 dpi render。

### Figure 13 (p.26 / appendix D.4 SDXL DDIM-50)
- **布局与职责**：`inset`；类型 `line`；purpose `ablation, robustness`；复杂度 2/5（1 panels、2 series、约 28 marks、0 legend items、2 annotations）。
- **Caption（逐字）**：*λ ablations on HPS and GenEval.（6 words；moves=title, setup, abbreviation_definition；headline_bold=False；self_contained=False；main_finding_stated=False）*。
- **Typography**：['DejaVu Sans', 'DejaVu Sans Mono', 'Nimbus Roman No9 L', 'Computer Modern math']，约 5.0–8.5 pt（median 6.5），weight `regular, bold`，style `roman, italic`，provenance `rendered_estimate`。
- **Color**：`categorical`，约 2 色，HEX `#2CA02C, #9467BD`；绿色左轴为 GenEval，紫色右轴为 HPS；相应虚线为 vanilla reference，颜色也对应双 y 轴标签。 grayscale_safe=`False`，redundant=`True`，provenance=`rendered_estimate`。
- **Plot grammar**：rendering `raster`；x/y `log/linear`；grid `both`；legend `False`（None，shared=`False`）；direct_labels=`True`；markers/line styles `1/2`；reference lines `2`；hatching=`False`；uncertainty `none`；line width `1.0` pt；provenance `rendered_estimate`。
- **Encodings**：`{"x": "λ (log scale)", "y": "GenEval average (left green axis) and HPS average (right purple axis)", "color": "metric identity and corresponding vanilla reference", "shape": "circles for sampled λ values", "line": "solid metric traces and dashed vanilla references", "facet": null, "text": "colored axis labels/ticks and λ ticks"}`
- **Data/statistics**：A single λ sweep uses dual y axes for GenEval and HPS, with dashed horizontal vanilla references. The curves are aggregated point estimates under SDXL DDIM-50; no error bars or run counts are provided.
- **Evidence relation**：Appendix D.4 Table 10 expands SDXL DDIM-50 multi-method results → Figure 13 checks λ robustness for two validation metrics → main Figure 8 provides the analogous SD v1.5 λ/EFR view.
- **设计优点**：
  - Colored y-axis labels keep the two metrics visually assigned.
  - Log λ range and dashed references expose saturation and baseline gap.
  - Small supplementary figure efficiently checks whether the main trend transfers to SDXL.
- **设计弱点：
  - Dual axes allow apparent co-movement despite different scales and units.
  - No legend or caption definition for dashed lines; reader must infer colors from axes.
  - Caption is too terse to identify SDXL/DDIM-50 setting, aggregation, or reference semantics.
- **可复用模式**：Use colored dual axes only for a compact validation slice; put model/solver and reference-line definitions directly in the caption.
- **证据**：p.26；rendered_observation；p.26 Figure 13 full object, labels, caption, and surrounding layout inspected from 200 dpi render。

### Figure 14 (p.27 / appendix D.5 sample comparison)
- **布局与职责**：`page_width`；类型 `qualitative_grid, image_montage`；purpose `qualitative_evidence, main_comparison, robustness`；复杂度 5/5（8 panels、4 series、约 40 marks、0 legend items、6 annotations）。
- **Caption（逐字）**：*Visual comparison of generated samples with different prompts using SD v1.5.（11 words；moves=title, setup；headline_bold=False；self_contained=False；main_finding_stated=False）*。
- **Typography**：['ArialMT', 'Nimbus Roman No9 L', 'Computer Modern math']，约 5.0–9.0 pt（median 7.0），weight `regular, bold`，style `roman, italic`，provenance `mixed`。
- **Color**：`mixed`，约 10 色，HEX `#2F6DB0, #2CA02C, #D62728, #E7B416, #FFFFFF, #333333, #BDBDBD, #111111, #8C6BB1, #55A868`；图像本身提供多色内容；方法标题为黑色，prompt 行以黑/灰文字标出两个 prompt；没有额外数据色阶或误差编码。 grayscale_safe=`False`，redundant=`True`，provenance=`rendered_estimate`。
- **Plot grammar**：rendering `mixed`；x/y `none/none`；grid `none`；legend `False`（None，shared=`False`）；direct_labels=`True`；markers/line styles `None/None`；reference lines `0`；hatching=`False`；uncertainty `none`；line width `None` pt；provenance `mixed`。
- **Encodings**：`{"x": null, "y": null, "color": "method group and natural image content", "shape": "photographic sample tiles", "line": null, "facet": "two prompts × Vanilla, Sequential Monte Carlo, Gradient Guidance, LiDAR (DPM-5/n=50)", "text": "method labels under each 2×2 method block and prompt text between blocks"}`
- **Data/statistics**：Two prompt-specific 2×4 qualitative grids compare Vanilla, Sequential Monte Carlo, Gradient Guidance, and LiDAR (DPM-5/n=50). The montage has no axes or numeric aggregation; it is a qualitative check of prompt-image alignment and diversity, with large unused whitespace above the figure on p.27.
- **Evidence relation**：Appendix D.5 extends main Figure 2’s single prompt with a green cup/red pizza prompt and a black car/green parking meter prompt → Table 2 metrics and Figure 7/8 provide quantitative and mechanism context.
- **设计优点**：
  - Two distinct compositional prompts test whether the visual pattern transfers beyond the main example.
  - Consistent method ordering and labels make pairwise qualitative comparison quick.
  - Page-width raster preserves image detail better than a narrow panel.
- **设计弱点：
  - Qualitative choices are not randomization evidence and lack seed/count/selection criteria.
  - Large page whitespace and small tile labels reduce information density; method distinction is mostly text/position.
  - Caption does not name the prompts or state which output is lookahead versus final target sample.
- **可复用模式**：Repeat a fixed method order across multiple compositional prompts, while reporting prompt selection, seeds, and sample-selection protocol to prevent cherry-picking.
- **证据**：p.27；rendered_observation；p.27 Figure 14 full object, labels, caption, and surrounding layout inspected from 200 dpi render。

## Table 逐对象审计
### Table 1 (p.1 / introduction)
- **布局与职责**：`single_column`；purpose `headline, experimental_design`。
- **Caption（逐字）**：*Comparison of test-time scaling methods for diffusion models. Efficient-Rollout indicates whether EFR estimation avoids per-timestep rollout sampling; Finite i.i.d. indicates whether performance remains consistent under a finite number of target sampling particles; No-Taylor indicates whether reward feedback can be obtained without Taylor approximation; and No-BackPropagation indicates whether neural differentiation is required.（51 words；moves=title, setup, encoding_key, abbreviation_definition；headline_bold=False；self_contained=True；main_finding_stated=False）*。
- **Typography**：['Nimbus Roman No9 L', 'Computer Modern math']，body/header 约 8.0/8.0 pt，header weight `mixed`，provenance `pdf_object`。
- **结构**：rows=4，columns=5，header_levels=2，row_groups=0，decimal_precision=None，rules=`booktabs`，highlighting=`text_color`。
- **Uncertainty**：No uncertainty; binary/ternary capability symbols only.
- **Header/data/statistics**：Four property rows (Effi.-Rollout, Finite i.i.d., No-Taylor, No-BackPro.) compare Backward Rollout, Sequential Monte Carlo, Gradient Guidance, and LiDAR (Ours). Header citations are stacked inside method columns; ✓/✗/△ encode capability status.
- **Evidence relation**：Introduction’s limitations matrix → Table 1 positions LiDAR against the four required properties → Figure 1 supplies method interface and Table 2 tests performance/cost.
- **设计优点**：
  - Compact capability matrix exposes the conceptual differentiators before methods/results.
  - Column grouping and multi-line citation headers retain provenance within the narrow introduction column.
- **设计弱点：
  - Citations and long headers make the table dense at single-column width.
  - △ is only defined visually by context and there is no caption explanation of the symbol itself; no empirical uncertainty is relevant.
- **可复用模式**：Use a small property matrix early to make method design commitments explicit, with a compact legend for all symbols.
- **证据**：p.1；rendered_observation；p.1 Table 1 headers, body, rules, highlighting, caption, and local placement inspected from 200 dpi render。

### Table 2 (p.6 / results)
- **布局与职责**：`page_width`；purpose `headline, main_comparison, efficiency_cost`。
- **Caption（逐字）**：*Performance on GenEval prompts. ImageReward (IR) is used as the reward for all sampling methods. All performance metrics are averaged over four images per prompt, while Time and Memory report the average cost of generating four images per run on a single A100 GPU. Bold values indicate the best results. *: Because generating four images simultaneously causes out-of-memory (OOM) errors, images are generated sequentially with a batch size of 1 over four runs.（73 words；moves=title, setup, encoding_key, comparison, abbreviation_definition, uncertainty_definition；headline_bold=False；self_contained=True；main_finding_stated=False）*。
- **Typography**：['Nimbus Roman No9 L', 'Computer Modern math']，body/header 约 7.5/8.0 pt，header weight `mixed`，provenance `pdf_object`。
- **结构**：rows=17，columns=8，header_levels=2，row_groups=3，decimal_precision=3，rules=`booktabs`，highlighting=`bold, cell_color`。
- **Uncertainty**：No SD/SE/CI or significance markers. Performance averages four images per prompt; cost averages generation of four images per run on one A100. OOM* rows use sequential batch size 1 over four runs.
- **Header/data/statistics**：Three backbone/target-solver row groups (SD v1.5 DDPM-100, SD v1.5 DDIM-50, SDXL DDPM-100) × guidance methods; columns are Backbone, Guidance Method, IR, CLIP Score, HPS, GenEval, Time (sec.), Mem. (GiB). Best values are bold; LiDAR rows have gray cell shading; arrow headers mark performance up and inference cost down.
- **Evidence relation**：Main comparison introduced in §4.1 → Table 2 is the central metric/cost evidence → Figure 4 visualizes the same frontier, Table 10 supplies SDXL DDIM-50 extension, and Tables 3–8 test transfer/orthogonal conditions.
- **设计优点**：
  - Puts four performance metrics and two cost metrics in one decision surface.
  - Row groups and gray LiDAR shading make backbone and method contrasts scannable; OOM protocol is captioned.
- **设计弱点：
  - 17 rows and eight columns are small at page width; citations/solver details compete with metric values.
  - Point estimates omit seed/run dispersion, so “best” bolding can overstate resolution of close differences.
- **可复用模式**：Align quality metrics, time, memory, and failure flags in one grouped table, but add explicit replication/uncertainty columns when available.
- **证据**：p.6；rendered_observation；p.6 Table 2 headers, body, rules, highlighting, caption, and local placement inspected from 200 dpi render。

### Table 3 (p.7 / results)
- **布局与职责**：`inset`；purpose `main_comparison, robustness`。
- **Caption（逐字）**：*Performance of LiDAR when applied to the flow matching model, FLUX (Labs, 2024).（13 words；moves=title, setup, abbreviation_definition；headline_bold=False；self_contained=True；main_finding_stated=False）*。
- **Typography**：['Nimbus Roman No9 L', 'Computer Modern math']，body/header 约 7.5/8.0 pt，header weight `mixed`，provenance `pdf_object`。
- **结构**：rows=4，columns=4，header_levels=1，row_groups=0，decimal_precision=3，rules=`booktabs`，highlighting=`bold`。
- **Uncertainty**：No uncertainty or error bars; one point estimate per n.
- **Header/data/statistics**：Rows n=0, 3, 50, 100; columns n, IR, CLIP, GenEval for FLUX flow matching. Bold marks best IR/CLIP at n=100 and best GenEval at n=50. Metrics are shown to three decimals.
- **Evidence relation**：Main Table 2 establishes diffusion-backbone evidence → Table 3 checks extension to FLUX/flow matching → Figure 3’s n scaling and method formulation explain why more lookahead samples help.
- **设计优点**：
  - Minimal four-row extension makes transfer trend easy to read.
  - Bold per-metric maxima preserve metric-specific trade-offs.
- **设计弱点：
  - Tiny inset has no explicit sample/prompt aggregation in caption.
  - Only three metrics and no cost column prevent direct efficiency comparison with Table 2.
- **可复用模式**：Use a compact transfer table with the same metric abbreviations as the main benchmark and preserve exact n sweep values.
- **证据**：p.7；rendered_observation；p.7 Table 3 headers, body, rules, highlighting, caption, and local placement inspected from 200 dpi render。

### Table 4 (p.7 / results)
- **布局与职责**：`inset`；purpose `main_comparison, robustness`。
- **Caption（逐字）**：*LiDAR performance on the discrete diffusion model UDLM (Schiff et al., 2025) trained on QM9 (Ruddigkeit et al., 2012), using the ring count reward.（24 words；moves=title, setup, encoding_key, abbreviation_definition；headline_bold=False；self_contained=True；main_finding_stated=False）*。
- **Typography**：['Nimbus Roman No9 L', 'Computer Modern math']，body/header 约 7.5/8.0 pt，header weight `mixed`，provenance `pdf_object`。
- **结构**：rows=4，columns=4，header_levels=1，row_groups=0，decimal_precision=3，rules=`booktabs`，highlighting=`bold`。
- **Uncertainty**：No uncertainty; counts and ring metrics are single reported values per n.
- **Header/data/statistics**：Rows n=0, 16, 1024, 4096; columns n, Num novel (↑), Novel ring count (↑), Total ring count (↑). Values use mixed integer/three-decimal precision; bold marks per-column maxima.
- **Evidence relation**：Discrete-diffusion extension in §4.2 → Table 4 tests the lookahead reward formulation on UDLM/QM9 → contrasts with Table 3’s continuous FLUX extension and supports stated token-independent guidance limitation.
- **设计优点**：
  - Names dataset/reward in caption and keeps the discrete extension separate from image metrics.
  - Small row sweep shows monotone novelty/ring trend with a non-monotone total-ring maximum.
- **设计弱点：
  - Header precision is mixed and count units are not further defined.
  - No inference cost, variation, or sample-size denominator is reported.
- **可复用模式**：Keep domain-specific extension metrics in a narrow table, while stating count denominators and evaluation protocol adjacent to it.
- **证据**：p.7；rendered_observation；p.7 Table 4 headers, body, rules, highlighting, caption, and local placement inspected from 200 dpi render。

### Table 5 (p.8 / results)
- **布局与职责**：`inset`；purpose `main_comparison, ablation`。
- **Caption（逐字）**：*Orthogonal improvements applied to a DPO-tuned SD v1.5 (Wallace et al., 2024). *: Fine-tuning costs are not included.（18 words；moves=title, setup, encoding_key, abbreviation_definition；headline_bold=False；self_contained=True；main_finding_stated=False）*。
- **Typography**：['Nimbus Roman No9 L', 'Computer Modern math']，body/header 约 7.5/8.0 pt，header weight `mixed`，provenance `pdf_object`。
- **结构**：rows=9，columns=6，header_levels=1，row_groups=2，decimal_precision=3，rules=`booktabs`，highlighting=`bold`。
- **Uncertainty**：No uncertainty; DPO rows mark time with * because fine-tuning costs are excluded.
- **Header/data/statistics**：Six columns Lookahead, n, IR, CLIP, GenEval, Time; two blocks compare base SD v1.5 and (+DPO) SD v1.5. DPM-5 n=50 is bold-best within each block; metric precision is three decimals and time uses two decimals.
- **Evidence relation**：Claim that test-time scaling is orthogonal to fine-tuning → Table 5 compares LiDAR n/solver settings before/after DPO → Table 2 supplies the primary base model anchor and Table 6 explores reward mixtures.
- **设计优点**：
  - Matched blocks isolate DPO as a backbone change while preserving LiDAR sweep.
  - The asterisk makes omitted fine-tuning cost explicit rather than silently mixing totals.
- **设计弱点：
  - No column identifies target sampler steps or batch/hardware; “Time” may be misread as full DPO-inclusive cost.
  - Small inset and repeated rows make exact metric differences difficult to inspect.
- **可复用模式**：Use vertically matched baseline/fine-tuned blocks to demonstrate orthogonality, with explicit cost exclusions and identical sweep columns.
- **证据**：p.8；rendered_observation；p.8 Table 5 headers, body, rules, highlighting, caption, and local placement inspected from 200 dpi render。

### Table 6 (p.8 / ablation)
- **布局与职责**：`inset`；purpose `ablation, main_comparison`。
- **Caption（逐字）**：*Performance on SD v1.5 using a weighted sum of ImageReward and CLIP as reward annotations. All results use a DPM-5 lookahead solver with n=50. Bold values indicate the best results.（30 words；moves=title, setup, encoding_key, comparison, abbreviation_definition；headline_bold=False；self_contained=True；main_finding_stated=False）*。
- **Typography**：['Nimbus Roman No9 L', 'Computer Modern math']，body/header 约 7.5/8.0 pt，header weight `mixed`，provenance `pdf_object`。
- **结构**：rows=6，columns=6，header_levels=2，row_groups=0，decimal_precision=4，rules=`booktabs`，highlighting=`bold`。
- **Uncertainty**：No uncertainty; point estimates from the fixed DPM-5/n=50 setting.
- **Header/data/statistics**：Two-level header groups Reward Mix (IR, CLIP) and Performance (IR, CLIP, HPS, GenEval). Six mixture rows (0/0, 0/100, 10/90, 40/60, 90/10, 100/0); CLIP columns retain four-decimal values where shown and best values are bold.
- **Evidence relation**：Figure 5’s reward-frontier question → Table 6 provides exact mixture weights and metric values → Figure 6 correlation matrix explains why IR guidance can improve validation metrics.
- **设计优点**：
  - Explicit mixture weights prevent the scatter panel from being purely qualitative.
  - Two-level header distinguishes annotation inputs from evaluated metrics.
- **设计弱点：
  - The 0/0 row is a baseline but its semantics are not labeled in the table.
  - Mixed precision and no uncertainty make close CLIP values look more decisive than warranted.
- **可复用模式**：Pair a reward-mixture ablation table with a 2-D frontier plot and a correlation diagnostic; preserve mixture weights in the table.
- **证据**：p.8；rendered_observation；p.8 Table 6 headers, body, rules, highlighting, caption, and local placement inspected from 200 dpi render。

### Table 7 (p.8 / results)
- **布局与职责**：`inset`；purpose `main_comparison, failure`。
- **Caption（逐字）**：*Performance comparison with other sample-based guidance methods using our lookahead samples based on DPM-5 lookahead solver with n=50.（18 words；moves=title, setup, comparison, abbreviation_definition；headline_bold=False；self_contained=False；main_finding_stated=False）*。
- **Typography**：['Nimbus Roman No9 L', 'Computer Modern math']，body/header 约 7.5/8.0 pt，header weight `mixed`，provenance `pdf_object`。
- **结构**：rows=4，columns=4，header_levels=1，row_groups=0，decimal_precision=3，rules=`booktabs`，highlighting=`bold, cell_color`。
- **Uncertainty**：No uncertainty; single ImageReward/CLIP/HPS point estimates.
- **Header/data/statistics**：Rows Vanilla, Safe-D, SR, LiDAR; columns Method, IR, CLIP, HPS. LiDAR row is gray-highlighted and bold across metrics; values use three decimals. All guidance rows use DPM-5 lookahead n=50 context from caption.
- **Evidence relation**：Negative/sample-based guidance limitations → Table 7 directly compares Safe-D/SR with LiDAR → Appendix Figure 9 checks SR hyperparameters; Figure 2 provides qualitative context for collapse/diversity.
- **设计优点**：
  - Short method comparison isolates the failure of simply repelling low-reward samples.
  - Highlighting makes the proposed method’s multi-metric result visually salient.
- **设计弱点：
  - No cost, sample count, or uncertainty columns; caption is not fully self-contained about fixed backbone/reward.
  - Gray/bold LiDAR emphasis is not a second-best/interval encoding and can invite conclusion before checking numbers.
- **可复用模式**：Use a focused competitor table for failure-mode contrasts, with a nearby sweep or mechanism figure explaining why the winner works.
- **证据**：p.8；rendered_observation；p.8 Table 7 headers, body, rules, highlighting, caption, and local placement inspected from 200 dpi render。

### Table 8 (p.9 / results)
- **布局与职责**：`single_column`；purpose `main_comparison, efficiency_cost`。
- **Caption（逐字）**：*ImageReward performance with LiDAR (DPM-5, n=50) orthogonally applied to target particle interaction methods. Since performance varies with the number of generated samples N on these methods, we report results for multiple values of N.（34 words；moves=title, setup, comparison, abbreviation_definition；headline_bold=False；self_contained=True；main_finding_stated=False）*。
- **Typography**：['Nimbus Roman No9 L', 'Computer Modern math']，body/header 约 7.5/8.0 pt，header weight `mixed`，provenance `pdf_object`。
- **结构**：rows=6，columns=8，header_levels=1，row_groups=3，decimal_precision=3，rules=`booktabs`，highlighting=`bold, cell_color`。
- **Uncertainty**：No uncertainty; ImageReward averages are reported for each method × N setting.
- **Header/data/statistics**：Eight columns: Method and target sample counts N=1,2,4,8,16,32,64; three two-row groups SMC/+LiDAR, BoN/+LiDAR, SMC(+BoN)/+LiDAR. LiDAR rows are gray and bold; values use three decimals.
- **Evidence relation**：Section 4.3 defines distinct SMC/BoN use cases → Table 8 gives exact ImageReward by N → Figure 7 visualizes time/performance trade-offs and sharing n across N.
- **设计优点**：
  - Wide N sweep makes the orthogonal improvement visible without separate panels.
  - Repeated group separators and shading pair baseline and +LiDAR rows.
- **设计弱点：
  - Single-column width makes seven N columns and method labels very small.
  - Only ImageReward is shown, so multi-metric reward hacking checks from Table 2 are absent.
- **可复用模式**：Use a method-pair × compute-budget matrix when the main question is scaling with N; complement it with cost curves and validation metrics.
- **证据**：p.9；rendered_observation；p.9 Table 8 headers, body, rules, highlighting, caption, and local placement inspected from 200 dpi render。

### Table 9 (p.25 / appendix D.1)
- **布局与职责**：`inset`；purpose `efficiency_cost, reproduction`。
- **Caption（逐字）**：*Computational cost for each component for LiDAR (DPM-5 / n=50) in Table 2.（13 words；moves=title, setup, abbreviation_definition, appendix_pointer；headline_bold=False；self_contained=True；main_finding_stated=False）*。
- **Typography**：['Nimbus Roman No9 L', 'Computer Modern math']，body/header 约 8.0/8.0 pt，header weight `mixed`，provenance `pdf_object`。
- **结构**：rows=4，columns=2，header_levels=1，row_groups=0，decimal_precision=2，rules=`booktabs`，highlighting=`bold`。
- **Uncertainty**：No uncertainty; component wall-clock costs are single reported values in seconds.
- **Header/data/statistics**：Two columns Component and Time (sec.); rows Lookahead sampling 5.69, Reward annotation 0.65, Target sampling 7.07, Total 13.41. A midrule separates total from components; precision is two decimals.
- **Evidence relation**：Table 2 default LiDAR DPM-5/n=50 total → Table 9 decomposes where its 13.41 seconds arise → Figure 10/11 test cost scaling and solver choice in Appendix D.
- **设计优点**：
  - Additive component rows reconcile to the reported total and make overhead concrete.
  - Simple booktabs design is legible beside two cost plots.
- **设计弱点：
  - No hardware/batch/repetition column even though the table is used for reproducibility.
  - Single total obscures per-prompt or n variability and includes no uncertainty.
- **可复用模式**：Report additive cost components beside the total, with hardware and aggregation protocol in the caption or header.
- **证据**：p.25；rendered_observation；p.25 Table 9 headers, body, rules, highlighting, caption, and local placement inspected from 200 dpi render。

### Table 10 (p.26 / appendix D.4)
- **布局与职责**：`page_width`；purpose `main_comparison, robustness, efficiency_cost`。
- **Caption（逐字）**：*All information is provided in Table 2（7 words；moves=title, appendix_pointer；headline_bold=False；self_contained=False；main_finding_stated=False）*。
- **Typography**：['Nimbus Roman No9 L', 'Computer Modern math']，body/header 约 7.5/8.0 pt，header weight `mixed`，provenance `pdf_object`。
- **结构**：rows=13，columns=8，header_levels=2，row_groups=1，decimal_precision=3，rules=`booktabs`，highlighting=`bold, cell_color`。
- **Uncertainty**：No uncertainty; SDXL DDIM-50 point estimates. OOM* denotes the same sequential batch-1 convention established in Table 2.
- **Header/data/statistics**：Two-level header repeats Table 2: Backbone, Guidance Method, Performance (ImageReward, CLIP Score, HPS, GenEval), Inference Cost (Time sec., Mem. GiB). One SDXL w/ DDIM (50 step solver) group has Vanilla, Powerful Solver (200 step), UG, DATE, DPM-8 n=3/9/50, LCM-4 n=3/16/100, DMD-1 n=3/16/100; LiDAR rows are gray and best values bold.
- **Evidence relation**：Appendix D.4 claims robustness under a faster SDXL DDIM-50 target solver → Table 10 reproduces the Table 2 metric/cost surface for that condition → Figure 13 sweeps λ, while Figure 4 and Table 2 provide DDPM-100 comparison.
- **设计优点**：
  - Same header schema as Table 2 enables direct solver/backbone comparison.
  - Thirteen LiDAR/baseline rows expose solver-family trade-offs and preserve memory/cost columns.
- **设计弱点：
  - Caption “All information is provided in Table 2” is not self-contained and does not define OOM* locally.
  - Large page-wide table is dense; close values and gray shading may overemphasize LiDAR rows without uncertainty.
- **可复用模式**：Reuse a stable multi-level benchmark schema for a new target-solver slice, but make the appendix caption self-contained and flag protocol changes.
- **证据**：p.26；rendered_observation；p.26 Table 10 headers, body, rules, highlighting, caption, and local placement inspected from 200 dpi render。

## 跨对象系统判断
- **Visual narrative**：引言 Table 1 先把 efficient rollout、finite-particle、No-Taylor、No-BackPropagation 属性矩阵化；Figure 1 接着给出 lookahead annotation→LiDAR target sampling 接口，Figure 2 用样本 montage 解释 SMC collapse/对齐现象。Figure 3 的 n/δ scaling 与 Table 2 的 backbone/metric/cost benchmark 构成主证据，Figure 4 把性能与时间放到同一 frontier；Tables 3–7 检查 FLUX、UDLM、DPO、reward mixture 与负 guidance，Figures 5–6 解释 mixture/correlation 机制。Figure 7/Table 8 处理 SMC/BoN 的 N 轴，Figure 8 连接 λ 选择与 EFR approximation，附录 Figures 9–14/Table 9–10 完成 failure、cost、diversity、SDXL solver 与额外 prompt 的边界检查。
- **Caption system**：Figure caption 通常先给对象标题/动作，再给设置和颜色/方法编码；仅 Figure 3、Figure 4、Figure 7、Figure 8 明确跨 panel 结构，qualitative captions 解释框/方法顺序。多数 caption 不报告 seed、run count 或统计不确定性；Table 2 是最完整的聚合/成本/OOM 说明。
- **Table header system**：主 benchmark Table 2 与 appendix Table 10 共享 Backbone/Guidance Method、Performance (↑)、Inference Cost (↓) 两级表头；扩展表沿用 IR、CLIP、HPS、GenEval 缩写，extension/ablation 表将 n、reward mix、N 或 solver 条件置于左侧。booktabs + partial cmidrule 是统一结构，LiDAR 行多用灰底/粗体。
- **Method/result/ablation**：Figure 1/Algorithms 1–2 给出方法接口，Figure 3/4 与 Table 2 验证 scaling、quality、cost，Tables 5–7/Figures 5–6 测正交性和 reward/negative-guidance 机制，Figure 8 与 Appendix Figures 9/12/13 检查超参、误差和多样性。
- **Main/appendix link**：主文 Figure 3→Appendix Figure 11（δ/solver cost），Table 2→Table 10（SDXL DDIM-50）与 Table 9（cost breakdown），Figure 8→Figure 13（λ transfer），Figure 2→Figure 14（额外 prompt），Figure 7/Table 8→Figure 10（N 与共享 lookahead cost）形成明确回指链。
- **Typography**：正文表格/数学 PDF 对象字体稳定，定量 PNG 内嵌 DejaVu Sans 风格稳定但字号偏小；Figure 1/2/14 的外部资产字体/图像标签来自不同导出路径，caption 与表格仍由 ICML 文本字体统一。
- **Color**：LiDAR 在主要定量图中稳定使用蓝色/蓝色阶，baseline 常为灰/红，SR/SMC/UG/DATE 等条件用固定额外颜色；表格用灰底突出 LiDAR。热图和自然图像是例外，且部分 inset 依赖颜色而缺乏充分的线型/直接标签冗余。

## 最终判断
- **最可复用模式**：
  - 以 Figure 1 的两阶段 schematic 把样本准备、reward annotation 和 target guidance 作为方法接口，并在后续图表中复用相同的 n、δ、λ、N 变量。
  - 用 Table 2/10 的统一多级表头同时放入性能、推理成本、显存和 OOM 失败值，把效率主张绑定到可复核数字。
  - 用 Figure 3/7 的同色 baseline/+LiDAR paired curves 与局部 zoom/直接 N 标注表达 scaling，同时以 Table 3–8 提供精确 operating points。
  - 将 reward-mixture scatter、correlation heatmap、λ/EFR error 和 diversity panels 组合成覆盖结果、机制和边界的诊断链，并保留胜负图之外的证据。
- **最高价值对象**：
  - Figure 4 + Table 2：把 LiDAR 的质量、时间、显存和 DATE/UG frontier 绑定，是 headline efficiency claim 的核心。
  - Figure 3：跨 ImageReward、CLIP、GenEval 的 n/δ scaling，是理论 scaling law 与实验趋势的最清晰接口。
  - Figure 7 + Table 8：显示 LiDAR 与 SMC/BoN 在不同 target sample count N 下的正交收益及共享 lookahead 的成本逻辑。
  - Figure 8 + Appendix Figure 13：把 λ 超参、EFR approximation error 与 SDXL transfer 放在同一可解释机制链。
- **失败模式**：
  - 几乎所有图表只给 point estimates，没有 seed/run、error bar、band、分布或显著性定义；“best”粗体尤其不能表达 close-value uncertainty。
  - 小 inset 中大量颜色、局部 legend 和 mixed axes/dual axes 依赖读者精细辨色，灰度和缩小阅读不稳。
  - 部分 caption 过短或不自包含：Figure 5/6/11/12/13、Table 7/10 未完整说明 aggregation、solver、reference line 或 cost protocol。
  - qualitative Figure 2/14 没有随机种子、样本选择、prompt 筛选或输出数量的可见协议，适合作为现象证据而非总体估计。
- **一句话视觉策略**：论文以蓝色 LiDAR 为视觉主线，将两阶段方法 schematic、n/δ/λ/N scaling 曲线、性能—成本表格和多样性/误差诊断串成一条从接口到效率 headline、再到机制和边界复核的证据链。

## 交付验证
- 只写入本 paper_id 的 `visual_audits/icml-2026-2801956159d6.md` 与原子发布的 `.json`；未修改 `readings/`、inventory、schema、prompt、脚本或其他论文审计。
- JSON 通过 `schemas/visual-audit.schema.json`；Figure/Table 数量、标签和页码与 PDF 清单一致。
