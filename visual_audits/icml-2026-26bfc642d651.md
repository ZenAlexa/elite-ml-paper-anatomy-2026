# icml-2026-26bfc642d651 视觉审计

## 审计边界与事实源

- **论文**：PRISM: Gauge-Invariant Tangent-Space Differentially Private LoRA。
- **PDF 事实源**：`corpus/preprints/icml-2026-26bfc642d651.pdf`，35 个物理页；主文 p.1–9，References p.10–12，Appendix Roadmap p.13，Appendix A–C p.13–35。
- **渲染检查**：使用 `pdftoppm -r 200 -png` 渲染全部 35 页；逐对象检查含 Figure/Table 的 p.2、4、7–9、26–35，并用 contact sheet 核对其余正文、References、Appendix A/B/C 页面没有漏列对象。
- **清单核对**：reading 的 visual_inventory 与 PDF 对齐：正文 Table 1–5、Algorithm 1、Figure 1–4；附录 Table 6–8、Figure 5–20。PDF 为对象数量、标签和页码的最终事实源。
- **写入边界**：本轮只产生 `visual_audits/icml-2026-26bfc642d651.md` 与 `.json`；没有修改 reading、source inventory、共享脚本或其他 paper。

## 1. 来源检索与源码状态

- **PDF/论文链接**：`https://arxiv.org/pdf/2606.00944`；OpenReview `https://openreview.net/forum?id=SiCjKmArjQ`；PDF 首页明确链接 `https://github.com/osu-srml/PRISM-DP-LoRA`。
- **source inventory**：`reports/tables/visual_source_inventory.csv` 将候选仓库标为 `repository_without_visual_source`，标题、README 与论文完全匹配。
- **本地视觉源**：`corpus/visual_sources/icml-2026-26bfc642d651/` 不存在；没有本地 figure/table generator、plot export 或 paper tex。
- **GitHub**：`gh repo view` 与 recursive tree 在 main commit `51cddadf19d7a2c118011b13286a0cfccc856e7c` 确认 README、`train_eval.py`、`src/prism_cli/optim/prism.py`、训练/评估代码和数据目录，但没有 `plot*`、`figure*`、`visual*`、目标表格生成器或论文源文件。
- **结论**：按 `repository_without_visual_source` 记录；视觉属性均来自 PDF object/raster 渲染观察，不能把公开训练代码误记为图表源。

## 2. 全文视觉版式

- **对象计数**：主文 4 figures + 5 tables + Algorithm 1；附录 16 figures + 3 tables；总计 20 figures、8 tables、1 algorithm。
- **页面结构**：p.2 Table 1；p.4 Algorithm 1；p.7 Table 2；p.8 Tables 3–5；p.9 Figures 1–4；p.26 Table 6–7；p.27 Table 8/Figure 5；p.28 Figures 6–7；p.29 Figures 8–9；p.30 Figures 10–11；p.31 Figures 12–13；p.32 Figures 14–15；p.33 Figures 16–17；p.34 Figures 18–19；p.35 Figure 20。
- **字体对象**：`pdffonts` 显示 Nimbus Roman、Computer Modern math、NimbusMono、DejaVu Sans/Oblique/Display、STIX symbols 等；正文/表格以 Roman/math 为主，图轴和 legend 以 DejaVu Sans 为主。
- **颜色系统**：蓝 Baseline、橙 PRISM；Figure 12 额外用绿线分离 PRISM/理论组件；Figure 19 用连续色条编码 training step；透明浅色轨迹表达 run dispersion。
- **矢量/栅格**：主体为 vector；`pdfimages -list` 仅在 p.34 发现 28×547 的色条类 raster resource，故按 vector-dominant mixed 记录。

## 3. PDF 对象清单

|类别|对象|PDF 页|模块|版式/职责|
|---|---|---:|---|---|
|Table|Table 1|2|introduction|single_column|
|Table|Table 2|7|results|page_width|
|Table|Table 3|8|results|double_column|
|Table|Table 4|8|ablation|single_column|
|Table|Table 5|8|results|single_column|
|Table|Table 6|26|appendix|single_column|
|Table|Table 7|26|appendix|page_width|
|Table|Table 8|27|appendix|double_column|
|Algorithm|Algorithm 1|4|method|single-column PRISM update|
|Figure|Figure 1|9|results|single_column|
|Figure|Figure 2|9|ablation|single_column|
|Figure|Figure 3|9|ablation|single_column|
|Figure|Figure 4|9|ablation|single_column|
|Figure|Figure 5|27|appendix|double_column|
|Figure|Figure 6|28|appendix|double_column|
|Figure|Figure 7|28|appendix|double_column|
|Figure|Figure 8|29|appendix|double_column|
|Figure|Figure 9|29|appendix|double_column|
|Figure|Figure 10|30|appendix|double_column|
|Figure|Figure 11|30|appendix|double_column|
|Figure|Figure 12|31|appendix|double_column|
|Figure|Figure 13|31|appendix|double_column|
|Figure|Figure 14|32|appendix|double_column|
|Figure|Figure 15|32|appendix|double_column|
|Figure|Figure 16|33|appendix|double_column|
|Figure|Figure 17|33|appendix|double_column|
|Figure|Figure 18|34|appendix|double_column|
|Figure|Figure 19|34|appendix|double_column|
|Figure|Figure 20|35|appendix|double_column|

### Algorithm 1（schema 外的 PDF 对象）

- **位置与版式**：p.4 右栏，单栏宽算法框；上下横线包围标题和主体。标题为 “Algorithm 1 One PRISM update across all LoRA modules”。
- **结构**：Input、Per-example intrinsic norms、Module-wise DP tangent update、Output 使用粗体层级；1–16 行编号，for-loop 缩进和 Eq. (14)–(26)/(22) 引用表达计算顺序。
- **视觉职责**：把 Table 1 的 desiderata 落到一个跨所有 LoRA modules 的可执行更新：tangent lift → global clipping → tangent noise → DP-aware invariant adaptive direction → rank-r retraction。
- **证据关系**：Section 3 的 tangent projection、global intrinsic clipping、noise sampler、adaptive floors 和 retraction 在此汇合；Tables 2–5 测试其 utility/scale/cost，Appendix A 给出证明和 Appendix C 给出诊断。
- **审计边界**：当前 schema 只有 figures/tables 数组，因此 Algorithm 1 仅保留在本 MD，不伪装成 JSON figure。

## 4. 逐对象审计

### Figure 1（p.9）

- **模块/版式**：`results`；`main`；`single_column`；类型：`line`；职责：`headline / mechanism / main_comparison`。
- **Design**：复杂度 score=2/5；panels=1；series=2；legend_items=2；annotations=1；data_marks≈600。
- **Typography**：family=DejaVu Sans, Computer Modern math；size≈6.0–10.0 pt（median≈8.0）；weight=regular, bold；style=roman, italic；provenance=pdf_object。
- **Color**：mode=categorical；近似 HEX=#1F77B4, #FF7F0E, #000000, #FFFFFF；蓝色为原始逐步 amplification，橙色为 w=25 moving average；黑色轴与白底提供结构。；冗余编码=True；灰度安全=True；provenance=rendered_estimate。
- **Plot grammar**：`{"rendering":"vector","x_scale":"linear","y_scale":"linear","grid":"none","legend_present":true,"legend_placement":"右上","shared_legend":false,"direct_labels":false,"marker_types":2,"line_styles":2,"hatching":false,"reference_lines":0,"uncertainty_display":"none","line_width_pt":1.5,"provenance":"rendered_estimate"}`。
- **Encodings**：x=update step 0–300；y=amplification ratio ∥N_Z∥_F^fac / ∥N_Z∥_F^PRISM；color=raw vs. MA(w=25)；shape=circle markers on raw curve; smooth line on MA；line=blue/orange line styles and marker visibility；facet=none；text=legend plus w=25 and >1 interpretation。
- **Caption（PDF 逐字，换行已合并）**：Figure 1. Intrinsic DP-noise amplification during training (Math-10K). We plot the per-step ratio ∥N_Z∥_F^fac / ∥N_Z∥_F^PRISM, where ∥N_Z∥_F is the Frobenius norm of the effective DP noise on the merged LoRA update Z. The blue curve reports the raw per-step ratio, and the orange curve reports its moving average (MA) with window size w = 25 updates. Values > 1 indicate that applying DP-SGD in factor space (A, B) injects a larger intrinsic perturbation into Z than PRISM under the same privacy budget.
- **Caption audit**：word_count=85；moves=title, setup, comparison, encoding_key, main_finding；headline_bold=True；self_contained=True；main_finding_stated=True。
- **Data/statistics**：300 update steps；蓝色为逐步比值，橙色为窗口 25 的移动平均；没有 seed band、error bar 或置信区间。
- **Evidence relation**：Issue III/overall mechanism claim → Figure 1 compares factor-space and PRISM intrinsic noise → Figure 2–4 isolate gauge and preconditioner mechanisms → Table 2 utility。
- **设计优点**：同一轴上把瞬时噪声放大和平滑趋势同时呈现。；>1 的基准语义让“factor-space 更 noisy”可直接读取。。
- **设计弱点**：原始曲线点密集且与平滑线重叠，局部异常难定位。；caption 没有给出每步重复数或 dispersion，不能从图中判断 run-to-run variability。。
- **可复用模式**：用细粒度原始轨迹加窗口平均线承接机制诊断，但把重复/不确定性单独标出。
- **Evidence**：p.9，`p.9 Figure 1`，basis=`rendered_observation`。

### Figure 2（p.9）

- **模块/版式**：`ablation`；`main`；`single_column`；类型：`line`；职责：`mechanism / ablation / robustness`。
- **Design**：复杂度 score=2/5；panels=1；series=2；legend_items=2；annotations=1；data_marks≈600。
- **Typography**：family=DejaVu Sans, Computer Modern math；size≈6.0–10.0 pt（median≈8.0）；weight=regular, bold；style=roman, italic；provenance=pdf_object。
- **Color**：mode=categorical；近似 HEX=#1F77B4, #FF7F0E, #000000；蓝色 Baseline 曲线和橙色 PRISM 曲线比较跨 gauge 的 ∆Z 范围；黑色坐标轴。；冗余编码=True；灰度安全=True；provenance=rendered_estimate。
- **Plot grammar**：`{"rendering":"vector","x_scale":"linear","y_scale":"log","grid":"none","legend_present":true,"legend_placement":"右上","shared_legend":false,"direct_labels":false,"marker_types":0,"line_styles":2,"hatching":false,"reference_lines":0,"uncertainty_display":"none","line_width_pt":1.5,"provenance":"rendered_estimate"}`。
- **Encodings**：x=update step 0–300；y=ρ_t = max_c ∥∆Z_t∥_F − min_c ∥∆Z_t∥_F；color=method；shape=smooth colored trajectories；line=blue/orange series；facet=none；text=log y ticks and ρ_t definition in caption。
- **Caption（PDF 逐字，换行已合并）**：Figure 2. Gauge sensitivity under DP (Math-10K). At step t we compute ρ_t = max_c ∥∆Z_t∥_F − min_c ∥∆Z_t∥_F across gauge-rescaled runs; smaller is better and ρ_t ≈ 0 indicates practical gauge invariance. PRISM drives ρ_t near zero, whereas factor-space DP exhibits persistently large ρ_t.
- **Caption audit**：word_count=44；moves=title, setup, comparison, main_finding；headline_bold=True；self_contained=True；main_finding_stated=True。
- **Data/statistics**：300 steps 的 gauge-rescaled runs；ρ_t 是每步跨 c 的 max–min，y 轴为对数显示；无误差编码。
- **Evidence relation**：Issue I gauge sensitivity → Figure 2 measures operational spread → Figures 5–11 appendix diagnostics expose clipping and realized-step components。
- **设计优点**：把 gauge variability 压缩成一个随时间的标量。；对数 y 轴同时保留早期大范围和后期接近零的 PRISM。。
- **设计弱点**：ρ_t 是跨 gauge 极差，不呈现每个 c 的轨迹或分布。；caption 没有给出 c 的五个具体值，需回到 Appendix C.1。。
- **可复用模式**：用 max–min index 将多次等价参数化压缩成一条稳定性曲线，并与机制原理绑定。
- **Evidence**：p.9，`p.9 Figure 2`，basis=`rendered_observation`。

### Figure 3（p.9）

- **模块/版式**：`ablation`；`main`；`single_column`；类型：`scatter / line`；职责：`mechanism / theory_mechanism / failure`。
- **Design**：复杂度 score=3/5；panels=1；series=3；legend_items=3；annotations=2；data_marks≈500。
- **Typography**：family=DejaVu Sans, Computer Modern math；size≈6.0–10.0 pt（median≈8.0）；weight=regular, bold；style=roman, italic；provenance=pdf_object。
- **Color**：mode=categorical；近似 HEX=#1F77B4, #FF7F0E, #000000, #FFFFFF；蓝色点为 Baseline measured，橙色点为 PRISM measured，蓝色拟合线标注线性拟合；黑白为坐标和背景。；冗余编码=True；灰度安全=True；provenance=rendered_estimate。
- **Plot grammar**：`{"rendering":"vector","x_scale":"linear","y_scale":"linear","grid":"both","legend_present":true,"legend_placement":"右上","shared_legend":false,"direct_labels":false,"marker_types":1,"line_styles":2,"hatching":false,"reference_lines":0,"uncertainty_display":"none","line_width_pt":1.3,"provenance":"rendered_estimate"}`。
- **Encodings**：x=S_t = Σℓ(mℓ∥Bℓ∥²_F+nℓ∥Aℓ∥²_F)；y=measured intrinsic noise energy ∥N_Z∥²_F；color=Baseline/PRISM and fitted baseline；shape=points plus fitted line；line=fitted baseline line；facet=none；text=R²=0.999 fit label and S_t formula。
- **Caption（PDF 逐字，换行已合并）**：Figure 3. Gauge-dependent intrinsic DP noise (Math-10K). We plot the measured intrinsic noise energy ∥N_Z∥_F² against the gauge-dependent statistic S_t. A strong linear trend indicates that the amount of DP noise injected into Z depends on the factorization; PRISM largely removes this dependence and keeps ∥N_Z∥_F² low.
- **Caption audit**：word_count=49；moves=title, setup, comparison, main_finding；headline_bold=True；self_contained=True；main_finding_stated=True。
- **Data/statistics**：固定 Math-10K 训练诊断中的测量点；x 为 gauge-dependent S_t，y 为 ∥N_Z∥²_F；Baseline 有线性拟合（图例写 R²=0.999），PRISM 点保持低位；无误差带。
- **Evidence relation**：Issue II Proposition 2.2/Eq. (8) → Figure 3 tests noise energy versus S_t → Figure 12/13 fixed-Z gauge sweep → PRISM tangent-noise theory。
- **设计优点**：散点、拟合线和 PRISM 对照在同一坐标系中直接检验 scaling prediction。；x 轴把 factor norm statistic 显式化，机制变量与结果变量相连。。
- **设计弱点**：Baseline 点云拥挤，PRISM 与基线的纵向差异压缩在同一 y 轴。；拟合优度标签只说明线性关联，不提供 seed 或 gauge-level uncertainty。。
- **可复用模式**：将理论预测的机制统计量作为 x 轴，叠加实测点和拟合线，而不是只报告相关系数。
- **Evidence**：p.9，`p.9 Figure 3`，basis=`rendered_observation`。

### Figure 4（p.9）

- **模块/版式**：`ablation`；`main`；`single_column`；类型：`line / scatter`；职责：`mechanism / ablation / robustness`。
- **Design**：复杂度 score=3/5；panels=1；series=2；legend_items=2；annotations=0；data_marks≈40。
- **Typography**：family=DejaVu Sans, Computer Modern math；size≈6.0–10.0 pt（median≈8.0）；weight=regular, bold；style=roman, italic；provenance=pdf_object。
- **Color**：mode=categorical；近似 HEX=#1F77B4, #FF7F0E, #BBD7EE, #FFD6B3；蓝/橙区分方法，浅色散点和误差条表达 seed spread；黑色网格和坐标。；冗余编码=True；灰度安全=True；provenance=rendered_estimate。
- **Plot grammar**：`{"rendering":"vector","x_scale":"linear","y_scale":"linear","grid":"both","legend_present":true,"legend_placement":"左下","shared_legend":false,"direct_labels":false,"marker_types":2,"line_styles":1,"hatching":false,"reference_lines":0,"uncertainty_display":"multiple","line_width_pt":1.5,"provenance":"rendered_estimate"}`。
- **Encodings**：x=noise multiplier σ ≈ 0.38–0.75；y=E∥P_t⁻¹ᐟ²ξ_intr∥_F, μ±σ_seed；color=Baseline vs. PRISM；shape=circle mean markers and faint seed points/error bars；line=solid blue/orange lines；facet=none；text=μ±σ_seed y-label。
- **Caption（PDF 逐字，换行已合并）**：Figure 4. Preconditioned intrinsic DP noise vs. σ (Math-10K). We report E∥P_t⁻¹ᐟ² ξ_intr∥_F; lower means the optimizer applies less stochastic perturbation after preconditioning. Factor-space DP becomes nearly σ-invariant, while PRISM keeps the preconditioned noise smaller via DP-aware floors.
- **Caption audit**：word_count=39；moves=title, setup, comparison, uncertainty_definition, main_finding；headline_bold=True；self_contained=True；main_finding_stated=True。
- **Data/statistics**：四个 privacy-engine noise multiplier 点；均值以实心点/线表示，浅色点与竖向误差条显示 seed dispersion；caption 没有定义具体重复数。
- **Evidence relation**：Issue III adaptive preconditioning → Figure 4 sweeps σ → Figures 14–18 appendix separate raw noise, amplification, scaling proxy and conditioning。
- **设计优点**：均值、seed spread 和两种方法在相同 σ 轴上对齐。；直接呈现“factor-space nearly σ-invariant / PRISM smaller”的比较。。
- **设计弱点**：σ 是 privacy-engine 实现返回值，caption 未解释 ε 到 σ 的映射。；误差条只标 σ_seed，未说明是否为标准差或其它 seed 汇总。。
- **可复用模式**：在主文用简洁 noise-multiplier sweep 建立预条件器效应，再把 raw/amplification 分解放入附录。
- **Evidence**：p.9，`p.9 Figure 4`，basis=`rendered_observation`。

### Figure 5（p.27）

- **模块/版式**：`appendix`；`appendix`；`double_column`；类型：`line`；职责：`mechanism / robustness`。
- **Design**：复杂度 score=2/5；panels=1；series=2；legend_items=2；annotations=0；data_marks≈400。
- **Typography**：family=DejaVu Sans, Computer Modern math；size≈6.0–10.0 pt（median≈8.0）；weight=regular, bold；style=roman, italic；provenance=pdf_object。
- **Color**：mode=categorical；近似 HEX=#1F77B4, #FF7F0E, #2CA02C, #BBD7EE；蓝色 Baseline、橙色 PRISM；浅色透明轨迹/分位带表达运行 dispersion；Figure 19 另以连续色阶编码 training step。；冗余编码=True；灰度安全=False；provenance=rendered_estimate。
- **Plot grammar**：`{"rendering":"vector","x_scale":"linear","y_scale":"linear","grid":"none","legend_present":true,"legend_placement":"左下","shared_legend":false,"direct_labels":false,"marker_types":2,"line_styles":2,"hatching":false,"reference_lines":0,"uncertainty_display":"multiple","line_width_pt":1.4,"provenance":"rendered_estimate"}`。
- **Encodings**：x=update step t (0–300)；y=mean dp_coef_mean across gauges；color=Baseline vs. PRISM; Figure 12 adds linear/bilinear components; Figure 19 adds training-step color；shape=circle markers, central lines, bands where shown；line=colored method lines/markers；facet=none；text=legend and axis annotation。
- **Caption（PDF 逐字，换行已合并）**：Figure 5. Over-time bands of dp_coef_mean across gauges (mean with IQR band; min/max lines).
- **Caption audit**：word_count=14；moves=title, uncertainty_definition；headline_bold=False；self_contained=False；main_finding_stated=False。
- **Data/statistics**：dp_coef_mean over 300 updates; five gauge rescalings c∈{0.25,0.5,1,2,4}; thick line is across-gauge mean, IQR fill and min/max lines are visible.
- **Evidence relation**：Figure 5 clipping-coefficient diagnostic → Figures 8–11 fixed-step gauge sweeps → Issue I clipping mechanism.
- **设计优点**：IQR and min/max encode both central tendency and extremes.；The two methods share one time axis and legend.。
- **设计弱点**：Caption does not define the five gauge runs beyond the surrounding paragraph.；Several faint trajectories overlap near 1.0, limiting small differences.。
- **可复用模式**：Use a mean line with explicit IQR and min/max overlays for gauge-sensitivity time diagnostics.
- **Evidence**：p.27，`p.27 Figure 5`，basis=`rendered_observation`。

### Figure 6（p.28）

- **模块/版式**：`appendix`；`appendix`；`double_column`；类型：`line`；职责：`mechanism / robustness`。
- **Design**：复杂度 score=2/5；panels=1；series=2；legend_items=2；annotations=0；data_marks≈400。
- **Typography**：family=DejaVu Sans, Computer Modern math；size≈6.0–10.0 pt（median≈8.0）；weight=regular, bold；style=roman, italic；provenance=pdf_object。
- **Color**：mode=categorical；近似 HEX=#1F77B4, #FF7F0E, #2CA02C, #BBD7EE；蓝色 Baseline、橙色 PRISM；浅色透明轨迹/分位带表达运行 dispersion；Figure 19 另以连续色阶编码 training step。；冗余编码=True；灰度安全=False；provenance=rendered_estimate。
- **Plot grammar**：`{"rendering":"vector","x_scale":"linear","y_scale":"log","grid":"none","legend_present":true,"legend_placement":"右上","shared_legend":false,"direct_labels":false,"marker_types":2,"line_styles":2,"hatching":false,"reference_lines":0,"uncertainty_display":"multiple","line_width_pt":1.4,"provenance":"rendered_estimate"}`。
- **Encodings**：x=update step t (0–300)；y=realized intrinsic step magnitude ‖ΔZ_t‖_F；color=Baseline vs. PRISM; Figure 12 adds linear/bilinear components; Figure 19 adds training-step color；shape=circle markers, central lines, bands where shown；line=colored method lines/markers；facet=none；text=legend and axis annotation。
- **Caption（PDF 逐字，换行已合并）**：Figure 6. Over-time bands of realized intrinsic step magnitude ∥∆Z_t∥_F across gauges (mean with IQR band; min/max lines).
- **Caption audit**：word_count=19；moves=title, uncertainty_definition；headline_bold=False；self_contained=False；main_finding_stated=False。
- **Data/statistics**：Actual ∥∆Z_t∥_F from Z_{t+1}−Z_t across five gauges; mean with IQR band and min/max lines; y is log-scaled.
- **Evidence relation**：Figure 6 realizes the intrinsic update behind Figure 2 → Figures 5/7–11 diagnose clipping → Issue I conclusion.
- **设计优点**：Measures the actual merged-update step rather than a formula proxy.；Log y scale preserves early transient and late small steps.。
- **设计弱点**：The broad band and many faint lines are hard to attribute to individual gauges.；No explicit legend for IQR versus min/max beyond caption text.。
- **可复用模式**：Plot realized update magnitude with a robust central line and range overlays, keeping the operational quantity separate from clipping proxies.
- **Evidence**：p.28，`p.28 Figure 6`，basis=`rendered_observation`。

### Figure 7（p.28）

- **模块/版式**：`appendix`；`appendix`；`double_column`；类型：`line`；职责：`mechanism / robustness`。
- **Design**：复杂度 score=2/5；panels=1；series=2；legend_items=2；annotations=0；data_marks≈400。
- **Typography**：family=DejaVu Sans, Computer Modern math；size≈6.0–10.0 pt（median≈8.0）；weight=regular, bold；style=roman, italic；provenance=pdf_object。
- **Color**：mode=categorical；近似 HEX=#1F77B4, #FF7F0E, #2CA02C, #BBD7EE；蓝色 Baseline、橙色 PRISM；浅色透明轨迹/分位带表达运行 dispersion；Figure 19 另以连续色阶编码 training step。；冗余编码=True；灰度安全=False；provenance=rendered_estimate。
- **Plot grammar**：`{"rendering":"vector","x_scale":"linear","y_scale":"linear","grid":"none","legend_present":true,"legend_placement":"右上","shared_legend":false,"direct_labels":false,"marker_types":2,"line_styles":2,"hatching":false,"reference_lines":0,"uncertainty_display":"none","line_width_pt":1.4,"provenance":"rendered_estimate"}`。
- **Encodings**：x=update step t (0–300)；y=range_c(dp_coef_mean) = max_c−min_c；color=Baseline vs. PRISM; Figure 12 adds linear/bilinear components; Figure 19 adds training-step color；shape=circle markers, central lines, bands where shown；line=colored method lines/markers；facet=none；text=legend and axis annotation。
- **Caption（PDF 逐字，换行已合并）**：Figure 7. Gauge-sensitivity index for clipping: rangec (dp_coef_mean) over time.
- **Caption audit**：word_count=10；moves=title；headline_bold=False；self_contained=False；main_finding_stated=False。
- **Data/statistics**：range_c(dp_coef_mean) = max_c−min_c across the five gauge runs over 300 steps; no uncertainty summary.
- **Evidence relation**：Figure 7 aggregates Figure 5 into a gauge-invariance index → Figures 8–11 show endpoint gauge slices.
- **设计优点**：One scalar index simplifies comparison across training.；The low PRISM curve makes the intended mechanism legible.。
- **设计弱点**：The index hides which gauge causes the extrema.；Caption leaves the c set and finite-run variability to surrounding prose.。
- **可复用模式**：Reduce multi-run invariance diagnostics to an explicit max–min index before showing endpoint sweeps.
- **Evidence**：p.28，`p.28 Figure 7`，basis=`rendered_observation`。

### Figure 8（p.29）

- **模块/版式**：`appendix`；`appendix`；`double_column`；类型：`line`；职责：`mechanism / ablation`。
- **Design**：复杂度 score=2/5；panels=1；series=2；legend_items=2；annotations=0；data_marks≈10。
- **Typography**：family=DejaVu Sans, Computer Modern math；size≈6.0–10.0 pt（median≈8.0）；weight=regular, bold；style=roman, italic；provenance=pdf_object。
- **Color**：mode=categorical；近似 HEX=#1F77B4, #FF7F0E, #2CA02C, #BBD7EE；蓝色 Baseline、橙色 PRISM；浅色透明轨迹/分位带表达运行 dispersion；Figure 19 另以连续色阶编码 training step。；冗余编码=True；灰度安全=False；provenance=rendered_estimate。
- **Plot grammar**：`{"rendering":"vector","x_scale":"linear","y_scale":"linear","grid":"none","legend_present":true,"legend_placement":"右上","shared_legend":false,"direct_labels":false,"marker_types":1,"line_styles":1,"hatching":false,"reference_lines":0,"uncertainty_display":"none","line_width_pt":1.5,"provenance":"rendered_estimate"}`。
- **Encodings**：x=log10(gauge c), c∈{0.25, 0.5, 1, 2, 4}；y=dp_clip_frac at step 1；color=Baseline vs. PRISM; Figure 12 adds linear/bilinear components; Figure 19 adds training-step color；shape=circle markers, central lines, bands where shown；line=colored method lines/markers；facet=none；text=legend and axis annotation。
- **Caption（PDF 逐字，换行已合并）**：Figure 8. Discrete gauge sweep of dp_clip_frac at step 1.
- **Caption audit**：word_count=10；moves=title；headline_bold=False；self_contained=False；main_finding_stated=False。
- **Data/statistics**：Five discrete gauges c={0.25,0.5,1,2,4} shown as log10(c); step-1 clipping fraction, no error bars.
- **Evidence relation**：Figure 8 is the step-1 clipping slice of Figures 5/10 → Eq. (4) explains gauge dependence.
- **设计优点**：Discrete gauge points make the reparameterization intervention explicit.；Baseline monotonic change and PRISM flatness are easy to compare.。
- **设计弱点**：Only one training step is shown.；No replicate or seed dispersion is shown.。
- **可复用模式**：Use a small, fixed gauge grid at an early step to expose a mechanism-level regime change.
- **Evidence**：p.29，`p.29 Figure 8`，basis=`rendered_observation`。

### Figure 9（p.29）

- **模块/版式**：`appendix`；`appendix`；`double_column`；类型：`line`；职责：`mechanism / ablation`。
- **Design**：复杂度 score=2/5；panels=1；series=2；legend_items=2；annotations=0；data_marks≈10。
- **Typography**：family=DejaVu Sans, Computer Modern math；size≈6.0–10.0 pt（median≈8.0）；weight=regular, bold；style=roman, italic；provenance=pdf_object。
- **Color**：mode=categorical；近似 HEX=#1F77B4, #FF7F0E, #2CA02C, #BBD7EE；蓝色 Baseline、橙色 PRISM；浅色透明轨迹/分位带表达运行 dispersion；Figure 19 另以连续色阶编码 training step。；冗余编码=True；灰度安全=False；provenance=rendered_estimate。
- **Plot grammar**：`{"rendering":"vector","x_scale":"linear","y_scale":"linear","grid":"none","legend_present":true,"legend_placement":"左下","shared_legend":false,"direct_labels":false,"marker_types":1,"line_styles":1,"hatching":false,"reference_lines":0,"uncertainty_display":"none","line_width_pt":1.5,"provenance":"rendered_estimate"}`。
- **Encodings**：x=log10(gauge c), c∈{0.25, 0.5, 1, 2, 4}；y=dp_coef_mean at step 1；color=Baseline vs. PRISM; Figure 12 adds linear/bilinear components; Figure 19 adds training-step color；shape=circle markers, central lines, bands where shown；line=colored method lines/markers；facet=none；text=legend and axis annotation。
- **Caption（PDF 逐字，换行已合并）**：Figure 9. Discrete gauge sweep of dp_coef_mean at step 1.
- **Caption audit**：word_count=10；moves=title；headline_bold=False；self_contained=False；main_finding_stated=False。
- **Data/statistics**：Same five log10(c) values at step 1; mean DP coefficient dp_coef_mean, no error bars.
- **Evidence relation**：Figure 9 smooths Figure 8’s clip fraction into mean shrinkage → Eq. (4) and Figure 8 support Issue I.
- **设计优点**：Pairs a hard clipping fraction with a continuous coefficient measure.；Same x grid as Figure 8 supports direct cross-panel comparison.。
- **设计弱点**：Legend placement differs from Figure 8.；The caption does not repeat gauge values or finite-run semantics.。
- **可复用模式**：Keep paired endpoint diagnostics on identical gauge coordinates while changing the mechanism statistic.
- **Evidence**：p.29，`p.29 Figure 9`，basis=`rendered_observation`。

### Figure 10（p.30）

- **模块/版式**：`appendix`；`appendix`；`double_column`；类型：`line`；职责：`mechanism / robustness`。
- **Design**：复杂度 score=2/5；panels=1；series=2；legend_items=2；annotations=0；data_marks≈10。
- **Typography**：family=DejaVu Sans, Computer Modern math；size≈6.0–10.0 pt（median≈8.0）；weight=regular, bold；style=roman, italic；provenance=pdf_object。
- **Color**：mode=categorical；近似 HEX=#1F77B4, #FF7F0E, #2CA02C, #BBD7EE；蓝色 Baseline、橙色 PRISM；浅色透明轨迹/分位带表达运行 dispersion；Figure 19 另以连续色阶编码 training step。；冗余编码=True；灰度安全=False；provenance=rendered_estimate。
- **Plot grammar**：`{"rendering":"vector","x_scale":"linear","y_scale":"linear","grid":"none","legend_present":true,"legend_placement":"左上","shared_legend":false,"direct_labels":false,"marker_types":1,"line_styles":1,"hatching":false,"reference_lines":0,"uncertainty_display":"none","line_width_pt":1.5,"provenance":"rendered_estimate"}`。
- **Encodings**：x=log10(gauge c), c∈{0.25, 0.5, 1, 2, 4}；y=dp_clip_frac at step 300；color=Baseline vs. PRISM; Figure 12 adds linear/bilinear components; Figure 19 adds training-step color；shape=circle markers, central lines, bands where shown；line=colored method lines/markers；facet=none；text=legend and axis annotation。
- **Caption（PDF 逐字，换行已合并）**：Figure 10. Discrete gauge sweep of dp_clip_frac at step 300.
- **Caption audit**：word_count=10；moves=title；headline_bold=False；self_contained=False；main_finding_stated=False。
- **Data/statistics**：Same five log10(c) values at step 300; clipping fraction, no uncertainty encoding; baseline near saturation and PRISM near zero.
- **Evidence relation**：Late-step counterpart to Figure 8 → surrounding text warns saturation can mask gauge dependence.
- **设计优点**：Makes the early/late saturation contrast visible.；Identical coordinate design to Figure 8 aids scanning.。
- **设计弱点**：Saturation makes a flat baseline curve ambiguous as an invariance test.；No error bars or individual runs.。
- **可复用模式**：Show late-step endpoint curves only as a saturation check, not as sole evidence of invariance.
- **Evidence**：p.30，`p.30 Figure 10`，basis=`rendered_observation`。

### Figure 11（p.30）

- **模块/版式**：`appendix`；`appendix`；`double_column`；类型：`line`；职责：`mechanism / robustness`。
- **Design**：复杂度 score=2/5；panels=1；series=2；legend_items=2；annotations=0；data_marks≈10。
- **Typography**：family=DejaVu Sans, Computer Modern math；size≈6.0–10.0 pt（median≈8.0）；weight=regular, bold；style=roman, italic；provenance=pdf_object。
- **Color**：mode=categorical；近似 HEX=#1F77B4, #FF7F0E, #2CA02C, #BBD7EE；蓝色 Baseline、橙色 PRISM；浅色透明轨迹/分位带表达运行 dispersion；Figure 19 另以连续色阶编码 training step。；冗余编码=True；灰度安全=False；provenance=rendered_estimate。
- **Plot grammar**：`{"rendering":"vector","x_scale":"linear","y_scale":"linear","grid":"none","legend_present":true,"legend_placement":"右上","shared_legend":false,"direct_labels":false,"marker_types":1,"line_styles":1,"hatching":false,"reference_lines":0,"uncertainty_display":"none","line_width_pt":1.5,"provenance":"rendered_estimate"}`。
- **Encodings**：x=log10(gauge c), c∈{0.25, 0.5, 1, 2, 4}；y=dp_coef_mean at step 300；color=Baseline vs. PRISM; Figure 12 adds linear/bilinear components; Figure 19 adds training-step color；shape=circle markers, central lines, bands where shown；line=colored method lines/markers；facet=none；text=legend and axis annotation。
- **Caption（PDF 逐字，换行已合并）**：Figure 11. Discrete gauge sweep of dp_coef_mean at step 300.
- **Caption audit**：word_count=10；moves=title；headline_bold=False；self_contained=False；main_finding_stated=False。
- **Data/statistics**：Same five log10(c) values at step 300; mean DP coefficient, no uncertainty encoding.
- **Evidence relation**：Late-step counterpart to Figure 9 → text interprets residual baseline dependence under clipping saturation.
- **设计优点**：Complements Figure 10 with coefficient rather than fraction.；Retains method color and discrete gauge intervention.。
- **设计弱点**：Late clipping saturation reduces discriminative resolution.；Caption is terse and not self-contained about c or step semantics.。
- **可复用模式**：Pair a late-step coefficient slice with the late-step clipping-fraction slice and state saturation explicitly.
- **Evidence**：p.30，`p.30 Figure 11`，basis=`rendered_observation`。

### Figure 12（p.31）

- **模块/版式**：`appendix`；`appendix`；`double_column`；类型：`line`；职责：`mechanism / theory_mechanism / robustness`。
- **Design**：复杂度 score=3/5；panels=1；series=3；legend_items=2；annotations=2；data_marks≈400。
- **Typography**：family=DejaVu Sans, Computer Modern math；size≈6.0–10.0 pt（median≈8.0）；weight=regular, bold；style=roman, italic；provenance=pdf_object。
- **Color**：mode=categorical；近似 HEX=#1F77B4, #FF7F0E, #2CA02C, #BBD7EE；蓝色 Baseline、橙色 PRISM；浅色透明轨迹/分位带表达运行 dispersion；Figure 19 另以连续色阶编码 training step。；冗余编码=True；灰度安全=False；provenance=rendered_estimate。
- **Plot grammar**：`{"rendering":"vector","x_scale":"log","y_scale":"log","grid":"both","legend_present":true,"legend_placement":"上方居中","shared_legend":false,"direct_labels":false,"marker_types":1,"line_styles":3,"hatching":false,"reference_lines":2,"uncertainty_display":"multiple","line_width_pt":1.3,"provenance":"rendered_estimate"}`。
- **Encodings**：x=gauge scale c in A→cA, B→c⁻¹B (10⁻³–10³)；y=intrinsic noise norm (median with 10–90% band)；color=Baseline vs. PRISM; Figure 12 adds linear/bilinear components; Figure 19 adds training-step color；shape=circle markers, central lines, bands where shown；line=colored method lines/markers；facet=none；text=legend and axis annotation。
- **Caption（PDF 逐字，换行已合并）**：Figure 12. Gauge sweep at fixed Zℓ: intrinsic-noise medians with 10–90% band.
- **Caption audit**：word_count=13；moves=title, uncertainty_definition；headline_bold=False；self_contained=False；main_finding_stated=False。
- **Data/statistics**：Gauge scale c swept over log10(c)∈[-3,3] with 61 values and 64 Monte-Carlo samples per c; medians and 10–90% bands for baseline linear, baseline bilinear, and PRISM.
- **Evidence relation**：Issue II fixed-Z protocol → Figure 12 separates linear/bilinear factor noise from PRISM → Figure 13 ratio and Figure 3 training-time relation.
- **设计优点**：Log–log axes expose orders-of-magnitude gauge amplification.；Separate linear and bilinear baseline components clarify the mechanism.。
- **设计弱点**：Three series plus bands and two vertical reference markers create a dense legend.；Caption does not state the 61×64 sampling protocol.。
- **可复用模式**：Use fixed-intrinsic-state log sweeps with component-wise baselines and quantile bands to separate mechanism terms.
- **Evidence**：p.31，`p.31 Figure 12`，basis=`rendered_observation`。

### Figure 13（p.31）

- **模块/版式**：`appendix`；`appendix`；`double_column`；类型：`line`；职责：`mechanism / theory_mechanism`。
- **Design**：复杂度 score=2/5；panels=1；series=1；legend_items=0；annotations=0；data_marks≈400。
- **Typography**：family=DejaVu Sans, Computer Modern math；size≈6.0–10.0 pt（median≈8.0）；weight=regular, bold；style=roman, italic；provenance=pdf_object。
- **Color**：mode=categorical；近似 HEX=#1F77B4, #FF7F0E, #2CA02C, #BBD7EE；蓝色 Baseline、橙色 PRISM；浅色透明轨迹/分位带表达运行 dispersion；Figure 19 另以连续色阶编码 training step。；冗余编码=True；灰度安全=False；provenance=rendered_estimate。
- **Plot grammar**：`{"rendering":"vector","x_scale":"log","y_scale":"log","grid":"both","legend_present":false,"legend_placement":null,"shared_legend":false,"direct_labels":false,"marker_types":1,"line_styles":1,"hatching":false,"reference_lines":0,"uncertainty_display":"none","line_width_pt":1.3,"provenance":"rendered_estimate"}`。
- **Encodings**：x=gauge scale c in A→cA, B→c⁻¹B (10⁻³–10³)；y=amplification factor (median baseline / median PRISM)；color=Baseline vs. PRISM; Figure 12 adds linear/bilinear components; Figure 19 adds training-step color；shape=circle markers, central lines, bands where shown；line=colored method lines/markers；facet=none；text=legend and axis annotation。
- **Caption（PDF 逐字，换行已合并）**：Figure 13. Gauge sweep: amplification factor (median baseline / median PRISM) vs. c.
- **Caption audit**：word_count=12；moves=title；headline_bold=False；self_contained=False；main_finding_stated=False。
- **Data/statistics**：Median baseline/PRISM intrinsic-noise amplification ratio over the same log-spaced gauge sweep; no band visible.
- **Evidence relation**：Figure 12 component curves → Figure 13 converts them to a ratio → Figure 1 links fixed-state amplification to training-time amplification.
- **设计优点**：Ratio removes the absolute noise scale and exposes V-shaped gauge dependence.；Log axes show large amplification factors compactly.。
- **设计弱点**：Single blue series has no visible baseline/PRISM legend because the ratio is already encoded.；Median ratio does not show Monte-Carlo spread.。
- **可复用模式**：Convert paired method measurements to an explicit amplification ratio when absolute units obscure the gauge effect.
- **Evidence**：p.31，`p.31 Figure 13`，basis=`rendered_observation`。

### Figure 14（p.32）

- **模块/版式**：`appendix`；`appendix`；`double_column`；类型：`line`；职责：`mechanism / robustness`。
- **Design**：复杂度 score=3/5；panels=1；series=2；legend_items=2；annotations=0；data_marks≈400。
- **Typography**：family=DejaVu Sans, Computer Modern math；size≈6.0–10.0 pt（median≈8.0）；weight=regular, bold；style=roman, italic；provenance=pdf_object。
- **Color**：mode=categorical；近似 HEX=#1F77B4, #FF7F0E, #2CA02C, #BBD7EE；蓝色 Baseline、橙色 PRISM；浅色透明轨迹/分位带表达运行 dispersion；Figure 19 另以连续色阶编码 training step。；冗余编码=True；灰度安全=False；provenance=rendered_estimate。
- **Plot grammar**：`{"rendering":"vector","x_scale":"linear","y_scale":"log","grid":"both","legend_present":true,"legend_placement":"左上","shared_legend":false,"direct_labels":false,"marker_types":1,"line_styles":1,"hatching":false,"reference_lines":0,"uncertainty_display":"multiple","line_width_pt":1.4,"provenance":"rendered_estimate"}`。
- **Encodings**：x=realized noise multiplier σ (four privacy-engine values)；y=mean raw intrinsic DP noise ‖ξ_intr‖_F；color=Baseline vs. PRISM; Figure 12 adds linear/bilinear components; Figure 19 adds training-step color；shape=circle markers, central lines, bands where shown；line=colored method lines/markers；facet=none；text=legend and axis annotation。
- **Caption（PDF 逐字，换行已合并）**：Figure 14. Mean raw intrinsic DP noise ∥ξ_intr∥_F vs. σ.
- **Caption audit**：word_count=10；moves=title；headline_bold=False；self_contained=False；main_finding_stated=False。
- **Data/statistics**：Noise-multiplier sweep at four realized σ values; means with seed scatter/error bars for raw intrinsic noise; values increase with σ.
- **Evidence relation**：Issue III raw-noise scaling prediction → Figure 14 sanity check → Figure 15 isolates preconditioner amplification.
- **设计优点**：Tests the privacy engine’s raw σ response before normalization.；Seed dots and error bars expose between-run spread.。
- **设计弱点**：Log y scale and sparse tick labels make absolute differences less immediate.；Caption does not define the seed aggregation despite visible μ±σ_seed.。
- **可复用模式**：Place a raw-noise calibration panel before any normalized amplification diagnostic.
- **Evidence**：p.32，`p.32 Figure 14`，basis=`rendered_observation`。

### Figure 15（p.32）

- **模块/版式**：`appendix`；`appendix`；`double_column`；类型：`line`；职责：`mechanism / robustness`。
- **Design**：复杂度 score=3/5；panels=1；series=2；legend_items=2；annotations=0；data_marks≈400。
- **Typography**：family=DejaVu Sans, Computer Modern math；size≈6.0–10.0 pt（median≈8.0）；weight=regular, bold；style=roman, italic；provenance=pdf_object。
- **Color**：mode=categorical；近似 HEX=#1F77B4, #FF7F0E, #2CA02C, #BBD7EE；蓝色 Baseline、橙色 PRISM；浅色透明轨迹/分位带表达运行 dispersion；Figure 19 另以连续色阶编码 training step。；冗余编码=True；灰度安全=False；provenance=rendered_estimate。
- **Plot grammar**：`{"rendering":"vector","x_scale":"linear","y_scale":"log","grid":"both","legend_present":true,"legend_placement":"右上","shared_legend":false,"direct_labels":false,"marker_types":1,"line_styles":1,"hatching":false,"reference_lines":0,"uncertainty_display":"multiple","line_width_pt":1.4,"provenance":"rendered_estimate"}`。
- **Encodings**：x=realized noise multiplier σ (four privacy-engine values)；y=mean amplification ‖P⁻¹ᐟ²ξ‖_F / ‖ξ‖_F；color=Baseline vs. PRISM; Figure 12 adds linear/bilinear components; Figure 19 adds training-step color；shape=circle markers, central lines, bands where shown；line=colored method lines/markers；facet=none；text=legend and axis annotation。
- **Caption（PDF 逐字，换行已合并）**：Figure 15. Mean amplification ∥P⁻¹ᐟ²ξ∥_F / ∥ξ∥_F vs. σ.
- **Caption audit**：word_count=8；moves=title；headline_bold=False；self_contained=False；main_finding_stated=False。
- **Data/statistics**：Same four σ values; amplification ratio ∥P⁻¹ᐟ²ξ∥_F/∥ξ∥_F with seed scatter/error bars; baseline decreases and PRISM is lower.
- **Evidence relation**：Figure 14 raw noise → Figure 15 preconditioner-only ratio → Figure 16 aggressiveness and Figure 17 conditioning.
- **设计优点**：Normalizes away raw noise and targets the adaptive-preconditioning failure.；Shared σ grid allows direct comparison with Figure 14.。
- **设计弱点**：The denominator and preconditioner are dense symbols for readers outside the method.；Seed spread is visible but not defined in the caption.。
- **可复用模式**：Follow raw scale with a normalized ratio so the optimizer’s contribution is separately auditable.
- **Evidence**：p.32，`p.32 Figure 15`，basis=`rendered_observation`。

### Figure 16（p.33）

- **模块/版式**：`appendix`；`appendix`；`double_column`；类型：`line`；职责：`mechanism / robustness`。
- **Design**：复杂度 score=3/5；panels=1；series=2；legend_items=2；annotations=0；data_marks≈400。
- **Typography**：family=DejaVu Sans, Computer Modern math；size≈6.0–10.0 pt（median≈8.0）；weight=regular, bold；style=roman, italic；provenance=pdf_object。
- **Color**：mode=categorical；近似 HEX=#1F77B4, #FF7F0E, #2CA02C, #BBD7EE；蓝色 Baseline、橙色 PRISM；浅色透明轨迹/分位带表达运行 dispersion；Figure 19 另以连续色阶编码 training step。；冗余编码=True；灰度安全=False；provenance=rendered_estimate。
- **Plot grammar**：`{"rendering":"vector","x_scale":"linear","y_scale":"log","grid":"both","legend_present":true,"legend_placement":"右上","shared_legend":false,"direct_labels":false,"marker_types":2,"line_styles":1,"hatching":false,"reference_lines":0,"uncertainty_display":"band","line_width_pt":1.4,"provenance":"rendered_estimate"}`。
- **Encodings**：x=update step t (0–300)；y=max preconditioner scaling (proxy)；color=Baseline vs. PRISM; Figure 12 adds linear/bilinear components; Figure 19 adds training-step color；shape=circle markers, central lines, bands where shown；line=colored method lines/markers；facet=none；text=legend and axis annotation。
- **Caption（PDF 逐字，换行已合并）**：Figure 16. Preconditioner aggressiveness (proxy) over training steps.
- **Caption audit**：word_count=8；moves=title；headline_bold=False；self_contained=False；main_finding_stated=False。
- **Data/statistics**：300-step diagnostic at ε=3 (σ≈0.62); max preconditioner scaling proxy for DP-AdamW versus PRISM, with translucent trajectories visible but replicate definition absent.
- **Evidence relation**：Figure 16 operationalizes Theorem 3.5’s scaling proxy → Figure 17 Gram stress → Figure 18 realized amplification.
- **设计优点**：Proxy tracks the optimizer control knob directly over time.；Log y scale preserves the large early baseline value.。
- **设计弱点**：The proxy is not the exact operator norm for every optimizer.；Caption does not define the faint trajectory/band statistic or repeat count.。
- **可复用模式**：Use a transparent scaling proxy as a bridge between a bound and realized noise amplification.
- **Evidence**：p.33，`p.33 Figure 16`，basis=`rendered_observation`。

### Figure 17（p.33）

- **模块/版式**：`appendix`；`appendix`；`double_column`；类型：`line`；职责：`mechanism / robustness / failure`。
- **Design**：复杂度 score=3/5；panels=1；series=2；legend_items=2；annotations=0；data_marks≈400。
- **Typography**：family=DejaVu Sans, Computer Modern math；size≈6.0–10.0 pt（median≈8.0）；weight=regular, bold；style=roman, italic；provenance=pdf_object。
- **Color**：mode=categorical；近似 HEX=#1F77B4, #FF7F0E, #2CA02C, #BBD7EE；蓝色 Baseline、橙色 PRISM；浅色透明轨迹/分位带表达运行 dispersion；Figure 19 另以连续色阶编码 training step。；冗余编码=True；灰度安全=False；provenance=rendered_estimate。
- **Plot grammar**：`{"rendering":"vector","x_scale":"linear","y_scale":"log","grid":"both","legend_present":true,"legend_placement":"右上","shared_legend":false,"direct_labels":false,"marker_types":2,"line_styles":1,"hatching":false,"reference_lines":0,"uncertainty_display":"band","line_width_pt":1.4,"provenance":"rendered_estimate"}`。
- **Encodings**：x=update step t (0–300)；y=max ‖M⁻¹ᐟ²‖₂ (Gram proxy)；color=Baseline vs. PRISM; Figure 12 adds linear/bilinear components; Figure 19 adds training-step color；shape=circle markers, central lines, bands where shown；line=colored method lines/markers；facet=none；text=legend and axis annotation。
- **Caption（PDF 逐字，换行已合并）**：Figure 17. Low-rank numerics stress: max ∥M⁻¹ᐟ²∥₂ (Gram proxy).
- **Caption audit**：word_count=9；moves=title；headline_bold=False；self_contained=False；main_finding_stated=False。
- **Data/statistics**：300-step ε=3 diagnostic; max ∥M⁻¹ᐟ²∥₂ Gram-proxy stress, with faint trajectories but no caption definition of their aggregation.
- **Evidence relation**：Low-rank numerical-stability issue → Figure 17 measures Gram inverse-square-root stress → Figure 18 checks realized amplification.
- **设计优点**：Directly visualizes ill-conditioning rather than only final utility.；Method curves remain on a common log scale.。
- **设计弱点**：M is a measured proxy and its construction is not in the caption.；Faint lines are not tied to a named uncertainty summary.。
- **可复用模式**：Plot a conditioning proxy beside a scaling proxy to separate numerical stress from update amplification.
- **Evidence**：p.33，`p.33 Figure 17`，basis=`rendered_observation`。

### Figure 18（p.34）

- **模块/版式**：`appendix`；`appendix`；`double_column`；类型：`line`；职责：`mechanism / robustness`。
- **Design**：复杂度 score=3/5；panels=1；series=2；legend_items=2；annotations=0；data_marks≈400。
- **Typography**：family=DejaVu Sans, Computer Modern math；size≈6.0–10.0 pt（median≈8.0）；weight=regular, bold；style=roman, italic；provenance=pdf_object。
- **Color**：mode=categorical；近似 HEX=#1F77B4, #FF7F0E, #2CA02C, #BBD7EE；蓝色 Baseline、橙色 PRISM；浅色透明轨迹/分位带表达运行 dispersion；Figure 19 另以连续色阶编码 training step。；冗余编码=True；灰度安全=False；provenance=rendered_estimate。
- **Plot grammar**：`{"rendering":"vector","x_scale":"linear","y_scale":"log","grid":"both","legend_present":true,"legend_placement":"右下","shared_legend":false,"direct_labels":false,"marker_types":2,"line_styles":1,"hatching":false,"reference_lines":0,"uncertainty_display":"band","line_width_pt":1.4,"provenance":"rendered_estimate"}`。
- **Encodings**：x=update step t (0–300)；y=realized amplification ‖P⁻¹ᐟ²ξ_t,intr‖_F / ‖ξ_t,intr‖_F；color=Baseline vs. PRISM; Figure 12 adds linear/bilinear components; Figure 19 adds training-step color；shape=circle markers, central lines, bands where shown；line=colored method lines/markers；facet=none；text=legend and axis annotation。
- **Caption（PDF 逐字，换行已合并）**：Figure 18. Amplification over training steps: ∥P⁻¹ᐟ²ξ∥ / ∥ξ∥.
- **Caption audit**：word_count=7；moves=title；headline_bold=False；self_contained=False；main_finding_stated=False。
- **Data/statistics**：300-step ε=3 diagnostic; realized amplification ∥P⁻¹ᐟ²ξ_t,intr∥_F/∥ξ_t,intr∥_F, two methods and faint variability traces.
- **Evidence relation**：Figures 16–17 proxies → Figure 18 direct realized ratio → Figure 19/20 mechanism relationships.
- **设计优点**：Moves from proxies to the measured amplification target.；The wide y range is legible with log scaling.。
- **设计弱点**：Caption does not state whether lines are means, medians, or one run.；Baseline saturation can hide temporal variation.。
- **可复用模式**：Use a direct realized ratio after proxy panels, retaining identical method colors and time axis.
- **Evidence**：p.34，`p.34 Figure 18`，basis=`rendered_observation`。

### Figure 19（p.34）

- **模块/版式**：`appendix`；`appendix`；`double_column`；类型：`scatter / line`；职责：`mechanism / theory_mechanism`。
- **Design**：复杂度 score=3/5；panels=1；series=2；legend_items=2；annotations=2；data_marks≈400。
- **Typography**：family=DejaVu Sans, Computer Modern math；size≈6.0–10.0 pt（median≈8.0）；weight=regular, bold；style=roman, italic；provenance=pdf_object。
- **Color**：mode=mixed；近似 HEX=#1F77B4, #FF7F0E, #2CA02C, #BBD7EE；蓝色 Baseline、橙色 PRISM；浅色透明轨迹/分位带表达运行 dispersion；Figure 19 另以连续色阶编码 training step。；冗余编码=True；灰度安全=False；provenance=rendered_estimate。
- **Plot grammar**：`{"rendering":"mixed","x_scale":"log","y_scale":"log","grid":"both","legend_present":true,"legend_placement":"左上 + 右侧 colorbar","shared_legend":false,"direct_labels":false,"marker_types":1,"line_styles":0,"hatching":false,"reference_lines":0,"uncertainty_display":"none","line_width_pt":1.4,"provenance":"rendered_estimate"}`。
- **Encodings**：x=preconditioner aggressiveness max‖P⁻¹ᐟ²‖ (log scale)；y=noise amplification ‖P⁻¹ᐟ²ξ‖_intr / ‖ξ‖_intr；color=Baseline vs. PRISM; Figure 12 adds linear/bilinear components; Figure 19 adds training-step color；shape=circle markers, central lines, bands where shown；line=colored method lines/markers；facet=none；text=legend and axis annotation。
- **Caption（PDF 逐字，换行已合并）**：Figure 19. Amplification vs. aggressiveness (color = step).
- **Caption audit**：word_count=7；moves=title；headline_bold=False；self_contained=False；main_finding_stated=False。
- **Data/statistics**：Step-wise scatter of amplification versus preconditioner aggressiveness on log axes; color gradient encodes training step 0–300; separate Baseline and PRISM point markers.
- **Evidence relation**：Figure 16 aggressiveness proxy → Figure 19 tests amplification relation with step as color → Figure 20 swaps in low-rank stress.
- **设计优点**：Color carries temporal progression without adding a third axis.；Log–log coordinates expose method regime separation.。
- **设计弱点**：Colorbar plus method legend increases decoding load.；Scatter is observational and does not show a fitted relation or uncertainty.。
- **可复用模式**：Use a colored trajectory/scatter cloud to connect a proxy to realized amplification while preserving method identity.
- **Evidence**：p.34，`p.34 Figure 19`，basis=`rendered_observation`。

### Figure 20（p.35）

- **模块/版式**：`appendix`；`appendix`；`double_column`；类型：`scatter`；职责：`mechanism / robustness`。
- **Design**：复杂度 score=3/5；panels=1；series=2；legend_items=2；annotations=0；data_marks≈400。
- **Typography**：family=DejaVu Sans, Computer Modern math；size≈6.0–10.0 pt（median≈8.0）；weight=regular, bold；style=roman, italic；provenance=pdf_object。
- **Color**：mode=categorical；近似 HEX=#1F77B4, #FF7F0E, #2CA02C, #BBD7EE；蓝色 Baseline、橙色 PRISM；浅色透明轨迹/分位带表达运行 dispersion；Figure 19 另以连续色阶编码 training step。；冗余编码=True；灰度安全=False；provenance=rendered_estimate。
- **Plot grammar**：`{"rendering":"vector","x_scale":"log","y_scale":"log","grid":"both","legend_present":true,"legend_placement":"左上","shared_legend":false,"direct_labels":false,"marker_types":1,"line_styles":0,"hatching":false,"reference_lines":0,"uncertainty_display":"none","line_width_pt":1.4,"provenance":"rendered_estimate"}`。
- **Encodings**：x=low-rank stress max‖M⁻¹ᐟ²‖₂ (Gram proxy; log scale)；y=noise amplification ‖P⁻¹ᐟ²ξ‖_intr / ‖ξ‖_intr；color=Baseline vs. PRISM; Figure 12 adds linear/bilinear components; Figure 19 adds training-step color；shape=circle markers, central lines, bands where shown；line=colored method lines/markers；facet=none；text=legend and axis annotation。
- **Caption（PDF 逐字，换行已合并）**：Figure 20. Amplification vs. low-rank stress (Gram proxy).
- **Caption audit**：word_count=8；moves=title；headline_bold=False；self_contained=False；main_finding_stated=False。
- **Data/statistics**：Step-wise scatter of amplification versus low-rank stress max∥M⁻¹ᐟ²∥₂ on log axes; Baseline and PRISM method colors/markers, no uncertainty summary.
- **Evidence relation**：Figure 17 low-rank stress → Figure 20 tests amplification coupling → final Issue III interpretation.
- **设计优点**：Final panel tests whether safeguards decouple stress from amplification.；Large blank area gives the single scatter panel visual separation.。
- **设计弱点**：Substantial page whitespace weakens page efficiency.；Caption does not define M proxy or the step sampling.。
- **可复用模式**：End a diagnostic sequence with a two-variable scatter that tests the claimed decoupling directly.
- **Evidence**：p.35，`p.35 Figure 20`，basis=`rendered_observation`。

### Table 1（p.2）

- **模块/版式**：`introduction`；`main`；`single_column`；职责：`method_interface / theory_mechanism / main_comparison`。
- **Design**：rows=3；columns=6；header_levels=1；row_groups=0。
- **Typography**：family=Nimbus Roman No9 L, Computer Modern math；body≈8.5 pt；header≈9.0 pt；header_weight=bold；provenance=pdf_object。
- **表头/规则/高亮**：rules=`booktabs`；highlighting=bold；decimal_precision=None。
- **Caption（PDF 逐字，换行已合并）**：Table 1. Comparison with other DP-LoRA design choices. We evaluate each variant against three desiderata: (a) gauge-invariant randomized mechanism, (b) additive perturbations on Z (i.e., no bilinear DP term), and (c) LoRA-scale efficiency. The naive factor-space variant violates (a) and (b), yielding unbounded effective intrinsic noise E_Z. The one-sided variant, which updates B only while freezing A, satisfies (b) and (c) but not (a). PRISM satisfies all three and admits a closed-form expression for E_Z.
- **Caption audit**：word_count=76；moves=title, setup, comparison, main_finding；headline_bold=True；self_contained=True；main_finding_stated=True。
- **Uncertainty**：No empirical uncertainty; E_Z is a closed-form symbolic quantity and “UNBOUNDED” is a qualitative failure value.
- **Data/statistics**：Rows are DP-LoRA, One-side, PRISM; columns are Method, Params, E_Z, and desiderata (a)–(c). PRISM is bold and has checkmarks in all three columns.
- **Evidence relation**：Issue I–III desiderata → Table 1 sets the comparison contract → Section 3/Algorithm 1 constructs the row marked PRISM → Tables 2–5 test utility, scaling and cost.
- **设计优点**：Compactly binds invariance, additivity, efficiency and effective-noise claims.；Checkmarks and bold row make the design decision scannable.。
- **设计弱点**：Symbolic E_Z expressions are dense at small size.；The table states qualitative properties rather than empirical magnitudes or uncertainty.。
- **可复用模式**：Use a compact desiderata matrix before the method details, with a symbolic cost/noise column and explicit pass/fail marks.
- **Evidence**：p.2，`p.2 Table 1`，basis=`pdf_object`。

### Table 2（p.7）

- **模块/版式**：`results`；`main`；`page_width`；职责：`headline / main_comparison / efficiency_cost`。
- **Design**：rows=18；columns=15；header_levels=2；row_groups=3。
- **Typography**：family=Nimbus Roman No9 L, Computer Modern math；body≈8.5 pt；header≈9.0 pt；header_weight=bold；provenance=pdf_object。
- **表头/规则/高亮**：rules=`booktabs`；highlighting=bold；decimal_precision=3。
- **Caption（PDF 逐字，换行已合并）**：Table 2. Utility on GLUE8 and Math-10K (higher is better). “Non-DP” uses the same setup without DP clipping/noise; ε ∈ {6, 3} uses DP-SGD with δ = 10⁻⁵. Avg is the unweighted mean over the 12 tasks; bold is best per column. Takeaway: Under DP, PRISM attains the best Avg and wins most tasks, especially on multi-step reasoning (GSM8K/MAWPS/SVAMP).
- **Caption audit**：word_count=55；moves=title, setup, comparison, uncertainty_definition, abbreviation_definition, main_finding；headline_bold=True；self_contained=True；main_finding_stated=True。
- **Uncertainty**：No SD/SE or intervals; each cell is a single task score, and Avg is an unweighted mean over 12 tasks.
- **Data/statistics**：Two header levels group GLUE8’s 8 tasks and Math-10K’s 4 tasks; Setting and Method are left columns and Avg is final column. Three row groups are Non-DP, ε=6, ε=3, each with six methods.
- **Evidence relation**：Method design contract/Table 1 → Table 2 headline utility on GLUE8 + Math-10K → Table 3–5 scaling/rank/overhead → Figures 1–4 mechanism diagnostics.
- **设计优点**：One wide decision surface compares privacy regime, method and 12 task outcomes.；Bold per-column winners and a final Avg make the main takeaway visible.。
- **设计弱点**：No replicate spread despite DP stochasticity; the caption only says Avg is unweighted.；Width and repeated method names create a high horizontal and vertical scanning cost.。
- **可复用模式**：Use grouped task headers and a final aggregate column for multi-suite privacy utility, with row blocks for privacy regimes.
- **Evidence**：p.7，`p.7 Table 2`，basis=`pdf_object`。

### Table 3（p.8）

- **模块/版式**：`results`；`main`；`double_column`；职责：`main_comparison / robustness / ablation`。
- **Design**：rows=9；columns=8；header_levels=1；row_groups=3。
- **Typography**：family=Nimbus Roman No9 L, Computer Modern math；body≈8.5 pt；header≈9.0 pt；header_weight=bold；provenance=pdf_object。
- **表头/规则/高亮**：rules=`booktabs`；highlighting=bold；decimal_precision=None。
- **Caption（PDF 逐字，换行已合并）**：Table 3. Math-10K results on Gemma-3-4B-pt, Gemma-2-9B, and Gemma-3-12B-pt under DP (r = 16, ε = 6, δ = 10⁻⁵). The “Type” column indicates the backbone family; all experiments use text-only inputs. Bold indicates the best result within each backbone.
- **Caption audit**：word_count=35；moves=title, setup, comparison, abbreviation_definition；headline_bold=False；self_contained=True；main_finding_stated=False。
- **Uncertainty**：No uncertainty; rows report single exact-answer accuracy values and Avg within each backbone.
- **Data/statistics**：Columns are Backbone, Type, Method, GSM8K, AQuA, MAWPS, SVAMP, Avg; three backbone row groups contain AdamW, LoRA+, PRISM.
- **Evidence relation**：Table 2 main DP utility → Table 3 tests backbone family/size persistence → Table 4 rank sensitivity and Table 5 overhead.
- **设计优点**：Explicit Type column distinguishes text-only from multimodal backbone families while holding text-only inputs fixed.；Within-backbone bolding avoids comparing raw scores across backbone scales.。
- **设计弱点**：Mixed three- and four-decimal precision reduces column uniformity.；The caption says text-only inputs but Type includes multimodal, which requires careful reading.。
- **可复用模式**：Repeat a compact method block per backbone and bold only within each block when scale changes alter the score regime.
- **Evidence**：p.8，`p.8 Table 3`，basis=`pdf_object`。

### Table 4（p.8）

- **模块/版式**：`ablation`；`main`；`single_column`；职责：`ablation / robustness / main_comparison`。
- **Design**：rows=9；columns=5；header_levels=1；row_groups=3。
- **Typography**：family=Nimbus Roman No9 L, Computer Modern math；body≈8.5 pt；header≈9.0 pt；header_weight=bold；provenance=pdf_object。
- **表头/规则/高亮**：rules=`booktabs`；highlighting=bold；decimal_precision=3。
- **Caption（PDF 逐字，换行已合并）**：Table 4. Rank sensitivity on Gemma-3-4B-pt under DP (ε = 6, δ = 10⁻⁵; text-only inputs). Avg is the unweighted mean over all 12 tasks. PRISM is best across all tested ranks.
- **Caption audit**：word_count=28；moves=title, setup, comparison, main_finding；headline_bold=False；self_contained=True；main_finding_stated=True。
- **Uncertainty**：No uncertainty; Avg columns are unweighted means over 12 tasks.
- **Data/statistics**：Columns Rank, Method, GLUE8 Avg, Math Avg, Avg; three row groups r=8, r=16, r=32, each with AdamW, LoRA+, PRISM.
- **Evidence relation**：Table 2 baseline utility → Table 4 varies LoRA rank → Figure 1/4 and Appendix diagnostics address mechanism and noise rather than rank.
- **设计优点**：Directly tests rank robustness with identical backbone and privacy setting.；Three aggregate columns keep the rank comparison compact.。
- **设计弱点**：Aggregates hide task-level reversals.；Only three ranks are sampled, so the table cannot establish a continuous scaling law.。
- **可复用模式**：Use repeated rank blocks with consistent aggregate metrics for a small, auditable sensitivity sweep.
- **Evidence**：p.8，`p.8 Table 4`，basis=`pdf_object`。

### Table 5（p.8）

- **模块/版式**：`results`；`main`；`single_column`；职责：`efficiency_cost / main_comparison / reproduction`。
- **Design**：rows=3；columns=3；header_levels=1；row_groups=0。
- **Typography**：family=Nimbus Roman No9 L, Computer Modern math；body≈8.5 pt；header≈9.0 pt；header_weight=bold；provenance=pdf_object。
- **表头/规则/高亮**：rules=`booktabs`；highlighting=none；decimal_precision=None。
- **Caption（PDF 逐字，换行已合并）**：Table 5. Runtime and memory profiling on Gemma-3-4B-pt for Math-10K (r = 16, ε = 6). Measurements use a single A100-40GB GPU with 10 warmup updates and 30 measured updates. PRISM roughly doubles step time in the current implementation, while peak memory is essentially unchanged.
- **Caption audit**：word_count=42；moves=title, setup, uncertainty_definition, main_finding；headline_bold=False；self_contained=True；main_finding_stated=True。
- **Uncertainty**：No uncertainty; 10 warmup and 30 measured updates on one A100-40GB GPU, with step time in seconds and peak memory in MB.
- **Data/statistics**：Columns Method, Step time (s), Peak memory (MB); rows LoRA+, AdamW, PRISM.
- **Evidence relation**：Table 2 utility → Table 5 quantifies the runtime/memory trade-off → Limitations attributes practical cost to geometry-aware operations.
- **设计优点**：Places runtime and memory on one compact cost surface.；Caption fixes hardware and warmup/measurement protocol.。
- **设计弱点**：Single GPU and one workload limit portability.；Mixed units and no variability hide run-to-run systems noise.。
- **可复用模式**：Pair performance claims with an explicit cost table using hardware and measurement protocol in the caption.
- **Evidence**：p.8，`p.8 Table 5`，basis=`pdf_object`。

### Table 6（p.26）

- **模块/版式**：`appendix`；`appendix`；`single_column`；职责：`experimental_design / dataset / reproduction`。
- **Design**：rows=14；columns=3；header_levels=1；row_groups=2。
- **Typography**：family=Nimbus Roman No9 L, Computer Modern math；body≈8.5 pt；header≈9.0 pt；header_weight=bold；provenance=pdf_object。
- **表头/规则/高亮**：rules=`booktabs`；highlighting=bold；decimal_precision=0。
- **Caption（PDF 逐字，换行已合并）**：Table 6. Dataset splits used in our experiments.
- **Caption audit**：word_count=8；moves=title；headline_bold=False；self_contained=False；main_finding_stated=False。
- **Uncertainty**：No uncertainty; counts are split sizes, with “–” for not applicable train/eval cells.
- **Data/statistics**：Columns Dataset/Split, Train, Eval; two bold group headers GLUE8 and Math-10K, with GLUE8 total and Math test rows.
- **Evidence relation**：Table 2 task suite → Table 6 makes train/eval denominators and split sizes explicit → Table 7/8 complete the reproduction configuration.
- **设计优点**：Separates training and evaluation counts in a compact schema.；Group headers and totals expose the composition of both benchmark suites.。
- **设计弱点**：MNLI matched/mismatched and mixture/test rows require special parsing.；No dataset version/date or filtering details appear in the table itself.。
- **可复用模式**：Use a two-column count table with group headers and explicit total/test rows for benchmark reconstruction.
- **Evidence**：p.26，`p.26 Table 6`，basis=`pdf_object`。

### Table 7（p.26）

- **模块/版式**：`appendix`；`appendix`；`page_width`；职责：`experimental_design / reproduction`。
- **Design**：rows=15；columns=3；header_levels=1；row_groups=0。
- **Typography**：family=Nimbus Roman No9 L, Computer Modern math；body≈8.0 pt；header≈8.5 pt；header_weight=bold；provenance=pdf_object。
- **表头/规则/高亮**：rules=`booktabs`；highlighting=none；decimal_precision=None。
- **Caption（PDF 逐字，换行已合并）**：Table 7. Common hyperparameters shared across methods.
- **Caption audit**：word_count=7；moves=title；headline_bold=False；self_contained=False；main_finding_stated=False。
- **Uncertainty**：No uncertainty; values are shared configuration settings, including ε∈{3,6}, δ=10⁻⁵, C=1.0 and seed 42.
- **Data/statistics**：Columns Setting, GLUE8, Math-10K; rows cover backbone, LoRA rank/scaling/dropout, target modules, steps/batches, sequence length, input masking, seed, DP and backend/accountant.
- **Evidence relation**：Table 6 split sizes → Table 7 common controls → Table 8 method-specific optimizer/LR settings → reproducibility of Tables 2–5 and Figures 1–20.
- **设计优点**：Centralizes shared controls instead of repeating them in every result table.；GLUE8/Math-10K differences are visible in one side-by-side column pair.。
- **设计弱点**：Dense long strings such as target modules and accountant citations are small.；“SAME” cells require looking back to the GLUE8 column.。
- **可复用模式**：Put invariant controls in a shared-configuration table and reserve the next table for method-specific deviations.
- **Evidence**：p.26，`p.26 Table 7`，basis=`pdf_object`。

### Table 8（p.27）

- **模块/版式**：`appendix`；`appendix`；`double_column`；职责：`experimental_design / reproduction / main_comparison`。
- **Design**：rows=6；columns=5；header_levels=1；row_groups=0。
- **Typography**：family=Nimbus Roman No9 L, Computer Modern math；body≈8.0 pt；header≈8.5 pt；header_weight=bold；provenance=pdf_object。
- **表头/规则/高亮**：rules=`booktabs`；highlighting=none；decimal_precision=None。
- **Caption（PDF 逐字，换行已合并）**：Table 8. Method-specific hyperparameters.
- **Caption audit**：word_count=4；moves=title；headline_bold=False；self_contained=False；main_finding_stated=False。
- **Uncertainty**：No uncertainty; learning rates are scientific-notation hyperparameters and the Supplementary column contains optional ratio/“–” entries.
- **Data/statistics**：Columns Method, Optimizer, LR (GLUE8), LR (Math), Supplementary; six method rows from AdamW, FFA, RITE, LoRA+ and LAMB to PRISM.
- **Evidence relation**：Table 7 common settings → Table 8 records optimizer/LR deviations → Tables 2–5 and diagnostic figures can be interpreted without hidden per-method defaults.
- **设计优点**：Makes baseline-specific optimizer choices and LRs auditable.；The Supplementary column preserves the sole LoRA+ ratio override.。
- **设计弱点**：Citation-heavy optimizer cells wrap across lines.；No run-level convergence or tuning budget is included.。
- **可复用模式**：Separate common hyperparameters from method-specific optimizer and learning-rate choices, retaining exceptions in a final column.
- **Evidence**：p.27，`p.27 Table 8`，basis=`pdf_object`。

## 5. 跨对象系统审计

- **Visual narrative**：Introduction 用 Table 1 预置 gauge invariance、additivity 和 efficiency 三项 desiderata；Section 3 以 Algorithm 1 和公式把 PRISM 组织为 tangent projection → clipping/noise → adaptive transform → retraction；Tables 2–5 先给 privacy utility、backbone/rank scaling 和 runtime cost，Figures 1–4 再逐项诊断三类 failure；Appendix C 的 Figures 5–20 把 clipping、fixed-Z noise、raw noise、preconditioner scaling、conditioning 和 realized amplification 串成闭环。
- **Caption system**：主文 Table 1–5 caption 同时交代设置、比较对象和部分主发现，Table 2 的 Takeaway 最完整；Figure 1–4 caption 定义主要指标、颜色/线型和主方向。附录 Figure 5–20 多为短标题，IQR、10–90% band、seed dispersion 或 proxy 的统计口径依赖邻近段落，caption 自包含性弱。
- **Table-header system**：结果表分别使用 desiderata matrix、GLUE8/Math-10K task groups、backbone/type/method、rank blocks 和 runtime/memory 列；附录表转为 split counts、common settings、method-specific settings。性能表多用 ↑/higher-is-better 语义和粗体，成本表用 seconds/MB 或 scientific notation。
- **Method → result → ablation**：Table 1 的设计契约 → Algorithm 1 与 Section 3.1–3.3 的 tangent mechanism → Table 2 utility headline → Table 3 backbone、Table 4 rank、Table 5 overhead → Figures 1–4 三个 issue diagnostics → Appendix C Figures 5–20 的 mechanism decomposition。
- **Main → appendix**：正文 p.1–9 保留 Table 1–5、Algorithm 1 和 Figures 1–4；References p.10–12 后，Appendix Roadmap p.13 开始，Tables 6–8 提供 split/hyperparameter reproduction，Figures 5–20 承载 Issue I–III 的追加诊断。p.35 Figure 20 保留大量空白，说明附录图按证据链追加而非版式填满。
- **Typography**：正文和表格以 Nimbus Roman/Computer Modern 为主；图内坐标、legend 和 colorbar 使用嵌入的 DejaVu Sans 家族。算法同时使用 Roman、数学和 monospace-like 字形。字号以 6–10 pt 图中文字为主，小图在双栏页中存在可读性压力。
- **Color**：蓝/橙始终标识 Baseline/PRISM，透明浅色轨迹承载 dispersion；Figure 12 用额外绿色标识 PRISM 并用点线标记理论 gauge，Figure 19 用色条承载 training step。颜色与 legend 有冗余，但灰度下蓝/橙和浅色 band 的区分明显减弱。

## 6. 最终判断

- **最可复用模式**：先用 desiderata matrix 固定机制比较维度，再用 Algorithm 1 把方法组件串成执行顺序。；将 headline utility、scale/rank、runtime cost 放在连续的主表决策面，并把解释性机制图放在同一结果页。；附录采用“时间带 → 固定状态 gauge sweep → σ sweep → proxy/realized amplification scatter”的诊断序列，把理论 failure mode 分解成可观察量。。
- **最高价值对象**：Table 1：用三个 desiderata 把 gauge invariance、additive noise 和 LoRA-scale efficiency 绑定到同一方法接口。；Table 2：在 GLUE8/Math-10K、三 privacy settings 和 12 tasks 上提供主效用决策面。；Figure 12 + Figures 16–20：分别把 gauge-dependent noise 与 adaptive-preconditioning amplification 拆成机制级证据。。
- **失败模式**：主效用与多数组件消融表只给点估计；DP seed variability 主要通过附录图的散点/带呈现，表格没有统一不确定性列。；附录短 caption 经常不定义透明带、proxy、seed 汇总或采样协议，读者必须往返正文段落。；结果图在蓝/橙之外混用绿色、连续色条、log 轴和不同 legend 位置；p.35 Figure 20 的大空白降低附录版式效率。。
- **一句话视觉策略**：论文先用 desiderata matrix 和 Algorithm 1 定义 intrinsic DP 机制，再用蓝/橙 utility 表与三类机制诊断，把 gauge dependence、bilinear noise 和 adaptive amplification 从理论公式连接到 fixed-Z sweep、σ sweep 与 low-rank stress evidence。

## 7. 复核记录

- `pdfinfo`：35 pages，letter 612×792 pt；`pdffonts`/`pdfimages -list` 用于字体与 vector/raster 判断。
- PDF 全页已用 200 dpi 渲染到 `/tmp/icml-26bfc-render-200/`；对象页逐页视觉检查，非对象页用 contact sheet 核对。
- Markdown 先于 JSON 写入；JSON 随后经同目录 `*.json.tmp` 临时文件原子替换，并执行 schema 校验。
