# 视觉审计：`iclr-2026-3c3e3a191645`

## 范围、事实源与对象对账

- **论文**：*On the Generalization Capacities of MLLMs for Spatial Intelligence*（ICLR 2026 Oral）。
- **PDF 事实源**：`corpus/pdfs/iclr-2026-3c3e3a191645.pdf`。`pdfinfo` 报告 15 个物理页、Letter 页面（612 × 792 pt）、PDF 1.5；正文为 p.1–9，参考文献为 p.10–12，附录 A–C 为 p.13–15。全文、参考文献和附录中的公式、实现细节、prompt 与 LLM disclosure 已读取；参考文献和 Appendix A/C 没有 Figure/Table，Appendix B 的 Table 6 除外。
- **渲染与检查**：整份 PDF 用 `pdftoppm -png -r 200` 渲染为每页约 1700 × 2200 px，达到至少 180 dpi。逐页核对了文字流和对象标签，并对含对象的 p.2、3、4、6、7、8、9、14 做 200 dpi 原页/局部放大检查；`pdfimages -list`、`pdffonts` 与 `pdftohtml -xml -hidden` 用于核对栅格/矢量、字体对象、图形边界和 caption 位置。
- **PDF 对象清单**：正文有 Figure 1–6、Table 1–5；附录有 Figure 7、Table 6。总计 7 幅 Figure、6 张 Table，与 `readings/iclr-2026-3c3e3a191645.json` 和 `reports/tables/visual_inventory_disagreements.csv` 的 13/13 对账一致；没有 reading-only 或 PDF-only 对象。Figure 4、Figure 5 共享物理页 6，Tables 2–4 共享 p.8，Table 5 与 Figure 7 共享 p.9；复合图、图像网格和 prompt 框均作为所属 Figure/Table 的一个对象记录。
- 下文 `bbox` 使用页面 PDF 点坐标的近似 `[x0, top, x1, bottom]`，来自 200 dpi 渲染边界和 PDF 文本/图形对象；用于定位，不表示精确裁切框。

## 公开视觉源获取

先核查 `reports/tables/visual_source_inventory.csv`：自动行将候选仓库记为 `Vegetebird/CA-MLLM`、`exact_visual_source`，列出 `src/qwen_vl/model/vggt/utils/visual_track.py`、`src/unidepth/utils/visualization.py`、`src/qwen_vl/visualization/img_drawer.py`、两个 BLINK YAML 等候选。`corpus/visual_sources/iclr-2026-3c3e3a191645/` 不存在已取得文件。随后只读使用 `gh`：

- `gh repo view Vegetebird/CA-MLLM --json nameWithOwner,description,url,defaultBranchRef,homepageUrl` 返回仓库描述 `[ICLR 2026] Official implementation of the paper "📷 On the Generalization Capacities of MLLMs for Spatial Intelligence"`、默认分支 `main`、仓库 URL `https://github.com/Vegetebird/CA-MLLM`。README 的链接标题和项目描述与论文标题、作者、arXiv `2603.06704` 严格匹配，并明确列出 Spatial Grounding/Spatial Reasoning 评测。
- 仓库树中 `assets/moti.png`（2796 × 1144）与 PDF Figure 1 的长宽比、长颈鹿/相机/射线/对话框逐项匹配；`assets/arch.png`（1230 × 1234）与 Figure 4 的 (a)/(b) GAVE 架构图匹配；`assets/results.png`（2040 × 550）与 Figure 6 的三联跨相机曲线、标题、颜色和数值标签匹配。这三个是经 README/仓库元数据验证的渲染资产，而非可编辑 LaTeX/plot source。
- `src/visualize_tools/img_drawer.py`、`src/qwen_vl/data/draw_marker.py` 和 `src/visualize_tools/default_color_map.py` 是通用 OpenCV/PIL/Open3D 3D box/point/color helper；树中没有与 Figure 2、3、5、7 或 Tables 1–6 对应的 figure-specific `plot*`、`figure*`、`table*`、`.tex`、`.tikz`、`.pgf` 或 notebook。BLINK YAML 是评测任务配置，不是本文表格/图形生成器，未当作视觉源。
- 因而源状态严格记为 **`partial_visual_source`**：Figure 1、4、6 有可验证渲染资产；Figure 2、3、5、7 和 Table 1–6 没有公开的直接绘图/表格/LaTeX 源。不能因为官方仓库存在就把所有内部可视化 helper 或自动候选标为本文图表源。

## 论文级视觉风格

- **版式**：正文为 Letter 双栏。Figure 1–5 主要在单栏浮动体中，Figure 6 跨双栏页宽；Table 1、2、3 跨双栏，Table 4、5 单栏，Table 6 为跨栏 prompt 框；Figure 7 为单栏图像网格。caption 一律位于对象下方，表格 caption 也位于表格上方/下方的标准浮动位置。
- **字体**：正文与 caption 主要使用 Nimbus Roman No9 L regular/medium/italic；数学公式使用 Computer Modern 变体（CMR/CMMI/CMSY），代码/prompt 使用 Courier/NimbusMon。嵌入图内的 sans-serif 主要是 HelveticaNeue、ComicSansMS、Cambria 或资产内渲染的 sans-serif。图内可见字号约 5–12 pt，caption/表体约 8.7–10 pt；Figure 6 的标题和轴标签较大，Figure 2/7 的图像内类别字较小。图形字体结论来自 PDF 对象和 200 dpi 渲染的 mixed evidence。
- **颜色**：Figure 1 用绿色/橙色射线和灰色对话框；Figure 2/7 使用真实照片叠加半透明彩色 3D box；Figure 3 使用紫—红—黄的 3D surface/频率色阶；Figure 4/5 使用黄、橙、绿、青和蓝色模块/光线；Figure 6 使用橙 `Qwen2.5-VL-3B`、绿 `VG-LLM-4B`、青 `Ours-4B`、浅蓝虚线 `Ours-4B (intrinsics unknown)`；Tables 1–5 黑白 booktabs 规则，表 2–4/5 用浅橙 cell highlight 或 bold 标出较高值。结果图经常依赖颜色表达模型身份，marker/布局提供部分冗余。
- **矢量/栅格**：Figure 1、2、3、4、5、7 的主要图像由嵌入 JPEG/JP2/PNG 等 raster assets 组成，外框、箭头、caption 和表格规则为 PDF text/vector；Figure 6 在 `pdfimages -list` 中无对应 raster，PDF 主要为矢量线、marker、文字和网格，仓库 `results.png` 是可验证的渲染资产。Tables 1–5 由 PDF 文本/规则线组成，Table 6 是 PDF 文本与圆角框。
- **一致性**：两栏骨架、caption 标签和 booktabs 风格稳定，但概念示意图、真实图像 overlay、3D surface、Matplotlib 曲线和 benchmark 表格各自形成子系统。图内字体、色板、标记和抗锯齿随嵌入资产变化；Table 2–4 的宽度和旋转表头增加了缩小阅读成本。

## 逐对象审计

### Figure 1（PDF p.2；`introduction` / §1；正文）

- **位置、图型与布局**：单栏宽浮动体，位于引言双栏的右栏；类型为 `conceptual_diagram`、`qualitative_evidence`；复杂度 3/5。画面从左到右展示相机、图像中的长颈鹿、两种 focal length 与两种 3D scene；上方灰色气泡写“Without camera intrinsics…”，左下气泡询问长颈鹿的 3D 位置。绿色与橙色成对射线连接两台相机/图像/物体，竖向箭头标 `h_1`、`h_2=h_1`，虚线标 `f_2=2f_1`、`f_1`。
- **绘图语法**：PDF 中为嵌入图片/图形混合的无轴示意图；x 是相机—图像—物体的流程/空间方向，y 是同一投影高度下的场景关系；没有正式 legend、grid、marker、hatching、reference line 或 uncertainty。所有模块、问题和公式直接标注；绿色/橙色实线和虚线承担光线/尺度关系，线宽约 1 pt（渲染估计）。
- **字体与颜色**：图内主要为手写/Comic-Sans 风格对话文字、衬线数学标签和黑色图标轮廓，约 6–16 pt，regular/bold/italic。绿色约 `#4F8E0A` 表示一条等价投影，橙色约 `#F2AA12` 表示另一条，灰色 `#D9D9D9` 为气泡，黑/白为轮廓和背景；资产 `moti.png` 与 PDF 图形逐项匹配，色值按渲染估计记录。
- **数据/证据**：同一 2D 长颈鹿图像对应不同 `(f,H,Z)`，是 Eq. (1)–(2) 的定性例子，不是实验样本。Figure 1 把引言的 focal–depth/size–depth ambiguity 连接到 §3.3 的理论、Figure 3 的 camera-intrinsics 分布和 Figure 4 的 camera-aware framework；不展示分母、重复、误差或定量结果。
- **Caption（PDF 逐字，60 个空白分词）**：
  > Figure 1: Illustration of the inherent geometric ambiguity in RGB-only spatial reasoning. The same 2D image can result from different 3D scenes: a nearby object with a wide-angle lens can appear identical to a distant object with a telephoto lens. This ambiguity makes generalizable 3D localization from a single RGB image an ill-posed problem, especially when camera intrinsics are unknown.
  含 `title`、`setup`、`encoding_key`、`comparison`、`main_finding`；没有粗体 headline。caption 解释了同图异景和 ill-posed 结论，脱离正文可理解主机制，但不定义公式/相机参数细节，因此 self-contained 取 true 但仅覆盖定性语义。
- **优点**：把不可辨识性、光学直觉和核心问题压缩到一个方向；绿色/橙色射线、相机图标和对话框形成多通道编码；仓库 `assets/moti.png` 提供可复用渲染资产。
- **缺陷**：示意比例不是量化坐标，数学标签/对话框较小；颜色承担射线身份，灰度下两条路径的区分减弱；caption 没写出 `f_2=2f_1` 和相同投影高度的具体条件。
- **可复用范式**：用“同一观测 → 两组相机/场景参数 → 相同投影”的双路径图引入几何歧义；复用时保留直接参数标注，并明确哪些关系是示意而非按比例测量。
- **证据**：p.2，`Figure 1: Illustration of the inherent geometric ambiguity in RGB-only spatial reasoning`，bbox `[188, 48, 408, 165]`，`rendered_observation`。

### Figure 2（PDF p.3；`results` / §3.1–3.2；正文）

- **位置、图型与布局**：单栏宽的 3 × 3 qualitative grid；类型为 `qualitative_grid`、`image_montage`；复杂度 3/5。三列分别是 `w/o rescale`、`x 0.7`、`x 1.3`，三行是不同室内 ScanNet 场景；每格为真实室内图和 3D bounding-box overlay。图中没有行名，列头直接位于图像上方。
- **绘图语法**：栅格 raster image，无坐标轴/数值 grid/legend/reference line/error bar；direct labels 只有三列变换名和图像内 object labels，marker 由半透明 3D boxes/边线承担；hatching、uncertainty 均无。x 是 scene/transform 列，y 是场景行；黑色边框和透视框表示预测空间位置。
- **字体与颜色**：图像内标签为小号 sans-serif/白字或彩色字，约 5–8 pt（渲染估计）；不同 object box 使用红、青、绿、橙等半透明色，底层为棕/灰室内照片，颜色按对象类别/预测框区分。颜色、3D 线框和列标签有部分冗余，但灰度无法稳定保留每个对象身份。
- **数据/证据**：9 个示例格展示 finetuned Qwen2.5-VL 在原图、0.7× 和 1.3× resize 下的 3D detections；无样本数、分母、重复或不确定性。它把 Table 1 的 resize performance collapse 具体化，服务 §3.2 对系统性 localization shift 的机制叙事，并由 Eq. (6)/Figure 3 解释为 camera geometry shift。
- **Caption（PDF 逐字，26 个空白分词）**：
  > Figure 2: Visualization of generalization failure in a camera-agnostic MLLM (finetuned Qwen2.5-VL). Simply resizing the input image during inference induces a systematic shift in 3D localization.
  含 `title`、`setup`、`comparison`、`main_finding`；无粗体 headline、编码键或误差定义。caption 说明了模型和 resize 方向，但不说明三行场景、box 语义或定量评测，self-contained=false。
- **优点**：3 × 3 同构网格让 resize 方向和空间偏移直观可见；列名简单，图像内容保留了真实场景语境。
- **缺陷**：无行名、没有 ground-truth/预测框图例或数值误差；图像内小标签在单栏缩放后难读，颜色和透明叠加不适合灰度打印；没有公开的 Figure 2 生成脚本。
- **可复用范式**：对同一场景沿一个预处理变量做横向 3 列对照，并保持图像裁切和 box overlay 一致；复用时补充行标签、框类型和定量偏移指标。
- **证据**：p.3，`Figure 2: Visualization of generalization failure in a camera-agnostic MLLM`，bbox `[326, 80, 505, 310]`，`rendered_observation`。

### Figure 3（PDF p.4；`theory` / §3.3；正文）

- **位置、图型与布局**：单栏右栏、上下 2 个 3D surface panels；类型为 `other`（3D surface）；复杂度 3/5。panel (a) 是 Qwen2.5-VL preprocessing 后的 `f_x/f_y` camera-intrinsics frequency 分布，panel (b) 是 VG-LLM；每个 panel 有紫色平面、多个尖峰和右侧颜色条 `Frequency`，x/y 轴为 focal lengths/pixels 量纲，z 轴为 Frequency。
- **绘图语法**：嵌入栅格 surface；x/y/z 均为 linear/连续，背景有三维网格；没有 legend，但每 panel 有 colorbar 作为连续编码键；无 marker、hatching、reference line、error bar 或 uncertainty。颜色由深紫到橙/黄编码频率，峰高也编码频率；panel 标题/下方 `(a)/(b)` 直接标注模型。
- **字体与颜色**：图内 3D matplotlib 风格 sans-serif 约 5–8 pt，轴标签/标题约 7–9 pt；紫 `#3B0F9F` 至黄 `#FDE725` 的 sequential/plasma-like palette，白/浅灰网格，colorbar 对应 Frequency；色值为 raster rendered estimate。单独灰度能看峰高，但颜色的频率层次会丢失。
- **数据/证据**：两组 preprocessing 后的相机内参联合分布，图旨在显示 mixed datasets 的多峰/多模态 intrinsics，而不是估计置信区间；无样本量、bin/kernel 设置、重复或不确定性。它支撑 §3.3 的“每个数据集有不同 clustered focal length 分布”机制，并连接 Table 1 的 mixed-source failure。
- **Caption（PDF 逐字，10 个空白分词）**：
  > Figure 3: Multi-modal distribution of camera intrinsics in mixed datasets.
  只有 `title`；没有 setup、编码键、模型对应、单位或不确定性定义，self-contained=false。
- **优点**：上下 panel 直接对齐两个 baseline 的分布，多峰 surface 与 colorbar 能把“相机来源冲突”视觉化。
- **缺陷**：3D 透视和重叠尖峰使峰高/面积难以定量比较；轴单位、频率归一化、样本数和带宽未说明；栅格图在单栏缩放后字号偏小，仓库中没有可验证源。
- **可复用范式**：对不同模型/数据来源使用同一相机内参坐标和 3D density surface，配 colorbar 与 panel label；复用时加样本量、归一化、带宽和二维投影辅助。
- **证据**：p.4，`Figure 3: Multi-modal distribution of camera intrinsics in mixed datasets`，bbox `[326, 380, 505, 735]`，`rendered_observation`。

### Figure 4（PDF p.6；`method` / §4.1–4.2；正文）

- **位置、图型与布局**：单栏宽复合 architecture diagram；类型为 `architecture`、`pipeline`、`conceptual_diagram`；复杂度 4/5。上半 panel `(a)` 从文本问题、四帧 giraffe 输入和四个 GAVE 模块流向 `Camera-Aware Multimodal Large Language Model` 与结构化 3D box answer；下半 panel `(b)` 把 visual tokens 分成 Camera Ray Embedding、2D Visual Encoder、3D Geometric Prior Distillator，经加法节点合入 MLLM，右侧用 MMDE 输出 Point Cloud。
- **绘图语法**：图像/矢量混合；无坐标轴、grid 或正式 legend；箭头和模块框为 direct labels，实线箭头表示数据流，虚线蓝色射线表示 camera ray，颜色块表示 token/encoder 分支；无 hatching、reference line 或 uncertainty，线宽约 1 pt。两 panel 由 `(a)/(b)` 直接标识，资产 `arch.png` 与 PDF 结构相同。
- **字体与颜色**：图内 sans-serif/Helvetica 风格约 7–14 pt，模块名和 answer label 有 bold/italic，数学/代码字符串为 roman/monospace；黄色 `#FDD420`=Camera-Aware MLLM，橙 `#FB916A`=Camera Ray Embedding，绿 `#71D29F`=2D Visual Encoder，青 `#6EC7D4`=3D Geometric Prior Distillator，浅灰=question/point-cloud模块，蓝虚线=视线。颜色、位置、箭头和模块文本提供冗余，灰度仍可读主流程但分支身份下降。
- **数据/证据**：这是方法接口而非统计图：`[x_center,...,roll]` 是 3D box 输出 schema；ray embedding 由 K 派生，2D encoder 提供 appearance，MMDE/point cloud 提供 geometric prior。它直接连接 §4.2–4.4 的三个组件，后续由 Figure 5 的 augmentation、Figure 6 的跨相机曲线和 Table 5 的消融验证；无样本/重复/误差。
- **Caption（PDF 逐字，28 个空白分词）**：
  > Figure 4: The proposed Camera-Aware MLLM Framework. (a) The overview of the architecture, where (b) Geometry-Aware Visual Encoder (GAVE) injects camera-awareness and 3D geometric priors into the MLLM.
  含 `title`、`setup`、`encoding_key`、`comparison`；无粗体 headline、main finding 或 uncertainty definition。它定义了 panel 与 GAVE，但没有解释每个 token 色块/输出字段，self-contained=false。
- **优点**：把端到端架构和 GAVE 内部三分支放在同一层级，箭头、模块色和输出 schema 让方法接口清楚；官方 `assets/arch.png` 可复用。
- **缺陷**：模块文字、四帧输入和 ray fan 在单栏中密集；没有显示融合层数、张量维度或训练/冻结边界；颜色仍是主要分支身份。
- **可复用范式**：上 panel 展示完整输入—输出接口，下 panel 展开一个关键 encoder 的多路几何注入，并复用箭头方向；复用时标注可训练/冻结模块与张量尺寸。
- **证据**：p.6，`Figure 4: The proposed Camera-Aware MLLM Framework`，bbox `[217, 65, 409, 300]`，`rendered_observation`。

### Figure 5（PDF p.6；`method` / §4.3；正文）

- **位置、图型与布局**：单栏右下的纵向 3-step geometric augmentation illustration；类型为 `pipeline`、`conceptual_diagram`；复杂度 3/5。三张带 camera-ray fan 的图像上下排列，中间标 `Camera Intrinsics Update`，两步分别标 `Rescale & Shift & Pad` 与 `Rescale & Shift & Crop`，箭头自上而下表示图像和 K 一致更新。
- **绘图语法**：raster photos + vector arrows/text 混合；无坐标轴、grid、legend、marker、hatching、reference line 或 uncertainty；direct labels true，蓝色虚线 fan 表示 camera rays，图像框和箭头表示 transform sequence，线宽约 1 pt。x 为空间并列 ray fan，y 为增强步骤顺序。
- **字体与颜色**：图内 sans-serif 约 7–9 pt（渲染估计），黑色标签/箭头，蓝色虚线 rays，照片中的橙/绿环境色不是数据色；模块标题 regular/bold，数学/参数由正文补充。颜色仅辅助，顺序和文字是主要编码，灰度仍可读步骤。
- **数据/证据**：没有实验数值；示意 scaling、principal-point shifting、padding/cropping，并强调图像与 `(f_x,f_y,c_x,c_y)` 同步变化。它把 §4.3 的方法动作连接到 Figure 6 的 rescale-factor evaluation 和 Table 5 的 x1.2 ablation；无重复或不确定性。
- **Caption（PDF 逐字，7 个空白分词）**：
  > Figure 5: Illustration of camera-aware geometric augmentation.
  只有 `title`；无 setup、编码键、比较、main finding 或 uncertainty definition，self-contained=false。
- **优点**：把两种常见变换和 intrinsics update 放入一条垂直流程，直接补充 Figure 4 架构的训练数据机制。
- **缺陷**：图像/相机框边界和蓝色射线在单栏中较小；没有写出 scale/offset 的具体数值或失败对照；caption 极短且没有说明 pad/crop 的区别。
- **可复用范式**：对每个图像变换同时画 K update 与视觉结果，使用统一箭头和步骤标签；复用时把变换参数、裁切锚点和几何不变量写进 caption。
- **证据**：p.6，`Figure 5: Illustration of camera-aware geometric augmentation`，bbox `[320, 370, 505, 735]`，`rendered_observation`。

### Figure 6（PDF p.7；`results` / §5.1；正文）

- **位置、图型与布局**：跨双栏页宽的三 panel line chart；类型为 `line`、`scatter`、`robustness`；复杂度 4/5。panel (a) `Single-Frame 3D Detection @ ScanNet`、(b) `Video 3D Detection @ ScanNet`、(c) `Video 3D Visual Grounding @ ScanRefer`。x 均为 `Rescale Factor` 0.5–1.5，y(a,b) 为 `F1@0.25 (20 Common Classes)`，y(c) 为 `Acc@0.25`；每 panel 有 `Original Scale` 的 x=1 竖向虚线。
- **绘图语法**：PDF 主要为 vector line/marker/text；x/y linear（x 是连续 rescale factor）；浅灰 x/y grid；每 panel 内置 legend，(a,b) 3 项、(c) 4 项，位置约为下部/右下；direct labels 在每个点旁标数值。Qwen 用橙 circle、VG-LLM 用绿 square、Ours-4B 用青 triangle，(c) 额外用浅蓝 dashed diamond 表示 unknown intrinsics；无 hatching、error bar、band 或 uncertainty，连接线约 1–1.5 pt。
- **字体与颜色**：图内 serif/sans 混合约 6–10 pt，panel title/axis label 较大、点标签较小，regular/bold；橙约 `#FF7F0E`=Qwen2.5-VL-3B，绿 `#2CA02C`=VG-LLM-4B，青 `#20BFC4`=Ours-4B，浅蓝约 `#9ECAE1`=Ours-4B (intrinsics unknown)，灰虚线/grid 为参考。颜色与 marker/legend/直接数值有冗余，灰度下 marker 仍保留身份。
- **数据/证据**：三个任务在 rescale factor 0.5–1.5 的 11 个点上比较跨相机泛化；例如单帧 0.5 时 Qwen/VG/Ours 为 15.6/5.8/52.1，1.5 时 20.4/19.9/45.5；视频 0.5 为 10.9/6.7/55.5，1.5 为 18.8/19.7/47.5；ScanRefer 0.5 为 4.5/2.9/34.9，unknown-K Ours 为 20.5，1.5 为 8.8/8.2/34.6，unknown-K 为 17.4。没有 seed、重复、误差或区间；`results.png` 与 PDF 三联图视觉匹配。Figure 6 是 Table 1 的 resize failure 的正向方法对照，也是 Table 5 消融和 Figure 7 定性案例的主结果桥接。
- **Caption（PDF 逐字，32 个空白分词）**：
  > Figure 6: Cross-camera generalization on spatially-grounded tasks. While camera-agnostic MLLMs (Qwen2.5-VL, VG-LLM) fail catastrophically on altered camera geometries by rescaling, our method maintains robust performance, proving its ability to generalize across cameras.
  含 `title`、`setup`、`encoding_key`、`comparison`、`main_finding`；无粗体 headline 或 uncertainty definition。caption 说明模型、扰动和方向性结论，但不列任务分母、unknown-K 颜色和具体点值，self-contained=false。
- **优点**：三任务共享 x 轴、模型顺序和视觉编码，能直接比较跨相机曲线；在点旁标值降低读图成本；官方 `assets/results.png` 提供可验证渲染资产。
- **缺陷**：每 panel legend 与点标签同时存在，缩小后密集；y 指标在 (c) 改为 accuracy，三 panel 的分数含义不完全同构；无误差/seed，且 unknown-K 只在 (c) 出现。
- **可复用范式**：将同一 perturbation 轴下的多个任务做 small multiples，统一 baseline/ours 的颜色和 marker，并以 x=1 参考线显示原始条件；复用时补齐误差、分母和 K availability。
- **证据**：p.7，`Figure 6: Cross-camera generalization on spatially-grounded tasks`，bbox `[92, 55, 520, 205]`，`rendered_observation`。

### Figure 7（PDF p.9；`results` / §5.4 Visualization Results；正文）

- **位置、图型与布局**：单栏右侧 4 × 3 qualitative image grid；类型为 `qualitative_grid`、`image_montage`；复杂度 4/5。三列分别为 `Qwen2.5-VL-3B`、`VG-LLM-4B`、`Ours`；上两行是 `Zoomed-in ScanNet`，下两行是 `(In-the-wild test) TUM-RGBD`，左侧括号/竖排标签标两类场景。每格包含真实室内照片和 3D detection boxes/labels。
- **绘图语法**：嵌入 JPEG2000/raster images；无坐标轴、grid、正式 legend、hatching、reference line 或 uncertainty；direct labels 为列名、场景组名和图像内 object/category labels；3D box 的透视边线/半透明面是 marker-like geometry，线宽约 1–2 px（渲染估计）。
- **字体与颜色**：图像标签为小号 sans-serif/白或彩色字，约 5–8 pt；box/label 颜色包括绿（如 keyboard/tellurion/dice）、青/蓝（couch/monitor）、橙/黄（chair/picture）及红/紫等，颜色来自类别 overlay 而非模型身份。场景布局和列标签提供模型冗余，灰度下类别色义明显下降。
- **数据/证据**：12 个 qualitative cells（4 场景 × 3 模型），上部为 zoomed-in ScanNet，下部为 unseen TUM-RGBD；不提供样本分母、重复、不确定性或 box error。它是 Figure 6 的 quantitative robustness 的视觉案例，展示相机参数和 camera-aware augmentation 对 3D localization 的定性效果；官方树中只有通用 `img_drawer`/color helpers，没有 figure-specific generator。
- **Caption（PDF 逐字，19 个空白分词）**：
  > Figure 7: Qualitative comparison of 3D object detection results. Our camera-aware MLLM can robustly generalize to new camera setups.
  含 `title`、`comparison`、`main_finding`；无粗体 headline、setup、编码键或 uncertainty definition。caption 不说明 ScanNet/TUM-RGBD 行组和模型列，self-contained=false。
- **优点**：4 × 3 同构 grid 同时覆盖合成 zoom 与真实 in-the-wild，列顺序固定，读者可比较 box 是否贴合物体。
- **缺陷**：无 ground-truth/预测框图例、置信度或数值误差；类别颜色与模型列混在真实照片中，缩小后难辨；caption 过短且来源脚本缺失。
- **可复用范式**：把合成扰动和真实场景放在上下两个 row group，模型固定为列，保持同一 3D box overlay；复用时加入 ground-truth 标记、误差摘要和场景元数据。
- **证据**：p.9，`Figure 7: Qualitative comparison of 3D object detection results`，bbox `[320, 300, 505, 565]`，`rendered_observation`。

### Table 1（PDF p.3；`results` / §3.2；正文）

- **结构与版面**：跨双栏页宽，8 个数据行（每个 Qwen2.5-VL-3B/VG-LLM-4B 四种 train/evaluate 条件）和 6 个逻辑列：`Model`、`Train Data`、`Evaluate Data`、`P_0.25`、`R_0.25`、`F1_0.25`。两层表头：`31 common classes` 跨最后三列；模型各自形成 4 行 row group。booktabs/partial horizontal rules，无竖线；表体约 8.7–9.5 pt，表头加粗，caption 约 9 pt。
- **内容与统计**：Qwen 单 ScanNet→ScanNet-val F1=45.7，mixed source=35.4，x0.8=24.3，x1.2=31.6；VG-LLM 对应 46.5、46.0、25.8、33.2。P/R/F1 是 31 common classes 的单点结果，没有 mean±SD、样本数、重复或显著性；脚注只解释 `ScanNet-val x0.8` 为 resize 0.8。
- **Caption（PDF 逐字，31 个空白分词）**：
  > Table 1: Generalization failure of camera-agnostic MLLMs. 3D object detection performance drops when trained on mixed data sources or evaluated on resized images, exposing a fundamental lack of robustness and generalization.
  含 `title`、`setup`、`comparison`、`main_finding`；无粗体 headline、uncertainty definition 或指标缩写定义。caption 给出 failure 方向，但 31 classes/训练条件需表头与正文，self-contained=false。
- **优点**：Train Data 与 Evaluate Data 分开，单数据/混合数据/resize 条件并置，直接支持失败机制；三项指标固定且规则克制。
- **缺陷**：长数据集名称造成列密度；P/R/F1 的定义、聚合和重复缺失；mixed-source 对 VG-LLM 的 F1 只从 46.5 到 46.0，caption 的“drops”并非同样幅度地适用于所有行。
- **可复用范式**：用两级模型 row group 和 train/evaluate condition 展开分布外失败，配固定 P/R/F1 列；复用时加样本数、方差和每个变换的参数说明。
- **证据**：p.3，`Table 1: Generalization failure of camera-agnostic MLLMs`，bbox `[88, 32, 525, 180]`，`rendered_observation`。

### Table 2（PDF p.8；`results` / §5.2 SPAR-Bench；正文）

- **结构与版面**：跨双栏宽的宽表；16 个数据行，分为 `SPAR-Bench (tiny)` 5 行和 `SPAR-Bench (full)` 11 行两个 row group。25 个逻辑列（`Methods`、`Avg.` 加 23 个低/中/高难度任务指标）；两层表头，Low/Medium/High 组下的 task names 竖排/旋转，任务列包含深度、距离、相机运动、位置等 `SPAR-Bench` 子任务。booktabs 上/中/下横线，无竖线；表体和旋转表头约 5.5–7 pt，组表头/方法名约 8–9 pt。
- **内容与统计**：tiny 行含 Human Level、GPT-4o、Claude-3.7-Sonnet、Qwen2-VL-72B、Qwen2.5-VL-72B；full 行含 InternVL2/2.5、LLaVA、Qwen、GPT-5、Gemini、VG-LLM-4B、SPAR-8B、Ours-4B。`Ours-4B` full Avg=68.35，VG-LLM-4B=60.36，SPAR-8B=63.25，Human Level tiny Avg=67.27。单元格为 benchmark score/accuracy，缺失项用 `-`，没有不确定性或重复统计。
- **表头/高亮**：任务按 Low/Medium/High 分组，`Avg.` 横向单列；部分高值/作者结果用浅橙背景或粗体（颜色在 200 dpi 下可见但规则不完全统一）。没有 mean±SD、rank 或 error bars。
- **Caption（PDF 逐字，10 个空白分词）**：
  > Table 2: Comparison of MLLMs’ spatial reasoning performance on SPAR-Bench.
  只有 `title`，没有 setup、编码键、难度/缺失值定义或主发现，self-contained=false。
- **优点**：tiny/full 和 Low/Medium/High 同时展开，`Avg.` 与细粒度任务并列，能看出 Ours 的整体和子任务表现。
- **缺陷**：25 列压缩导致旋转表头和数值极小；高亮语义不在 caption 定义，任务名缩写难解；不同 benchmark/模型的缺失值没有说明是否影响 Avg。
- **可复用范式**：用难度组的两级表头配 Avg 与 task-level score，row group 区分 benchmark variant；复用时减少列密度、说明缺失值和高亮规则，并统一不确定性。
- **证据**：p.8，`Table 2: Comparison of MLLMs’ spatial reasoning performance on SPAR-Bench`，bbox `[88, 28, 525, 300]`，`rendered_observation`。

### Table 3（PDF p.8；`results` / §5.2 VSI-Bench；正文）

- **结构与版面**：跨双栏、10 个逻辑列（`Methods`、`Avg.`、Numerical Answer 4 列、Multiple-Choice Answer 4 列），20 个数据行，分为 `Proprietary Generalist MLLMs`、`Open-Sourced Generalist MLLMs`、`MLLMs for Spatial Intelligence` 三个 row group。两层表头，顶部按 Numerical Answer/Multiple-Choice Answer 分组，子任务表头旋转；booktabs 上/中/下线、无竖线；体字号约 7–8 pt，表头约 7–9 pt。
- **内容与统计**：列包括 `Obj. Count`、`Abs. Dist.`、`Obj. Size`、`Room Size`、`Rel. Dist.`、`Rel. Dir.`、`Route Plan`、`Appr. Order`。Ours-4B Avg=46.8，VG-LLM-4B=47.3，Gemini-2.5-Pro-0617=52.7；Ours 在 Obj. Count=71.3、Abs. Dist.=39.3、Room Size=65.9 等列有高值，但 Avg 不是表中最高。单点 benchmark scores，缺失用 `-`，无重复/误差。
- **Caption（PDF 逐字，10 个空白分词）**：
  > Table 3: Comparison of MLLMs’ spatial reasoning performance on VSI-Bench.
  只有 `title`；没有 setup、编码键、缺失值/平均方式或 main finding，self-contained=false。
- **优点**：按模型来源/用途分 row group，并把 numerical 与 multiple-choice 子任务放在同一表头，便于检查能力异质性。
- **缺陷**：宽表旋转表头密集；Ours 的 Avg 低于若干模型，而正文将 Tables 3–4 总括为 SOTA，caption 没有解释 aggregate 与分项差异；高亮和缺失值规则不完整。
- **可复用范式**：用来源 row group + 任务类型二级表头展开 benchmark 的能力剖面；复用时在 caption 说明 Avg 计算和缺失值，并分别陈述 aggregate/分项结论。
- **证据**：p.8，`Table 3: Comparison of MLLMs’ spatial reasoning performance on VSI-Bench`，bbox `[115, 300, 455, 500]`，`rendered_observation`。

### Table 4（PDF p.8；`results` / §5.2；正文）

- **结构与版面**：右栏单栏表，11 个模型行，9 个逻辑列：`Model`；CV-Bench-3D 的 `Avg., Depth, Dist.`；BLINK-Spatial 的 `Avg., Depth, Spatial Rel., Multi. View`。两层表头，两个 benchmark group；booktabs/partial horizontal rules，无竖线；表体约 7.5–8.5 pt，caption/表头约 8–9 pt。
- **内容与统计**：Ours-4B 的 CV-Bench-3D Avg=90.7、Depth=89.8、Dist.=91.5；BLINK-Spatial Avg=77.0、Depth=77.4、Spatial Rel.=66.4、Multi. View=87.2。VG-LLM-4B CV Avg=91.3，Gemini-2.5-Pro CV Avg=90.8。单点 benchmark scores，`-` 表示缺失，无误差/重复/样本数。
- **Caption（PDF 逐字，11 个空白分词）**：
  > Table 4: Comparison of model performance on various spatial understanding datasets.
  只有 `title`；没有 setup、列方向、缺失值、主发现或不确定性，self-contained=false。
- **优点**：两个 benchmark group 共享模型行，aggregate 与子任务并置，表宽小于 Tables 2–3。
- **缺陷**：Ours 并非每个 aggregate/子任务最高，但正文用无条件 “state-of-the-art”；单元格高亮未在 caption 定义；缺失值会让模型间列覆盖不对称。
- **可复用范式**：在共享模型行下并列多个 benchmark 的 Avg/子任务组；复用时说明每个 Avg 的构成、覆盖范围和缺失值影响。
- **证据**：p.8，`Table 4: Comparison of model performance on various spatial understanding datasets`，bbox `[320, 300, 505, 505]`，`rendered_observation`。

### Table 5（PDF p.9；`ablation` / §5.3；正文）

- **结构与版面**：右栏单栏表，6 个数据行，6 个逻辑列：组件三列 `Ray Emb.`、`Geom. Aug.`、`Prior Dist.`，指标三列 `P_0.25`、`R_0.25`、`F1_0.25`。两层表头：`Components` 与 `20 Common Classes` 分组，第二层展开组件/指标；无 row group；booktabs 规则、无竖线；体表约 8–9 pt，最终行指标加粗。
- **内容与统计**：六种组合依次为空、仅 Ray Emb.、仅 Geom. Aug.、仅 Prior Dist.、Ray+Prior、全组件；空基线 P/R/F1=40.8/37.7/39.1，全组件=54.6/50.1/52.1。测试为 ScanNet-val x1.2、20 common classes、single-frame 3D detection；没有 seed、重复、误差或训练成本。
- **Caption（PDF 逐字，21 个空白分词）**：
  > Table 5: Ablation study on the components of our Camera-Aware MLLM framework. Performance measured on ScanNet-val x1.2 to test cross-camera generalization.
  含 `title`、`setup`、`comparison`；无粗体 headline、组件缩写定义、main finding 或 uncertainty definition，self-contained=false。
- **优点**：三组件开关结构简单，空基线、单组件和全组件共存，能看出组合增益；P/R/F1 列固定，最终行加粗。
- **缺陷**：只比较一个 x1.2 synthetic setting，未显示重复/方差或计算开销；Ray/Geom/Prior 缩写依赖正文；组合空间没有完整三组件子集（缺少 Ray+Geom、Geom+Prior 等）。
- **可复用范式**：用组件开关表 + 固定跨相机指标识别单组件与联合增益；复用时覆盖完整子集或解释缺失组合，并报告不确定性和成本。
- **证据**：p.9，`Table 5: Ablation study on the components of our Camera-Aware MLLM framework`，bbox `[320, 115, 505, 290]`，`rendered_observation`。

### Table 6（PDF p.14；`appendix` / B Implementation Details）

- **结构与版面**：跨文本宽度的单个圆角 prompt/code block，作为 1 个逻辑示例单元、1 列、1 个标题层；没有数据行/列网格，规则为 `minimal`（外框）。框内以 Courier/等宽体展示 `X_User: <image><image><image><image>…`、五个类别、JSON list 指令、`X_Agent` 及两个示例 box entries；约 9–10 pt，标题约 9 pt，代码/字符串保持等宽。
- **内容与编码**：prompt 明确输入类别 `curtain, bathtub, towel, doorframe, bar`，要求输出 `label` 和 `bbox_3d`/`box 3d`，格式 `[x center, y center, z center, x size, y size, z size, yaw, pitch, rolll]`；示例包含负坐标/尺寸。它是 reproduction/method interface，不是 benchmark 结果表；没有小数精度、均值、排名或不确定性。
- **Caption（PDF 逐字，9 个空白分词）**：
  > Table 6: The prompt for 3D video object detection.
  含 `title`、`setup`、`encoding_key`、`reproduction`；无粗体 headline、主发现或 uncertainty definition。caption 说明 prompt 用途，但字段拼写和 ellipsis 细节需查看框内代码，self-contained=false。
- **优点**：把多帧输入、类别约束、JSON schema 和示例输出集中展示；正文明确说明显式类别用于减少不存在类别的 hallucination。
- **缺陷**：外框承载的代码块被命名为 Table，但没有普通表头/行列统计；`rolll` 与 `bbox 3d`/正文术语可能存在拼写/字段不一致；没有给完整 prompt、采样参数或失败处理。
- **可复用范式**：用带标题的单元 prompt block 同时给输入模板、约束类别和结构化输出 schema；复用时统一字段命名并说明示例是否完整。
- **证据**：p.14，`Table 6: The prompt for 3D video object detection`，bbox `[90, 540, 522, 750]`，`pdf_object` + `rendered_observation`。

## 跨对象系统

- **视觉叙事**：Figure 1 先以投影等价说明 RGB-only 的几何歧义；Table 1 与 Figure 2 给出 mixed-source/resize 的 camera-agnostic failure；Figure 3 将失败归因于多模态 camera intrinsics；Figure 4/5 给出 ray embedding、geometric prior 和一致增强的架构接口；Figure 6 在三项 spatially-grounded task 上画跨相机 robustness 曲线；Tables 2–4 展开 SPAR/VSI/CV/BLINK benchmark；Table 5 做三组件消融；Figure 7 用 ScanNet/TUM-RGBD 图像案例补充定性证据；Table 6 提供视频 3D detection prompt 的复现接口。
- **Caption 系统**：所有 Figure/Table 使用 `Figure n:`/`Table n:` 标签，caption 通常紧跟对象。Figure 1/6 caption 包含 setup、比较和主发现；Figure 2/4/7 只提供有限上下文；Figure 3/5、Table 2–4/6 主要是标题式 caption；没有 caption 定义 error/uncertainty，表格高亮和缺失值规则也少有说明。
- **表头系统**：Table 1 用模型/数据条件 + 31 classes 的两层结构；Table 2 用 benchmark variant + Low/Medium/High 任务分组；Table 3 用模型来源 + Numerical/Multiple-Choice 分组；Table 4 用两个 benchmark 分组；Table 5 用 Components/20 Common Classes；Table 6 是 code block 而非数据表。分组结构能表达任务异质性，但宽表旋转表头、缩写和 `-` 缺失值使阅读成本偏高。
- **方法—结果—消融链接**：Figure 1/Eq. (1)–(2) 提出 ambiguity；Figure 3/Eq. (3)–(7) 解释 K/ray distribution；Figure 4/5 实现 camera-aware components；Figure 6 验证跨相机曲线；Table 5 识别组件协同；Figure 7 提供 qualitative generalization；Tables 2–4 检查一般 spatial reasoning；Table 6 复现视频 prompt。论文的主机制链闭合于 synthetic resize/ScanNet，但真实相机的定量证据仍有限。
- **字体一致性**：正文/表格 Nimbus Roman + Computer Modern 稳定；Figures 1–5/7 的资产字体与 Figures 6 的 chart font 不同，Table 2/3 的旋转表头明显缩小。表体和 caption 的字号大体一致，图像内 object labels 是最小文本。
- **颜色一致性**：同一模型颜色在 Figure 6 内一致（橙/绿/青/浅蓝），但 Figure 2/7 的颜色按 object class，Figure 4/5 按模块，Figure 3 按 frequency，benchmark tables 按局部高亮；没有全篇统一 palette。Figure 6 的 marker/线型提供较好冗余，Figure 2/7 仍依赖 box 颜色和空间叠加。

## 最终判断

- **最可复用模式**：
  1. Figure 1 的双场景/双射线示意，将相同 2D 观测与不同 3D 参数直接对照。
  2. Figure 4 的 architecture overview + encoder detail 双 panel，明确输入、几何分支和输出 schema。
  3. Figure 6 的三任务 small multiples，以统一 rescale 轴、原始尺度参考线、颜色和 marker 表达跨相机 robustness。
  4. Table 5 的组件开关消融，空基线、单组件和联合组件在固定指标下对齐。
  5. Figure 7 的“合成 zoom + 真实 in-the-wild”上下 row group 与模型列对照，以及 Table 6 的可复现 prompt block。
- **最高价值对象**：Figure 1（核心几何机制）、Table 1 + Figure 2（失败证据）、Figure 4（方法接口）、Figure 6（跨任务主结果）、Table 5（组件消融）。Figure 7 和 Table 6 对部署/复现有补充价值。
- **失败模式**：
  1. Figure 2/3/5/7 和 Table 6 的 caption 过短，缺少模型/行组/编码、分母或复现边界。
  2. Tables 2–4 宽度高、旋转表头和小字号明显；局部高亮/缺失值没有统一语义。
  3. 结果对象没有 error bars、seed variance 或显著性；单点 benchmark 和单次 qualitative grid 不能支持稳定性结论。
  4. Figure 6/benchmark tables 的 aggregate 与正文 SOTA 总括存在边界差异：VSI-Bench Ours Avg=46.8 低于 VG-LLM=47.3、Gemini=52.7，CV-Bench-3D Ours Avg=90.7 低于 VG-LLM=91.3。
  5. Figures 2/7 的 raster overlay 与 Figure 6 的 vector chart 视觉系统不一致，灰度下类别/模型色义减弱；官方仓库只提供部分渲染资产，没有完整 figure/table source。
- **一句话视觉策略**：论文按“几何歧义 → 失败可视化 → camera-aware 架构/增强 → 跨相机曲线 → benchmark 与组件消融 → 定性/提示复现”组织视觉证据，机制与主结果连接清楚，但宽表可读性、统计不确定性和部分 caption/source 完整性仍未闭合。
