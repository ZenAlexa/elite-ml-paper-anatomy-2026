# Visual audit — `iclr-2026-fe86008e62d4`

## 范围、事实源与复核方法

- 论文：*The Polar Express — Optimal Matrix Sign Methods and their Application to the Muon Algorithm*，ICLR 2026 oral。
- PDF 事实源：`corpus/pdfs/iclr-2026-fe86008e62d4.pdf`，官方 proceedings URL 为 `https://proceedings.iclr.cc/paper_files/paper/2026/file/df88b275bef31ac96c85f0c4013734fc-Paper-Conference.pdf`；OpenReview forum 为 `https://openreview.net/forum?id=yRtgZ1K8hO`。
- PDF 共 38 个物理页：正文 p.1–10，致谢/复现/参考文献 p.11–14，目录 p.15，附录 A–J p.16–38。以 PDF 页码和实际 caption 为准，不把 Algorithm 箱误记为 Figure/Table。
- `readings/iclr-2026-fe86008e62d4.json` 的 `visual_inventory` 与 PDF 逐页核对一致：Figure 1–6 为正文，Figure 7–18 为附录；PDF 没有 Table。全文的 “Table 2/3” 只是在引用其他论文，不是本 PDF 的表格。
- 全部 38 页使用 `pdftoppm -r 200` 渲染（每页 1700×2200 px，满足至少 180 dpi）；先用四张全页 contact sheet 检查页序、分页和漏图，再以 200 dpi 原图逐一检查 p.2、5、8、9、10、27、29、31、32、33、34、36、37 的全部 18 幅 Figure。`pdfimages -list` 显示仅 p.5 有 1592×471 RGB 图像及 alpha mask；其余图形主要是 PDF vector paths/Form，caption、公式和正文为 vector text。未发现跨页 Figure/Table、qualitative grid、截图或 inset。

## 源码与视觉源核查

- `reports/tables/visual_source_inventory.csv` 的原始行状态为 `no_public_source_found`，候选为 `modichirag/GPT-opt`（score 9）和 `NoahAmsel/PolarExpress`（score 2）；`corpus/visual_sources/iclr-2026-fe86008e62d4/` 不存在/为空，`visual_source_files_local.csv` 无该 paper 的本地记录。
- PDF 正文/脚注明确链接 `https://github.com/modichirag/GPT-opt/tree/polar`；其 `polar` branch HEAD `cf8f059c790a7351843cbf1e6b12a03910bd122a` 的 README 明确说明该仓库用于本论文，且 `plot.py`/`gptopt/plot_utils.py` 提供 GPT-2 learning-rate、epoch 和 time 曲线的绘图实现。该树没有已导出的 Figure PDF/PNG，也没有 Figure 3、5、7–10、14–18 的匹配绘图脚本。
- `https://github.com/NoahAmsel/PolarExpress` README 与论文标题直接对应；`plotsforpolar.ipynb` 的 Markdown 明确写着 reproduces Figure 2，cell 4 调用 `plot_figure2(l1=0.03, n_steps=3, degree=5)`，并在 cell 1 给出 serif/Computer Modern、字号、线宽、网格、300 dpi 导出等参数。该 notebook 是 Figure 2 的可复用视觉源，但不是整篇论文所有图的源；仓库没有已导出的论文 Figure 资产。
- 因而本审计的源状态为 **`partial_visual_source`**：Figure 2 和 GPT-2 训练曲线家族（Figure 1、4、6、11–13）的绘图代码/样式有直接依据；其余图以 PDF rendered observation 和 PDF object 为事实。没有抓取数据集、checkpoint、模型权重或完整训练环境。

## 论文级视觉系统

- **版式**：ICLR 双栏正文；18 幅图均为跨双栏/页宽复合图，未见单栏图和表格。Figure 1/3/4/6 是两-panel 左右比较；Figure 5 是两-panel bar；Figure 2 是三-panel 理论图；Figure 7/9 为上下组合；Figure 8/10/11/12/13/14/15/16 为 2×2 或上下排列的复合图；Figure 17/18 为单-panel 全宽图。
- **字体**：PDF 文字对象主要为 Nimbus Roman No9 L 与 Computer Modern 数学字形（另有 Nimbus Mono L 用于代码/等宽标识）；图中可见 serif/数学字体。图内字号约 6–14 pt：刻度约 6–8 pt，轴标题约 8–10 pt，panel 标题约 12–14 pt，caption 对象为 9.963 pt。`plot.py` 明确 `font.family='serif'`、`font.size=12`、`usetex=True`、曲线 linewidth=2；Figure 2 notebook 明确 serif、mathtext cm、`font.size=12`、标题 13、轴标签 12、保存 300 dpi。
- **调色板**：训练图以方法语义映射为主：PolarExp 黑色 `#000000`，You 紫色 `#8A2BE2`，Jordan 红色 `#FF0000`，AdamW 深蓝（源码 `#00518F`，PDF 渲染略有变化）；Newton-Schulz 绿色、SGD-m 橙色等为附录局部扩展。理论图 Figure 2 使用紫/青/黄三条多项式曲线；Figure 17 使用蓝/橙表示方法、线型表示初始化；Figure 18 用三种颜色表示 n、三种线型表示 restart interval。颜色与线型/marker 部分冗余，但大多数训练图灰度分离仍依赖颜色。
- **叙事**：Figure 2 把 odd-polynomial composition 的区间收缩具象化；Figure 3 再把误差收敛与 GPT-2 gradient 接上；Figure 1/4/6 将该近似接到 Muon 的 validation-loss headline；Figure 5 给出精度—运行时间消融。附录 Figure 7–10 解释有限精度、Frobenius/cosine 和小奇异方向，Figure 11–16 扩充训练/任务切片并暴露跨任务优势不稳定，Figure 17–18 展示未用于主要 Muon 实验的谱隙初始化和矩形矩阵 runtime 扩展。

## 逐对象审计

### Figure 1 — GPT-2-Large validation loss（p.2，正文）

- **设计/布局**：跨双栏、左右 2 panel；左为 final validation loss–learning rate 的超参数 sweep，右为最佳学习率下 validation loss–epoch 曲线。左 panel 右上放三项方法 legend，右 panel 复用该方法语义但不重复 legend；留白和坐标框充足。
- **类型与绘图语法**：`line`；x 为混合（左 log learning rate，右 linear epoch），y 为 linear validation loss；浅灰双向虚线网格；3 条系列；实线 PolarExp、点线 You、点划线 Jordan；无 marker、无 band/error bar、无 reference line；linewidth 约 2 pt。单一左 panel legend 实际服务左右比较。
- **编码与数据**：x=学习率/epoch，y=最终或逐 epoch validation loss，颜色/线型=Muon 的 polar-factor 实现；1B FineWeb、GPT-2-Large 774M 参数。左图是每个学习率 run 的 final 值，右图是每个方法最佳学习率的 epoch trajectory；没有 seed、区间或 inferential test。
- **Caption（PDF 原文）**：`Figure 1: Training a GPT-2-Large model (774M params) on 1 billion tokens from the FineWeb dataset (Penedo et al., 2024). The label muon-<name> refers to implementing Muon using <name> to compute the polar factor. Left: final validation loss across learning rates. Right: validation loss across epochs using the best learning rate. The best learning rate (lr) and final validation loss for each method were muon-You (lr = 0.02): 3.399, muon-Jordan (lr = 0.02): 3.398 and muon-PolarExp (lr = 0.02): 3.340.` Caption 有 setup、缩写/方法定义、panel key、比较和数值 headline；脱离正文可读，但没有 seeds/不确定性定义。
- **证据关系**：引言把 Polar Express 接入 Muon → Figure 1 将 polar-factor 替换固定、训练条件相同的三个实现放到同一 validation-loss 面 → Figure 5 检查所需迭代数和 SVD 成本；Figure 11/12/13 在附录补 training loss、time、weight decay 和 10B tokens。
- **优点**：左 sweep 与右 trajectory 同时显示调参结果和动态；黑色 PolarExp 在同一坐标上易追踪；caption 直接给 best lr/loss。
- **弱点**：主 headline 依赖最佳学习率选择且无主实验 seed/区间；右 panel 只有左侧 legend，读者需跨 panel 复用；颜色/线型在灰度小尺寸下不够稳健。
- **可复用模式**：用“超参数 sweep + 最佳点 trajectory”成对呈现方法优劣，并在 caption 给出每组的最优点和训练预算。

### Figure 2 — optimal-polynomial interval contraction（p.5，正文）

- **设计/布局**：页宽 3 panel 横排，每 panel 一条 odd polynomial 曲线；面板阅读方向 p1→p2→p3。每个 panel 画输入区间到输出区间的虚线矩形、浅色填充及 y=1 黑色水平线，并在右上给出 p_t legend；无数据点或不确定性带。
- **类型与绘图语法**：`line` + `conceptual_diagram`；x/y 均 linear，范围约 [0,2]；无网格；每 panel 1 主曲线、1 黑色 reference、4 条 bounds 虚线；3 个局部 legend；无 shared legend、无 marker、无 hatch；linewidth 为曲线约 3.5 pt、reference 约 2.5 pt、bounds 约 1.2 pt。p.5 的 Figure 2 是唯一由 `pdfimages` 识别的 1592×471 RGB+mask 图像，caption/正文仍是 vector。
- **编码与数据**：x=奇异值输入，y=p_t(x)；panel 色彩依次编码 p1（紫）、p2（青）、p3（黄）；虚线标记 `[ell_t,u_t]` 与 `[ell_{t+1},u_{t+1}]`，黑线为 y=1。`d=5, ell_1=0.03,u_1=1`，区间递推为 `ell_{t+1}=p_t(ell_t), u_{t+1}=2-ell_{t+1}`；这是理论构造/示意，不是重复实验数据。
- **Caption（PDF 原文）**：`Figure 2: The evolution of the first three optimal polynomials p1, p2, and p3 and the corresponding lower bounds ell_{t+1} = p_t(ell_t) and upper bounds u_{t+1} = 2 − ell_{t+1}, as described in Theorem 3.1. The horizontal black line shows y = 1. The polynomial degree is d = 5. We set ell_1 = 0.03 and u_1 = 1.` Caption 定义了理论对象、线型语义、参数和定理指针，脱离正文仍可读；不是粗体结论标题。
- **证据关系**：公式 (5)→(6) 的矩阵/标量等价 → Theorem 3.1 的 greedy-is-optimal → Figure 2 显示每一步的 polynomial 与 bounds contraction → Algorithm 1 在线组合这些多项式；Appendix C/E 给 proof/equivalence，Appendix A 给实现。
- **优点**：把抽象区间递推和 odd-polynomial composition 变成可扫描的三步 storyboard；虚线矩形同时编码输入/输出 bounds，y=1 reference 使逼近目标明确。
- **弱点**：三 panel 的彩色曲线没有冗余方法编码；区间标签在小尺寸下很小；曲线是理论计算实例，不显示误差数值或不同 degree 对照。
- **可复用模式**：用“每步函数 + 输入/输出集合 + reference target”的横向分镜呈现可组合理论算法，caption 同步写递推式和初始化。

### Figure 3 — degree-5 convergence（p.8，正文）

- **设计/布局**：跨双栏左右 2 panel；左 synthetic matrix，右 GPT-2 layer-4 gradient；legend 置于两 panel 下方并横向排列。左 y 轴为对数 spectral error，右 y 轴为 [0,1] 的线性归一化误差；右 panel 具方法曲线的浅色 band。
- **类型与绘图语法**：`line`；x 两 panel 均 linear iteration，y 为混合（左 log、右 linear）；双向浅灰虚线 grid；6 条方法/参数系列（Newton-Schulz、Jordan、You、PolarExp 的三种 ell）；legend shared；方法由颜色、marker 和线型共同编码；reference line 0；uncertainty `band` 仅右 panel，caption 定义为 20 batches 的 90% interval；linewidth 约 2 pt。
- **编码与数据**：x=iteration，y=谱范数误差；左矩阵 `sigma_max=1,sigma_min=10^-6`，奇异向量随机、奇异值按 log scale 均匀；右为随机初始化 GPT-2 的 layer-4 `mlp.c-proj` gradient。结果为 per-iteration 曲线，右 band 跨 20 个 batch；不是训练 seed 的区间。
- **Caption（PDF 原文）**：`Figure 3: Convergence of degree-5 polynomial methods. Polar Express outperforms other methods at every iteration when tuned properly. Left panel: synthetic matrix with sigma_max = 1, sigma_min = 10^-6. Right panel: gradient from randomly-initialized GPT-2 model on a batch of language modeling data. Shaded region shows 90% interval over 20 batches of data.` Caption 直接给主发现、设置、panel 解释和不确定性定义，基本自包含。
- **证据关系**：Theorem 3.3 的 degree-dependent convergence → Figure 3 对 synthetic 与真实 gradient 的 per-iteration spectral-error 检验 → Figure 5 的 task-utility iteration ablation → Appendix H.1 的 Frobenius/cosine 与 top-spectrum 诊断。
- **优点**：理论 worst-case 误差与真实 gradient 分开 panel；log y 让长尾收敛可见；90% band 的定义比主训练图完整。
- **弱点**：右 panel 用不同 y scale，左右直接高度比较容易误读；6 条系列主要依赖颜色/legend，灰度鲁棒性有限；最佳 ell 的选择仍是调参后的展示。
- **可复用模式**：用 synthetic 极端条件 + 真实中间表示的并列机制图，把 theorem metric 与应用对象连起来，同时明确 band 的 batch 分母。

### Figure 4 — GPT-2-Small validation loss（p.9，正文）

- **设计/布局**：跨双栏左右 2 panel；左为 final validation loss 对 log learning rate，右为最佳学习率下 epoch 曲线。左上有四项方法 legend（AdamW、Jordan、You、PolarExp），右 panel 复用该 legend 语义。
- **类型与绘图语法**：`line`；x 混合（左 log learning rate，右 linear epoch），y linear；浅灰双向虚线 grid；4 条系列；AdamW 蓝、PolarExp 黑、You 紫点线、Jordan 红点划线；无 marker/band/reference；linewidth 约 2 pt；legend single/shared。
- **编码与数据**：GPT-2-Small 124M，FineWeb 1B tokens，无 weight decay；左为各学习率 run 的 final validation loss，右为最佳 lr 的 validation trajectory；caption 给每个方法的 best loss/lr，但没有 seed 或 inferential interval。
- **Caption（PDF 原文）**：`Figure 4: Training a GPT-2-Small (124M) model on 1 Billion tokens of the FineWeb data set (Penedo et al., 2024). muon-<method> denotes Muon with 5 iterations of <method> to compute polar(M ). No weight decay is used. Left: final validation loss vs. learning rate. The best final validation losses for each method were adamw(lr =0.0005): 4.197, muon-Jordan(lr =0.01): 3.639, muon-You(lr =0.01): 3.629 and muon-PolarExp(lr =0.005): 3.588. Right: Validation loss vs. training iteration.` Caption 定义 method、预算和两个 panel，并给 best values；未定义 seed/spread。
- **证据关系**：Muon 方法接口 → Figure 4 在较小 GPT-2 上复现 Figure 1 的 loss 比较 → Figure 5 解释五/六次迭代的 utility 成本 → Figure 11–13 追加 training/time、weight-decay/10B 结果。
- **优点**：含 AdamW 的外部基线；左 sweep 与右动态互补；黑色 PolarExp 曲线易追踪。
- **弱点**：学习率点数有限且没有不确定性；图内 legend/颜色负担较高；“consistent”结论依赖同一训练设置和 best-lr 选择。
- **可复用模式**：在同一图内把优化器相对比较和 training trajectory 配对，并在 caption 明确迭代次数与 weight decay。

### Figure 5 — iteration/SVD ablation（p.10，正文）

- **设计/布局**：跨双栏左右 2 panel bar chart；左 y=Final Validation Loss，右 y=Median Training Step Time (s)，x 为 categorical iterations `2,3,4,5,6,7,10,20,30,SVD`。左 legend 说明蓝色 PolarExpress 与橙色 SVD，右 panel 沿用颜色而不重复 legend。
- **类型与绘图语法**：`bar`；x categorical，y linear；无显著 grid；每 panel 10 根 bars、无 marker、无不确定性；颜色=方法（蓝/橙），黑色轴和文本；linewidth 约 0.8 pt；shared legend true。
- **编码与数据**：左为最终 validation loss，右为单步中位 runtime；GPT-2-Small、FineWeb 1B、lr 0.05、weight decay 0.1。重复次数、median 的样本数及 seed 未在 caption 中给出；“>6 不改善”和“SVD doubles runtime”是视觉/正文摘要而非 inferential test。
- **Caption（PDF 原文）**：`Figure 5: Ablating the number of iterations of Polar Express used to implement Muon, and comparing to computing polar(M ) exactly via an SVD. Left: using > 6 iterations or the SVD does not improve final validation loss. Right: Runtime of Muon is not sensitive to the number of iterations of Polar Express, but the SVD makes it significantly slower. All runs use GPT-2-Small with 1 Billion tokens of FineWeb data, learning rate 0.05, and weight decay 0.1.` Caption 同时写设计、比较、结论和条件；没有 runtime 重复/方差定义。
- **证据关系**：Figure 3 的谱误差优势 → Figure 5 检验“更精确是否提高 task utility”及成本 → 结论选择 5–6 iterations 而不使用 exact SVD → Appendix H.1 Figure 9/10 检查小奇异方向和 top-spectrum。
- **优点**：把 accuracy 与 cost 放在同一对象中；categorical x 直接显示 iteration saturation；SVD 作为橙色对照很醒目。
- **弱点**：左右 y 单位不同且未给误差条；bar 顶端差异在 3.58–3.60 区间很小；没有把实际 step-time 分布/硬件说明放入 caption。
- **可复用模式**：用并列“质量 bar + 成本 bar”表达近似精度的 diminishing returns，并把 exact baseline 放在同一 categorical axis。

### Figure 6 — 10B-token GPT-2-Large（p.10，正文）

- **设计/布局**：跨双栏、上下 2 row × 左右 2 panel 的视觉区块中，Figure 6 为下方一对图：左 final validation loss–log learning rate，右 validation loss–epoch；左上有三项 Muon legend，右复用语义。
- **类型与绘图语法**：`line`；x 混合（左 log learning rate，右 linear epoch），y linear；浅灰虚线 grid；3 条方法系列；黑/紫/红及实线/点线/点划线；无 marker/band/reference；linewidth 约 2 pt；single/shared legend。
- **编码与数据**：GPT-2-Large、FineWeb 10B tokens、weight decay 0.1；左为 learning-rate sweep 的 final validation loss，右为 best lr 的 epoch loss。caption 给三方法 best loss/lr；无 seed/区间。
- **Caption（PDF 原文）**：`Figure 6: Training GPT-2-Large on 10 billion tokens of FineWeb with weight decay 0.1. Best final validation losses were muon-Jordan (lr = 0.002): 2.921, muon-You (lr = 0.002): 2.919 and muon-PolarExp (lr = 0.002): 2.913.` Caption 短而自包含 setup/headline，但没有明确左右 panel 轴语义（轴标签可见）。
- **证据关系**：正文 1B-token Figures 1/4 → Figure 6 检查更长训练和 weight decay → Appendix Figure 13 给 10B-token 的 training/validation/time 全部 panel → 结果边界是小幅 lead 而非跨任务保证。
- **优点**：用额外 token budget 检查 headline 的持续性；loss 差异和 best lr 直接可读。
- **弱点**：只有一个长训练配置且无不确定性；panel caption 未写清右图最佳 lr trajectory；颜色仍是主要系列身份。
- **可复用模式**：对 headline 追加一个更长预算/正则化条件，并用同一 sweep+trajectory 视觉合同保证横向可比。

### Figure 7 — finite-precision stabilization（p.27，附录 G）

- **设计/布局**：页宽单 panel 函数图；蓝色 Before stabilizing、橙色 After stabilizing、黑色 `0.236x` legend 位于图上方。虚线标出 `ell_{t+1}`、`u_{t+1}` 和 x≈1.01 的竖界；淡灰网格很弱。
- **类型与绘图语法**：`line`；x/y linear；3 条曲线/参考，3 条 bounds reference；legend top centered；无 marker/band/hatch；line styles solid 与 dashed；linewidth 约 2.5 pt。
- **编码与数据**：x 为归一化 singular value `x`，y 为 polynomial output/ratio 关系；颜色=稳定化前后，黑线=安全下界；区间 `[0.005,1]`、degree 5，caption 解释 0.8 附近映射到近零及 `1+epsilon` 放大问题。没有重复/不确定性统计。
- **Caption（PDF 原文）**：`Figure 7: Effects of stabilizing the update rules with a safety factor and cushioning, as described in Appendix G. The blue curve is the optimal degree-5 polynomial for the interval [0.005, 1]. It is has numerical issues because it maps singular values near 0.8 down to almost zero and maps 1 + epsilon to approximately u_{t+1} + 25epsilon. The stabilized version is better because it ensures p_t(x)/x is at least 0.236 and maps all x less than or equal to 1.01 to at most u_{t+1}.` PDF caption 保留 “It is has” 原文措辞；定义设置、语义、问题和修复结论，但没有数值实验聚合。
- **证据关系**：Appendix G 的有限精度 failure mode → Figure 7 显示 safety factor/cushioning 如何改函数几何 → Figure 3/5 的稳定训练实现接口；是 robustness/mechanism 证据，不是 task performance。
- **优点**：把 bfloat16 风险写成可见的函数形状；0.236x 和 bounds 是可验证 reference。
- **弱点**：曲线没有直接标出 0.8/1.01 的文字 callout；“better”是几何判据，未在图内给具体误差或 bit-level 结果；颜色不适合纯灰度。
- **可复用模式**：对数值稳定化先展示原始函数的坏区间，再叠加修复函数和安全下界/reference。

### Figure 8 — Frobenius/cosine convergence（p.27，附录 H.1）

- **设计/布局**：页宽 2×2；列为 GPT-2 Gradient Layer 1 `attn.c_attn` 与 Layer 3 `mlp.c_fc`，行分别为 Relative Frobenius Error 与 Cosine Similarity。共享 legend 位于底部，左列提供 y 轴标题，底行提供 x=Iteration。
- **类型与绘图语法**：`line`；x linear iteration，y linear [0,1]；双向浅灰虚线 grid；4 方法系列；共享 legend；marker 为 circle/x/square/plus，line styles 也区分方法；浅色 band 可见（caption 未定义其聚合）；无 reference line；linewidth 约 2 pt。
- **编码与数据**：x=迭代，y=相对 Frobenius error 或 Frobenius-inner-product cosine similarity；两层随机初始化 GPT-2 gradient、一个 language-modeling batch。曲线显示逐 iteration 指标，浅色包络在 PDF 可见但 caption 没有 seed/覆盖率/聚合定义。
- **Caption（PDF 原文）**：`Figure 8: Convergence of degree-5 polynomial methods measured in Frobenius norm and cosine similarity. Test matrices are gradients of two layers of a randomly-initialized GPT-2 model on a batch of language modeling data. Polar Express outperforms other methods.` Caption 有 metric、对象和主发现，但 band 的统计语义不自包含。
- **证据关系**：Figure 3 的 spectral-error claim → Figure 8 检查更宽松的 task-relevant metrics → Figure 5 的 5–6 iteration utility → Figure 10 进一步只看 top singular values。
- **优点**：2×2 结构把两个梯度层和两个指标对齐；marker/line 冗余增加方法追踪性；共享 legend 减少重复。
- **弱点**：band 未定义；底部 legend 与 caption距离较近；Frobenius/cosine 结论容易被误读为所有 gradient/task 的统计保证。
- **可复用模式**：用“对象列 × 指标行”的小倍图比较机制 metric，并将宽松 metric 与主谱范数 metric 明确区分。

### Figure 9 — small singular directions（p.29，附录 H.1）

- **设计/布局**：页宽上下 2 panel；上图 final validation loss 对 cutoff gamma 的 log x，比较 Exact/Truncated/Reverse Muon；下图为五次 Polar Express composition 的 polynomial map 对 gamma。上 panel 左上 legend，底 panel 无重复 legend；上图点线连接离散 cutoff，右端 blue reverse 明显恶化。
- **类型与绘图语法**：`line`；x log cutoff gamma，y linear（上 final loss、下 polynomial output）；上 panel 有浅灰 grid，下 panel 极简；3 个 optimizer 系列 + 1 true-polar reference；marker 2 种（circle/x）与不同线型；reference line 1；无 uncertainty band；linewidth 约 2 pt。
- **编码与数据**：x=small/large singular cutoff gamma，颜色/marker=Exact green、Truncated orange、Reverse blue；下 panel y=PolarExp(gamma)。GPT-2 Small 1B FineWeb、lr 0.05、wd 0.1、bfloat32；上图是固定设置的 final loss，不是三 seed 区间。
- **Caption（PDF 原文）**：`Figure 9: Impact of small singular directions of momentum matrix on optimization quality. We compare three variations of the Muon update rule. Exact Muon (green) processes the momentum M = U Sigma V^T by mapping each singular value to 1: polar(M) = U V^T. Truncated Muon (orange) maps the larger singular values to 1 and the smaller singular values to 0. Reverse Muon (blue) maps the larger ones to 1 and the smaller ones to −1. Computations are performed in bfloat32. All runs train GPT-2 Small on 1 billion tokens of FineWeb data with learning rate 0.05 and weight decay 0.1. When the cutoff that defines “large” and “small” singular values is gamma approximately 10^-3, all three methods perform well, showing that the small singular directions do not matter. Bottom panel shows the polynomial defined by composing five iterations of Polar Express. Five iterations is just enough for singular values greater than or equal to 10^-3 to nearly converge.` Caption 极其完整，定义矩阵映射、精度、训练条件、结论和 bottom panel。
- **证据关系**：Figure 5 的 iteration saturation → Figure 9 把小奇异方向作为机制变量隔离 → Figure 10 将同一 cutoff 投影到 top-spectrum convergence；它是主 headline 与方法实现之间的关键边界证据。
- **优点**：上/下 panel 将 optimization quality 与 polynomial map 对齐；颜色、marker、文字定义完整；log cutoff 揭示阈值区间。
- **弱点**：green/orange/blue 在灰度下较难区分；上 panel 离散 cutoff 点少，无法表达连续不确定性；lower curve 的 y 语义需要读 caption。
- **可复用模式**：把“任务输出随机制 cutoff”与“机制函数本身”上下对齐，直接检验内部近似误差是否真的影响任务。

### Figure 10 — top-spectrum convergence（p.29，附录 H.1）

- **设计/布局**：页宽 2×2，列为两个 GPT-2 gradient layer，行是 Relative Frobenius Error/Cosine Similarity；结构与 Figure 8 对齐，底部共享 legend。只保留大于 `sigma_max/10^3` 的奇异值。
- **类型与绘图语法**：`line`；x linear iteration，y linear；双向浅灰虚线 grid；4 方法系列；shared legend；circle/x/square/plus marker 和方法线型；浅色 band 可见但 caption 未定义；无 reference line；linewidth 约 2 pt。
- **编码与数据**：x=iteration，y=top-spectrum 投影上的 relative Frobenius error/cosine；两层随机初始化 GPT-2 gradient、language-modeling batch；截断阈值 `sigma_max/10^3`。统计为曲线比较，无显式 seeds/区间定义。
- **Caption（PDF 原文）**：`Figure 10: Convergence of degree-5 polynomial methods, considering only singular values larger than sigma_max / 10^3. Test matrices are gradients of two layers of a randomly-initialized GPT-2 model on a batch of language modeling data. Polar Express converges in just five or six iterations and outperforms other methods.` Caption 写清投影条件、对象、结论，但没有解释浅色包络。
- **证据关系**：Figure 9 的 gamma≈10^-3 任务边界 → Figure 10 直接测 relevant top subspace 的 5–6 step convergence → Figure 5 将该步数用于 Muon utility；是“精度足够而非全谱精确”的机制桥。
- **优点**：与 Figure 8 同构，隔离 top singular directions 后读者可直接比；指标和 cutoff 明确。
- **弱点**：band 统计含义缺失；同构 Figure 8/10 需要 caption 更明确强调差异；仍主要依赖颜色。
- **可复用模式**：在全空间 metric 之后使用同布局的任务相关子空间 metric，展示近似器的有效精度边界。

### Figure 11 — 1B-token training/time（p.31，附录 H.2）

- **设计/布局**：页宽 2×2×2 复合图：每个 row 为 GPT-2-Large/GPT-2-Small；左列上/下分别 final validation loss、final training loss 对 learning rate，右列上为 validation loss 对 iterations，右列下为 training loss 对 time。左列各自放方法 legend；右列复用语义。
- **类型与绘图语法**：`line`；x 混合（learning-rate log、iteration/epoch/time linear），y linear loss；浅灰双向虚线 grid；最多 4 条系列（Large 三种 Muon，Small 含 AdamW）；legend 非全局共享而是左列重复；无 marker/band/reference；linewidth 约 2 pt。
- **编码与数据**：1B FineWeb、无 weight decay；caption 的 subfigure(a) Large 最佳 validation 为 You 3.390、Jordan 3.401、PolarExp 3.344，(b) Small 为 AdamW 3.370、Jordan 3.233、You 3.234、PolarExp 3.231。左 sweep、右 best-lr trajectory/time，未给 seed/区间。
- **Caption（PDF 原文）**：`Figure 11: Training GPT-2 on 1 billion tokens of FineWeb data (Penedo et al., 2024) without weight decay. The label muon-<method> denotes Muon with 5 iterations of <method> to compute polar(M ). Top left: final validation loss vs. learning rate. Bottom left: final training loss vs. learning rate. Top right: validation loss vs. number of iterations for best learning rate. Bottom right: training loss vs. time for best learning rate.` Caption 说明矩阵、四 panel 和 method 定义，但最佳数值在 subcaption/图下方而非 Figure 总 caption。
- **证据关系**：Figure 1/4 的 validation headline → Figure 11 补 training loss 与 wall-clock 视角 → Figure 12/13 加 wd/10B 对照。它把“step 优势⇒time 优势”的叙述扩展为可见曲线。
- **优点**：四 panel 覆盖 validation/training/steps/time；Large/Small subfigure 对齐；同一图可读出 loss 与计算进度。
- **弱点**：混合 x 轴和重复 legend 增加扫描成本；time 是按 step time 缩放的曲线，caption 未写硬件/聚合细节；不同模型的 y 范围不完全一致。
- **可复用模式**：将质量、训练过程和 wall-clock 放在 2×2 复合图中，同时用 row 区分模型规模。

### Figure 12 — 1B-token + weight decay（p.32，附录 H.2）

- **设计/布局**：与 Figure 11 相同的 2×2 结构；上下 row 为 GPT-2-Large/Small，左列 learning-rate sweep（validation/training），右列 best-lr 的 epoch/time。legend 放在左列每个 row 的上方。
- **类型与绘图语法**：`line`；x 混合 log/linear/time，y linear；浅灰双向虚线 grid；Large/Small 均主要呈现 3 条 Muon 方法线；legend duplicated by row；无 marker/band/reference；linewidth 约 2 pt。
- **编码与数据**：1B FineWeb、weight decay 0.1、5 iterations。subcaption(a) Large best validation：Jordan 3.390、You 3.401、PolarExp 3.344；(b) Small：Jordan 3.638、You 3.641、PolarExp 3.587。左为 final loss sweep，右为 validation/training trajectory/time；无 seed/区间。
- **Caption（PDF 原文）**：`Figure 12: Training GPT-2 on 1 billion tokens of FineWeb data (Penedo et al., 2024) with weight decay 0.1. The label muon-<method> denotes Muon with 5 iterations of <method> to compute polar(M ). Top left: final validation loss vs. learning rate. Bottom left: final training loss vs. learning rate. Top right: validation loss vs. number of iterations for best learning rate. Bottom right: training loss vs. time for best learning rate.` Caption 对设置和 panel key 自包含，但不定义 spread/重复数。
- **证据关系**：Figure 1/4 baseline → Figure 12 直接控制 weight decay → Figure 6/13 检查更长 token budget；其结果支撑“ranking broadly unchanged”，不能替代跨任务验证。
- **优点**：保持 Figure 11 的视觉合同，weight-decay 变量易于做横向审阅；四种指标/视角完整。
- **弱点**：图内 y 轴和 legend 较小；没有把无 weight decay 与本图的差值直接画在一个面板；未报告 seed/spread。
- **可复用模式**：对正则化干预复用基准复合图布局，减少视觉混淆并让控制变量成为唯一显著变化。

### Figure 13 — 10B-token training（p.33，附录 H.2）

- **设计/布局**：与 Figure 11/12 同构的 2×2×2 复合图；Large row 和 Small row 各包含 learning-rate sweep、training sweep、validation-vs-epoch、training-vs-time；左列显示 legend。
- **类型与绘图语法**：`line`；x 混合（左 log learning rate、右 linear epoch/time），y linear；浅灰双向虚线 grid；最多 4 条系列（Small row 含 AdamW）；legend 按 row 重复、非单一 shared legend；无 marker/band/reference；linewidth 约 2 pt。
- **编码与数据**：FineWeb 10B tokens、5 iterations。subcaption(a) Large best validation Jordan 2.921、You 2.919、PolarExp 2.913；(b) Small best validation AdamW 3.370、Jordan 3.233、You 3.234、PolarExp 3.231。展示 final validation/training sweep 与 best-lr 的 epoch/time；无不确定性。
- **Caption（PDF 原文）**：`Figure 13: Training GPT-2 on 10 billion tokens of FineWeb data (Penedo et al., 2024). The label muon-<method> denotes Muon with 5 iterations of <method> to compute polar(M ). Top left: final validation loss vs. learning rate. Bottom left: final training loss vs. learning rate. Top right: validation loss vs. number of iterations for best learning rate. Bottom right: training loss vs. time for best learning rate.` Caption 给预算、方法和四 panel，但最佳值在 subcaption中，缺少 seed/重复说明。
- **证据关系**：Figure 6 的主文 10B validation → Figure 13 提供同条件 training/time 和 Small 对照 → J.1 再检查 Algorithm 3/head split 在 GPT-Small 的 12-run runtime。它支撑长预算下的小幅 lead，不支撑泛化保证。
- **优点**：保持同构便于与 Figures 11/12 比较；同时显示质量和计算时间。
- **弱点**：信息量大且 caption 依赖 subcaption；长时间轴让曲线差异很细；无不确定性/统计重复。
- **可复用模式**：用固定 2×2 dashboard 扩展训练预算，确保不同 budget 的 loss/time 曲线保持相同阅读顺序。

### Figure 14 — CIFAR10 ResNet20（p.34，附录 H.3）

- **设计/布局**：页宽上下 2 panel，左 validation score–learning rate，右 training loss–learning rate；每个 panel 上方都有六方法 legend。x 为多数量级 log learning rate；浅色 band 围绕三 seed 范围。
- **类型与绘图语法**：`line`；x log，y 混合（validation linear、training log）；双向浅灰虚线 grid；6 条系列（AdamW、muon-Newton、muon-You、muon-Jordan、muon-PolarExp、sgd-m）；marker 以圆点为主、颜色为主编码；legend per panel（非单一 shared）；uncertainty `band`，无 reference/hatch；linewidth 约 1.5 pt。
- **编码与数据**：x=learning rate，左 y=validation score，右 y=training loss；band=three random seeds 的 range。best validation：sgd-m 0.855 (0.1)、AdamW 0.878 (0.01)、You 0.887、Newton 0.890、Jordan 0.891、PolarExp 0.893 (均 0.001)。
- **Caption（PDF 原文）**：`Figure 14: CIFAR10 with a RESNET20. Shaded regions show range over three random seeds. The best validation accuracy for each method was sgd-m (lr = 0.1): 0.855 Adamw (lr = 0.01): 0.878 muon-You (lr = 0.001): 0.887, muon-Newton (lr = 0.001): 0.890, muon-Jordan (lr = 0.001): 0.891, muon-PolarExp (lr = 0.001): 0.893.` Caption 明确定义 band、任务、最佳值；缺少训练 loss 的具体统计说明但图轴自解释。
- **证据关系**：主文 GPT-2 headline → H.3 跨任务检查 → Figure 14 显示 ResNet20 上 PolarExp 的小幅 best accuracy lead；Figures 15/16 显示该排序不稳定，因此附录对“consistent advantage”构成边界。
- **优点**：三 seed range 是主训练图缺失的重要不确定性编码；validation 与 training loss 同时显示；log sweep 覆盖宽。
- **弱点**：六色 legend 在灰度/小尺寸下较难读；best value 仍是 sweep 极值，未给跨方法统计比较；上下 panel y scale 不同。
- **可复用模式**：跨任务图使用统一“validation score + training loss”双 panel，并在 band caption 写明 seed 数和聚合方式。

### Figure 15 — CIFAR100 ResNet110（p.34，附录 H.3）

- **设计/布局**：与 Figure 14 同构的上下 2 panel；左右分别 validation score、training loss，两个 panel 各自 legend；x 为 log learning rate，band 贯穿曲线。
- **类型与绘图语法**：`line`；x log，y 混合（上 linear、下 log）；双向浅灰虚线 grid；6 条方法系列，主要由颜色和圆点 marker 区分；legend per panel；uncertainty `band` over 3 seeds；无 reference/hatch；linewidth 约 1.5 pt。
- **编码与数据**：CIFAR100/ResNet110；左 validation score，右 training loss；三 random seeds range。best validation：sgd-m 0.602 (0.1)、AdamW 0.643 (0.01)、Jordan 0.660、Newton 0.663、PolarExp 0.663、You 0.665（Muon 均 0.001）。
- **Caption（PDF 原文）**：`Figure 15: CIFAR100 with RESNET110. Shaded regions show range over three random seeds. The best validation accuracy for each method was sgd-m (lr = 0.1): 0.602, Adamw (lr = 0.01): 0.643, muon-Jordan (lr = 0.001): 0.660, muon-Newton (lr = 0.001): 0.663. muon-PolarExp (lr = 0.001): 0.663, muon-You (lr = 0.001): 0.665,` PDF caption 末尾保留逗号；前三 seed/range 语义明确，最后一项仍依赖文本延续但不影响对象识别。
- **证据关系**：Figure 14 的 CIFAR10 结果 → Figure 15 更难 CIFAR100/更深 ResNet → PolarExp 与 Newton 并列而 You 略高，说明主文 GPT-2 lead 不是跨任务普适规律。
- **优点**：与 Figure 14 对齐，任务变化成为主要变量；band 和 log sweep 可比较。
- **弱点**：caption 末尾不完整标点；颜色数量高且无方法形状冗余；没有把训练预算/epoch/seed 聚合细节写入 caption。
- **可复用模式**：用同构图比较任务难度变化，同时让负面/并列结果在视觉上保留而不是只写正文。

### Figure 16 — CIFAR10 ViT（p.34，附录 H.3）

- **设计/布局**：与 Figures 14–15 同页、同构上下 2 panel；左 validation score 对 log lr，右 training loss 对 log lr；每 panel legend，band 贯穿 seed range。x 轴覆盖范围更宽，曲线交叉更多。
- **类型与绘图语法**：`line`；x log，y 混合（上 linear、下 log）；双向浅灰虚线 grid；6 方法系列；圆点 marker/颜色为主，legend per panel；uncertainty `band` over 3 seeds；无 reference/hatch；linewidth 约 1.5 pt。
- **编码与数据**：CIFAR10/ViT；左 validation score，右 training loss；三 random seeds range。best validation：sgd-m 0.809 (10^-1)、PolarExp 0.860 (10^-5)、AdamW 0.861 (10^-3)、Jordan 0.861 (10^-5)、You 0.865 (10^-5)、Newton 0.874 (10^-4)。
- **Caption（PDF 原文）**：`Figure 16: CIFAR10 with a VIT. Shaded regions show range over three random seeds. The best validation accuracy for each method was sgd-m (lr = 10^-1): 0.809, muon-PolarExp (lr = 10^-5): 0.860, Adamw (lr = 10^-3): 0.861, muon-Jordan (lr = 10^-5): 0.861, muon-You (lr = 10^-5): 0.865, muon-Newton (lr = 10^-4): 0.874.` Caption 明确定义 band 与 best values，且完整。
- **证据关系**：H.3 的跨架构检查 → Figure 16 显示 Newton/You/AdamW 优于 PolarExp → 与 Figures 14/15 一起反驳“consistent advantage”作为一般性结论，限定其主要适用于 GPT-2/FineWeb 条件。
- **优点**：交叉曲线和 band 直观暴露方法排序变化；与 ResNet 图保持同一视觉合同。
- **弱点**：六色 legend + 宽 log sweep 信息密度高；best sweep 极值仍不等于总体显著性；训练 loss 的尺度与 validation 不同。
- **可复用模式**：把 negative transfer/failure case 置于与 headline 完全相同的 panel grammar 中，避免只报告成功任务。

### Figure 17 — spectrum-aware initialization（p.36，附录 I）

- **设计/布局**：页宽单 panel；四条曲线由两种方法（Newton-Schulz、PolarExp）× 两种初始化（Standard、Spectrum-Aware）组成。legend 位于左下，分两段解释颜色和线型；x=Iteration，y=Spectral Error；曲线在 10–20 iteration 区间出现明显下降。
- **类型与绘图语法**：`line`；x/y linear；无 grid 或极浅 grid；4 条系列；颜色=method（蓝/橙），线型=initializer（实线/虚线），legend inside lower-left；无 marker/band/reference；linewidth 约 2 pt。
- **编码与数据**：x=iteration，y=spectral error；32×32 synthetic matrix，`sigma_j(M)=j^-5`；spectrum-aware initialization 算作 additional iteration。没有 seed/区间；该算法在正文脚注声明不用于 Muon experiment。
- **Caption（PDF 原文）**：`Figure 17: Benefits of the spectrum-aware initialization scheme of Appendix I. Using this scheme improves convergence of both Newton-Schulz and Polar Express on a synthetic 32×32 matrix with sigma_j(M) = j^-5. Note that we count the spectrum-aware initialization as an additional iteration.` Caption 写清方法、对象、谱和计数规则，结论直接。
- **证据关系**：Appendix I 的谱隙初始化构造 → Figure 17 检查两方法的 convergence acceleration → 与 Figure 3 的标准初始化 synthetic baseline 对照；属于扩展方法证据，不直接支持 Muon GPT-2 headline。
- **优点**：颜色/线型正交编码使四条曲线可分；额外 iteration 计数在 caption 明确；合成谱条件可复现。
- **弱点**：legend 两级语义仍需来回查；没有 runtime/成本 side panel；合成谱隙与实际 gradient 的关联未在图中呈现。
- **可复用模式**：用颜色表示算法、线型表示初始化/条件，把两个因素的交互放在一张低复杂度曲线图中。

### Figure 18 — Algorithm 3 runtime（p.37，附录 J）

- **设计/布局**：页宽单 panel；x 为 log aspect ratio alpha=m/n，y 为 log runtime seconds；三种 n 用绿色/橙色/蓝色，三种 restart interval 用实线/虚线/点线。legend 位于右上、分两组说明 n 和 Restart Interval。
- **类型与绘图语法**：`line`；x/y log；浅灰双向网格；6 条系列（3 n × 3 restart interval）；shared legend in upper right；无 marker、无 band/reference/hatch；linewidth 约 2 pt，line styles 3。
- **编码与数据**：x=aspect ratio，y=GPU runtime；`T=6`、degree-5 polynomial、n=1024/2048/4096；restart interval=1 是 baseline、6 是 Algorithm 3、3 是两次三步重启。runtime 是 GPU timing，不报告重复次数/误差。
- **Caption（PDF 原文）**：`Figure 18: Effects of using Algorithm 3 on runtime on a GPU. We run T = 6 iterations of a degree-5 polynomial method on matrices with various dimensions n and aspect ratios alpha. Restart interval = 6 is Algorithm 3, restart interval = 1 is equivalent to the baseline (that is, not using Algorithm 3), and restart interval = 3 is an intermediate method that calls Algorithm 3 once to do the first three iterations and again to do the last three iterations for greater stability. When alpha is much greater than 1, increasing the restart interval significantly reduces the runtime.` Caption 定义方法、条件、线型语义、稳定性方案和趋势；无 timing spread。
- **证据关系**：Appendix J 的 rectangular fast iteration → Figure 18 测 aspect-ratio/runtime trade-off → J.1 将 head splitting/Algorithm 3 接回 GPT-Small 12-run practical test；是 efficiency/cost 扩展，不能直接替代主 Muon accuracy 证据。
- **优点**：log–log 坐标揭示 aspect-ratio scaling；颜色/线型正交编码；baseline/intermediate/Algorithm 3 都在一图内。
- **弱点**：runtime 没有误差条/重复统计；legend 远离曲线且六系列交叠；“significantly”是趋势描述而非统计检验。
- **可复用模式**：对矩形算法同时用 log–log scaling 和“规模颜色 × 稳定性线型”双因素编码，并把 baseline 作为同一图内参照。

## Table 核对

PDF 正文与附录均无 Figure 以外的 Table，`tables=[]`。Algorithm 1（p.7）、Algorithm 2（p.25）和 Algorithm 3（p.36）是算法框，不计入 Table；PDF 中的 “Table 2/3” 是引用文献的表，不属于本论文对象清单。

## 跨对象关系与最终判断

- **方法—结果—消融**：公式 (5)/(6) 和 Theorem 3.1/3.3 定义组合多项式与收敛目标；Figure 2 是 method/theory interface，Figure 3 是 spectral mechanism，Figures 1/4/6 是 GPT-2 main comparisons，Figure 5 把 spectral accuracy 与 Muon utility/runtime 分离；Figures 7–10 是 finite-precision/top-spectrum mechanisms，Figures 11–16 是 additional results/failure boundary，Figures 17–18 是未用于主 Muon 实验的扩展效率。
- **caption 系统**：所有 caption 均以 `Figure N:` 开始而非粗体结论标题；正文图通常在 caption 中写 setup、panel key 和 best value，机制图写线型/区间定义；附录 H.2/H.3 追加 subfigure 条件和 seed band，Figure 8/10 的 band 语义仍不完整。
- **最有价值的对象**：Figure 2（公式—算法桥）、Figure 3（理论误差—真实 gradient）、Figure 5（utility/cost 决策面）、Figure 9/10（小奇异方向与 top-spectrum 桥）、Figures 14–16（对 headline 的跨任务边界）。
- **主要失败模式**：主 GPT-2 图多为最佳学习率 sweep、没有 seed/区间；颜色承担过多方法身份，灰度下可读性有限；Figure 8/10 可见 band 未在 caption 定义；Figure 15 caption 末尾残留逗号；Figure 7 的 “It is has” 是 PDF 原文语病；Figures 17/18 的合成/矩形 runtime 扩展与主要 Muon accuracy 证据边界需明确。
- **可迁移规则**：对可组合迭代方法，先用区间/谱图给出可验证的组件状态，再用 task-quality 与 runtime 并列消融定义“足够精度”；若 headline 来自 best sweep，必须用 seed/重复和失败任务图限定其适用边界。
